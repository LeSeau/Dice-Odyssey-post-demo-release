# Dice Odyssey — CLAUDE.md

Roguelike deckbuilder en Godot 4 (GDScript) où les dés remplacent l'énergie classique. Ce fichier résume l'architecture réelle du code (vérifiée par exploration directe le 2026-06-22), pas le design visé. Voir aussi [dice-odyssey-context.md](dice-odyssey-context.md) pour le design intentionnel — il contient des divergences avec le code actuel, listées en bas de ce fichier.

L'auteur (Julien) n'est pas développeur de formation — le code a beaucoup de duplication / spaghetti. Ne pas supposer une cohérence architecturale qui n'existe pas ; vérifier le code réel avant d'agir, surtout pour les mécaniques de power/dés où plusieurs variables se chevauchent.

## TL;DR — où on en est (fin de session du 2026-07-19)

Lire ceci avant de re-creuser quoi que ce soit — ça évite de redécouvrir des choses déjà établies. Session très chargée : garder cette liste à jour au fil des prochaines sessions plutôt que de tout re-résumer depuis zéro.

- **REFONTE BOUTIQUES (2026-07-23, verdicts Julien sur 5 propositions : « Do 1 & 2 & 5 ») — VÉRIFIÉ PAR RENDU HEADLESS (harnais `debug_shop_rework.gd`/`.tscn` laissé à la racine, non committé : dice shop + card shop riche + card shop pauvre), NON PLAYTESTÉ.** ⚠️ **Une V1 mettait le dé discounté DANS la dice shop — corrigée sur retour immédiat de Julien : le deal die se vend dans la BOUTIQUE DE CARTES** (« a dice that is not one of the 3 present in the dice shop » = une offre complémentaire chez le marchand de cartes, pour provoquer des pivots de plan). La dice shop est revenue à ses 3 colonnes/gros X d'origine. Trois morceaux :
  - **Colonne « services » à droite de la boutique de cartes** (`card_shop.tscn`/`.gd`, x 1032-1232, au-dessus du bouton Leave) — deux « stalls » du même langage (titre 20pt or/accent + visuel + bouton shop-style 200px, styleboxes partagées + **`disabled` mappé sur le stylebox pressed comme les Buy de `shop_card.tscn`**, labels boutons en `[font_size=14/20]` bbcode — **la taille thème wrappe/clippe sur 200px, et tout texte runtime doit garder le même wrapper que le .tscn** sinon le 1er refresh écrase la taille) :
    - **Deal die (haut, y 130-380) — dans un PANNEAU dédié bordure or + header « Daily Deal »** (sub_resource `StyleBoxFlat_dealpanel`, ajouté sur 2e retour Julien : sans le cadre, le dé lisait comme « tiens, un dé ici ? » au lieu de « chaque boutique a ce slot ») **+ AURA du type derrière le dé** (3e retour Julien « same glow as in combat » : ColorRect `DealAura` + le `*_dice_shader.tres` du type chargé en `CACHE_MODE_IGNORE` — l'instance CACHÉE de ces .tres est celle que dice.gd MUTE en combat via les tweens de charge, un load frais du disque donne l'état repos + une instance à soi) : UN dé à **−20%**, TOUJOURS d'un type HORS des 3 de la dice shop (`Global.pick_dice_deal_index()` tire dans les 6 restants), re-pické à chaque reroll de la dice shop ET à chaque `populate_shop()` si l'index vaut −1 (acheté/vieille save) — chaque visite de boutique a donc son deal. Titre = `KeywordColorizer.dice_display_name(type) + " Dice"` coloré `DicePalette.accent(type)` (Pixie inclus), art = même face que la colonne dice shop (`DICE_DEAL_TEXTURES`, copie synchro de dice_shop.tscn), badge « -20% » Panel+Label construit en code (`_build_deal_badge`), label « Buy (~~270G~~ 216G) » (barré gris + vert `#5CD95C`, le vert des titres upgraded). **Tooltip de dé au survol** (même `dice_tooltip.tscn` que la dice shop, posé à gauche du stall (810,180), cleanup `mouse_exited` + `_exit_tree` — pattern anti-leak habituel). **Achat = MÊME bookkeeping que les handlers de la dice shop** (max+current+inventory+`purchased_dice_counts` donc compte dans l'escalation ×1.4, `dice_bought`+`update_dice_top_bar`+`gold_changed`+`dice_price_changed`, achievements customer/dice_bought_from_shop) puis `shop_dice_deal_index = -1` + bouton « Sold! » + art dimmé.
    - **Service « Remove a Card » (bas, y 396-588 — dans le MÊME panneau bordure or que le deal depuis le 3e retour Julien, le titre était illisible sur le tableau « Roll Your Destiny » du fond ; Leave descendu à y 612-670 pour l'espace ; 11e passe : `CardFan` descendu (`offset_top 0→16`) + rangée prix descendue (`_removal_row.position.y 134→150`) car le fan collait au titre)** : titre or + visuel éventail de 2 dos de carte (`Panel` + `card_ui_normal.tres` légèrement pivotés, le stylebox des mini card-backs du reshuffle) + « X » rouge par-dessus, bouton « Remove (50G) ». **1 retrait par visite**, prix **50 + 25 par retrait déjà acheté dans le run** (`Global.card_removals_bought`). Réutilise le flow des events (`Events.open_deck_view` → deck view mode removal → `Events.card_removed`) avec un flag `_removal_pending` armé au clic — annuler (Back) ne coûte rien. Prix rouge + disabled si or insuffisant.
  - **Retune des prix** : reliques 120-170 → **85-120** (`shop_relic.gd` — au vieux prix une relique côtoyait un dé à 150-270 en étant un bien plus petit power spike : slot mort) ; cartes Common 40-55 → **30-45**, Uncommon 60-80 → **50-70**, **Rare inchangé 95-125** (`shop_card.gd`). Échelle voulue : cartes < reliques ≈ carte rare < dés.
  - **Prix façon STS2 = « price tags » nus + CLIC SUR L'OBJET (6e passe, état final)** — historique : 4e passe « embossait » les boutons Buy (retour GPT) → REJETÉE ; 5e passe posait des price-tags nus mais gardait un Button invisible comme click-target ; **6e passe (décision Julien « make it a label, click on the card he wants to purchase instead, like Slay the Spire ») = PLUS AUCUN bouton d'achat, le prix est un pur Label, on CLIQUE L'OBJET**. Mécanique : chaque item a un handler `gui_input` sur son visuel — **carte** = `CardMenuUI.Visuals.gui_input` (même hook que l'écran de récompense) ; **relique** = `RelicUI.gui_input` (+ **Icon/Counter forcés `mouse_filter IGNORE` en instance** sinon ils gobent le clic avant RelicUI — les combat relics ne sont pas touchés) ; **dé de la dice shop** = `Dice{N}Texture.gui_input` (les 9 boutons `BuyDice{N}` deviennent des Labels passifs : `mouse_filter IGNORE` + StyleBoxEmpty posés en code dans `_ready`) ; **deal die & removal** = `gui_input` sur tout le `DealStall`/`RemoveStall` (die art PASS + tout le reste IGNORE, donc clic n'importe où dans le stall = achat). **Prix = pièce + montant nu**, or si abordable / **rouge sinon (cartes incluses)**, outline sombre épais (lisible sans boîte), item DIMMÉ (modulate 0.55) si pas les moyens. Hover = **scale pop** sur l'objet (carte via son propre hover, dé/deal/fan via tween code). Coins : cartes `[img=20]`, **reliques `[img=20]` (montées de 14 → « really small » sur retour Julien ; la scène relique est ×3.2 donc rend ~64px), dice shop `[img=26] font 24`**, deal/removal `[img=17]`. `_sold`/`removal_used_this_visit` gardent le clic post-achat (early-return). **Les 3 styleboxes `shop_price_*.tres` de la 5e passe sont donc ORPHELINES** (plus référencées ; laissées sur disque). Les `shop_button_*.tres` embossées ne servent plus qu'aux boutons d'ACTION (Leave, Reroll/X dice shop, menus). **Reroll du dice shop reste un bouton** (action, pas un prix) — garde « Reroll (🪙20G) ».
  - **Glow du deal die repassé en style INFUSION (retour Julien « similar to the glow in the dice infusion tab, not combat »)** : l'ancien `_setup_deal_aura` (copie du `*_dice_shader.tres` de combat en `CACHE_MODE_IGNORE`) est REMPLACÉ par `_setup_deal_glow` = halo radial doux additif (recette copiée de `dice_infusion.gd::_get_glow_texture`/`_get_additive_material` : `GradientTexture2D` radial blanc→transparent + `CanvasItemMaterial` BLEND_ADD), teinté `DicePalette.accent(type)`, pulse « breathing » en boucle, tué + caché à l'achat. Les helpers `_get_glow_texture`/`_get_additive_material` sont des statics locaux à `card_shop.gd`.
  - **⚠️ 7e passe (3 retours Julien sur screenshots) — état RÉELLEMENT final** : (1) **Dice shop RE-DOTÉ de vrais boutons Buy** (« keep a buy button, it looks bad with no button, click the button to buy ») — le clic-sur-le-dé de la 6e passe est ANNULÉ pour la dice shop uniquement : les 9 `BuyDice{N}` redeviennent des boutons cliquables (leurs styleboxes locales `StyleBoxFlat_buy_*` + `pressed` du .tscn), le clic-sur-le-dé est retiré. **Cartes/reliques/deal/removal gardent le clic-sur-l'objet.** (2) **Icône d'or « toujours trop petite » + mal centrée verticalement (screenshot du bas)** — root cause : une image inline bbcode `[img=N]` rend petit ET s'aligne sur la BASELINE du texte (donc trop bas). **Fix : la pièce devient un vrai `TextureRect` sprite + un `Label` séparé dans un HBox centré** (`size_flags_vertical SHRINK_CENTER`) — pour cartes (`shop_card.tscn` : Coin 30px + font 24), reliques (`shop_relic.tscn` : Coin 22px + font 18), et les 9 boutons dice (rangée construite EN CODE dans `shop.gd::_build_coin_row`, l'ancien RichTextLabel du bouton caché). **⚠️ Reliques : le `scale=3.2` de `ShopRelic` NE rend PAS ×3.2 en pratique** (la relique rend ~taille native dans la HBox `Relics` ; un premier essai à font 8/coin 9 « pensant ×3.2 » a rendu MINUSCULE — corrigé à font 18/coin 22 pour matcher visuellement les cartes, calé empiriquement au rendu). Deal/removal restent en bbcode (prix barré / état « Sold!/Removed! ») avec `[img]` monté à 24-26. Les 3 `shop_price_*.tres` de la 5e passe sont retirés des .tscn cartes/reliques (orphelins). (3) **Glow du deal = MOTES qui montent (« the small orbs rising to the top, like in the dice infusion screen, not the circle glow — you can keep a small glow too »)** : `_setup_deal_glow` garde un PETIT halo (150px, alpha 0.28-0.45) + ajoute un `Timer` (0.35s) → `_spawn_deal_mote` = orbes accent additifs qui s'élèvent du bas du dé en fondu (copie de `dice_infusion.gd::_spawn_mote`), timer stoppé à l'achat.
  - **8e passe (2 retours) — état final** : (1) Bouton Reroll du dice shop = « Reroll 🪙 20 » (le « G » retiré, parenthèses retirées, `[img]` monté à 24). (2) **⚠️ « gold amount not centered, even in top bar » = DÉFAUT DE L'ART, pas du layout : `gold_icon_v2.png` avait un padding vertical ASYMÉTRIQUE (26px haut / 41px bas dans 500×500) donc le disque visible flottait haut PARTOUT où l'icône est utilisée (top bar `GoldUI` incluse), et le nombre semblait bas à côté. Fix GLOBAL = `gold_icon_v2.png` RECENTRÉ sur sa bbox alpha (crop→paste centré, padding 33/34) + `--headless --import` obligatoire (source PNG écrasée).** Backup de l'original dans le scratchpad de session (`gold_icon_v2_ORIG_backup.png`). + les Labels de prix passés en `size_flags_vertical SHRINK_CENTER` (comme le Label du top bar `GoldUI`) pour un centrage cohérent. **⚠️ « big space between icon and number » = la séparation par défaut de l'HBox (4) + le ~15% de padding transparent de l'art de pièce donnaient un gap ~9px (mesuré) : passé à `separation = 0` sur les 3 rangées (dice `_build_coin_row`, `shop_card.tscn`, `shop_relic.tscn`) → gap ~4px snug (essai à −3 = pièce/nombre qui se touchent, trop). Le centrage vertical était en fait déjà bon (mesuré coin centre 401.5 vs nombre 402), c'est le gap horizontal qui faisait lire la paire « décentrée ».** Vérifié au rendu zoomé.
  - **9e passe** : Reroll (dice shop), deal die & removal passés eux aussi en rangée coin-sprite (`_build_coin_row` pour reroll avec préfixe Label « Reroll » + spacer 12px ; `_build_deal_row`/`_setup_removal_stall` dans card_shop.gd avec helpers `_make_coin`/`_make_num`) — le bbcode inline `[img]` gardait un gros gap + baseline basse. **⚠️ Le prix barré du deal (RichTextLabel pour le `[s]`) doit avoir `autowrap_mode = AUTOWRAP_OFF`** sinon il collapse à 0 largeur dans l'HBox. Les DealBuyLabel/RemoveCardLabel .tscn ne servent plus qu'aux états « Sold! »/« Removed! ». Un fix top bar « BOTTOM align » tenté dans cette passe a été **REJETÉ par Julien et REVERTÉ** (voir 10e passe — il avait été calibré sur un harnais qui rendait GoldUI à ~40px de haut alors que la vraie barre fait 80px : le bottom-align collait les chiffres au plancher de la barre).
  - **10e passe (Julien : « find a solution to make it look good like in Slay the Spire ») — LA solution définitive du centrage pièce/nombre, TOUT mesuré au pixel.** Deux root causes distinctes trouvées par mesure (harnais `debug_price_align.gd`/`.tscn` à la racine + `measure_align.py` au scratchpad — rend une bande top-bar FIDÈLE de 80px + les 2 boutiques avec `Global.gold=10` pour des chiffres ROUGES ségmentables de la pièce or, puis mesure centre-pièce vs centre-chiffres par rangée) : (1) **CinzelDecorative est INAPTE aux prix : ses chiffres ne sont pas des lining figures uniformes** — « 37 » et « 180 » se centrent différemment (deltas mesurés +0.5 à +3.0px pour la même police/taille), AUCUN offset unique ne peut tous les centrer. (2) **LuckiestGuy assoit ses glyphes ~0.18×font_size trop HAUT dans sa line-box** (deltas -2.5 à -4.5px uniformes et proportionnels à la taille). **Solution : TOUS les chiffres à côté d'une pièce utilisent `fonts/luckiest_guy_numbers.tres`** — une `FontVariation` de LuckiestGuy avec **`baseline_offset = 0.2` CALIBRÉ par mesure** (2 runs : delta(0)≈-4px à taille 22, delta(0.1)≈-2px → 0.2 ; vérif finale : **tous les deltas ≤ ±1px, la plupart 0.0 pile**). Consommateurs : `topbar_label_settings.tres` (gold/HP/Floor — labels revenus en `vertical_alignment CENTER` + SHRINK_CENTER), `PRICE_FONT` dans shop.gd/card_shop.gd (rangées construites en code), `theme_override_fonts/font` des PriceLabel de shop_card.tscn (21pt) / shop_relic.tscn (15pt). Le mot « Reroll » reste en Cinzel (label de bouton, pas un nombre) ; reroll affiche maintenant son prix en rouge si <20G comme le reste. **Bonus design : les prix de boutique sont maintenant en LuckiestGuy = la police « nombres » établie du jeu (top bar, scoreboard des end screens) — c'est AUSSI le look chunky STS.** **⚠️ Règle à retenir : tout futur nombre posé à côté d'une icône doit utiliser `fonts/luckiest_guy_numbers.tres` (jamais LuckiestGuy brut, jamais Cinzel) ; et ne JAMAIS calibrer un alignement sur un harnais qui ne reproduit pas la hauteur de conteneur réelle.** Le harnais trompeur de la passe 9 (`debug_topbar_measure`) est SUPPRIMÉ ; `debug_price_align` le remplace.
  - **Économie des dés CENTRALISÉE dans `global.gd`** (conséquence du deal cross-boutiques) : `DICE_TYPE_ORDER` (l'espace d'index de `shop_dice_selection`/`shop_dice_deal_index` : evil, giant, magma, even, odd, blue, red, green, mech), `DICE_BASE_PRICES`, `DICE_PRICE_ESCALATION` (1.4, ex-`GLOBAL_DICE_PRICE_ESCALATION` de shop.gd), `DICE_DEAL_DISCOUNT` (0.8), + `current_dice_price(type)` / `ensure_dice_shop_state()` (init premier-arrivé de la sélection, appelée par LES DEUX boutiques) / `pick_dice_deal_index()`. `shop.gd` (dice shop) n'a plus de prix de base locaux — **tout tuning de prix de dé se fait dans global.gd désormais.**
  - **Nouveau state run-scopé** : `Global.shop_dice_deal_index` (−1 = aucun/acheté, défaut vieilles saves via `.get`) et `Global.card_removals_bought` — tous deux dans `reset_run_state()` ET le save dict (règle habituelle respectée).
  - **Piège re-confirmé** : `var x := Global.gold >= prix` ne compile PAS (Global.gold non typé → inférence impossible) et **gdtoolkit ne l'attrape pas** (parser syntaxique seulement) — seul le boot Godot du harnais l'a révélé. Typer explicitement `: bool`.
  - **Décisions de la même discussion** : « plus de 5 cartes en boutique » refusé (contre-proposition retenue pour plus tard mais PAS construite : un slot carte pré-upgradée à prix premium ~1.6×) ; « garantir 1 rare par boutique » était déjà fait depuis le 07-15 (`SHOP_COMPOSITION`). **À surveiller au premier playtest : accumulation d'or en act 2** (revenus ×1.5 + tout est moins cher + le seul nouveau sink est le removal) — le bouton de tuning est désormais `Global.DICE_PRICE_ESCALATION`.

- **Écrans de fin de run (win/lose) REFAITS (2026-07-21, demande Julien "make it really pretty, direct to Discord") — VÉRIFIÉ PAR RENDU HEADLESS (3 screenshots : lose, win act-1, win final), NON PLAYTESTÉ EN JEU.** Trois écrans partagent le même langage (panneau navy 855×550 bordure or, titre CinzelDecorative, art encadré à gauche, scoreboard à droite, CTAs en bas) : **Game Over** (`battle_over_panel.tscn` — l'ancien VBox Label/Continue/Restart SUPPRIMÉ, remplacé par un LostPanel complet : titre rouge sang, `gameover.png` encadré, stats, boutons Main Menu (sobre) / Try Again (or, = reload) / **Join Discord (bleu blurple #5865F2 + logo)**) ; **Act 1 Complete** et **Dungeon Conquered** (les 2 variantes du `GGPanel` de `battle_reward.tscn`, art `gg.png`). Act-1 : message "Act 2 is an early preview: most of it will be properly reworked for the official launch… join the Discord!" + boutons Discord ET Continue to Act 2 côte à côte (avant, Discord était CACHÉ sur cette variante). Final : Discord (primaire) + Main Menu (le panneau final n'avait AUCUNE sortie avant — soft-lock réparé au passage). Main Menu = recette quit du pause menu (MusicPlayer.stop + SFXPlayer.stop + change_scene).
  - **Nouveau composant partagé `scenes/ui/run_stats_panel.gd`** (script sur un PanelContainer dans les 2 .tscn, **volontairement SANS class_name** — pas de régen de cache nécessaire, pattern achievement_toast) : scoreboard "Your Odyssey" construit 100% en code — 7 lignes (Floor Reached / Dice Rolled / Power Generated / Biggest Hit / Enemies Slain / Cards Played / Damage Taken), icônes = assets existants recyclés (map_button_icon, blue6, charge_dice_icon, swordicon, **runic_bones** pour les kills, card_cover_icon, heart), valeurs LuckiestGuy or avec séparateur de milliers, reveal en stagger + **count-up animé** (tweens bound aux enfants → tournent sur l'arbre PAUSÉ du game over, BattleOverPanel est PROCESS_MODE_ALWAYS). L'écran propriétaire appelle `animate_in()` après son entrance pop (scale 0.93→1 + fade).
  - **7 nouveaux compteurs `Global.run_stat_*`** (dice_rolled, power_generated, biggest_hit, damage_taken, cards_played, enemies_slain, highest_floor) — hooks aux choke points déjà utilisés par les achievements : `dice.gd::_apply_roll_result`, `damage_effect.gd::execute` (branche Enemy = biggest hit + branche Player = damage taken), `card.gd::play()`, `enemy.gd` à l'emit d'`enemy_died`, `run.gd::_update_floor_label` (maxi monotone, offset d'act inclus). **Reset dans `reset_run_state()` ET sauvegardés/restaurés** (clé `"run_screen_stats"` du save dict, `.get` avec défauts pour les vieilles saves) — la règle "tout state run-scopé va dans les DEUX endroits" respectée.
  - **Pièges rencontrés** : gdtoolkit refuse les strings multi-lignes à quotes simples (Godot les accepte) → le texte act-1 de `battle_reward.gd` est en `"""triple quotes"""` ; RichTextLabel affichait un ascenseur sur le message act-1 (débordement) → zone agrandie + `scroll_active = false` posé sur les 2 MessageLabels ; `var x := load(...).instantiate()` ne compile pas (type non inférable), typer explicitement.
  - **Écran de confirmation "Load Run" du main menu REFAIT dans le même langage (même session, 2e demande Julien)** : l'ancien panneau teal à une ligne de texte devient une carte "save slot" navy/or 640×420 — titre Cinzel "Continue Your Odyssey?", sous-titre "Act N · Floor N" (numérotation avec offset d'act, comme la top bar), scoreboard 5 lignes (Gold/Health/Cards in Deck/Relics (icône crown.png)/Dice Owned) avec count-up, boutons "Yes, that's my run!" (or shop-style) + Cancel (sobre) + X. **`run_stats_panel.gd` a été généralisé pour ça** : `build_rows(defs)` public (clear+rebuild), defs = `{label, icon, value:int}` (count-up) OU `{label, icon, text:String}` (statique, ex. HP "47 / 72") — le panneau du main menu lit le SAVE DICT (pas Global), `main_menu.gd::_populate_load_confirm_panel` construit les rows puis `_show_load_confirm_panel` joue le même pop+reveal que les end screens.
  - **Écran de RÉCOMPENSES post-combat restylé dans la même passe (3e demande Julien, choix via AskUserQuestion "Rewards panel")** : `RewardContainer` passe du `card_pressed_stylebox` gris au panneau navy/or (réutilise le sub_resource `StyleBoxFlat_ggpanel`), les rows de `reward_button.tscn` refaites (fond translucide + bordure or subtile, hover = bordure or vive + glow, texte Belwe 22 crème, icône 54px), bouton Continue = or shop-style Belwe 230×54 (était un bouton défaut 60×15), "127 gold"→"127 Gold" (`GOLD_TEXT`). **Le `WarningPanel` "You still have rewards to claim!" du même écran suit** (navy/or + NotoSans + bouton confirm or). Les 2 sub_resources orphelins (`StyleBoxFlat_8ubid`, ext `card_pressed_stylebox`) retirés du .tscn, load_steps décrémenté.
  - **⚠️ Positionnement vertical des récompenses — DEUX itérations, atterri sur HAUTEUR FIXE (choix final de Julien)** : (1) 1er retour "big empty space at the top" → j'ai essayé size-to-content (`Vector2(550, 0)` largeur seule + `alignment=1` sur la VBox EXTÉRIEURE) ; (2) **REJETÉ par Julien : "the panel changes depending on rewards, looks really bad when you take rewards and the panel shrinks. Keep size fixed like before"** — le size-to-content faisait rétrécir la carte quand on prenait une récompense (rows retirées dynamiquement par `_on_pressed` queue_free). **Solution finale : carte FIXE `custom_minimum_size = Vector2(550, 300)` + VBox interne `Rewards` en TOP-ALIGN (pas d'`alignment`, défaut BEGIN).** La carte ne change JAMAIS de taille : 2 rows (normal) ET 3 rows (élite, 276px < 300) tiennent dans la même hauteur fixe, et retirer une récompense ne rétrécit rien (min height tient). **3e retour Julien : `alignment=1` (centré) restait moche à <3 rows ("still empty space on top ... make first reward start from top, don't just vertically center") → repassé en top-align : les rows s'empilent depuis le HAUT, le vide tombe en BAS de la carte.** La VBox EXTÉRIEURE garde `alignment=1` (cluster centré à l'écran). **Leçon : Julien préfère une carte de taille STABLE avec contenu top-aligné (vide en bas) à (a) une carte qui se redimensionne dynamiquement, ET (b) un contenu vertically-centered qui laisse un gap en haut. Ne jamais faire size-to-content sur un conteneur dont le contenu est retiré interactivement.**
  - **Bouton "Join Discord!" ajouté au MAIN MENU (demande Julien, complète le "Feedback funnel" du launch checklist)** : blurple #5865F2 + logo, top-right à gauche de l'engrenage Settings (même langage que le bouton Discord du HUD in-game `run.tscn`), pattern Panel+Label+TextureRect+Button transparent identique aux end screens, `main_menu.gd::_on_discord_button_pressed` → `OS.shell_open` de l'URL Discord. **Il reste le lien Discord sur le main menu = FAIT ; le "Feedback funnel" du checklist est maintenant complet (death/victory/main menu).**
  - **Harnais `debug_endscreens.gd`/`.tscn` laissé à la racine (non committé)** : rend 7 écrans (rewards 2-row / rewards élite 3-row / lose / win act-1 / win final / load confirm / main menu) avec des stats factices dans un SubViewport et sauve les PNG — relancer après toute retouche visuelle de ces écrans. **Julien a playtesté les end screens + load card en jeu et ADORE.** Les items "Feedback funnel" et "Run summary on death/victory" de `launch_checklist_2026-07.md` sont couverts (il reste le lien Discord sur le MAIN MENU, non fait).

- **Bug Dice Avalanche corrigé (2026-07-21, retour Julien "occultism into dice avalanche didn't throw giant dice")** : le check "quels dés je possède" utilisait UNIQUEMENT `%s_dice_max_amount` (le loadout permanent, acheté en boutique) — un Giant Dice obtenu SEULEMENT via Charge (Occultism, un Charge/relique quelconque, jamais acheté) a `max_amount = 0` donc était skippé du jet, alors que le joueur en avait bel et bien un disponible ce tour-ci. **Fix : le check devient `max_amount > 0 OR current_amount > 0`** (élargissement strict, ne régresse rien — un type possédé en permanence mais entièrement dépensé ce tour reste inclus grâce à `max_amount`) — appliqué aux 2 endroits qui dupliquaient l'ancien check (la boucle de jet ET `_owned_type_count()` utilisé par le nombre affiché au survol "(N Dice)") dans `dice_avalanche.gd` ET `dice_avalanche_plus.gd`.
- **20 NOUVELLES CARTES DRAFTABLES (2026-07-20, pool 67→87) sur specs filtrées par Julien (brainstorm "make Strike less good" → 2 vagues d'idées → verdicts numérotés, puis 2e passe de corrections avant son coucher) — NON PLAYTESTÉ (gdtoolkit parse-check seulement, AUCUN rendu/boot).** 10 Commons (Meteor, Cursed Toss, Pixie Volley, Double or Nothing, Low Roller, Earthquake, War Ritual, Compound, Rampart, Kickstart), 5 Uncommons (Fastball, Mirror Blow, Stampede, Coiled Spring, Windfall), 5 Rares (Dice Avalanche, Blackjack, Overclock, Grand Scheme, Dicelord's Gift — le pool rare DOUBLE 5→10). Décisions Julien via AskUserQuestion : **Blackjack (Exact 21 : Kill) tue TOUT y compris boss/Leviathan** ; Compound = Common SANS exhaust ; **versions "+" DIFFÉRÉES** (il les spec plus tard — les 20 ne sont PAS upgradables à la campfire d'ici là) ; "Summon Meteor" renommé **Earthquake** (collision de nom avec Meteor + l'art orphelin `earthquake.png` collait). "Gift From The Dicelord" renommé **"Dicelord's Gift"** (22 chars, aurait forcé le title system à 9pt ou moins). **Corrections 2e passe (verdicts Julien)** : Coiled Spring = Celestial + no-reset ; War Ritual et Compound RESETTENT le Power (dice_roll_reset ajouté aux scripts, rarity flag repassé NORMAL) ; Compound charge **2** Blue Dice au lieu d'1 ; Overclock/Grand Scheme confirmés Celestial+no-reset (déjà le cas). **+3 cartes "throw non-attack" validées en passant** : Windfall (U, "Throw a Giant Dice: add its roll to your Power", no-reset par construction, art `high_roller.png`), Rampart (C, "Block X. Throw an Even Dice that Blocks its roll", reset, art `gptbarricade.png`, son blocksound), Kickstart (C, "Throw an Odd Dice: Boost its roll" — Odd sur demande explicite de Julien, no-reset, art `tuning_fork.png`) — leurs effets (power/block/boost) sont DIFFÉRÉS à l'atterrissage du dé (timer partagé DICE_THROW_FLIGHT_TIME, target null dans dice_thrown = le dé atterrit en l'air au-dessus de la carte, fallback prévu du handler).
  - **Nouveau primitif visuel "dé jeté"** : `Events.dice_thrown(throws, origin)` → `dice.gd::_spawn_thrown_dice()` (icône de face qui vole en arc vers l'ennemi en CYCLANT des faces aléatoires mid-flight — `_start_die_tumble` — puis snap sur la face FINALE à l'atterrissage + punch ; crack sound si evil 0). `Events.coin_flip(heads, origin)` → toss de `daiso_coin_icon.png` (asset orphelin), spin par oscillation de scale.x, reveal doré (heads) ou désaturé+chute (tails). **Timing partagé via `Global.DICE_THROW_FLIGHT_TIME`/`_STAGGER`/`COIN_FLIP_TIME`** — les dégâts sont schedulés côté carte par `Card._land_thrown_die()` (SceneTreeTimer pause-safe) pour atterrir PILE quand le dé/la pièce arrive ; si la cible meurt mid-flight, le dégât REBONDIT sur un ennemi vivant aléatoire (le visuel, lui, garde la position capturée au spawn — mensonge spatial mineur assumé). **REFAIT EN 3 BEATS le 2026-07-21 (demande Julien "plus long, plus hype, plus d'impact") — NON PLAYTESTÉ** : (1) WINDUP — le dé pop hors de la carte et plane au-dessus en tumblant, coil (stretch vertical) avant le launch ; (2) FLIGHT — whoosh `whipsound.mp3` (priorité -1, pitch jitter) + arc accélérant, swell mid-arc, traînée de motes accent (throttle 28ms temps réel) ; (3) IMPACT — flash surbrillant + squash dur + burst radial de motes + shock puff additif (alpha 0.5, leçon additive-stacking), linger 0.45s puis le dé TOMBE du corps (cible) ou se dissout (air-land). **⚠️ `DICE_THROW_FLIGHT_TIME` (0.55→0.95) est désormais le temps TOTAL emit→atterrissage** — le windup (`DICE_THROW_WINDUP_TIME` 0.32) est taillé DEDANS par dice.gd, les 10 scripts de cartes n'ont pas bougé et restent synchro par construction ; stagger 0.12→0.15. **Vrai bug corrigé au passage : le dé atterrissait sur le `global_position` du ROOT ennemi, ~124px À GAUCHE du corps** (le Sprite2D est baké à x=124 local, même baseline que le fix de centrage du nom) — vise maintenant `sprite_2d.global_position` (centre du torse) via `_thrown_die_impact_pos()`, + scatter ±16px par dé sur les volleys. Face MAX du type = flash plus chaud + burst doré (`DicePalette.burst`) + puff plus gros (micro-jackpot). Air-lands (Windfall/Rampart/Kickstart) : clack d'atterrissage ajouté (`dicerollsound3.mp3` pitché 1.35-1.5, placeholder) — ils étaient 100% silencieux avant.
  - **⚠️ Décision design INVERSÉE le 2026-07-21 : la Strength s'applique bien aux dégâts d'un dé jeté** (le premier jet du 2026-07-20 disait l'inverse — "son roll BRUT, jamais modifié" — Julien a tranché explicitement pour la cohérence avec le reste du pool, aucune autre carte du jeu n'a de dégâts qui ignorent la Strength). Appliqué via `modifiers.get_modified_value(value, Modifier.Type.DMG_DEALT)` sur les 4 cartes qui infligent des dégâts (Meteor, Cursed Toss, Pixie Volley, Dice Avalanche) juste avant `_land_thrown_die()` — le DMG_TAKEN de la cible (Exposed) s'appliquait déjà dans `take_damage`, inchangé. **Dice Avalanche (jusqu'à 9 dés) est de loin le plus gros multiplicateur de Strength du pool** — Strength s'applique une fois PAR dé jeté, à surveiller en priorité au playtest, watch-list si un futur rééquilibrage est nécessaire. Windfall/Rampart/Kickstart non concernés (Power/Block/Boost, jamais touchés par un modifier DMG_DEALT). `Card.DICE_FACE_VALUES` (const partagé dans card.gd, copie de la table d'all_in.gd) = source des faces par type pour tous les jets.
  - **5 nouveaux statuts** : 4 one-shot "next turn" (`status_earthquake` — stacks = dégâts X2 lockés au play, AoE au début du tour suivant ; `status_war_ritual` — Charge 2 random ; `status_compound` — draw 2 + Charge 1 blue ; pattern = START_OF_TURN + can_expire + duration 1, le decrement de StatusHandler les retire après le payout) + `status_coiled_spring` (EVENT_BASED façon Opening Gambit : s'ARME au player_turn_started suivant, premier roll compte TRIPLE via `roll_value += last_roll*2`, puis self-remove par `duration = 0` — **avec garde _owner + disconnect explicite, contrairement à Opening Gambit qui LEAK cross-battle, chip spawn_task créé pour ce bug latent**) + `status_dicelord_gift` (permanent, Charge 1 random chaque tour, cadence Steady Hand). **Ordre vérifié : le refill de dés de dice_interface est SYNCHRONE sur player_turn_started, les statuts s'appliquent APRÈS (chaîne tween relics→statuts) donc une Charge de statut n'est jamais écrasée par le refill ; la main est piochée APRÈS les statuts** (donc le draw de Compound rejoint la main du tour). Icônes = placeholders PIL (or/brun, `status_*_icon.png` à la racine) à régénérer par Julien via son pipeline Firefly.
  - **Découverte structurante : le reset de Power est 100% CARD-DRIVEN** — chaque carte émet elle-même `Events.dice_roll_reset` dans apply_effects, il n'existe AUCUN reset central keyé sur `rarity` (le `rarity != SUPPORT` de hand.gd:321 documenté comme "le flag no-reset" n'est en réalité QUE de la logique de glow rouge). Double or Nothing repose dessus : le reset n'est émis QUE sur heads, au REVEAL de la pièce (callback différé 0.6s) — edge assumé : re-roller pendant le flip puis heads mange aussi le nouveau roll.
  - **Art : la plupart des cartes recyclent l'art ORPHELIN déjà dans le repo** (scan systématique des images non-référencées + art des cartes coupées) : javelin→Fastball, fireflies→Pixie Volley, nova→Dice Avalanche, jackpot→Double or Nothing, siphon→Mirror Blow, executioner→Blackjack, earthquake→Earthquake, lunge→Stampede, elasticity→Coiled Spring, inauguration→War Ritual, stockpile→Compound, resonance_good→Overclock, sigil→Grand Scheme, high_roller→Windfall, gptbarricade→Rampart, tuning_fork→Kickstart. **ART RÉEL LIVRÉ par Julien le 2026-07-21** (dossier `C:\Users\julie\Desktop\more daiso art`, traité rembg+crop) : `meteor.png` (Meteor), `low_roller.png` (Low Roller), `dicelord_gift.png` (Dicelord's Gift — figure encapuchonnée cornue offrant des dés, remplace le placeholder scroll.jpg et le choix annulé "The One.png"), **`cursed_toss.png`** (Cursed Toss, figure violette maudite à sigil-œil — DÉPLACÉ hors de `dark_pact.png` sur demande de Julien : `dark_pact.png`/`dark_pact_v2.png` sont déjà pris par la carte coupée Dark Pact et par Necromancy). **Les 5 icônes de statut placeholders PIL remplacées par le vrai art** (die-cut sticker, rembg+normalize) : `status_earthquake_icon.png` (ambre/terre), `status_war_ritual_icon.png` (rouge+cercle runique), `status_dicelord_gift_icon.png` (violet/or halo+couronne), `status_compound_icon.png` (spirale bleue), `status_coiled_spring_icon.png` (ressort teal). Les .tres des cartes coupées (jackpot/stockpile/etc.) référencent toujours ces PNG — sans conséquence (non-draftables). **Bonus non câblés dans le dossier de Julien** (art qu'il aime, "pas encore utilisé") : executioner cloaked / high-roller gambler / tuning fork dédiés / arcane sigil (pourraient upgrader Blackjack/Windfall/Kickstart/Grand Scheme qui sont sur de l'art recyclé), + tidal wave, corrode rust, energy beam, armor cracking, brass metronome device, etc. (mappent vers cartes existantes/coupées hors de ce batch) — laissés à trancher par Julien.
  - **⚠️ Avant tout playtest/rendu : redémarrer l'éditeur** (5 nouveaux `class_name` de statuts pas dans le cache + nouvelles PNG à importer) ; pour un rendu headless, `--headless --editor --quit-after 900` d'abord (piège class_name cache documenté). Mirror Blow lit le NOMBRE affiché de l'intent (`current_action.intent.current_text`, parse "12" et "6x2", gated sur la présence d'une propriété `damage` sur l'action) — si un futur ennemi formatte son intent autrement, ce parse est le point fragile.
  - **⚠️ Règle confirmée par Julien (2026-07-21) : aucune carte Celestial (`can_play_without_dice=true`) ne doit resetter le Power** — audit complet des 87 cartes draftable (script + `can_play_without_dice` réels, pas juste le flag `rarity`) fait pour répondre à "les 47 cartes no-reset c'est obvious non ?" : **le flag `rarity=SUPPORT` NE GARANTIT PAS l'absence de reset** (6 paires de cartes flag SUPPORT resettent bel et bien, intentionnellement — Recombobulate/Voodoo/Catalyst/Occultism/Supplication/Double or Nothing, le refuel EST censé resetter). **3 vraies violations de la règle Celestial trouvées et CORRIGÉES** (ligne `Events.dice_roll_reset.emit()` retirée, 6 fichiers touchés base+plus) : **Adrenaline** (Charge 2/3 Red Dice resettait), **Occultism** (Charge 1 Giant Dice resettait), **Supplication** (Charge 3/4 Pixie Dice resettait dans sa branche "no dice left"). **Spark vérifié DÉJÀ correct** (ne resette jamais, malgré le soupçon initial de Julien). **Faux positif détecté et corrigé sur Shockwave** : un bug dans le script d'audit Python (ne strippait pas les commentaires `#` avant de chercher `dice_roll_reset.emit`) avait signalé Shockwave comme resettant — en réalité son unique ligne de reset était DÉJÀ commentée (`#Events.dice_roll_reset.emit()`) par une session antérieure, le code matche déjà son texte "Does not reset your Power", **aucun fix nécessaire**. Bolster pareillement mal classé par le même bug (mais non-Celestial, sans conséquence, resette seulement quand Muscle s'applique réellement — comportement normal). **Tableau complet des 87 cartes (nom/description/celestial/reset réel) livré à Julien dans le chat**, pas committé dans ce fichier — à reconstruire via le script d'audit si besoin (non committé, scratchpad de session).
  - **Les 17 versions "+" CONSTRUITES le 2026-07-21** (sur specs de Julien, plus DIFFÉRÉES) — les 17 cartes sont maintenant upgradables à la campfire. **2 changements de carte de BASE inclus** : Meteor base passe **Min 5** (`requirement=1/n=5`, gate `meets_requirement()` ajouté à `meteor.gd`, + retire le gate en `requirement=0`) ; **base Fastball applique maintenant la Strength** (oubli de la passe "Strength sur dés jetés" du 2026-07-21 — Fastball inflige aussi des dégâts de dé jeté, corrigé pour cohérence). **7 "+" REUTILISENT le script de base** (changement 100% dans le .tres : Meteor+ req0, Pixie Volley+ Max6, Mirror Blow+ no-req, Coiled Spring+/Grand Scheme+/Dice Avalanche+... non — Dice Avalanche+ a son propre .gd car sa dynamic desc hardcode "Exhaust" ; donc 6 reuse : meteor/pixie_volley/mirror_blow/coiled_spring/grand_scheme/dicelord_gift) — **11 nouveaux .gd** (fastball/cursed_toss/double_or_nothing/low_roller/blackjack/earthquake/stampede/war_ritual/overclock/compound/dice_avalanche) pour les changements de nombre hardcodé (triple/×3/20-x/3 hits/etc.). **Blackjack+ = kill + `Global.gold += 21`** (nouveau, via `Events.gold_changed`). **Compound+ a son propre statut `CompoundPlusStatus`** (draw 3 / charge 2 découplés, le statut de base couple 2/2 via stacks — même pattern que Marionette+/Steady Hand+/Critical Edge+). Overclock+ garde Exhaust (Julien n'a pas dit de le retirer) ; Dice Avalanche+/Coiled Spring+/Grand Scheme+ retirent Exhaust. **Les "+" ne sont PAS dans le pool draftable** (atteignables uniquement par upgrade campfire, comme toutes les "+" — référencées par `upgraded_version` sur la base, load_steps bumpé 4→5). Tout vérifié : parse gdtoolkit OK, wiring upgraded_version/upgraded=true/uids uniques/flips req+exhaust confirmés au champ près. **NON PLAYTESTÉ.**
  - **Nouveau mot-clé "Throw" (2026-07-21)** ajouté à `KeywordColorizer.KEYWORDS` + `tooltip.gd` ("Rolls an extra Dice, separate from your own, that resolves moments later with its own result") et tagué sur les 7 cartes qui conjurent un dé bonus (Meteor, Cursed Toss, Pixie Volley, Dice Avalanche, Windfall, Rampart, Kickstart) — **Fastball exclu à dessein** : il DÉPENSE un dé de votre pool actif au lieu d'en conjurer un gratuit, donc le tagger "Throw" aurait effacé la distinction coût-vs-bonus. Le tooltip reste volontairement agnostique sur CE QUE fait le résultat (dégâts/Block/Boost/Power varie par carte, chaque carte le précise déjà dans son propre texte) — seul Windfall ajoute réellement à la Power, les 6 autres résolvent en un effet séparé (dégâts/Block/Boost) livré ~0.5s APRÈS l'effet principal de la carte, jamais avant. Vérifié par simulation Python de la regex de `colorize()` sur les 7 descriptions avant de committer — "Throw" ne s'absorbe jamais dans la couleur du type de dé (toujours séparé par "a"/"an"/"X"), rend donc en 2 couleurs distinctes (or + couleur du dé) sur chaque carte, aucun conflit avec Exhaust/Boost déjà tagués sur Dice Avalanche/Kickstart.

- **Passe de fixes sur retour de playtest de Julien (2026-07-20, act 1 + act 2 joués « très bien » globalement) — NON RE-PLAYTESTÉ.** 5 points :
  - **Power qui restait ROUGE en passant vite du dé Rouge (carte jouée) au Bleu** : les flashs de couleur du nombre de Power (roll + arrivée d'orbe, `dice.gd`) tweenent `current_power.modulate` vers un SNAPSHOT de la couleur prise au lancement du flash — changer de dé pendant le flash (< 0.2s) restaure la couleur de l'ANCIEN dé par-dessus la nouvelle posée par `update_dice_display()`. Fix : les 3 flashs (roll/arrivée → nouveau membre `_power_color_flash_tween`, clang → `_power_clang_flash_tween` existant) sont tués dans `_on_active_dice_changed` avant `update_dice_display()`.
  - **Statuts d'ennemi cachés derrière la main de cartes** (Julien : « on va rater le statut du Lich ») : **⚠️ PREMIER JET RATÉ ET REVERTÉ** — j'ai d'abord déplacé le `StatusHandler` dans une `CanvasLayer` (layer=10) pour le rendre AU-DESSUS des cartes. Deux problèmes : (a) ça a CASSÉ l'affichage des statuts EN JEU (marchait pourtant en rendu harnais — l'offset `get_canvas_transform()` posé à l'instanciation devait mal résoudre en jeu, cause exacte non creusée puisque revert de toute façon), et (b) **Julien : rendre les statuts PAR-DESSUS les cartes n'est pas voulu non plus** (« on ne voudrait même pas que le statut aille SUR les cartes »). **Reverté à 100%** (StatusHandler redevient enfant direct de l'Enemy, positionnement/clamp/contre-scale d'origine). **La vraie cause + fix** : mesuré au probe (`probe_status.gd`, supprimé après) la position écran du statut de chaque ennemi vs le haut de l'éventail de cartes (fan qui remonte de 20px au-dessus de la baseline y581 de la Hand → haut de carte à y561-581). **UN SEUL ennemi chevauche vraiment : le Lich** (statut à y576, soit ~20px PLUS BAS que tous les autres solos ~553 — parce que c'est un gros élite `height=292`, sa barre pend plus bas). Tous les autres sont clairs ou marginaux (Defender de `tier_2_defender_machopeur` à −4px, laissé). **Fix = bouger le Lich légèrement** (solution demandée par Julien) : `tier_elite_lich.tscn` `position.y` 396→376 (monté 20px), son statut monte à ~y556 = même niveau que les autres élites, dégagé des cartes comme eux. Vérifié par rendu : le Lich reste grounded (pieds près du sol, ombre présente — 20px imperceptible sur un solo). **Piège à retenir : les statuts sont collés SOUS la barre de vie, elle-même à la ligne de sol ; comme le bas de l'écran est occupé par les cartes, un ennemi dont la barre pend trop bas (gros élite) a son statut dans la zone des cartes. On ne peut pas monter le statut sans heurter la barre → il faut monter TOUT l'ennemi (léger float accepté sur un solo), ou accepter le chevauchement marginal. Rendre le statut au-dessus des cartes = refusé par Julien.**
  - **Pile Exhaust qui chevauchait les ennemis (droite)** : **⚠️ trois itérations avant la bonne** — bas-gauche rejeté (trop près du perso joueur), puis haut-droite deviné à l'aveugle contre le mauvais repère (j'avais supposé une icône Discord DANS `battle.tscn`, or celle-ci — `JoinDiscordControl`, ligne ~490 — est `visible=false` et positionnée à GAUCHE ; l'icône Discord réellement visible en permanence en haut à droite vit dans le `CanvasLayer layer=100` de **`run.tscn`** (au-dessus de TOUTES les vues, donc visible même pendant un combat), rect design x:[1210,1276] y:[83,124] — trouvé seulement en grep-ant tout le repo pour "discord", pas en devinant sur une capture). **Fix final, calé sur ce vrai repère** : `battle.tscn` `ExhaustPileButton` anchors_preset 1 (top-right), offset_left -116 / offset_top 132 / offset_right -20 / offset_bottom 216 → design x:[1164,1260] y:[132,216], juste sous l'icône Discord (gap 8px), largeur 96px alignée sur Draw/DiscardPileButton. **Tooltip** (`battle.gd::EXHAUST_PILE_TOOLTIP_POS`) : le panneau `tooltip.tscn` fait 204×108, `show_tooltip(pos)` pose son coin HAUT-GAUCHE — posé à (950,120) pour qu'il POP à GAUCHE de l'icône (gap ~10px) et reste entièrement à l'écran (panel_left=950 > 0), plutôt qu'en dessous (chevaucherait l'ennemi de droite) ou à droite (déborderait). ⚠️ Non vérifiable en harnais (BattleUI ne se peuple pas) — à confirmer en jeu.
  - **Texte d'achievement trop long pour le toast** (ex. Glass Cannon « Take 20 or more damage in a single hit and live to tell the tale. ») : `achievement_toast.gd` — toast 92→110px de haut, boîte de desc 252×32 → 258×50 à (86,52), police 13→12. Vérifié PIL avec la vraie police (Roboto Condensed SemiBold) que les 4 descriptions les plus longues wrap toutes en 2 lignes (~30px) largement dans la boîte de 50px.
  - **Carte « Oracle » de Calculations renommée au standard Scout** : `card_oracle_exhaust.tres` (donnée en main par `calculations.gd`) passait « Oracle » / « Exhaust: Scout 3 » → **« Scout 3 » / « Scout 3. Exhaust »**, alignée sur les autres cartes Scout (`card_scout3.tres` : name « Scout 3 », desc « Scout 3. Exhaust »). Standard confirmé : toute carte Scout qui exhaust = name « Scout N » + desc « Scout N. Exhaust » ; seule la Scout 3 du starter deck (`card_scout3_no_exhaust.tres`) n'exhaust pas. `calculations_plus.gd` donne déjà `card_scout5.tres` (correct). **`card_oracle.tres`** (non-exhaust, name « Oracle », desc « Scout 3 ») laissé tel quel — référencé seulement dans `warrior_draftable_cards_copy.tres` et `warrior_draftable_support_cards.tres` (pools `_copy`/`_support` probablement morts, PAS le pool draftable live) ; à vérifier/corriger si Julien le voit apparaître en jeu.
- **REFONTE SYSTÉMIQUE du positionnement d'ennemi (2026-07-19, même session que le reskin act-2 ci-dessous, choix explicite de Julien via AskUserQuestion : « Systemic fix » plutôt que des point-fixes ou une réduction globale de taille) — vérifiée par re-rendu complet des 57 combats act-1 + 22 act-2, PAS ENCORE PLAYTESTÉE.** Contexte : après ~5 sessions de corrections de positionnement au coup par coup (la tâche la plus re-reviewée du projet, constat de Julien), 3 nouveaux bugs sur screenshots (pieds du Marauder sur sa barre de HP, statuts d'Oculus sous le bouton End Turn, pattes arrière du Lava Hound flottantes). Julien proposait de réduire la taille des ennemis façon STS2 — **répondu honnêtement que la taille n'aurait corrigé AUCUN des 3 bugs** (ancrage/clamp/pose, pas d'échelle) ; il a retenu le fix systémique. Trois mécanismes, tous dans `enemy.gd::update_enemy()` :
  - **`feet_line_y` = source unique de vérité pour « où sont les pieds », TOUT en dérive (refonte 2026-07-20 après ~6 itérations ratées de chevauchement pieds↔barre — Julien : « overlapping destroys readability »).** Deux morceaux dans `enemy.gd::update_enemy()` :
    - **Ancrage des pieds sur la bbox alpha** : `feet_line_y = sprite_y_offset + sprite_display_height/2` (le BAS DE LA BOÎTE = le repère de sol que le level designer a tuné via `position.y`, en supposant que le sprite remplit sa boîte). Le bas du CONTENU réel (`_get_content_rect`/`get_used_rect`, caché dans `_content_rect_cache`) est posé sur `feet_line_y` : no-op pour une texture sans padding, descend le sprite si padding bas (pieds visibles au sol au lieu de flotter). **⚠️ NE JAMAIS ancrer à la barre** (fait puis reverté) — un combat peut remonter sa barre hors des pieds (`stats_ui_y_offset` négatif), ancrer dessus a fait flotter l'ennemi de ~30px + intent sur le visage.
    - **⭐ LE VRAI FIX du chevauchement pieds↔barre : la barre de vie et les statuts DÉRIVENT de `feet_line_y`, ne peuvent JAMAIS remonter dans le sprite.** `stats_ui.position.y = feet_line_y + maxf(stats_ui_y_offset, 0.0)` — la barre est POSÉE à la ligne des pieds ; les `stats_ui_y_offset`/`status_handler_y_offset` ne survivent que comme nudges VERS LE BAS (`maxf(.,0)`). Avant, la barre était calculée depuis la boîte + offset per-fight, et un offset NÉGATIF (Marauder −24, Vortex −10, Lurker+Crab −8) la tirait DANS le corps → chevauchement. Le `maxf(.,0)` rend ça structurellement impossible quel que soit le tuning présent OU futur (la spaghetti de Julien ne peut plus casser ça). **Zéro changement pour les 30+ ennemis à offset 0** (formule identique), fixe les 3 à offset négatif. `intent_ui` reste box-based (au-dessus de la tête, `intent_ui_y_offset` toujours honoré). `_name_label_local_y`/statuts dérivent de `stats_ui.position.y`. Vérifié par re-rendu complet 57 combats : barres SOUS les pieds partout, aucun chevauchement.
    - Ombre de sol posée à `feet_line_y + 5` (inchangé, voir plus bas).
  - **Ombre de sol** (`_ground_shadow`, `Sprite2D` + `GradientTexture2D` radial partagé en static, construit en code — zéro asset) : ellipse douce posée à la ligne des pieds (`feet_line_y + 5`), **enfant du root Enemy et PAS de SpriteRoot** (l'idle bob anime SpriteRoot — le corps bobbe, l'ombre reste posée, look STS), `move_child(0)` pour rendre derrière le sprite. Vend le grounding même pour un art en pose diagonale (Lava Hound : AUCUN offset ne peut « poser » ses 4 pattes, l'art est dessiné en bond — l'ombre le fait lire comme bondissant au-dessus de son ombre ; c'était la vraie réponse au screenshot). Largeur = 0.82 × largeur de contenu réel, centrée sur le contenu réel. Fonctionne aussi pour les floaters (Lurker/Maelstrom) : l'ombre suit sous les tentacules/la traîne qui descendent vers la barre.
  - **Clamp de la rangée de statuts (⚠️ recalibré après une régression)** : `STATUS_ROW_MAX_GLOBAL_X = 970` + garde verticale `STATUS_ROW_CLAMP_MIN_GLOBAL_Y = 500` — le start de l'HBox de statuts est tiré vers la gauche seulement si l'ennemi est à la fois assez à DROITE (start > 970) ET assez BAS (> 500) pour heurter le bouton End Turn (x 1060-1254). **Le premier jet à 910 était trop agressif et a DÉTOURÉ le statut de Gargantua de 41px vers la gauche** (régression signalée par Julien) : mesuré au probe, Oculus démarre à gx=1017 (le vrai coupable qui chevauchait End Turn), Gargantua à 951 (largement dégagé, ne doit PAS bouger). Le seuil 970 sépare les deux ; la garde Y évite de tirer le statut d'un ennemi placé haut. Constante écran assumée (caméra de combat identity, pas de handle propre vers la CanvasLayer BattleUI depuis enemy.gd).
  - **Piège d'enquête à retenir** : le fix marchait en rendu HARNESS mais Julien voyait toujours l'overlap EN JEU — soupçonné `get_image()` qui échouerait en jeu (FAUX, il marche) puis le bruit alpha (FAUX, ≤1px) puis le bob d'idle (FAUX, 4px max). **La vraie cause était une erreur de logique (ancrage boîte vs barre), pas une divergence harness/jeu** — mais il a fallu écrire un `probe_feet.gd` qui mime le chemin `enemy_handler` (instantiate→duplicate→dans l'arbre) et imprime pieds/barre/statut au pixel pour la trouver. Leçon : quand harness et jeu semblent diverger sur du positionnement, mesurer les NOMBRES sur le vrai chemin d'instanciation avant de blâmer le runtime — c'était de la géométrie fausse, visible dès qu'on imprime `gap(feet-bar)` (+15 pour Marauder, ~-9 pour les autres).
- **Roster de l'act 2 reskinné (2026-07-19, demande de Julien « je ne veux pas d'assets d'ennemis répétés » pour donner l'illusion d'un vrai act 2, lazy explicitement assumé « je le ferai proprement plus tard ») — NON PLAYTESTÉ EN JEU (vérifié par rendu headless des 22 combats act-2 + parse gdtoolkit, jamais joué dans Godot).**
  - **Les 17 types d'ennemis atteignables en act 2 reçoivent une nouvelle illustration + un nouveau nom AU RUNTIME**, au lieu d'être des ennemis d'act 1 avec un buff Strength visible. **Zéro nouveau fichier `.tres`/`.tscn` d'ennemi** — tout est un swap runtime dans `battle.gd::_apply_act2_scaling()` : nouveau dict `ACT2_RESKIN` (nom de design act-1 → `{art, nouveau nom}`) + helper `_reskin_enemy()` qui pose `stats.art`/`_display_name`/`content_center_x=0.5` puis rappelle `update_enemy()`. **Les moves/AI restent ceux d'act 1** (même scripts d'action, mêmes intents) — seuls le look, le nom, les HP (mult inchangé) et le flat damage changent. Keyé sur `enemy._display_name` (le nom de design, fiable, posé dans `set_enemy_stats` avant le duplicate). `load()` au runtime (pas `preload`) → si une texture n'est pas importée, le swap est skippé (garde l'art d'act 1) plutôt que de planter.
  - **Dégâts « bakés » dans la base au lieu d'un statut Strength visible** : `_apply_act2_scaling` n'ajoute plus un statut Muscle (l'icône Strength trahissait l'ennemi recyclé) mais une `ModifierValue` FLAT invisible de source `"act2_power"` sur le modifier `DMG_DEALT` (`_bake_bonus_damage()`) — **MÊMES chiffres qu'avant** (l'ancienne table `ACT2_MUSCLE_BASE` renommée `ACT2_DAMAGE_BASE`, valeurs identiques 2/3/4/5/4), nourrit dégâts réels ET intent exactement comme Muscle le faisait, mais **aucune icône**. C'est exactement le « mettre la base à 16 au lieu de 11 base + 5 strength » demandé par Julien (effet identique, invisible). Clé de source distincte → empile proprement par-dessus le Muscle propre d'un ennemi (Goblin qui se buff, Oculus/Parasite, Gargantua/Greedy) sans l'écraser.
  - **Assets** = les 17 « Full-body single creature illustration » générés par Julien (Adobe Firefly, dans `Adobe_2026-07-15 (2)/(3).zip` de Downloads — le dossier qu'il a pointé). **Piège évité** : un PREMIER essai a extrait des créatures depuis des planches Firefly « 4 créatures par image » (Downloads, `Firefly_GeminiFlash_...enemy creature...`) — écarté quand Julien a précisé le bon dossier ; les single-creature sont bien plus propres. Pipeline : rembg (les PNG Firefly sont des RGB opaques avec un damier de fausse-transparence PEINT, cf. mémoire `feedback_ai_icon_cleanup_pipeline`) + retrait des specks + crop bbox alpha, sortie dans `assets/enemies_act2/act2_*.png`.
  - **Mapping (design act-1 → nom act-2 = créature)** : Skeleton→**Gnawer** (sandworm), Satyr→**Screecher** (harpie), Kraken→**Deepling** (pieuvre rocheuse), Goblin→**Bog Hag** (sorcière-gobelin), Venom Bloom→**Thornheart** (tréant), Lurker→**Harlequin** (bouffon-assassin), Marauder→**Ravager** (ogre en armure à morgenstern, sheet 4-pack `D`), Temple Defender→**Warden** (tréant #2), Oculus→**Onlooker** (pieuvre borgne — 1 œil, colle à Oculus), Sigil Slug→**Wisp** (feux-follets/lanternes hantées), Lava Hound→**Ember Fiend** (démon de lave), Medusa→**Gorgon** (méduse — littéralement), Maelstrom→**Tempest** (aigle-tempête), Lich→**Necromancer** (nécromancien à orbes rose/bleu, sheet 4-pack `D`), Dragon Priest→**Cinderlord** (démon ailé de lave), Gargantua→**Devourer** (sandworm #2), Leviathan→**The Dicelord** (nécromancien-dés à capuche cornue, boss). Doublons de concept assumés (2 sandworms, 2 tréants) posés sur des ennemis qui ne coexistent jamais à l'écran.
  - **⚠️ INCIDENT à retenir pour tout futur lot d'assets « neufs » : DEUX des 17 singles étaient DÉJÀ intégrés au jeu comme art d'act 1** — le troll de pierre (z3/763788) EST `machopeur_v2_no_shadow.png` (le Marauder actuel) et le nécromancien-dés (z3/746663) EST `gptlich.png` (le Lich actuel) : les remplacements d'art du 2026-07-16 venaient du MÊME batch Adobe. Attrapés (le troll par Julien en jeu, le lich par scan) via **comparaison systématique par corrélation** (alpha-crop → 48×48 gris normalisé → corrélation ; les dupes sortent à 0.997+, le max des non-dupes ~0.66 — seuil net). Remplacés par 2 créatures du sheet 4-pack `D` (Downloads, `Firefly_GeminiFlash_Create four different enemy creature assets...157903`). **Toujours scanner un nouveau lot d'assets IA contre l'art déjà intégré avant de l'utiliser — les batches de génération de Julien recoupent ce qu'il a déjà shippé.**
  - **Bug de centrage du nom d'ennemi au survol corrigé pour TOUT LE JEU au passage** (signalé par Julien : « WARDEN pas centré sous sa barre 45/45 ») — `enemy.gd::_on_mouse_entered` centrait le label sur le centre du SPRITE (baseline 124) en utilisant des coords CANVAS brutes comme coords ÉCRAN. Deux erreurs : (a) le vrai repère visuel est la barre de vie rouge (`StatsUI/Health/HealthBar`), pas le sprite — et **`StatsUI` est un `HBoxContainer`, donc son propre `get_global_rect()` center ≠ le centre de la barre** (la barre est un enfant parmi Block/Health/Margin) ; (b) le label vit dans un `CanvasLayer` (espace écran), il faut donc passer sa position canvas par `get_viewport().get_canvas_transform()`. **Fix : centre le label sur `stats_ui.get_node("Health/HealthBar").get_global_rect()` transformé en écran** (fallback sur le rect StatsUI si le nœud manque). Vérifié par rendu A/B : centre act 1 (Temple Defender/Marauder) ET act 2 pareil. `content_center_x` devient de fait inutilisé pour le nom (laissé en place, inoffensif). **Piège méthodo confirmé (mémoire `feedback_enemy_name_label_centering`) : mesurer le centrage sur des PIXELS rendus, pas sur la géométrie in-engine du label (le rect du label CanvasLayer-nested est trompeur) — et attention, un seuil couleur naïf attrape le dallage du sol + les chiffres dans la barre, préférer l'inspection visuelle zoomée.**
  - **Revue de positionnement des 22 combats act-2 faite** (harnais de rendu headless `debug_act2_reskin.gd`/`.tscn` laissé à la racine, non committé, même principe que `debug_bg_audit` mais applique le vrai `ACT2_RESKIN` puis screenshoot sur le bon fond act-2) : grounding/chevauchements OK partout, noms centrés. **Seul point faible : le boss (Leviathan→The Dicelord) hérite de la position/taille du nœud Leviathan** (décalé à droite, un peu petit pour un boss, la place de l'ancien sprite large de Leviathan). À recentrer/agrandir plus tard si voulu — **nécessiterait un override runtime dans le reskin** (position/scale) puisque le nœud `tier_boss_leviathan.tscn` est partagé avec le Leviathan d'act 1 (donc pas éditable dans le `.tscn` sans casser l'act 1). Pas fait (lazy assumé).
- **Curseur personnalisé ajouté au jeu + portrait de l'event Deonassius remplacé (2026-07-19, idée en passant de Julien en cours de session) — NON PLAYTESTÉ EN JEU (vérifié seulement par inspection pixel des assets et parse-check gdtoolkit, jamais vu tourner dans Godot).**
  - **Curseur** : process itératif de prompt sur 3 rounds avant convergence. V1 (un dé littéral posé sur le curseur) écarté par Julien lui-même AVANT génération — le jeu a déjà de vrais dés à l'écran, un curseur qui leur ressemble aurait été illisible/confondant. V2 (flèche fine courbée/crochet + gemme émeraude, pour rester distincte du curseur triangle plat de STS2 que Julien a montré en référence) générée et intégrée à 48px, puis jugée "too small... not super clear" une fois en jeu → bump à 64px. V3 (reformulée "thick, chunky... roughly as wide as it is long near the tip") pour un look plus massif façon STS2 SANS copier sa silhouette triangle — c'est la version finale retenue, silhouette flèche crochetée conservée (toujours distincte de STS2), juste beaucoup plus épaisse/lourde visuellement.
  - **État "pressed" ajouté via un edit de la MÊME image** ("keep the same shape, outline, gem placement... but make it read as pressed/activated... warm glow") plutôt qu'une génération indépendante — teinte plus saturée + halo chaud radiant, silhouette quasi identique à l'idle (confirmé par bbox alpha : idle 665×748 offset (280,70), pressed 669×760 offset (277,64) — quelques px de bleed du glow seulement).
  - **Piège rencontré, déjà documenté pour d'autres icônes IA** (mémoire `feedback_ai_icon_cleanup_pipeline`) : GPT Image peint un DAMIER LITTÉRAL en pixels plats (RGB, pas de canal alpha) au lieu d'une vraie transparence malgré le prompt "transparent background" — `rembg` appliqué systématiquement sur les 2 candidats avant tout crop pour obtenir un vrai canal alpha.
  - **Alignement idle/pressed critique pour le hotspot** : les deux images ont été croppées sur une bounding box **PARTAGÉE** (union des bbox alpha des deux images, pas le bbox propre à chacune) avant resize — sinon la pointe (et donc le hotspot de clic) aurait légèrement bougé entre les deux états au clic, jitter perceptible. Résultat final : 56×64px, hotspot `Vector2(3, 1)`.
  - **Implémentation** (`global.gd`, l'autoload `Global`) : précharge les 2 textures en `const`, pose le curseur par défaut dans `_ready()` via `Input.set_custom_mouse_cursor()`. Nouveau `_input(event)` swap idle↔pressed sur press/release du clic gauche uniquement (`MOUSE_BUTTON_LEFT`) — lecture seule, **n'appelle jamais `set_input_as_handled()`**, sinon ça aurait intercepté tous les clics du jeu (cartes, boutons, dés). `Global.process_mode` passé à `PROCESS_MODE_ALWAYS` (même raison que le CanvasLayer de toast d'`AchievementManager`, voir plus bas) pour que le swap pressed continue de marcher même quand l'arbre est en pause (menu pause, consultation de map, panel de fin de combat).
  - **Portrait Deonassius** : `assets/images/relicwizard.jpg` écrasé en place avec la nouvelle illustration fournie par Julien (mage extravagant "diceologist" couvert de dés, cohérent avec le texte de l'event "Deonassius the third, almighty diceologist") — même nom/uid, **0 édit `.tscn`/`.tres`**, même pattern que les remplacements d'art déjà documentés ailleurs dans ce fichier. Recadré en carré (896×896) depuis la génération source 1200×896, biaisé pour garder visage + main tendue + table de dés dans le cadre, puisque le `TextureRect` réellement visible dans `event_deonassius.tscn` est en 500×500 `KEEP_ASPECT_CENTERED`. **Piège à connaître si ce fichier est retouché à nouveau** : la scène contient un SECOND `TextureRect` de fond pointant vers `scroll.jpg`, mais il est `visible = false` — c'est un nœud mort, pas le vrai portrait affiché (celui-ci vit dans `TextureRect/MarginContainer/Panel/MarginContainer/VBoxContainer/HBoxContainer/TextureRect`, `ExtResource("1_1m62g")`).
- **9 nouveaux achievements (2026-07-19, demande explicite de Julien "put some more achievement tiers"), pool 16→25** — même philosophie "zéro nouvelle mécanique" que la 2e vague du 07-16 : Untouchable (gagner un combat sans perdre de HP), Full Cycle (reshuffle ≥5 cartes d'un coup), Loaded (avoir 300 Or en même temps), Hoarder (posséder 8 Reliques en même temps, un seul run), Conqueror (rebattre le Leviathan une 2e fois = finir un run complet), Treasure Hunter / Retail Therapy / Well Traveled (stats lifetime : coffres ouverts / cartes achetées en boutique / events narratifs complétés), Dice Addict (stat lifetime : dés achetés en boutique).
  - **Untouchable / Full Cycle / Loaded : 100% self-contained dans `achievement_manager.gd`**, juste de nouveaux `Events.*.connect()` sur des signaux déjà déclarés mais jamais écoutés par le manager (`battle_started`/`player_hit`/`battle_won` pour Untouchable — `player_hit` ne fire QUE si les HP baissent réellement, donc un dégât entièrement bloqué ne casse pas l'achievement ; `deck_reshuffled`, `gold_changed`).
  - **Hoarder** : nouveau `AchievementManager.report_relic_count(count)`, appelé depuis `relic_handler.gd::add_relic()` — le seul choke point par lequel passe chaque relique ajoutée à la barre du joueur.
  - **Dice Addict** : n'utilise PAS `Events.dice_bought` (ce signal est aussi émis par des events narratifs comme Dice Forge, donc gonflerait le compteur sans vrai achat en boutique — même raison que "Customer" l'évite déjà) — `AchievementManager.add_stat("dice_bought_from_shop", 1)` ajouté à côté des 9 `unlock("customer")` déjà existants dans `shop.gd` (un par type de dé), via un seul `replace_all`.
  - **Conqueror** : `unlock("conqueror")` ajouté dans `run.gd::_on_battle_won`, à l'intérieur du bloc `Global.current_act >= 2` déjà existant qui pose `run_finished = true` (fin de run réelle, pas juste "un boss est mort" — `marine`, lui, se déclenche sur n'importe quel combat de type BOSS, act 1 ou 2).
  - **Bug trouvé et corrigé le jour même : Untouchable se débloquait gratuitement sur le combat TUTORIEL.** `tutorial_director.gd::_on_victory_continue_pressed` pose `Global.tutorial_on = false` AVANT d'émettre `Events.battle_won` (pour que son propre écran de victoire scripté ne se fasse pas court-circuiter par l'auto-advance normal de `battle_over_panel.gd`) — le garde-fou habituel de `unlock()` (qui checke `Global.tutorial_on` AU MOMENT de l'unlock) ne l'attrapait donc pas, et le tuto scripté ne laisse jamais le joueur perdre de HP. **Fix : nouveau `_battle_was_tutorial`, snapshot de `Global.tutorial_on` pris dans `_on_battle_started()` (donc encore `true` pour le combat tuto) plutôt que de faire confiance à sa valeur au moment de `battle_won`.** Piège méthodologique à généraliser : pour tout futur achievement/check one-shot gated sur `Global.tutorial_on`, ne pas supposer que le flag est encore vrai au moment où le signal déclencheur arrive — snapshot-le tôt si le timing compte.
  - Vérifié seulement par gdtoolkit parse-check (`achievement_manager.gd`/`shop.gd`/`run.gd` propres ; `relic_handler.gd` a un blocage de parser préexistant SANS RAPPORT sur `Array [RelicUI]` ligne 20 — confirmé antérieur à cette édition via `git diff`) — **PAS PLAYTESTÉ, pas de rendu headless.**
- **Trajectoire de carte jouée refaite façon STS2 (2026-07-18, sur retour de Julien "on non-attack cards it feels like the card is JUMPING above" + une vidéo de référence STS2 qu'il a enregistrée lui-même) — NON PLAYTESTÉ, "okay for now, might change".** Analyse frame-par-frame de ses vidéos (ffmpeg → contact sheets, méthode de la mémoire `reference_video_frame_analysis`). **ÉTAT FINAL de `card_ui.gd::_fly_to_discard_and_free`, UNIFIÉ pour TOUS les types de cartes (plus de branche single-target ; attaques comprises — sa vidéo STS2 fait pareil, la carte d'attaque tient en présentation pendant que le slash tombe)** — 3 beats :
  - **GLIDE 0.3s `TRANS_CUBIC/EASE_OUT` (zéro overshoot) vers UN point de présentation FIXE `STAGE_HOLD_CENTER` (470,405), en IGNORANT le curseur** + scale→1.12 + redressement rotation→0 (cet entrance grow EST le punch de play, pas de punch de scale séparé — deux tweens simultanés sur `scale` se battraient).
  - **HOLD 0.24s** (raccourci plusieurs fois : 0.5→0.32→0.24).
  - **EXIT = streak direct vers la pile de défausse** (`COMET_TO_PILE_TIME` 0.42s `TRANS_QUAD/EASE_IN`, shrink→0.12 + bank 20°, fondu 0.2s près de l'arrivée pour qu'on VOIE la carte voler jusqu'à la pile). Motes denses via `_comet_trail_step` (mote-only, throttle temps-réel 20ms) sur son propre tween indépendant ; punch de réception sur un tween possédé par le BOUTON de pile (`pile_button.create_tween()`), PAS chaîné sur `fly_tween` (la carte `queue_free` en arrivant → un callback en attente sur `fly_tween` ne partirait jamais).
  - **Vol enrichi (passe du 2026-07-17, toujours en place)** : flash "resolve" surbrillant au play, traînée de motes sparse couleur du dé actif sur tout le vol (`_emit_flight_trail`, motes parentées ui_layer avec leur propre tween pour survivre au `queue_free`), teinte braise sur les cartes qui exhaust.
  - **Leviers de tuning : `STAGE_*`/`COMET_*` en tête de `card_ui.gd`.** Pour bouger le spot de présentation : `STAGE_HOLD_CENTER` (centre design de la carte ; x plus bas = plus à gauche, y plus bas = plus haut à l'écran).
  - **⚠️⚠️ LE grand piège de cette saga (~7 itérations ratées avant que Julien tranche "Fixed spot (STS2)" via AskUserQuestion) : le "up AND right" qu'il voyait au play n'était PAS l'animation** — le lift était vérifié vertical dans le code. **C'était le SYSTÈME DE DRAG qui snap la carte au curseur au moment du play** (`card_dragging_state.enter` : `global_position = mouse - size/2`, saut d'1 frame), donc la carte finissait là où le curseur avait été traîné (souvent le centre), et TOUTE animation de lift partait de là. Aucune trajectoire de lift ne pouvait retirer ce rightward car il vient d'AVANT l'animation. **Fix = ignorer entièrement le curseur : la carte vole TOUJOURS vers le point fixe.** Piège méthodo à retenir : quand un mouvement "part de travers", vérifier si la position de DÉPART est fixée par un AUTRE système avant de re-tuner la trajectoire — j'ai gâché ~6 tours à tuner le lift/l'exit alors que le grief venait du placement pré-animation par le drag. Historique des hold spots essayés avant le fixe final : fixe haut (520,240, "jumping/leap") → fixe bas (520,430) → relatif au release (`release-90px`, x libre — a ré-exposé le rightward du drag) → **fixe assumé (470,405)**. Idem l'EXIT a traversé comète-bezier-unique → 2-beats (launch+streak, le launch lisait "top-right") → "rise & dissolve sur place" (fait-puis-reverté, les attaques se dissolvaient sur le dé) → **streak direct final**.
- **Passe "animations de cartes" (2026-07-17, demande explicite de Julien : orbes de power pour les cartes support + rendre le cycle jouer/piocher/défausser/reshuffle moins inerte) — NON PLAYTESTÉ (gdtoolkit + boot harnais seulement, ces animations sont interactives par nature).** 7 fichiers touchés, 5 morceaux :
  - **Orbes "carte → nombre de Power" pour les cartes qui génèrent du Power (Reinforce, Blaze, From Nothing...)** — même langage visuel que les orbes de roll mais plus rare donc plus cérémoniel : moins d'orbes mais plus grosses (`CARD_ORB_*` dans `dice.gd`, 5-12 selon le delta, vol 0.5-1.1s), qui **s'épanouissent vers l'extérieur** depuis la carte (control point bezier flingué dans un éventail ~220° vers le haut) avant d'être aspirées dans le nombre — au lieu de l'arc-en-ciel des rolls. **Détection : "le Power a réellement AUGMENTÉ dans la même frame qu'un `card_played`"** (`_last_card_played_frame` posé par un nouveau connect dans `dice.gd::_ready`, comparé via `Engine.get_process_frames()` dans `_on_change_current_power`) — `Card.play()` émet `card_played` en 1ère ligne et les cartes émettent `change_current_power` de façon synchrone dans ce même play, donc les sources roll-adjacentes (Blood Sword, Metronome, Opening Gambit — frame de roll) et les ~19 émissions refresh-only (pas de delta) sont exclues par construction. **Origine des orbes : le point de RELEASE de la carte (`last_played_card_position`) pour tous les chemins de play sauf le socket rouge (centre du dé, `Global.playing_red_card` — ce chemin ne passe jamais par `CardUI.play()` donc la position serait périmée)** — une V1 faisait partir le burst du point de staging au-dessus de la dice interface avec un délai de 0.24s, rejetée par Julien au premier essai ("seem to come from way above? I'd like them to shoot from where you release the card"). Orbes parentées à la `ui_layer` z=150 (convention refuel/All In — les deux mélangent déjà librement coordonnées base-canvas et CanvasLayer, pattern éprouvé). Réutilise `_orb_bezier_step`/wobble/`_play_power_orb_arrival_reaction`/land SFX existants.
  - **Vol de carte jouée enrichi (`card_ui.gd::_fly_to_discard_and_free`)** : pivot recentré (`pivot_offset = size/2` posé au départ du vol — avant, le spin/shrink tournait autour du coin haut-gauche ; la cible vise maintenant centre-bouton moins pivot), flash "resolve" surbrillant + micro punch de scale à l'instant du play (punch calé pour finir à 0.13s, AVANT le premier scale-down des branches à 0.16s — pas de bagarre sur `scale`), **traînée de motes additives couleur du dé actif** sur tout le vol (`_emit_flight_trail` via `tween_method`, motes parentées à la ui_layer avec leur PROPRE tween `mote.create_tween()` pour survivre au `queue_free` de la carte), **punch de réception sur le bouton de pile à l'atterrissage**, et **teinte braise sur les cartes qui exhaust** (tween modulate vers orange 0.18s avant le fade — "brûlée", pas "classée"). **⚠️ Piège trouvé au 1er retour de Julien ("je ne vois la traînée que sur les cartes d'attaque")** : les motes rendent SOUS la carte (z 90 vs 100) et la V1 les spawnait à ±7px du CENTRE d'une carte de 140×210 — entièrement cachées tant que la carte ne s'éloigne pas vite (les arcs d'attaque le font, une carte support qui pause sur son point de staging non). Fix : scatter élargi à la quasi demi-emprise de la carte (`TRAIL_MOTE_SCATTER_X/Y` 34/46, **multiplié par `scale` courant** pour que le nuage se resserre avec la carte qui rétrécit en fin d'arc) + motes plus grosses/lumineuses/persistantes (10-20px, alpha 0.85, vie 0.55s, intervalle 0.045s).
  - **Nouveau `CardPileOpener.receive_punch(strength)`** (`card_pile_opener.gd`) : squash centré réutilisable, appelé par le vol de carte jouée, le sweep de fin de tour, la pioche et le reshuffle — tween tracké, les atterrissages rapides redémarrent le punch au lieu d'empiler.
  - **Fin de tour : la main est balayée vers la défausse** (`hand.gd::discard_card` appelle le nouveau `CardUI.fly_hand_discard()` au lieu de `queue_free`) — version courte du vol (0.4s, pas de staging/flash/traînée : rien ne "résout", et jusqu'à 5 se chevauchent). **Reparente vers la ui_layer immédiatement**, donc la carte cesse d'être un enfant de la main à l'instant même — l'itération de `discard_cards()` et le re-fan différé la voient partie exactement comme avec l'ancien `queue_free` ; fallback `queue_free` sec si pas de ui_layer (ne jamais tweener un enfant d'HBoxContainer).
  - **Pioche : entrée "materialize"** (`hand.gd::_play_draw_entrance`) — fade-in + flash surbrillant qui retombe sur le modulate courant (capturé APRÈS `set_playable_visual`, donc une carte piochée dimmée reste dimmée). **Volontairement modulate-only** : le fan stompe position/rotation de TOUTES les cartes à chaque carte ajoutée du même deal, et scale/pivot appartiennent au hover — toute entrée sur ces propriétés se fait écraser ou change le comportement établi. Le punch du bouton Draw pile vit dans `player_handler.draw_card()` (pas dans `hand.add_card` — les cartes accordées directement en main par des effets n'ont jamais touché la pioche, punch = mensonge).
  - **Reshuffle défausse→pioche enfin visible** : nouveau signal `Events.deck_reshuffled(count)` émis par `player_handler.reshuffle_deck_from_discard()` **seulement si des cartes ont réellement bougé** (jamais au start de combat, garde early-return déjà en place ; jamais si les deux piles sont vides). `battle_ui.gd::_on_deck_reshuffled` : jusqu'à 5 mini "dos de carte" (des `Panel` portant `card_ui_normal.tres` — à 30×44 ça lit instantanément comme une carte face cachée, zéro nouvel asset) volent en arc bezier de la défausse vers la pioche, spin + stagger 0.07s + fade-absorption + punch par atterrissage, son = `drawcardsound.wav` pitché à 0.72 (placeholder "riffle", à remplacer si Julien trouve un vrai son de shuffle).
- **Repasse positionnement ennemis sur retour playtest de Julien (2026-07-17, 2 rounds sur 5 screenshots), 7 fichiers `battles/*.tscn` corrigés — vérifié par le harnais `debug_bg_audit.gd` + comparaisons avant/après, pas encore re-confirmé en jeu par Julien.** Round 1 (3 screenshots signalés) : deux Skeletons solo flottaient ~20px au-dessus de la ligne de sol établie (`tier_0_crab`, `tutorial_fight`, même bug — `sprite_y_offset=10` déjà posé mais `position.y` jamais recalé après un art plus récent) ; un Satyr flottait 30px au-dessus de son jumeau identique dans `tier_0_bigger_satyrs_2` (même taille = lit comme un flottement, pas une profondeur voulue) ; deux paires de Krakens se chevauchaient jusqu'à la fusion visuelle (`tier_1_octopus_2_satyrs_2` 29-33px, `tier_2_machopeur_octopus` 33px + le Marauder qui mordait sur un Kraken de 17px — ce dernier aussi rétréci 0.75→0.65, cohérent avec son statut de squishy 21 HP déjà établi). Round 2 (Julien a retrouvé le MÊME bug de Skeleton flottant sur un fight qu'il n'avait pas encore croisé, `tier_1_lurker_crab`, floor 11 Lurker+Skeleton) : Skeleton corrigé pareil (partage `crab_enemy_tier2.tres` avec `tier_2_plant_crab`, lui aussi flottant du même bug, corrigé en même temps par prévention). **Le Lurker de ce même fight flottait aussi, cause différente et plus subtile** : une session précédente avait posé `sprite_y_offset = -35` pour remonter TOUT le groupe visuel (sprite+barre HP+badge de statut, `enemy.gd::update_enemy()` les lie tous par un même terme additif) afin de dégager le badge de statut de la main de cartes — ce lift a réglé le problème de la main mais a fait flotter le sprite lui-même. **Fix : découplé le sprite du reste du groupe** — `sprite_y_offset` réduit à `-10` (sprite redescend, pieds au sol) compensé par `stats_ui_y_offset`/`status_handler_y_offset`/`intent_ui_y_offset` ajoutés pour maintenir la barre HP et le badge de statut À LA MÊME position écran qu'avant (vérifié par comparaison numérique des rects avant/après : `status_rect` identique au pixel près, marge de 36px avec la main de cartes préservée). **Piège rencontré en l'appliquant** : la première tentative de compensation totale (tout annuler) a fait replonger la barre HP TROP HAUT, la patte du Lurker chevauchant maintenant le texte "47/47" — `stats_ui_y_offset` retuné une seconde fois (compensation partielle plutôt que totale) pour laisser la barre suivre un peu le sprite vers le bas, dégageant la patte tout en gardant un espace propre avec le badge de statut en dessous. **Généralise un principe déjà entrevu mais jamais formulé aussi clairement : `sprite_y_offset` déplace le sprite ET la UI ensemble, mais `stats_ui_y_offset`/`status_handler_y_offset`/`intent_ui_y_offset` sont des termes ADDITIFS INDÉPENDANTS — pour bouger UNIQUEMENT le sprite sans perturber une UI déjà calée ailleurs (barre de vie, badge de statut), changer `sprite_y_offset` et immédiatement compenser par un delta opposé sur les 3 offsets UI plutôt que de les laisser suivre.** Les fights `tier_0` restants signalés comme "float/overlap" par le harnais (swarms octopus/satyr en arrière-plan, scale réduite) ont été re-vérifiés visuellement et confirmés comme de la profondeur voulue, pas des bugs — non touchés, cohérent avec l'audit du 07-11.

- **Regroupement de l'icône Map + nouveau badge "tu peux t'acheter un dé" sur la map (2026-07-17) — parti d'une discussion pure UX (pas d'implémentation demandée au départ), NON PLAYTESTÉ.** Trois points soulevés par Julien sur une capture d'écran de la top bar/map : (1) l'icône actuelle du Dice Shop (un dé rouge sur une pile de pièces, `res://dice shop clean outline.png`) jugée pas assez claire — prompt fourni pour une regénération future ("market stall + 3 dés colorés + pile de pièces"), Julien a généré un candidat via GPT Image mais **n'a pas encore transmis le fichier**, donc l'icône en jeu reste inchangée pour l'instant ; (2) `ConsultMapButton` flottait seul juste après `FloorLabel`, orphelin visuellement avec un grand vide avant le cluster Dice Shop/Deck/Settings — **regroupé avec ce cluster** dans `scenes/run/run.tscn` (juste après `ExpandSpacer`), simple réordonnancement de nodes, `unique_name_in_owner` donc aucun script à toucher. (3) **Le vrai fix UX, sur un problème récurrent signalé par Julien lui-même ("il m'arrive d'oublier que j'ai assez d'argent pour un dé en entrant en combat")** : Julien a explicitement rejeté une confirmation bloquante avant d'entrer en salle (économiser pour un dé plus cher est un choix légitime, pas une erreur — un dialogue punirait ce comportement correct ou se ferait spam-cliquer). Solution retenue : un petit badge vert (réutilise l'icône Dice Shop actuelle, à remplacer quand le point (1) sera résolu) apparaît sur **chaque salle de map actuellement cliquable** dès que `Global.gold >= Global.cheapest_dice_price` (même seuil que le glow existant du bouton top-bar) — rattrape le joueur au moment où il s'apprête à cliquer une salle plutôt que seulement sur l'icône shop, facile à louper en chemin vers un combat.
  - **Architecture** : `map.gd::refresh_affordable_badges()` calcule le check une fois et boucle sur `rooms.get_children()`, appelé depuis `unlock_floor()`/`unlock_next_rooms()`/`_on_map_room_selected()` (changements de disponibilité) et depuis `run.gd::_on_check_if_can_purchase_dice()` (déjà déclenché à chaque changement d'or via `Events.check_if_can_purchase_dice`, avant ça ne pilotait que le glow top-bar). `map_room.tscn`/`.gd` : nouveau `AffordableBadge` (`Area2D` séparée avec sa propre petite `CollisionShape2D`, ses propres signaux `mouse_entered`/`mouse_exited`) positionné à un offset local fixe (34, -34) du centre de la salle, caché par défaut. Survol = tooltip via le même cycle instantiate-on-hover/`_exit_tree()` que tout le reste du projet (voir mémoire `feedback_tooltip_leak_pattern`).
  - **`IconTooltip` (`scenes/ui/icon_tooltip.gd`) a gagné un second point d'entrée, `show_body_tooltip()`**, à côté du `show_tooltip()` d'origine utilisé par les 4 tooltips existants (Map/Dice Shop/Deck/Settings). Le texte demandé par Julien pour le badge est une vraie phrase de 2 propositions ("You have enough gold to buy a Dice in the shop. It doesn't mean you always should!...") — `show_tooltip()` est calé pour un court label unique (Cinzel-Bold, centré, gras, doré, panneau 150px) qui aurait rendu une phrase longue illisible. **Deux pièges Godot rencontrés en écrivant la variante** : (a) Julien a signalé que le texte "avait l'air en majuscules" — ce n'était PAS un `.to_upper()` caché (vérifié par grep avant de creuser ailleurs), mais un simple effet du FONT Cinzel-Bold, dont les minuscules sont dessinées façon small-caps, lisible pour "Map"/"Dice Shop" mais qui fait "crier" une phrase complète ; fix = police alternative `Noto_Sans/static/NotoSans-Medium.ttf` (la même déjà utilisée pour le corps de texte des tooltips carte/statut dans `tooltip.tres`), panneau élargi à 260px (au lieu de 150px, qui aurait donné une colonne étroite et haute). (b) `RichTextLabel` **n'a pas de propriété `horizontal_alignment`** (contrairement à `Label`) — une première tentative de forcer l'alignement à gauche a été retirée après vérification (aucun `RichTextLabel` du projet ne pose cette propriété, et l'alignement par défaut est déjà à gauche, donc pas nécessaire de toute façon).
  - **Non vérifié en jeu** : taille (~26px)/position du badge au vrai zoom, et si l'offset fixe (34, -34) reste proportionné sur la salle Boss (icône/collision bien plus grande que les salles normales, badge non scalé en conséquence).

- **⚠️ Toute cette session vit sur la branche `art/starter-deck-art-refresh`, poussée sur origin mais PAS mergée sur `main`** — aucun PR GitHub réel n'a été créé (la commande `/create-pr` a été déclenchée par erreur, le `gh` CLI n'est même pas installé dans cet environnement, vérifié directement via l'API GitHub : zéro PR pour cette branche). À merger manuellement par Julien quand il est prêt.
- **Audit d'art complet + refonte du starter deck (2026-07-16) — sur demande explicite de Julien ("trouve les plus gros écarts de style avant ma prochaine release publique").** Montages comparatifs générés (cartes/reliques/events/ennemis actuellement utilisés, PAS tout le disque) pour repérer les offenders : `focus.jpg` (peinture abstraite 2023, aucun rapport stylistique) et `block_card_v2.png` (vecteur plat cartoon sur un deck sinon peinture sombre) identifiés comme les pires ; Lich/Dragon Priest (rendu flou "gpt-painterly" sans contour net) comme pires ennemis. **Process de prompt itératif documenté pour toute future régénération de carte** : le levier "intensité visuelle = puissance de la carte" (starters = impact contenu + fond sombre, pas d'explosion plein cadre) a émergé après plusieurs rounds où Strike lisait "punch de super-héros" puis "trop faible/vide" — la bonne formule finale = poing + dé comme arme, flash contenu, burst radiant en arrière-plan qui remplit le cadre SANS geste "hold/present" statique. **8 fichiers remplacés en place (même nom/uid, donc 0 édit `.tres` — toutes les copies Strike×4/Block×4/Blessing+ héritent automatiquement)** : `strike_v3.png`, `block_card_v2.png`, `nano_low_blow_ok.png`, `gptrecombobulate.png`, `gptreinforce.jpg`, `assets/images/focus.jpg`, `gptoracle.png` (**couvre les 5 variantes Scout d'un coup** : Scout 2/3/3-no-exhaust/5/5-no-exhaust, toutes pointaient déjà vers ce seul fichier), `bullseye.png` (recadré 1:1→3:2 depuis la génération carrée). **Low Blow reprend en 2e passe une composition "David contre Goliath"** (rogue minuscule frappant la cheville d'un géant partiellement cadré, botte simplifiée pour ne pas rivaliser en détail avec le rogue) — décision assumée par Julien de sacrifier l'iconographie dé/dice pour ce concept narratif, contrairement à Strike qui garde le poing-sur-dé.
- **3 ennemis remplacés (Skeleton/Marauder/Lich, 2026-07-16) + repasse qualité + vrai bug de centrage de nom trouvé et corrigé.** Skeleton = nouvel art de Jenya (photo Telegram compressée, 469×365 — qualité source limite, original PNG demandé si dispo) ; Marauder/Lich = choisis parmi des générations IA. **Pipeline de détourage changé pour ces 3 : chroma-key + hole-punch (composants connectés) au lieu de rembg** — rembg laissait un halo blanc semi-transparent sur les bords ET gardait les gaps intentionnels (espaces entre côtes du squelette) comme blanc opaque, un vrai problème sur de l'art cel-shaded à fond blanc plat que rembg segmente mal. Nouvelle méthode : tout pixel quasi-blanc touchant le bord = fond ; tout blob quasi-blanc ENCLAVÉ entre 35px et ~2% de la surface = un vrai trou (gap de côte) ; en dessous de 35px = bruit JPEG à ignorer (calibré sur la distribution réelle des tailles de composants, séparation nette dans les deux cas testés). **Lich agrandi via `height=292` sur le node Enemy de `battles/tier_elite_lich.tscn`, PAS via l'image** — son échelle finale est *height-bound* (256 vs width_scale plus large), donc grossir le CANVAS n'aurait rien changé (le contenu remplissait déjà 91% de la hauteur) ; seul agrandir la target_height du node fonctionne, ~14% plus gros, les formules Y (intent/HP-bar/nom/statuts) s'adaptent automatiquement puisqu'elles dérivent du même `tex_size`/`final_scale`. **Vrai bug de code trouvé en creusant le centrage du nom au survol** : `NAME_LABEL_SPRITE_CENTER_X = 124.0` (constante plate unique pour tous les ennemis) n'était PAS un réglage à la main comme le suggérait le commentaire — c'est littéralement la position `Sprite2D.position.x` codée en dur dans le TEMPLATE `enemy.tscn` de base (`position = Vector2(124, 3)`), jamais réassignée par le code. Un premier fix avait remplacé cette constante par le seul terme de correction de contenu (`content_center_x`), OUBLIANT cette baseline → nom qui partait très à gauche (repéré net sur Lich, plus subtil sur Skeleton/Marauder). **Fix définitif** : `_name_label_local_x = sprite_2d.position.x` (la vraie baseline, lue en live) `+ (content_center_x - 0.5) * tex_size.x * final_scale` si mesuré. Nouveau champ `EnemyStats.content_center_x` (sentinelle -1 = "non mesuré, comportement legacy inchangé") posé sur les 8 `.tres` des 3 ennemis touchés (crab×4, machopeur×3, lich×1) — **les 16 autres ennemis restent intouchés/non vérifiés**, sentinelle par défaut. Découverte au passage : Marauder avait un écart énorme (offset correct calculé à -8.96 contre 124 legacy) jamais signalé par Julien mais confirmé cassé par calcul — probablement invisible/pas remarqué avant.
- **6 icônes UI top-bar/combat remplacées (Map, Deck, Dice Shop, Settings, Draw pile, Discard pile) façon STS2 (2026-07-16)** — pipeline identique aux gemmes de rareté/icônes de statut (rembg + nettoyage single-island + recentrage sur canvas carré transparent à ~82% de fill). Tous les fichiers cibles étaient déjà des PNG transparents référencés par UN SEUL `TextureButton` chacun (sauf **Settings** = `assets/images/pause_menu_icon.png`, partagé par le bouton pause en combat ET le bouton du menu principal — un seul fichier écrasé couvre les deux) donc écrasés en place, **0 édit `.tres`/`.tscn`** après avoir vérifié chaque `stretch_mode` (`KEEP_ASPECT_CENTERED` pour 5 des 6, tolère n'importe quel ratio ; seul Dice Shop est en `STRETCH_SCALE`, d'où l'attention à garder cette sortie carrée).
- **Pile Exhaust construite de zéro (2026-07-16) — bug réel trouvé au passage : les cartes qui s'exhaustaient disparaissaient sans être trackées nulle part.** `player_handler.gd::_on_card_played()` faisait juste `return` sur `card.should_exhaust()`, sans jamais ajouter la Card à un quelconque pile — et `card_ui.gd::_fly_to_discard_and_free()` envoyait TOUJOURS le visuel vers le bouton Discard, y compris pour une carte qui n'y atterrissait jamais réellement (mensonge visuel). **Fix complet** : nouveau `CharacterStats.exhaust: CardPile` (même pattern que `draw_pile`/`discard`, reset à chaque combat dans `player_handler.gd::start_battle()`) ; `_on_card_played()` ajoute maintenant la carte à `character.exhaust` au lieu de no-op ; le vol visuel checke `card.should_exhaust()` (sûr à ré-appeler — `Events.card_played.emit()` est littéralement la 1ère ligne de `Card.play()`, signal synchrone, donc les deux checks ne peuvent jamais diverger) et route vers `ExhaustPileButton` au lieu de `DiscardPileButton`. **UI** : nouveaux `ExhaustPileButton`/`ExhaustPileView` dans `battle.tscn`, copie conforme du pattern `CardPileOpener`/`CardPileView` déjà utilisé pour Draw/Discard/Deck, câblés dans `battle_ui.gd`/tooltip dans `battle.gd`. Caché tant qu'aucune carte n'a exhausté (la plupart des combats n'exhaustent jamais rien), ne se re-cache jamais une fois montré (rien ne retire de cartes de cette pile en cours de combat). **⚠️ Position du bouton (empilé au-dessus d'End Turn, même colonne x que Discard) calculée par les offsets du `.tscn`, PAS vérifiée visuellement** — le harnais `debug_bg_audit.gd` instancie la scène de combat directement sans passer par la vraie transition map→combat, donc `BattleUI` (main, End Turn, boutons de pile) ne se peuple jamais dans ce harnais ; seuls le parsing (`--headless --import` propre) et l'absence d'erreur runtime sont vérifiés. À confirmer par Julien en jeu.
- **Nouvelles esquisses de Jenya reçues pour remplacer/étoffer le roster (Marauder v2, 4 candidats ; monstres divers pour l'act 2, ~14 candidats déjà triés en shortlist par Claude) — AUCUNE décision finale prise, en attente du prochain aller-retour avec Jenya.** Retour donné à Julien sur les 4 sketches Marauder : préférence pour combiner la posture/l'arme du #3 avec le corps plus massif du #1/#2 (le #3 seul est trop "gladiator humain lean" pour le rôle de bruiser tanky du Marauder — sa hiérarchie de taille face à Kraken/Satyr a déjà été spécifiquement corrigée une fois cette session, ne pas la re-casser) ; l'accessoire "potion rouge à la ceinture" suggéré par Julien écarté au profit d'un accessoire "butin de pillard" (pochettes de dés volées, dés-trophées, bourse déchirée) — cohérent avec le nom "Marauder" et le langage "objet = identité du perso" déjà établi sur le Lich (dés jonglés) plutôt que d'inventer un nouveau mécanisme visuel. Sketches de monstres act-2 : picks favoris communiqués (nécromancien-dés #24 → devenu le nouveau Lich séance tenante, ver des sables #22, kelpie #26, troll #27, minotaure #9, oiseau-tempête #11, bouffon-assassin #15, naga #7 + 4 sans pair : treant/harpie/sorcière-marais/feux-follets) — **aucun de ceux-là encore intégré au jeu**, juste sélectionnés/commentés en attendant le prochain lot final de Jenya.

- **Bug de SFX manquant en jeu rapide corrigé (2026-07-16, sur repro de Julien : "si je joue vite, le son de Recombobulate manque parfois")** — root cause confirmée par test headless : `global/sound_player.gd` (le script derrière l'autoload `SFXPlayer`, pool fixe de **8** `AudioStreamPlayer`) faisait juste `if not player.playing: ... break` sans aucun fallback — si les 8 étaient occupés, `play()` ne faisait RIEN, silencieusement. Suspicion de Julien confirmée : les **orbes de power qui atterrissent** (`dice.gd::_play_power_orb_land_sfx`, jusqu'à ~15 par gros roll, chacune un appel `SFXPlayer.play()` séparé) peuvent occuper plusieurs voix d'un coup pendant un tour rapide, ne laissant plus rien de libre pour un son qui arrive juste après (ex. le son de refuel de Recombobulate, `SupportEffect.execute()`). **Fix : système de priorité + vol-stealing dans `sound_player.gd`** — `play()` gagne un 5e paramètre optionnel `priority := 0` (rétrocompatible, les 57 sites d'appel existants dans le projet n'ont pas besoin d'être touchés) ; si aucune voix libre, vole la voix occupée de PLUS BASSE priorité (jamais un son plus important qu'elle-même) plutôt que d'abandonner le son entrant. Seul appelant retouché : `_play_power_orb_land_sfx()` passe désormais `priority = -1` (les plinks décoratifs sont les premiers sacrifiés, jamais l'inverse) — tout le reste du jeu garde la priorité par défaut 0. Vérifié par test headless dédié (8 orbes en priorité -1 saturent le pool, puis `recompose.wav` — le son de Recombobulate — vole bien une voix et joue réellement, confirmé `PASS`).
- **Système d'achievements (9) IMPLÉMENTÉ (2026-07-16, sur specs de Julien) — NON PLAYTESTÉ (vérifié par rendu headless : toast, panneau, file d'attente, états locked/unlocked/progression).** Nouveau autoload **`AchievementManager`** (`global/achievement_manager.gd`, enregistré dans `project.godot` — **⚠️ REDÉMARRER l'éditeur avant de tester, un éditeur déjà ouvert ne connaît pas le nouvel autoload**). Persistance profil (PAS run) dans `user://achievements.cfg` (miroir de SettingsManager) : unlocks écrits immédiatement, compteurs lifetime flushés sur fin de tour/combat/mort/fermeture fenêtre. **Toast Steam-style bas-droite** (`scenes/ui/achievement_toast.gd`, construit 100% en code — pas de .tscn ni class_name, préchargé par le manager), CanvasLayer `layer=90` `process_mode=ALWAYS` (s'anime par-dessus le pause menu et pendant la pause), jingle = `victory_daiso.mp3` à -6dB via SFXPlayer, plusieurs unlocks simultanés = file séquentielle. **Page Achievements dans le menu pause** (bouton + `AchievementsPanel` dans `pause_menu.tscn`, lignes construites en code dans `pause_menu.gd::_populate_achievements()` — trophée gris si locked, nom or si unlocked, progression "x/y" pour les 3 à compteur ; Esc recule d'un niveau : achievements → settings → resume) — accessible aussi depuis le menu principal (instance standalone déjà en place). Icône `assets/images/achievement_trophy.png` (générée PIL, script scratchpad non committé). **Où vivent les triggers** : `dice_rolled`/`red_dice_rolled`/`change_current_power`/`refuel_happened` connectés dans le manager (Reach 15 Power ; 20 Blue rolls par run via `Global.blue_dice_rolled_this_run` — reset dans `reset_run_state()` + clé `"blue_rolls_this_run"` du save dict ; refuel ≥3 dés à <5 Power = `roll_history.size()` au moment de l'emit) ; `dice.gd` juste après `power_generated_this_turn +=` (200 Power lifetime) ; `card.gd::play()` ouvre/ferme une fenêtre de dégâts + `damage_effect.gd` y reporte chaque hit ennemi (Kaboom = total d'UN play, AoE sommée, strictement >20) ; `shop.gd` appelle `unlock("customer")` directement (**PAS via `Events.dice_bought`** — ce signal est aussi émis par les events narratifs Dice Forge/Crimson Eclipse/Hollow Idol, qui ne doivent pas compter) ; `run.gd::_on_battle_won` unlock Marine (salle BOSS — le Leviathan est le boss des 2 acts) et Not impressed (salle ELITE). **2 interprétations à valider par Julien** : "Fueled by ambition" = somme du Power ABANDONNÉ en refuelant (le signal `refuel_happened` porte `roll_value`, et refuel → reset du power juste après) — si l'intention était "100 dés refuelés", changer `_on_refuel_happened` ; le 9e achievement ("Generate a total of 200 power", nom à trouver) a été nommé **"Unlimited Power"** (lifetime, pas par run). ⚠️ Piège GDScript rencontré : appeler une méthode de script custom sur une var typée `Control` = erreur de compilation — la var toast du manager reste volontairement non-typée. **7 achievements "fun/goofy" supplémentaires ajoutés la même session (sur demande explicite de Julien : "faciles à obtenir en 3 runs, situations rigolotes")** — pool total 16, tous vérifiés par le même rendu headless (toast + panneau scrollable, capture des 7 nouvelles lignes). Aucun nouveau mécanisme : tous branchés sur des hooks déjà traversés à chaque roll/hit. **Snake Eyes... Sort Of** (crack Evil 0 3× d'affilée), **Hot Hand** / **Ice Cold** (meilleure/pire face du dé actif 3× d'affilée, PAS scopé par tour ni par type de dé — changer de dé en pleine série et continuer à faire des max compte, c'est le genre de moment loufoque visé), **Scorched Earth** (un seul roll Magma touche ≥3 ennemis), **Turbo Mode** (≥8 dés roll dans un même tour, lit `Global.dice_amount_rolled_this_turn` déjà existant), **Overkill** (coup fatal ≥15 de plus que l'HP restant de la cible — capture `target.stats.health` AVANT `take_damage`), **Glass Cannon** (encaisse ≥20 dégâts en un seul coup et survit). Nouveaux hooks directs (pas de signal `Events.*` créé) : `AchievementManager.report_dice_roll()` appelé dans `dice.gd::_apply_roll_result` juste après `Global.last_roll` assigné (values.min()/max() du roll en cours, infusions incluses) ; `report_dice_rolled_this_turn()` juste après l'incrément existant ; `report_magma_hit()` dans le bloc magma après `damage_effect.execute()` ; `report_enemy_hit()`/`report_player_hit()` dans `damage_effect.gd::execute()`. **`unlock()` bloque maintenant tout achievement tant que `Global.tutorial_on` est vrai** (nouveau garde-fou, rétroactif aux 9 premiers aussi) — les rolls forcés du tuto (4,3,6,6) ne doivent pas déclencher un toast pendant la séquence scriptée ; les compteurs sous-jacents (stats/streaks) continuent d'avancer normalement, seul le pop visuel est différé jusqu'à la fin du tuto.

- **Tooltips stylés ajoutés aux 4 icônes de la top bar sans tooltip habillé (Map, Dice Shop, Deck, Settings), 2026-07-16, retour Julien sur screenshot ("tooltip ugly for Settings")** — `PauseButton` utilisait le `tooltip_text` NATIF de Godot (`Control.tooltip_text`), le popup gris/carré par défaut du moteur, visuellement à côté de la plaque par rapport au reste de l'UI (bordure or, fond navy, police Cinzel utilisés PARTOUT ailleurs — cartes, reliques, statuts, dés). **Nouveau composant réutilisable `scenes/ui/icon_tooltip.gd`/`.tscn` (`class_name IconTooltip`)** : panel compact auto-hauteur (réutilise `tooltip.tres`, le même stylebox navy/or que tous les autres tooltips), une seule ligne de texte Cinzel-Bold or centré, positionné sous l'icône et clampé aux bords d'écran. API factory statique `IconTooltip.spawn_below(button, text) -> Node` : instancie la scène, l'ajoute sous `get_tree().root` (même pattern que `tooltip.gd`/`relic_ui.gd` — échappe à la CanvasLayer de la TopBar pour toujours rendre par-dessus), positionne, pose un timeout de sécurité 8s. Câblé dans **4 endroits** : `run.gd` (`_on_consult_map_button_mouse_entered/exited` → "Map", `_on_pause_button_mouse_entered/exited` → "Settings", remplace le `tooltip_text` natif retiré du `.tscn`), `scenes/run/dice_shop.gd` (le bouton icône top-bar, PAS le script de la boutique elle-même) → "Dice Shop", et `scenes/ui/card_pile_opener.gd` (nouveau `@export var hover_tooltip_text`, vide par défaut) → seul le `DeckButton` de `run.tscn` le pose à "Deck" ; les 3 autres instances de cette scène partagée (Draw/Discard/Exhaust pile dans `battle.tscn`) restent sans tooltip, non câblées, comportement inchangé.
  **Trois vrais bugs trouvés en implémentant, tous corrigés avant que le code ne soit fonctionnel** : (1) `@export var tooltip_text` sur `CardPileOpener` (qui `extends TextureButton`) **redéfinissait une propriété NATIVE de `Control`** (`Control.tooltip_text` existe déjà) — Godot refuse ça comme erreur de compilation ("Member tooltip_text redefined"), qui a fait planter la compilation de `card_pile_opener.gd` ET cascadé en échec de compilation de `battle_ui.gd`/`battle.gd`/`run.gd` (qui référencent tous ce type). **Renommé en `hover_tooltip_text`** — toujours vérifier qu'un nouveau `@export` sur une classe `extends <NodeType natif>` ne collisionne pas avec une propriété déjà existante sur la classe native. (2) **Deadlock de chargement circulaire** : un `const SCENE := preload("res://scenes/ui/icon_tooltip.tscn")` EN HAUT du script `icon_tooltip.gd` — cette scène elle-même attache CE MÊME SCRIPT à un de ses nœuds enfants, donc précharger la scène pendant que le script est encore en train d'être PARSÉ crée une résolution circulaire qui a fait tourner le moteur en boucle infinie (confirmé : un rendu headless restait bloqué indéfiniment, tué manuellement après 60s+). **Fix : `load()` à l'exécution (dans `spawn_below()`) au lieu de `preload()` au niveau du script** — le script est déjà entièrement chargé au moment où la fonction tourne, donc plus de circularité. Piège à généraliser : tout script avec `class_name` qui a besoin de précharger SA PROPRE scène (pattern factory statique) doit `load()` paresseusement, jamais `preload()` en haut du fichier. (3) **`SCENE.instantiate()` renvoie la racine `CanvasLayer` de la scène, pas le `Panel` enfant qui porte le script** (même piège que `tooltip.gd`/`dice_tooltip.gd`, qui font tous les deux `tooltip.get_node("Tooltip")` après `instantiate()`) — un premier essai typait directement `var instance: IconTooltip = SCENE.instantiate()`, qui aurait planté à l'exécution (type mismatch). **Fix : `spawn_below()` retourne la `CanvasLayer` (typée juste `Node`), pas le `Panel`** — libérer ce `Node` retourné libère tout le sous-arbre d'un coup ; libérer seulement le `Panel` aurait fuité une `CanvasLayer` vide à chaque cycle survol/sortie. Vérifié par rendu headless dédié (`debug_icon_tooltip_render.gd`/`.tscn`, scratch non committé) : les 4 tooltips ("Map"/"Dice Shop"/"Deck"/"Settings") rendent avec le style navy/or attendu. **Piège de méthodologie de vérif retenu** : une fois `IconTooltip` doté d'un `class_name` neuf, un premier rendu headless a échoué avec "Identifier IconTooltip not declared" — piège déjà documenté (le cache `global_script_class_cache.cfg` n'est pas à jour tant que l'éditeur n'a pas rescanné), fixé par `--headless --editor --quit-after 900` avant de relancer le rendu. Et capturer `get_tree().root` (la vraie fenêtre de jeu, hors-écran via `--position 2000,2000`) via `get_viewport().get_texture()` direct donne un screenshot vide/gris uni — seule une `SubViewport` explicite avec `UPDATE_ALWAYS` capture de façon fiable en rendu headless (déjà le cas dans tous les harnais existants du projet, `debug_bg_audit` etc. — jamais remis en question avant cette session).
- **Deux bugs du cycle de vie "carte socketée sur dé Rouge" corrigés (2026-07-16, sur repro de Julien — NON PLAYTESTÉ) + Marauder rétréci.** (1) **Duplication de carte (2 Low Blow en main avec 1 seule au deck)** : une carte jouée DEPUIS le socket rouge ne passe jamais par le drag state sur son play final (transition forcée BASE→AIMING→RELEASED dans `card_ui.gd::_on_red_dice_rolled`), donc contrairement à tous les autres chemins de play, son CardUI restait ENFANT DE LA MAIN pendant son vol vers la défausse (~1,3s) — `card.play()` avait déjà ajouté la Card à la pile de défausse via `card_played`, et un End Turn pendant la fenêtre (quasi garanti après un requirement raté : rien ne se passe → on end tout de suite) faisait que `discard_cards()` ré-ajoutait LE MÊME objet Card une 2e fois → 2 copies piochées au reshuffle. Aggravant : sur un requirement raté (apply_effects no-op → n'émet jamais `reset_charged_card`), `card_released_state.gd` clearait `playing_red_card` AVANT d'émettre → `dice.gd::_on_reset_charged_card` ratait sa branche "carte jouée" → `socketed_card_ui` restait pointé sur la carte jouée → le `clear_socket` d'End Turn la "cancel"ait de retour en main. **3 fixes complémentaires** : (a) `CardUI._fly_to_discard_and_free()` reparente vers `ui_layer` si encore enfant de Hand (tue la double-défausse à la racine) ; (b) released state émet `reset_charged_card` AVANT de clearer `playing_red_card` (les 2 branches), idem ajouté dans le chemin non-SINGLE_ENEMY de `_on_red_dice_rolled` (qui ne clearait jamais le flag du tout sur un whiff) ; (c) `dice.gd::_on_reset_charged_card` nulle `socketed_card_ui` + `charged_card_instance_id` DÈS le début du vol (pas au callback de fin — qui pouvait aussi écraser une NOUVELLE carte socketée mid-anim, garde ajoutée). Bonus fix au passage : le fallback "cleanup générique" de `_on_reset_charged_card` ne vide plus un socket encore légitimement occupé (jouer une Celestial pendant qu'une carte est socketée wipait l'affichage du socket → dé rouge inrollable).
  (2) **Celestial (ex. Slash) injouable sur dé Rouge** (défaussée sans pouvoir viser) : `card_dragging_state.gd` court-circuitait AIMING pour toute carte single-target sur red (→ RELEASED direct), et la branche celestial de `card_released_state.gd` jouait immédiatement avec `targets` vide → aucun effet, carte perdue. Fixes : les 2 gates du dragging state laissent les Celestial passer en AIMING même sur red ; la branche celestial du released state renvoie en AIMING si single-target sans cible ; `card_aiming_state.gd` autorise le cancel (right-click/snapback) sur red quand `playing_red_card` est false (le gate ne doit bloquer que l'aim forcé post-roll d'une carte socketée) ; et le play direct d'une Celestial sur red ne touche plus `charged_card_instance_id` (il écrasait l'id d'une carte réellement socketée → son roll rouge ne la jouait plus jamais).
  (3) **Octet drainait aussi la Strength des ENNEMIS (même session, repro Julien : Goblin à "0x2" le tour d'après)** : `muscle.gd::_on_check_if_losing_strength` était connecté par CHAQUE instance de statut Muscle du combat — le signal `check_if_losing_strength` est global, donc l'expiration de la Strength one-turn du joueur (Octet +8, fury) soustrayait 8 aussi au Muscle de tout ennemi qui en avait (buffs Goblin, scaling act 2), jusqu'en négatif. Fix : le handler reçoit maintenant le `target` (via `.bind(target)` dans `initialize_status`) et early-return si pas dans le groupe `"player"`. Bug latent depuis fury.gd (personne ne draftait Fury + le global ne se resetait jamais avant le fix du 07-14) — Octet l'a rendu systématique. **Piège générique : tout statut EVENT_BASED connecté à un signal global `Events.*` doit vérifier son OWNER avant d'appliquer un effet pensé pour un seul camp.**
  (4) **Dice Shop : titre de chaque dé remonté, dégagé de la texture (retour Julien sur screenshot)** — `scenes/shop/dice_shop.tscn` : les 3 colonnes de dé (`Dice1`-`9`, VBoxContainer `separation=0`) avaient leur bandeau de titre COLLÉ directement contre le haut de la texture du dé, sans aucun espace. **Un premier essai (augmenter `separation` du VBoxContainer à 14) a été annulé** : ce `separation` s'applique à TOUS les écarts internes (titre↔texture ET texture↔bouton Buy), donc ça poussait aussi le bouton Buy 28px plus bas — juste assez pour chevaucher le bouton Reroll (positionné en dur, indépendant du VBoxContainer) de ~10px, invisible dans un premier rendu rapide mais confirmé par calcul de coordonnées puis par re-rendu. **Fix retenu : un `Control` spacer dédié (12px, `custom_minimum_size = Vector2(0, 12)`) inséré UNIQUEMENT entre le Panel de titre et la texture** dans chacune des 9 colonnes (`separation` remis à 0) — dégage la texture sans toucher à l'espacement texture↔Buy↔Reroll, déjà correct. Vérifié par rendu headless dédié (`debug_dice_shop_render.gd`/`.tscn`, nouveau harnais scratch au même principe que `debug_bg_audit`, non committé) : gap propre au-dessus de chaque dé, aucun chevauchement Buy/Reroll.
  (5) **Green Dice renommé en Pixie Dice (display-only, décision Julien après une question ouverte sur le nom qui collait le mieux au d3)** — le type interne reste `"green"` partout (les 9 variables `Global.green_dice_*`, le save format, `green_dice_shader.tres`, `green1-3.png`) : rename volontairement scopé au texte visible par le joueur uniquement, pas un renommage de type façon crab→Skeleton. **Julien a explicitement gardé le nom de l'infusion "Gnome" telle quelle** (pas de renommage en cascade). Sweep : `KeywordColorizer.KEYWORDS`/`DICE_KEYWORD_COLORS`/`DICE_TOOLTIP_TEXT` (clé "Green Dice"→"Pixie Dice"), les 2 seules cartes qui mentionnent ce dé en texte (Shattering + Supplication, base et "+", description/tags des 4 `.tres` + les 6 `return "..."` dans les 2 `.gd`), et le label statique du dice shop (`dice_shop.tscn`). **Nouveau `KeywordColorizer.dice_display_name(type)` + `DICE_TYPE_DISPLAY_NAME_OVERRIDES`** : `dice_tooltip.gd` (tooltip au survol en combat ET en boutique) construisait son titre via `dice_type.capitalize() + " Dice"` directement depuis le type interne — sans ce helper, le tooltip du dé Pixie aurait affiché "Green Dice" en titre (et la lookup dans `DICE_TOOLTIP_TEXT` aurait échoué silencieusement, body vide) puisque la clé du dict est maintenant "Pixie Dice" mais le type reste `"green"`. **Vérifié : aucune relique ne mentionne "Green Dice"** (les 3 reliques qui référencent `"green"` — arcane_hat/dice_chip/runic_shield — le font seulement comme string de type interne dans un array/somme, jamais en texte affiché). `dices/DiceData` (`custom_resources/dice_data.gd`) et `scenes/ui/dice_ui.tscn` sont un système legacy non câblé (seuls Blue/Evil/Red ont un `.tres`, aucune scène de combat ne les référence) — pas touchés, hors scope. **Piège à retenir pour toute future distinction "nom affiché" vs "type interne" sur les dés : toute fonction qui dérive un nom depuis `dice_type.capitalize()` (au lieu de lire un texte déjà écrit) doit passer par `KeywordColorizer.dice_display_name()` plutôt que de capitaliser le type brut.** **Un 2e site oublié dans la même passe, trouvé sur retour de Julien (screenshot de l'écran Dice Infusion affichant encore "GREEN DICE")** : `scenes/dice_infusion/dice_infusion.gd::_build_option_panel()` construisait aussi son titre via `("%s Dice" % dice_type).to_upper()` — même piège, corrigé pour passer par `KeywordColorizer.dice_display_name()`. Vérifié par rendu headless dédié (`debug_dice_infusion_render.gd`/`.tscn`, force `Global.green_dice_max_amount=1` pour garantir Pixie comme candidat, attend ~200 frames pour laisser l'entrance tween se terminer avant le screenshot — sinon panels encore à `modulate.a=0`, écran noir). **Les 2 seuls sites qui dérivaient un nom de dé depuis le type brut (`dice_tooltip.gd` et `dice_infusion.gd`) sont maintenant les 2 seuls qui passent par le helper** — grep `capitalize()` combiné à "Dice" pour vérifier qu'aucun 3e site n'apparaît si un futur écran ajoute encore un titre de dé.
  (6) **Greedy (Gargantua) invisible avant le premier roll du combat, corrigé (retour Julien juste après le rééquilibrage ci-dessus)** — `hide_when_zero` (utilisé pour Parasite/Oculus, voir plus haut) cache l'ICÔNE ENTIÈRE tant que `stacks == 0`, et `greedy.tres` avait ce même flag à `true` depuis le début : au tout premier tour, avant le moindre roll, `stacks` vaut 0 → l'icône de Greedy restait invisible jusqu'au 1er roll, alors que "scale avec la durée du combat" est justement l'info qu'un joueur devrait voir AVANT de commencer son tour (contrairement à Parasite, un vrai palier "ce tour" qui n'a effectivement rien à montrer tant qu'on n'a pas roll). **Nouveau flag distinct `Status.hide_counter_when_zero`** (`custom_resources/status.gd`) : cache uniquement le LABEL numérique à 0, laisse l'icône (et son tooltip) visible tout le combat. `status_ui.gd::_on_status_changed()` recalcule maintenant `stacks.visible` à chaque changement (avant, posé une seule fois dans `set_status()` et jamais réévalué) en plus de `visible` (le hide-tout de `hide_when_zero`, inchangé pour Parasite). `greedy.tres` : `hide_when_zero = false` + `hide_counter_when_zero = true`. Vérifié par rendu headless (`debug_bg_audit`, filtré sur "gargantua") : icône Greedy visible dès l'entrée en combat, aucun "0" résiduel.
  (7) **Batch de petits fixes (même session, sur retours de Julien)** : badge "0" de statut — `statuses/parasite.tres` passe à `hide_when_zero = true` (Greedy l'avait déjà, Parasite sur Oculus était l'oubli — le mécanisme existait, `status_ui.gd` le lit déjà) ; **Gargantua rééquilibré "moins fort au début, scale plus"** — les 3 attaques à 11 dégâts passent à 8 (`gargantua_attack_action.gd`/`_attack_turn_2.gd`/`_buff_action.gd`, defaults de script, aucun override dans l'AI .tscn) et Greedy donne **2 Strength** par palier de 6 rolls au lieu de 1 (`greedy.gd` + tooltip dans `status_tooltip.gd`) ; **Electrify+ récupère son Depleted 1** (le pass "+" l'avait retiré par erreur, confirmé par Julien — `electrify_plus.gd` reprend le bloc StatusEffect+`blue_dice_bonus_amount -= 1` du script de base, description/tags du `.tres` mis à jour) ; **Inferno (infusion Magma) : 2e burn décalé de 0.35s** (`INFERNO_SECOND_BURN_DELAY` dans `dice.gd`, timer scene-tree pause-safe, cibles re-queried au moment du tir car le 1er burn peut avoir tué des ennemis — les deux hits lisaient comme un seul nombre avant).
  (8) **Marauder rétréci ~12%** (retour Julien "really huge" après le trim de texture du 07-16 qui l'avait fait +30%) : `tier_0_machopeur.tscn` 320→280, `tier_0_machopeur_satyr`/`tier_1_machopeur_satyr`/`tier_1_machopeur_octopus` 256 (défaut)→225, chaque `position.y` descendu de Δhauteur/2 pour garder les pieds au sol (texture 384×372 → width-constrained, hauteur affichée = boîte×0.969) ; `tier_2_machopeur_octopus` (220) et `tier_2_defender_machopeur` (0.75 scale) déjà plus petits, non touchés. Vérifié par re-rendu harness (`debug_bg_audit`) sur les fights machopeur act 1+2 : grounded, hiérarchie Marauder > Kraken/Satyr conservée.

- **Passe "ground line" (même session, 3e retour de Julien avec screenshot Goblin/Oculus) : 14 combats corrigés — l'ennemi de GAUCHE de presque tous les combats multi-ennemis flottait 40-80px au-dessus de ses voisins.** Règle établie et vérifiée : **les pieds (alpha bbox, PAS la boîte du node) de tous les ennemis d'un combat doivent partager la même ligne de sol, tolérance ~25-30px — un ennemi plus BAS que ses voisins lit comme "plus proche de la caméra" (OK, pattern établi des B-variants tier 0 à ~565-573), un ennemi plus HAUT lit comme "il flotte" (jamais OK pour une créature terrestre)**. Mon audit précédent vérifiait pieds↔barre de vie (le collage) et boîte↔zones UI, jamais la cohérence de sol ENTRE ennemis du même combat — c'est exactement ce que le screenshot de Julien montrait (Goblin pieds y=482, Oculus y=540). Bande de sol des combats solo : ~497-545. Fixes appliqués (`position.y` descendu, delta = médiane des voisins − pieds − ~10) : `tier_1_oculus_goblin` Goblin +48, `tier_1_crab_satyr` Crab +52, `tier_1_machopeur_octopus` Machopeur +52, `tier_1_plant_goblin` Plant +33, `tier_1_plant_octopus` Plant +43, `tier_2_defender_satyr` Defender +26, `tier_2_machopeur_octopus` Machopeur +47, `tier_1_octopus_2_satyrs_2` S Octopus +51, `tier_0_octopus_3` S Octopus +45, `tier_0_satyrs_1_octopus_2` S Octopus 2 +43, `tier_0_satyrs_2_octopus_1` S Octopus +47, `tier_0_octopus_1_satyrs_2` S Satyr3 +40, `tier_0_satyrs_3` S Satyr2 +32, `tier_1_machopeur_satyr` Machopeur +25 (appliqué APRÈS l'édit de la session parallèle, voir ⚠️ ci-dessous). Non touchés (stagger accepté) : `tier_0_bigger_satyrs_2` (le mid est plus BAS = plus proche), `tier_0_bigger_octopus_2` (krakens flottants, léger stagger naturel). Vérifié par re-rendu complet 57 combos + zooms sur 8 combats, act 1 ET act 2. **Effet de bord attrapé par Julien au playtest : descendre un ennemi peut poser sa barre de HP sur l'art d'un voisin qui chevauchait déjà "invisiblement"** — `tier_2_machopeur_octopus` : le Marauder descendu de 47px a mis sa barre sur les tentacules du Kraken du milieu (chevauchement d'art de ~30px préexistant, invisible à l'ancienne hauteur) → Octopus1 poussé 905→955 en x, vérifié sur les 2 fonds. Après tout futur fix de ground line, re-vérifier les chevauchements barre↔art-du-voisin, pas seulement pieds↔sol. **⚠️ COLLISION DE SESSION PARALLÈLE EN DIRECT pendant cette passe** : une autre session Claude retaillait le Marauder au même moment (width 225 + repositionnements Δh/2, l'entrée "Marauder rétréci ~12%" du TL;DR) — mon premier sed sur `tier_1_machopeur_satyr` a silencieusement no-opé (le fichier avait déjà bougé 371→386). Détection : le sed "réussit" mais un grep de contrôle montre une valeur qui n'est ni l'ancienne ni la mienne + `git diff --stat battles/` montre des fichiers modifiés qu'on n'a pas touchés. Toujours re-grep la valeur cible après un sed sur les fichiers battles/ tant que Julien fait tourner des sessions parallèles.
- **Suite immédiate (même session, sur screenshot de Julien) : centrage du nom au survol + intent trop proche de la tête, tous deux sur Skeleton.** (1) **Centrage du nom** — mesuré au pixel près sur un rendu frais (post-trim/post-reimport) via 3 méthodes indépendantes (grid overlay visuel, alpha-bbox du sprite, match de couleur exact sur `font_color` de `enemy_name_label_settings.tres`) : le mécanisme `content_center_x` (ajouté par une session parallèle le même jour, voir mémoire `feedback_enemy_name_label_centering`) calcule et positionne CORRECTEMENT le label (mesuré à 1015-1016px, cible théorique 1016px, sur Skeleton ET Marauder) — **aucun bug de code trouvé**. Le "toujours pas centré" du screenshot de Julien est quasi certainement un état PÉRIMÉ de son build/éditeur (déjà tourné en continu tout le long de cette session très chargée, avant que le mécanisme de centrage + mon retrim de texture n'existent) — `_ctrl_rect()` du harness reste trompeur pour ce nœud spécifique (retourne le rect LOCAL non transformé, `[0,0,206,22]`), d'où l'ajout de champs de debug dédiés (`enemy_global_pos`/`name_label_layer_offset`) dans `debug_bg_audit.gd` pour calculer le VRAI centre attendu sans dépendre de ce rect. **Pas de fix appliqué — juste vérifié, conseillé un restart complet de l'éditeur + relance du build pour rafraîchir.**
  (2) **Intent trop proche de la tête — VRAI bug, confirmé visuellement ET numériquement, causé par mon propre retrim de texture de la session précédente.** Root cause : `intent_ui.position.y` est calculé relatif au sommet de la BOÎTE du sprite (`-sprite_display_height/2 - 30 - intent_ui_y_offset`), pas au sommet du CONTENU alpha réel — le `-30` avait été implicitement calibré sur l'ancien `skeleton4.png` qui avait 25% de marge transparente en haut (donc le crâne était naturellement loin du sommet de la boîte). En retirant cette marge par le trim, le crâne s'est retrouvé quasi au sommet de la boîte, et l'épée de l'intent (déjà callée à seulement 30px du sommet de boîte) se retrouve à transpercer le crâne. **Diagnostic en 2 temps** : un premier passage par métrique géométrique (`intent_rect.bottom` vs `head_top` alpha-bbox) a donné des FAUX POSITIFS sur presque tous les ennemis du jeu (y compris des combats connus pour être nickel, ex. Satyr/Octopus) — `intent_rect` (le rect COMPLET du Control IntentUI) contient une marge interne variable au-dessus du VRAI glyphe visible (flèche/épée), donc cette métrique n'est fiable qu'EN RELATIF entre deux ennemis dans le MÊME rendu, jamais en absolu. Confirmation définitive par mesure de pixels directe (grid overlay 4×, comparaison côte-à-côte Skeleton vs Satyr dans `tier_1_crab_satyr` — l'épée du Skeleton touchait/traversait visiblement le crâne, celle du Satyr flottait avec un vrai espace). **Fix : nouveau `intent_ui_y_offset = 25` sur les 4 combats Skeleton à cible 256×256 par défaut (`tier_0_crab`, `tutorial_fight`, `tier_1_crab_satyr`, `tier_2_plant_crab`) et `= 33` sur `tier_1_lurker_crab`** (Crab y a un override `width=320/height=320` PLUS un `scale=0.75` sur le nœud Enemy racine — l'offset local doit être plus grand pour produire le même déplacement à l'écran, 25/0.75 ≈ 33 ; **le Marauder n'a PAS eu besoin de ce fix** — vérifié par rendu direct sur ses 5 combats, son intent (essentiellement un buff "flèche montante" dans mes rendus, jamais son épée d'attaque capturée par le harness) garde déjà un espacement correct, cohérent avec le fait que son padding pré-trim était bien plus faible (6% en haut contre 25% pour le skeleton) donc l'effet de "fermeture" du trim y a été négligeable ; **Lich non plus** (padding pré-trim déjà faible, jamais retrimmé cette session, juste un retrait d'offsets Y). Vérifié par re-rendu sur les 5 combats Skeleton (act 1 ET act 2 pour les 3 qui existent en tier 1-2), tous grounded avec un espacement épée-crâne cohérent avec le reste du roster. **⚠️ Piège méthodologique à retenir : pour tout futur retrim de texture d'ennemi, vérifier aussi le rapport intent↔tête, pas seulement pieds↔barre — un trim symétrique (padding haut ET bas) déplace le sommet du contenu visible vers le haut de la boîte, ce qui rapproche mécaniquement tout élément d'UI callé "depuis le haut de la boîte" (ici l'intent), même si le bas (pieds/barre) est ce qui semblait initialement cassé.**
- **Repasse positionnement après le nouvel art Skeleton/Marauder/Lich (2026-07-16, sur signalement de Julien : Marauder flottant loin de sa barre + plus petit que le Kraken pourtant moins tanky)** : root cause = padding transparent des nouveaux PNG (`machopeur_v2_no_shadow.png` ne remplissait que 73% de sa boîte — 23% de vide en haut, 6% en bas ; `skeleton4.png` 25% de vide en haut). **Fix à la racine : les 2 textures TRIMMÉES sur leur bbox alpha (+8px de marge), en place sur disque** (originaux récupérables via git, commit `5aa0b61`) — corrige d'un coup flottement ET taille apparente (+30% Marauder, +7% Skeleton) dans TOUS les combats qui les utilisent, au lieu de 10 patchs d'offsets par fight. Conséquences appliquées : `content_center_x` re-mesuré (machopeur 0.465→0.499, crab 0.499→0.5, sur les 7 `.tres`) ; offsets périmés retirés (`tier_0_machopeur.tscn` -24/-24, `tier_elite_lich.tscn` -15/-15 — le nouveau gptlich.png est déjà serré, la barre CHEVAUCHAIT la robe de 7px avec les vieux offsets). Vérifié par re-rendu complet des 19 combats machopeur/crab/lich/tutorial : gaps barre-pieds tous à 1-10px (étaient 11-16 + chevauchement lich), hiérarchie taille↔HP correcte (Marauder 255px domine Kraken 167px), règle mobilier act-2 respectée (pieds ≥507 à x>1000). **⚠️ Piège réappris : après avoir écrasé un PNG source, `--headless --import` OBLIGATOIRE avant tout rendu headless** (sinon l'ancien art rend depuis le cache .godot/imported). **Même session : statuts de Blessing nettoyés sur retour de Julien** — (1) badge de stacks/durée masqué sur les 4 statuts de blessing qui en affichaient un (`status_marionette`/`_plus`/`status_berserk`/`status_emanation` : `stack_type` 2/1→0 (NONE), changement DATA-ONLY sans risque de strip — sans effet mécanique vérifié : `can_expire=false` sur les 4 donc la ré-application était déjà un no-op dans les deux branches, aucun script ne lit leurs stacks/duration, l'effet réel d'Emanation vit dans `Global.blue_dice_bonus_amount_fight`) ; (2) titre du tooltip de statut : `status_tooltip.gd` fait maintenant `id.trim_suffix("_plus").capitalize()` — "Marionette Plus"→"Marionette" pour TOUS les statuts "+" présents et futurs (les descriptions restent celles du `.tres`, déjà correctes).
- **Audit backgrounds × positionnement ennemis (2026-07-15, après le passage aux 6 fonds de combat par act/tier)** : les 34 combats du pool + le combat tutoriel rendus sur CHAQUE fond où ils peuvent réellement apparaître en run (57 rendus — tier 0 → act1 hallway seul ; tiers 1-2 → act1 + act2 hallway via le recyclage `ACT2_SOURCE_TIER` ; élites → les 2 fonds élite ; boss → les 2 fonds boss ; les fonds sont choisis par `battle.gd::_select_background_texture()` keyé sur `act_tier`+`Global.current_act`, fallback `20-2.jpg` pour les lancements debug sans act_tier — volontaire). **Verdict : UN seul vrai problème trouvé et corrigé** — le Marauder de `battles/tier_2_defender_machopeur.tscn` "marchait sur les jardinières" de la bibliothèque act 2 (pieds alpha à y=495 dans la bande de mobilier du bord droit, x>1000 / y<~507) → `position.y` 411→433 (pieds ~517, sur le dallage), vérifié par re-rendu sur les DEUX fonds (bonus : les 2 barres de HP du combat sont maintenant quasi alignées). Tout le reste tient : règle x≥750 du 07-11 toujours respectée, AUCUN intent sur le nombre de Power, chevauchements de tentacules connus/cosmétiques inchangés, statuts sous les barres. **Harnais réutilisable laissé à la racine (`debug_bg_audit.gd`/`.tscn`, non committé)** : boot d'une VRAIE scène (autoloads garantis, contrairement à `--script`) qui instancie chaque fight dans un SubViewport 1280×720 avec le vrai fond+shader+Player+intents forcés (`enemy.update_action()`), PNG + `geometry.json` (rects sprite/intent/stats/status calculés par le moteur) ; env `BG_AUDIT_OUT` (dossier de sortie) + `BG_AUDIT_ONLY` (filtre substring pour itérer sur un seul combat) ; cmdline dans l'en-tête du fichier. **Observations non traitées (à trancher par Julien)** : (1) Oculus et Gargantua affichent dès l'entrée en combat une icône de statut minuscule avec un badge "0" (compteurs Parasite/Greedy à 0 stacks — préexistant, rien à voir avec les fonds) ; (2) lisibilité act 2 : le Marauder sombre se fond un peu dans les étagères sombres, et les intents près de l'orbe lumineuse droite / de la statue verte perdent un peu de contraste — jugés lisibles sur zooms, pas retouchés.
- **Système de rareté de carte (Common/Uncommon/Rare) IMPLÉMENTÉ (2026-07-15), design discuté et split validé par Julien avant codage — draw vérifié par run headless (150 écrans NORMAL sans slot perdu, 30 écrans BOSS tout-rare sans doublon, 100 écrans ELITE avec le boost attendu), visuel partiellement playtesté par Julien (gemmes/titres v2 pas encore vus en jeu).**
  - **⚠️⚠️ INCIDENT MAJEUR À CONNAÎTRE (même session, DEUX strips successifs) : l'éditeur Godot de Julien a STRIPPÉ les 58 lignes `rarity_tier` des `.tres` — deux fois.** Root cause confirmée par forensics (mtimes + PID) : son process éditeur tournait depuis LA VEILLE (démarré avant que `rarity_tier` n'existe dans `card.gd`), et **Godot ne rafraîchit pas la liste des propriétés exportées d'un script édité en externe tant que le process éditeur n'est pas redémarré**. Mécanisme du strip : à chaque F5, l'éditeur reprend le focus → rescanne → détecte les `.tres` modifiés → les recharge avec SON script périmé → droppe la propriété "inconnue" → le save-on-play les réécrit strippés sur disque (il ajoute des `uid=` aux lignes ext_resource au passage, signature d'un re-save éditeur). Le second strip a annulé un re-patch en ~3 minutes, au F5 suivant. **Symptôme : toutes les cartes lisent `rarity_tier = 0` au runtime → les tirages Uncommon/Rare renvoient null → écrans de récompense à 0-2 cartes au lieu de 3** — c'était le bug "only 2 cards" de Julien, PAS un bug de logique de draw. **Protocole de fix appliqué et validé : fermer COMPLÈTEMENT l'éditeur → re-patcher → rouvrir** (l'éditeur frais connaît le champ, les saves suivants le préservent — autoguéri définitivement une fois qu'il re-sauvegarde les cartes lui-même). **Règle générale pour tout futur patch scripté de `.tres` ajoutant une propriété @export fraîchement introduite : exiger un redémarrage de l'éditeur de Julien AVANT le patch, sinon le prochain F5 le détruit.** Si les écrans à 2 cartes reviennent : `grep rarity_tier characters/warrior/cards/card_all_in.tres` — vide = re-strippé (listes de tiers pour re-patcher dans la mémoire `project_card_rarity_system.md`). Piège de diagnostic secondaire : en mode `--script` headless (sans autoloads), `card.gd` ne compile pas (`Global` introuvable) et TOUTES les propriétés script des `.tres` chargent silencieusement à leur valeur par défaut — ne JAMAIS diagnostiquer un problème de chargement de ressource dans ce mode ; lancer une vraie scène (`--headless res://debug_scene.tscn`) à la place, qui boote les autoloads.
  - **Nouveau `Card.RarityTier {COMMON, UNCOMMON, RARE}` + `@export var rarity_tier`** (`custom_resources/card.gd`) — **distinct et sans rapport avec l'ancien `Card.Rarity {NORMAL, SUPPORT}`**, qui reste exactement ce qu'il a toujours été (le flag mécanique "cette carte ne reset pas le Power", lu dans `hand.gd:296`) — volontairement non renommé/touché pour ne pas casser les ~219 `.tres` existants ni conflater un mécanisme de gameplay avec une propriété de table de loot. COMMON = valeur par défaut de l'enum, donc seules les cartes Uncommon/Rare ont besoin d'une ligne `rarity_tier = N` dans leur `.tres`.
  - **Split final (pool 67 cartes après 4 coupes)** : **34→38 commons, 24 uncommons, 5 rares** — voir la review complète (verdicts de Julien carte par carte) dans la mémoire `project_card_rarity_system.md`. **Coupées du pool draftable** (fichiers intacts sur disque, juste retirées de `warrior_draftable_cards.tres` — ext_resource ET entrée du tableau `cards`) : Swipe, Resonance, Jackpot, Stockpile. **5 rares retenues** : Berserk, Crescendo, All In (carte préférée de Julien), Transmutation, Tidal Force — décision explicite de Julien de garder la rareté RARE comme une barre de qualité plutôt qu'un quota (pool volontairement mince, à étoffer plus tard avec du contenu du calibre d'All In/Crescendo plutôt que de promouvoir des cartes "juste correctes"). **Règle préservée : aucune carte Blessing n'est jamais Common** (les 7 Blessings du pool sont Uncommon ou Rare). Patché sur 58 fichiers `.tres` (29 cartes de base + leurs 29 `_plus.tres`, tous scriptés via insertion de ligne après le `rarity = ` existant — chaque `_plus` hérite du tier de sa base).
  - **Visuel : gemme dans la bannière, PAS sur l'artwork — et sur TOUTES les cartes, Common inclus (passe de polish post-screenshot du même jour)** — un TextureRect `RarityGem` dans le slot droit de `CardBanner` (repris de l'ancien `SupportIcon`, un nœud mort jamais réellement affiché — renommé dans `card_ui.tscn` ; nœud équivalent ajouté à la main dans `card_menu_ui.tscn`, qui n'avait aucun slot similaire). **Deux itérations avant validation par Julien** : un premier essai plaçait la gemme sur le coin de l'illustration (indépendant de la longueur du titre) — **rejeté** ("could be easily missed by players"). **3 gemmes désormais (décision Julien après avoir vu le premier build : "a bit weird to have nothing on some (most) cards")** : Common = gemme gris pierre mate (halo d'ombre noir), Uncommon = argent/bleu, Rare = or à halo chaud — halos cuits dans les PNG (pas d'équivalent CSS `box-shadow` dans Godot). Les cartes starter (Strike/Block/etc., non-draftables) affichent la gemme Common gratuitement puisque COMMON est la valeur par défaut de l'enum — aucun `.tres` starter à toucher. **Assets v2 (`assets/images/rarity_gem_common/uncommon/rare.png`, script PIL)** : la v1 dessinait le diamant sur 34% du canvas seulement → rendu ~7px en jeu, quasi invisible (signalé par Julien vs l'artifact de preview) ; v2 remplit ~75% du canvas, slot `RarityGem` 16×18px à 4px du bord droit. **Centrage du titre : insets symétriques 18px cuits dans les DEUX `.tscn`** (`Title.offset_left=18/offset_right=-18`) — la v1 ne resserrait que `offset_right`, ce qui décalait visiblement les titres à gauche (signalé par Julien sur screenshot). **`title_font_size_for()` réécrit : mesure la VRAIE largeur rendue** (`Font.get_string_size` sur la police du `card_title.tres`) contre `TITLE_MAX_WIDTH=104`, candidats `[15,12,10,9]`pt, premier qui rentre gagne — les seuils par nombre de caractères sont morts (impossible à calibrer : "Necromancy+" fait 11 caractères mais rend plus large que plusieurs noms de 13). Vérifié par mesure PIL sur les ~219 noms : 162 restent à 15pt, 43 à 12pt, 12 à 10pt, seuls "Perpetual Motion"/"+" tombent à 9pt — **Julien a confirmé être à l'aise de renommer ce genre de nom plutôt que de complexifier le layout.** Plus aucun juggling d'offset au runtime ni de branche else de reset : la texture de gemme est écrasée inconditionnellement à chaque `set_card()`, donc les nœuds réutilisés (`UpgradeConfirmPanel.BeforeCard`/`AfterCard`) ne peuvent plus fuiter la gemme d'une carte précédente par construction. **Gemmes v3 (retour playtest de Julien, même soir)** : l'Uncommon argent/bleu v2 était quasi indistinguable du gris Common ET se fondait dans la bannière bleu-acier des Celestial — v3 = Uncommon **bleu franc saturé**, Common gris pierre, Rare or inchangé ("reads great"), + **liseré sombre en losange cuit derrière les 3 gemmes** pour se détacher de n'importe quelle couleur de bannière. **Tooltip de rareté au survol de la gemme, UNIQUEMENT dans `CardMenuUI`** (rewards/deck view/shop — la gemme de la carte en main reste inerte, demande explicite) : entrées "Common"/"Uncommon"/"Rare" dans `tooltip.gd::get_tooltip_content`, gemme en `mouse_filter=1` (PASS : reçoit le hover, laisse les clics remonter au gui_input de la carte — un STOP aurait créé une zone morte de 16px sur le clic de sélection de reward) côté menu, `mouse_filter=2` (IGNORE) côté `card_ui.tscn` (sinon zone morte de drag en main — le défaut Control est STOP, piège). Cleanup anti-leak : `_exit_tree()` ajouté sur `CardMenuUI` (choisir un reward libère l'écran pendant que la gemme est encore survolée → `mouse_exited` ne fire jamais, pattern de leak documenté) + timer de secours 6s.
  - **Tirage pondéré étendu, pas remplacé** — `battle_reward.gd` avait déjà un système de poids + pity (`_setup_card_chances`/`_modify_weights`), mais construit sur le MAUVAIS axe (`Card.Rarity.NORMAL/SUPPORT`, le flag mécanique). Rebâti sur `Card.RarityTier` : nouveaux poids `RunStats.common_weight`/`uncommon_weight`/`rare_weight`. **⚠️ Retuné le soir même après playtest (Julien : "insane amount of uncommon & rare") : la V1 tickait le pity PAR SLOT (+0.3 ×3 par écran → cap atteint en ~3 combats → ~24% de rare par slot, >50% des écrans avec un rare).** Version finale : **base 6.0/3.7/0.3 = 60/37/3% par slot (les odds hallway de STS, demande explicite de Julien)**, pity **par ÉCRAN** (`_update_rare_pity(rare_drawn)` appelé UNE fois après la boucle des 3 slots : +0.2 par écran sans rare, cap 2.0 ≈ 17%/slot, reset à 0.3 dès qu'un écran contient un rare ; `_setup_card_chances()` sorti de la boucle du coup, les poids ne bougent plus en cours d'écran). **Vérifié par simulation headless de 80 runs persistants × 13 écrans : 20% des écrans contiennent un rare (~2.6/run), distribution globale 58/35/7 — l'unique bouton de tuning si Julien veut plus/moins de rares est `RARE_WEIGHT_PITY_CAP`.** **Nouveau `BattleReward.RewardContext {NORMAL, ELITE, BOSS}`** (`@export`, posé par `run.gd::_on_battle_won` selon `map.last_room.type`, AVANT que le bouton "Add New Card" soit cliquable — les rewards d'event via `_on_show_reward()` ne le posent jamais donc restent NORMAL par défaut, correct, pas de odds boost sur un reward d'event). **BOSS = les 3 offres sont TOUJOURS Rare** (contourne entièrement le tirage pondéré, ne touche jamais le pity) ; **ELITE = multiplicateurs locaux** (`ELITE_UNCOMMON_MULT=1.4`, `ELITE_RARE_MULT=4.0` ≈ 48/42/10 par slot, proche du 50/40/10 élite de STS — appliqués uniquement au poids de CE tirage, jamais écrits dans `run_stats`).
  - **Nouveau helper partagé `custom_resources/card_rarity_draw.gd` (`class_name CardRarityDraw`, statique)** : `pick_card(pool, tier, owned)` filtre par tier puis, **uniquement pour RARE**, exclut les cartes déjà possédées (`owned` = `character_stats.deck.cards`) avec repli sur le pool complet si tout est déjà possédé — avec seulement 5 rares au total, réoffrir un rare déjà en deck (ex. boss d'act 2 après le boss d'act 1) lirait comme un slot mort plutôt qu'un jackpot. Utilisé à la fois par `battle_reward.gd` (hallway/elite/boss) ET `card_shop.gd` (slot rare de la boutique) — un seul endroit à retoucher si cette règle doit changer.
  - **Boutique repensée** : `card_shop.gd::_generate_shop_cards()` passe d'un shuffle aveugle (`slice(0,5)`, pouvait afficher 0 carte "spéciale") à une **composition garantie 2 Common / 2 Uncommon / 1 Rare** (`SHOP_COMPOSITION`, via `CardRarityDraw`) — chaque visite de boutique contient maintenant exactement une tentation chère. **Prix par tier** au lieu d'un `randi_range(30,80)` plat (`shop_card.gd`) : Common 40-55, Uncommon 60-80, Rare 95-125 (délibérément sous les reliques à 120-170) — `gold_cost` calculé dans `set_card()` une fois `card.rarity_tier` connu, plus à l'`@onready` (qui tournait avant que la carte soit assignée).
  - **Sauvegarde** : `normal_weight`/`support_weight` retirés du dict `run_stats` sauvegardé (`run.gd`), remplacés par le seul `rare_weight` (common/uncommon ne bougent jamais à l'exécution, pas besoin de les persister) — `data.get("rare_weight", RunStats.BASE_RARE_WEIGHT)` au chargement pour les vieilles saves sans cette clé.
  - **Non fait / laissé pour Julien** : contenu des futures cartes rares (il a dit vouloir y réfléchir séparément — les 5 rares actuelles sont toutes orientées dégâts/finisher, aucune n'est un capstone block/dice-economy/scout/Evil-0) ; aucun playtest en jeu, seule la syntaxe est vérifiée.

- **Système "Dice Infusion" post-boss act 1 IMPLÉMENTÉ (2026-07-14) — l'équivalent des ancient relics de STS2 : après les récompenses du boss d'act 1, un écran cérémoniel propose d'infuser (upgrader) UN de ses types de dés parmi 2 candidats, puis on arrive sur la map d'act 2. NON PLAYTESTÉ** (l'écran a été vérifié par rendu headless — 4 captures : entrance/sélection/cérémonie/settle — mais les 2 effets gameplay n'ont jamais tourné dans un vrai combat).
  - **Flow** : `run.gd::_show_map()` intercepte `act_transition_pending` (posé au boss win) → `_show_dice_infusion()` (`_change_view(DICE_INFUSION_SCENE)`, l'ancien appel direct à `_enter_act_2()` depuis `_show_map` est SUPPRIMÉ) → le joueur choisit (pick OBLIGATOIRE, pas de skip — décision Julien) → `Events.dice_infusion_completed` (nouveau signal) → `_on_dice_infusion_completed()` = `_enter_act_2()` + `_show_map()` (le heal/nouvelle map/bannière ACT 2 ne se déclenchent donc qu'APRÈS l'infusion, pas sous l'écran). Quitter pendant l'écran = reprendre au checkpoint pré-boss (même comportement assumé que quitter sur l'écran de récompense).
  - **Données** : `custom_resources/dice_infusions.gd` (`class_name DiceInfusions`, statique) = source unique des designs (`INFUSIONS` dict par type : id/name/description/accent/outline/aura_magic/aura_accent, + `preview_face` et `roll_values` optionnels). `roll_candidates()` = types possédés (`max_amount > 0`) ayant une entrée INFUSIONS et pas déjà infusés, shuffle, prend 2 — le pool s'élargit tout seul avec chaque entrée. État : `Global.dice_infusions` ({type: id}) + helper `Global.is_dice_infused(type)`, reset dans `reset_run_state()`, **sauvegardé/restauré** (clé `"dice_infusions"` dans le save dict, restore via `data.get(..., {})` pour les vieilles saves).
  - **⚠️ 9 infusions au total désormais — 1 par type (2026-07-14, seconde passe, sur specs de Julien). NON PLAYTESTÉES EN COMBAT (seul l'écran de choix des 9 est vérifié par rendu headless).** Blue→**Arcane** (roll 6 naturel → draw 2), Red→**Berserker** (carte socketée +50% dégâts). Nouvelles : Evil→**Repented** (faces 6,6,6 — retire le 0), Giant→**Bulky** (faces 7-12 — retire 1-6), Green→**Gnome** (roll 1 naturel → Charge 1 Blue Dice, mêmes 3 appels que le trigger Sigil), Mech→**Clockwork** (2 ajustements ±1 au lieu d'1), Magma→**Inferno** (1er roll du tour brûle 2× = double AoE), Odd→**Bulwark** (chaque roll donne du Block = valeur, EN PLUS du Power — décision Julien, additif), Even→**Octet** (roll 8 naturel → +8 Strength ce tour seulement). Toutes les couleurs/faces d'aperçu de première passe, tunables dans le seul dict INFUSIONS.
  - **Hooks des 7 nouvelles** (tous dans `dice.gd` sauf indiqué) : **Repented/Bulky** changent le SET de valeurs via `DiceInfusions.roll_values_override(type)` appliqué à `values`/`faces` dans `roll_dice()` ET au preview Scout dans `battle.gd::_on_scout_effect` (les 2 doivent rester synchro, sinon le scout ment) ET dans `lucky.gd`/`unlucky.gd` (sinon Unlucky forcerait un "1" absent du Bulky → find()=-1 → roll random). **Gnome/Bulwark/Octet** = `_apply_infusion_roll_effects()` (appelé dans `_apply_roll_result`, keyé sur `Global.last_roll` NATUREL comme Arcane, AVANT `next_roll_modifier`) : Gnome charge blue, Bulwark applique un `BlockEffect` (son `art/block.ogg`) sur `Global.player`, Octet applique Muscle 8 + `Global.lose_strength_next_turn += 8`. **Inferno** = double `DamageEffect.execute` dans le bloc magma existant, gardé par `_magma_burned_this_turn` (reset dans `_on_player_turn_started`). **Clockwork** = `mech_adjustment_used` (bool) converti en compteur `mech_adjustments_used` (int) + helper `_mech_adjustments_allowed()` (2 si infusé, sinon 1).
  - **⚠️ Bug latent corrigé au passage (Octet en dépendait)** : `Global.lose_strength_next_turn` n'était JAMAIS remis à 0 après consommation (fury.gd le posait, muscle.gd le soustrayait à `check_if_losing_strength`, mais rien ne le nettoyait) → un gain de Strength permanent ultérieur se serait fait drainer à chaque tour par une valeur périmée. Fix : `player_handler.start_turn()` remet `lose_strength_next_turn = 0` juste après l'emit (corrige fury du même coup).
  - **Arcane (trigger)** : dans `dice.gd::_apply_roll_result`, sur `dice_type == "blue" and Global.last_roll == 6` — donc la **face NATURELLE**, AVANT application de `next_roll_modifier` : un 6 garanti par Scout/Lucky compte (ils forcent la vraie face), un 5+Boost→6 ne compte PAS (décision explicite de Julien). **Effet changé le 2026-07-14 (2e passe) : n'est plus "draw 2 cards" mais "deal 5 damage to all enemies"** (`ARCANE_AOE_DAMAGE` const) via un `DamageEffect` AoE (même pattern que magma — prend les modifiers DMG_TAKEN de la cible type Exposed mais PAS la Strength joueur ; `berserker_boost_active` est false ici car ça part d'un roll, pas d'un play de carte socketée).
  - **Berserker (+50%)** : `Global.playing_red_card` est true EXACTEMENT pendant le play d'une carte socketée sur le dé rouge (les deux chemins : instantané ET aim-then-release) → `card.gd::play()` pose un flag scopé `Global.berserker_boost_active` autour d'`apply_effects()` (garde le boost hors des dégâts de reliques réagissant au même roll, ex. House Money) → `damage_effect.gd` applique `ceili(amount * 1.5)` **sur les cibles Enemy uniquement** (le self-damage type backfire n'est pas puni) ; shake/hit-stop scalés sur le montant boosté. **Preview du +50% CORRIGÉE (même session, sur demande de Julien)** : `Card.apply_target_modifier()` applique maintenant le même `ceili(×1.5)` quand `instance_id == Global.charged_card_instance_id` et red infusé — ce helper est préview-only par construction (jamais appelé depuis un `apply_effects`, audit 2026-07-10), donc pas de double-compte avec damage_effect ; boost appliqué AVANT le modifier de cible (même ordre que le vrai flow). Vérifié par test headless (10→15, 9→14, non-socketée/non-infusée inchangées). **Piège d'ordre corrigé au passage** : `card_released_state.gd` émet `card_charged` AVANT d'assigner `charged_card_instance_id` → le refresh initial du socket dans `dice.gd::_on_card_charged` est passé en `call_deferred` pour voir l'id.
  - **Identité visuelle des dés infusés** : `DicePalette.accent()/outline()` sont maintenant infusion-aware (levier unique → nombre de Power, orbes, particules, popups, glow de carte recolorés d'un coup) ; l'aura du shader passe par un nouveau `dice.gd::_resolve_aura_material()` qui recolore une **COPIE dupliquée** du material préchargé (cache d'instance par combat — ne JAMAIS muter le .tres partagé, même bug que l'anim charge d'avant) ; `dice_tooltip.gd` renomme le tooltip ("Arcane Dice" dans la couleur d'accent) + ajoute la ligne d'effet — **et le panel s'auto-dimensionne maintenant en hauteur** (le 204×88 fixe du `.tscn` tronquait la ligne Berserker, signalé par Julien) : `fit_content = true` sur les 2 RichTextLabels dans un nouveau `_ready()` + `_fit_to_content()` (1 frame d'await pour que la largeur soit posée, puis hauteur = `vbox.get_combined_minimum_size().y` + marges, plancher 88px pour que les dés non-infusés gardent le look d'origine ; vérifié par rendu headless green/arcane/berserker). ⚠️ En mode `--script` headless, configurer un node ajouté pendant `_initialize` doit attendre 1 frame (les nodes n'entrent dans l'arbre qu'au 1er tour de main loop — `@onready`/`get_tree()` sont null avant, artefact de debug uniquement, pas un bug du jeu). `keyword_colorizer`/le texte des cartes gardent volontairement les noms/couleurs de base ("Blue Dice" dans un texte de carte reste bleu de base).
  - **Écran** (`scenes/dice_infusion/dice_infusion.tscn` + `.gd`) : fond quasi-noir, titre MinionPro or (langage act banner), panels d'option construits en code (comme tutorial_overlay — le thème Cinzel global est illisible en corps de texte), dé face 6 en grand + halo additif pulsant + motes montantes dans la couleur d'infusion, sélection = bordure/glow accent + l'autre panel dim, bouton INFUSE (désactivé avant sélection) → cérémonie gather→détonation (flash plein écran + hit-stop 0.12 + surbrillance) → settle → emit. Le contenu des panels est centré verticalement (`vbox.alignment = CENTER`) et le dé settle à scale 1.0 (1.12 chevauchait le label du nom).
  - **⚠️ Piège rencontré (méthode de rendu headless)** : un `class_name` fraîchement créé n'est PAS dans `.godot/global_script_class_cache.cfg` tant que l'éditeur n'a pas rescanné → le run headless échoue en cascade ("Identifier not declared" y compris dans des scripts sans lien direct, via DicePalette→card_particles etc.). Fix : `--headless --editor --quit-after 900` régénère le cache (`--quit` seul abort le scan de fichiers avant la fin, warning "Scan thread aborted"). À refaire pour tout futur nouveau `class_name` testé en headless avant ouverture de l'éditeur.
- **Passe de polish sur le tutoriel de combat (2026-07-14), sur retours de Julien après 5 screenshots — NON ENCORE PLAYTESTÉ.** Tout dans `scenes/battle/tutorial_director.gd` + `scenes/tutorial_overlay.gd` (+ 2 wrappers dans `scenes/ui/hand.gd`).
  - **Étape relique déplacée après les 3 rolls Blue (plus avant).** `_step_t1_relic` ouvrait le tuto avant même le premier clic sur ROLL, lu comme un aparté "attends, quoi ?" hors-sujet. Déplacée dans `_steps` juste après `_step_t1_3b` (donc après les 3 rolls Blue forcés, quand le slot est retombé à 0/2) — texte réécrit au passé ("tu viens de rolled 3 fois avec seulement 2 Blue Dice"), ne dépend donc plus du nombre "3/2" affiché AVANT le premier roll (qui redevient un 2/2 normal dès le 1er roll, donc invisible après coup). Pointe maintenant sur l'icône de relique de la top bar (`_relic_icon_rect()`, helper qui récupère la 1re `RelicUI` de `RelicHandler.relics` via `get_tree().root.find_child("RelicHandler", ...)` — vit dans `run.tscn`, hors de `battle.tscn`, hors de portée directe du director) plutôt que sur le slot de dé. **⚠️ 1er essai ciblait toute la `RelicBar` (~450px de large) → le glow lavait tout l'écran ; corrigé pour ne cibler que l'icône (64px).**
  - **Rectangle "spotlight" (le cutout à 4 masks plats) remplacé par un glow additif pour tout highlight PUREMENT informationnel** (nombre de Power, icône d'intent ennemi, icône de relique) — Julien : "looks like a very rough, obvious rectangle" sur le nombre de Power, et une ligne horizontale parasite visible sur un des screenshots (T1.5, "Power crashed to 0") a disparu du même coup en arrêtant d'y découper un trou (cause exacte de cette ligne non identifiée avec certitude, mais elle n'existait que sur ce spotlight-là). Nouveau `TutorialOverlay.show_glow(rect, color)`/`hide_glow()` : texture radiale douce en cache + `CanvasItemMaterial` blend additif (même recette que `dice.gd::_get_power_orb_texture`), pulse scale/alpha en boucle. **⚠️ Un premier essai (diamètre `max(w,h)*2.2 + 40`, alpha peak 0.85) a été jugé "insanely intense & big" par Julien (lavait tout l'écran en or) — retaillé beaucoup plus petit et doux : `GLOW_DIAMETER_CAP=100`, `GLOW_SIZE_FACTOR=0.9`, `GLOW_ALPHA_PEAK=0.32`, un halo à peine plus grand que la cible. La leçon `feedback_flat_fills_preferred` s'applique : Julien préfère toujours discret+net à extravagant.** `GLOW_COLOR_GOLD` (= couleur de bordure du pulse_frame) par défaut, `GLOW_COLOR_THREAT` (rougeâtre) pour l'intent ennemi (T1.6, différencie "info" de "danger"). `set_spotlight`/le cutout à 4 masks reste utilisé tel quel pour tout ce qui est CLIQUABLE (ROLL, slots de dé, End Turn) — distinction : glow = "regarde", pulse_frame/spotlight = "clique". **Glow du nombre de Power recentré : `_power_number_rect()` renvoie la moitié GAUCHE du rect du Label `CurrentPower` — le Label fait 90px de large mais son chiffre est aligné à gauche, donc centrer le glow sur le rect complet le posait trop à droite (Julien : "off-centered, too much on the right").** **Flèche sur le nombre de Power : d'abord ajoutée (T1.5), retirée (mal comprise), puis RÉTABLIE — Julien voulait bien une flèche (glow + flèche). Comme le nombre de Power est glow'd DANS le même step qu'un autre pointer (ROLL en T1.3, carte en T2.7), l'overlay a maintenant un DEUXIÈME nœud flèche `info_arrow` + `show_info_pointer()`/`hide_info_pointer()` (la flèche principale `pointer_arrow` reste sur la cible d'ACTION, l'info_arrow pointe la cible INFORMATIVE) ; flèche Power posée en T1.3/T1.5/T2.7. Idem la relique (T1.relic) a maintenant sa flèche — `pointer_arrow` en mode UP posée SOUS l'icône (elle est tout en haut de l'écran, une flèche DOWN au-dessus sortirait du cadre — nouveau helper `_point_below()`).**
  - **Highlight de carte en main ne suivait pas le fanning (rotation) des cartes** (`overlay.show_pulse(_card_rect(...))` dessinait un rectangle axis-aligned autour d'une carte tournée de plusieurs degrés par `hand.gd::_update_card_positions`) — déjà signalé une fois pour le glow "jouable". Fix : lift+dé-rotation+scale de la carte réelle (le même visuel que le hover) via 2 wrappers `Hand.highlight_card_lift()`/`clear_card_lift()`. **⚠️ Piège rencontré et corrigé : NE PAS réutiliser `_on_card_mouse_entered` pour ça — il early-return sur `card.disabled`, et le tuto lift une carte DANS le même step qui la ré-active (le gate est appliqué juste après le lift), donc au moment du lift la carte est encore `disabled` → le hover no-op silencieusement (c'est exactement pourquoi le highlight "ne marchait plus" au 1er playtest de cette passe). Les wrappers dupliquent donc la logique de lift/reset en ignorant `disabled` volontairement.** `TutorialDirector._lift_card(card_id)`/`_unlift_card()` (un seul `_lifted_card` traqué, nettoyé dans `_reset_between_steps()`), remplace les 8 `show_pulse(_card_rect(...))`/`_card_requirement_rect(...)` — ces 2 helpers de lookup rect supprimés (morts). **⚠️ 2e correction : le lift retombait dès qu'on survolait PUIS quittait la carte (le `mouse_exited` du hover normal la resettait). Fix : `Hand.tutorial_locked_card` — tant qu'une carte est lockée, `_on_card_mouse_entered/_exited` ré-appliquent le lift au lieu de reset (sauf en plein drag, où le drag possède la position) ; nettoyé par `clear_card_lift` et par `discard_card` (garde anti-référence-freed).** **⚠️ 3e correction (VRAI bug) : `highlight_card_lift` faisait `if not _original_positions.has(card): return` — donc si le cache des positions de l'éventail n'était pas encore peuplé au moment de l'appel, le lift ne s'appliquait PAS (seul le lock était posé), la carte restait à plat (Julien : "highlight on strike way too low" = pas liftée du tout). Fix : fallback sur `card.position.y` courant quand le cache est vide.** **⚠️ 4e correction : une fois le lift qui marche, `-64`+scale `1.22` a été jugé "weird" — Julien voulait juste "lift it slightly from the hand and keep it there". Ramené à `TUTORIAL_LIFT=-30`, `TUTORIAL_SCALE=1.06` (léger, les autres cartes gatées/dimmées font le gros du "c'est celle-là") ; le lock de persistance reste.** **⚠️ Contrainte CanvasLayer : la carte (dans `Hand`, layer bas) ne peut JAMAIS passer au-dessus de la boîte de texte de l'overlay (layer 20) — la zone `above_hand` est remontée (`y1`→450) mais avec un lift léger la carte reste largement sous la boîte de toute façon.**
  - **Flèche au-dessus de l'ennemi mal positionnée** (`_enemy_arrow_point()` utilisait un `Vector2(0, -90)` codé en dur, jamais mesuré). Nouveau `_enemy_intent_rect()` réutilise la position de `IntentUI` (déjà calibrée par ennemi dans `enemy.gd::update_enemy()`). La flèche pointe `_point_above(rect) + Vector2(0, ENEMY_ARROW_DROP=34)` — le `+34` est le "move it slightly down / closer to the skeleton" de Julien (au-dessus PILE du haut de l'icône, c'était encore un peu trop haut). Corrige les 3 usages (T1.4, T2.5, T3.6).
  - **Boîte de texte "near_dice" repositionnée + tail façon bulle de dialogue.** Julien : trop loin à gauche + devrait "look like it's the actual main character talking". Zone `near_dice` décalée à droite (`x: 20→90, w: 380→360`) + champ `"speaker": true` déclenche un petit tail à 2 triangles (`Polygon2D` bordure + fill). **⚠️ Itéré 2 fois : (1) le tail était accroché au coin bas-GAUCHE du panneau, pointait dans le vide → suit maintenant `SPEAKER_HEAD_X=240` (design-x de la tête du perso, clampé dans la largeur) ; (2) le tail est accroché au bord BAS de la boîte, or `near_dice` était en `attach:center` → le bord bas (et donc le tail) descendait quand le texte s'allongeait, décalant le tail sur les boîtes hautes (relique) → `near_dice` passé en `attach:bottom` (`y1=335` fixe), le bord bas reste à hauteur constante quelle que soit la longueur du texte, tail cohérent.** Approximation fixe (le perso est à une position fixe bottom-left), pas un calcul dynamique — à retuner si mal calé. Tout en code dans `set_text()`/`hide_text()`, pas de nœud `.tscn`.
  - **Round 4 de playtest (mêmes fichiers) : flèche Power statique + petite note, flèche relique réorientée, boîtes remontées.** (1) `show_info_pointer` rendu STATIQUE (plus de bob) — Julien voulait que la flèche Power ne bouge pas (lit comme un label, pas un "fais qqch ici"). (2) Nouvelle petite boîte d'annotation `show_info_note(text, anchor_rect)`/`hide_info_note` (`info_panel`+`info_label`, ~224px, réutilise le stylebox/police de la boîte principale en plus petit) posée À DROITE de l'ancre (l'espace au-dessus du nombre de Power est encombré par le panneau/slots de dés) — utilisée en T1.3 pour "This is your Power..." pendant que la boîte-héros ne garde que l'action ; texte de T1.3 scindé en conséquence. (3) `_power_number_rect()` resserré sur une boîte 60×60 CENTRÉE sur le glyphe (le Label fait 90×96, chiffre aligné gauche+centré vertical) — avant la flèche pointait très au-dessus du chiffre (haut du Label vide). (4) **Flèche relique réorientée : le PNG `tutorial_arrow.png` (UP) ne pointe PAS vers le haut → `show_pointer` utilise maintenant TOUJOURS la texture DOWN + `flip_v` pour UP** (relique = flèche UP sous l'icône, elle est coincée sous la top bar donc pas de place pour une flèche DOWN au-dessus). (5) `near_dice` remontée (`y1` 335→310) pour dégager le bob d'idle du héros ; `above_hand` remontée (`y1` 524→450) — T2.5 raccourcie pour que sa boîte (haute) ne recouvre pas trop le dé. (6) **T3.3 (scout) : boîte déplacée de `top` → `above_hand` — le panneau Scout (3 faces au choix) s'ouvre en haut-centre et la boîte `top` le recouvrait (Julien : "explanation box is hiding the scout panel; put it lower").**
  - **Non résolu / à surveiller au prochain playtest** : la carte liftée vs la boîte `above_hand` reste le point le plus fragile (valeurs `TUTORIAL_LIFT=-64` / `above_hand y1=450` estimées à l'œil, contrainte CanvasLayer non contournable) — si la carte dépasse encore ou si une boîte de card-step longue recouvre le dé, retuner ces 2 valeurs ensemble. `SPEAKER_HEAD_X`/`ENEMY_ARROW_DROP`/position de `show_info_note` = constantes estimées sur screenshots. Cause exacte de la ligne parasite jamais isolée. Vérifié seulement par `gdtoolkit`, jamais lancé dans l'éditeur.

- **Pause menu / settings in-run implémenté (session mi-juillet 2026, ~07-16) — TESTÉ par Julien (« looks amazing »), 2 fixes post-playtest appliqués (voir plus bas).** Bouton engrenage dans la top bar (`%PauseButton`, TextureButton 54×54 entre DeckButton et le bord droit, + `PauseHoverGlow` — calqué sur ConsultMapButton, réutilise le sub_resource `StyleBoxFlat_map_icon_hover_glow` de run.tscn, `process_mode=3`) OU touche Esc → `scenes/ui/pause_menu.tscn`/`.gd` (`class_name PauseMenu`, CanvasLayer `layer=60` `process_mode=ALWAYS`, instancié dans run.tscn en `%PauseMenu`). Contenu : sliders Master/Music/Effects (styleboxes custom + grabber `assets/images/slider_knob.png`), toggle Fullscreen, Resume, "Quit to Main Menu" (avec confirmation), note "Your run is saved at the last map checkpoint". Pause l'arbre à l'ouverture mais **retient `_was_paused` et le RESTAURE à la fermeture** — cohabite avec map consult / battle over panel (ouvrir puis fermer le menu pendant une consultation ne dépause pas la consultation). Icône engrenage `assets/images/pause_menu_icon.png` générée par PIL (or cartoon + outline brun, même langage que `map_button_icon.png` ; script dans le scratchpad de session, non committé).
  - **`global/settings_manager.gd`** (`class_name SettingsManager`, tout statique, miroir de SaveManager) : `user://settings.cfg`. **Volumes stockés linéaires 0..1 et appliqués en ATTÉNUATION par-dessus les dB autorisés de `default_bus_layout.tres`** (Music est à ~-7.6dB de mix — écrire le dB du slider directement dans le bus aurait détruit ce mix au premier mouvement de slider ; baselines capturées au premier chargement, au boot via le nouveau `Global._ready()`). Volume ≤0.005 = mute du bus. Fullscreen via `DisplayServer.window_set_mode` (FULLSCREEN borderless, pas EXCLUSIVE).
  - **`global/sfx_player.tscn` : root passé `process_mode=3`** (MusicPlayer l'avait déjà) — sans ça AUCUN SFX n'est audible pendant que l'arbre est en pause (le menu joue un click de preview au `drag_ended` des sliders). Effet de bord assumé : les queues de SFX ne se figent plus pendant une pause.
  - **"Quit to Main Menu" DOIT appeler `MusicPlayer.stop()`** : le main menu joue son thème via SFXPlayer et ne stoppe jamais MusicPlayer lui-même — sans ce stop, la musique de combat continuerait en boucle sous le thème du menu après un quit mid-fight.
  - **`card_pile_view.gd::_input` : guard `if not visible: return` + `set_input_as_handled()` sur Esc.** Avant, les 3 instances cachées de battle (+ DeckView) traitaient chaque Esc appuyé n'importe où (no-op silencieux) ; maintenant un Esc ferme SOIT la pile view ouverte SOIT toggle le pause menu (`_unhandled_input`), jamais les deux d'un coup. Esc dans le menu recule d'un niveau : confirm quit → settings → resume.
  - **Fix post-playtest #1 (signalé par Julien : la map scrollait/hoverait SOUS le menu)** : `Map` est `PROCESS_MODE_ALWAYS` à dessein (map consult) — la pause de l'arbre ne la fige donc pas, et `map.gd` lit `Input.is_action_pressed` dans `_process` + un `_input` brut, qui **ignorent totalement le dimmer plein écran** (jamais routés par la GUI) ; les Area2D des salles continuaient aussi de recevoir le hover. Fix : `run.gd::_on_pause_menu_opened` passe `map.process_mode = PAUSABLE` le temps que le menu est ouvert, `_on_pause_menu_closed` (nouveau signal `closed` sur PauseMenu) restaure `ALWAYS`. **Piège générique à retenir : un dimmer/écran modal ne bloque QUE la GUI — tout node en `Input`-polling, `_input` brut ou picking Area2D passe au travers ; il faut le geler par `process_mode`, pas par l'UI.**
  - **Fix post-playtest #2** : confirmation avant de quitter (`ConfirmQuitPanel` dans pause_menu.tscn — "Quit to main menu?" / "Don't worry — your progress is saved at each floor." / Yes, quit + Cancel) — le panneau settings se cache pendant la confirmation, misclick impossible.
- **Session du 2026-07-12 : refonte complète du tutoriel de combat IMPLÉMENTÉE — voir [tutorial_redesign_2026-07.md](tutorial_redesign_2026-07.md) pour le design d'origine.** Plan écrit + revu par Julien (3 corrections : Recombobulate reset le power, "Support" abandonné comme mot-clé joueur, All In hors starter deck) puis implémenté en une passe dans la même session. **⚠️ Implémenté par Claude, PAS ENCORE JOUÉ/PLAYTESTÉ par Julien — aucun moyen de lancer Godot interactivement dans cet environnement, seule la syntaxe GDScript a été vérifiée (gdtoolkit) sur les 11 fichiers .gd touchés/créés.** Utiliser la checklist de playtest §8 du doc au premier lancement.
  - **Architecture livrée** : `scenes/battle/tutorial_director.gd` (nouveau, ~550 lignes, `class_name TutorialDirector extends Node`, ajouté comme DERNIER enfant du root Battle dans battle.tscn — remplace entièrement l'ancien système de 15 panels + web de flags `Global.tutorial_*`) pilote les 26 sous-étapes (T1.1-T1.10, T2.1-T2.9, T3.1-T3.7) via un tableau de `Callable` (`_steps`), avance sur UN SEUL signal one-shot par étape (`Events.dice_rolled`, `card_played`, `card_charged`, `active_dice_changed`, `scout_effect`, `next_roll_determined`, `player_turn_started`, `battle_over_screen_requested`, ou `overlay.continue_pressed`), et gate les inputs à chaque étape (cartes par `card.id`, dés par slot, ROLL, End Turn, faces de Scout) — `_apply_gate({})` par défaut entre étapes = tout verrouillé. `scenes/tutorial_overlay.gd`/`.tscn` (nouveau, CanvasLayer layer=20, remplace le scaffold jamais câblé) = kit d'affordances réutilisable : dimmer à découpe rectangulaire (4 ColorRects encadrant un trou, pas de shader), flèche qui bob, cadre pulsant, panneau de texte à 5 zones fixes (non vérifiées visuellement en éditeur — à ajuster par Julien si un texte chevauche autre chose), bouton Skip toujours visible.
  - **Piège de coordonnées résolu** : `ActiveDice`/`DiceInterface` sont des enfants DIRECTS du root Battle (`Node2D`, sans `CanvasLayer`), donc leurs `get_global_rect()` vivent dans l'espace "canvas de base" (affecté par la Camera2D) — alors que l'overlay est son propre `CanvasLayer` (espace écran pur, ignore la caméra). Un helper `_world_to_screen()` (`get_viewport().get_canvas_transform() * pos`) corrige ce mismatch partout où c'est nécessaire (flèche au-dessus de l'ennemi, spotlight sur l'IntentUI) ; les Controls déjà sous `BattleUI` (Hand, EndTurnButton) n'en ont pas besoin (déjà en espace écran).
  - **Piège d'ordre de `_ready()` résolu par construction** : TutorialDirector ne lit JAMAIS le cache `@onready` d'un sibling (ex. `battle.battle_ui.hand` aurait pu être `null` si BattleUI n'était pas encore ready) — toujours `get_node()`/`find_child()` directs à la place, qui ne dépendent que de l'arbre déjà instancié (garanti dès l'instanciation, avant tout `_ready()`), jamais de l'ordre d'exécution des `_ready()` entre siblings.
  - **Déterminisme** : `Global.tutorial_forced_rolls: Array[int]` (remplace l'ancien `tutorial_forced_roll` int + son hack de chaînage "2 devient 1") consommé en FIFO par `dice.gd::roll_dice()` ; `Global.tutorial_forced_scout_faces: Array[int]` consommé par `battle.gd::_on_scout_effect()` via un nouveau helper `_find_dice_face_texture()` (réutilise le même regex de parsing de nom de fichier que `_on_scout_dice_clicked`). Poussés au début de chaque tour concerné (T1.2, T2.1, T3.3), consommés un par un par les rolls/scout réels qui suivent.
  - **Mains forcées unifiées** : `player_handler.gd` avait deux blocs dupliqués (un dans `start_battle()`, un dans `start_turn()` gated sur `tutorial_second_turn`) — remplacés par UNE table `TUTORIAL_HAND_BY_TURN` keyée sur `Global.fight_turn` (0/1/2) + un helper `_force_tutorial_hand()` qui cherche la carte dans `draw_pile` ET `discard` avant de la remettre en tête de pile (nécessaire pour T3 : par ce tour-là, les 3 Strike sont déjà passées par la défausse au moins une fois). Corrige au passage le bug de la carte fantôme (`warrior_axe_attack1.tres` n'est plus référencé nulle part).
  - **Combat dédié** : `battles/tutorial_fight.tres`/`.tscn` + `enemies/crab/crab_enemy_tutorial.tres` (Skeleton, HP voir point (7) ci-dessous pour la valeur à jour) + `enemies/crab/crab_tutorial_ai.tscn` (une seule action toujours performable, réutilise `crab_attack_action.gd` tel quel — ce script était DÉJÀ générique/toujours-performable, aucun nouveau script nécessaire) — découplé de `tier_0_crab`/`Global.tutorial_enemy_attack` (ce flag reste utilisé par le VRAI Crab AI partagé, `crab_enemy_ai.tscn`, pour son propre "opening move" one-shot par run — **ne pas le supprimer/renommer**, ce n'est pas tutorial-only malgré le nom).
  - **Victoire T3.7** : `battle_over_panel.gd` skip désormais son auto-advance de 1s vers l'écran de récompense `if Global.tutorial_on` — TutorialDirector affiche son propre panneau de victoire et émet lui-même `Events.battle_won` au clic sur "Begin the run", pour que l'écran de récompense n'coupe pas le beat de fin du tuto.
  - **Nettoyage** : `Global.tutorial_forced_roll/block/low_blow/red_dice/charging_card/red_attack/end_turn/blue_dice/second_turn/recombobulate` supprimés (plus aucune référence) ; `Events.tutorial_step_requested` supprimé (signal mort) ; `run.gd::SAVED_TUTORIAL_FLAGS` synchronisé ; All In retiré de `warrior_starting_deck.tres` **et `warrior_axe_attack1.tres` restauré à sa place** (précision de Julien : All In était un REMPLACEMENT temporaire de cette Strike, pas un ajout) — deck starter = 12 cartes : 4 Strike, 4 Block, Low Blow, Reinforce, Recombobulate, Scout 3. **⚠️ La comptabilité de dégâts d'origine (7+9+6 = 22 HP) a depuis changé, voir point (7) ci-dessous — le Skeleton est maintenant à 28 HP (13+9+6).**
  - **Passe de fixes post-playtest (même session, 5 allers-retours de screenshots avec Julien)** : (1) **piège Godot 4 majeur — connecter un callable à 0 argument sur un signal qui en émet N est accepté à la connexion mais ERREUR à l'émission, le handler ne tourne JAMAIS** : tous les `Events.dice_rolled/card_played/card_charged/active_dice_changed/scout_effect.connect(_advance)` étaient silencieusement morts → le tuto restait bloqué sur "Click ROLL" (le roll marchait, l'avancement jamais) ; fix = helper `_wait(signal, arg_count)` avec `_advance.unbind(n)` + tracking du pending pour cleanup — **voir la mémoire dédiée `feedback_signal_arity_gotcha`, piège réutilisable partout ailleurs dans le projet** ; (2) **cycle de vie de l'overlay** : instancié depuis le `_ready()` du director (pendant que Battle entre encore dans l'arbre), il faut `setup()` manuel (refs par `$Path`, pas de `@onready`) + `battle.call_deferred("add_child", overlay)` (un CanvasLayer ajouté à un parent mi-entré peut ne jamais s'enregistrer au rendu) + `get_viewport()` interdit dans l'overlay (canvas fixe 1280×720 en dur à la place) ; (3) **panneau de texte auto-dimensionné** : les zones fixes (5 rects codés en dur) ont été abandonnées entièrement après un premier essai jugé "ugly, way too big" par Julien — remplacées par des BANDES (x, largeur, bande y0-y1, attache top/center/bottom) + hauteur calculée par `RichTextLabel.get_content_height()` après pose de la largeur (label/bouton Continue/panel avec `anchor_* = 0` forcés à chaque appel pour empêcher tout reflow implicite) ; police de l'overlay = Roboto Condensed SemiBold + Roboto Black en bold (le thème global sans override rendait tout en Cinzel décoratif illisible) ; print de debug `[tutorial-overlay]` par étape (rect réel du panneau) gardé en debug build ; (4) `battle_ui.gd` : les 2 ré-activations inconditionnelles d'End Turn (hand_drawn + timer 3s post-clic) sont gatées sur `not Global.tutorial_on` ; le gate se ré-applique sur `player_hand_drawn` (les cartes piochées après l'application du gate arrivent `disabled=false`) ; bouton Skip caché sur l'étape victoire (skipper là aurait soft-lock, le director possède l'émission de `battle_won`) ; (5) **le dé actif PERSISTE d'un tour à l'autre** (rien ne le remet sur Blue par défaut) — le tour 1 se termine sur Red (Block socketé dessus pour T1.9), donc le tour 2 démarrait sur Red avec ROLL inerte sans carte socketée et tout le reste gaté : nouvelle étape `_step_t2_switch_blue` insérée en tête du tour 2 (calque de `_step_t1_7`, gate sur le seul slot Blue, avance sur `active_dice_changed`) ; (6) **la carte restante de T2 ne doit JAMAIS être une Strike** : `cards_per_turn=5` mais le script de T2 n'en joue que 4, donc 1 carte reste en main à End Turn. Le Skeleton est laissé à EXACTEMENT 6 HP en entrant dans T3 (finale Scout→6 garanti→Strike) — une Strike en rab + le Red Dice frais = "socket + roll un 6 = kill maintenant", ce qui tente le joueur ET court-circuite la finale. **Première tentative rejetée** (garder la Strike + texte "you don't have to spend everything" pour l'assumer) : mauvais conseil, Julien a raison — les dés se rechargent chaque tour (use-it-or-lose-it), un joueur malin utiliserait ce Red Dice sans hésiter. **Fix correct** : `TUTORIAL_HAND_BY_TURN[1]` force un 2e Block au lieu de la Strike (`warrior_block2` en rab, self-target, ne peut pas toucher l'ennemi) ; texte de `_step_t2_9` réorienté vers la finale ("one clean hit next turn ends this") sans jamais impliquer qu'on "garde" un dé (les dés ne se reportent pas — T3 en redonne un neuf). La compta de dégâts ne bouge pas (T2 ne fait toujours que les 9 de Low Blow).
  - **Suite de la passe (même session, retour de Julien après avoir vu le tuto en jeu) : Dice Bag réactivée + ouverture T1 en 3 rolls + explication relique.** Julien a remarqué que Dice Bag (relique de départ, +1 Blue Dice au 1er tour) était désactivée pendant le tuto (`if Global.tutorial_on == false` dans `mana_potion.gd` — un garde-fou d'AVANT cette session, jamais remis en question) et a proposé de la laisser active pour (a) éviter que le vrai combat n°2 surprenne le joueur avec 3 Blue Dice jamais expliquées, et (b) ouvrir sur un premier Strike plus satisfaisant ("dopamine du premier coup"). **Garde-fou retiré** dans `mana_potion.gd` — la relique est maintenant active dès le tuto, comme dans n'importe quel vrai combat. Conséquences : (1) nouvelle étape `_step_t1_relic` insérée juste après le Welcome (avant le 1er roll) — seul moment où le slot Blue affiche "3/2" (current inclut le bonus, max non ; dès le 1er roll ça retombe à un "2/2"-like normal) donc unique fenêtre pour l'expliquer ; spotlight sur le slot Blue de la DiceInterface (`_dice_slot_rect("blue")`, déjà exposé), texte volontairement léger (juste "il existe des Reliques, la tienne fait ça") sans creuser le système complet (tiers, élites, etc. — demande explicite de Julien) ; (2) **3e roll ajouté au tour 1** (`_step_t1_3b`, entre l'ancien T1.3 et T1.4) — forced_rolls passe de `[4, 3, 6]` à `[4, 3, 6, 6]` (3 Blue puis le Red du T1.9), le 3e roll est forcé à 6 (max roll sur d6) pour profiter GRATUITEMENT du flash doré/hit-stop déjà câblé sur les max rolls dans `dice.gd` — le point culminant du "stacking" tombe pile sur la meilleure célébration existante plutôt que d'ajouter un nouvel effet ; total désormais 4+3+6=13 Power ("modestly bigger" demandé par Julien, pas un chiffre extrême) ; (3) **Skeleton remonté à 28 HP** (`enemies/crab/crab_enemy_tutorial.tres`, était 22) pour que la finale reste un kill exact : 13 (T1) + 9 (T2 Low Blow) + 6 (T3) = 28, le Skeleton entre toujours dans T3 à EXACTEMENT 6 HP comme prévu, aucun autre chiffre de la comptabilité ne bouge (T2/T3 intacts). `_steps` array mis à jour (28 sous-étapes désormais, `_step_t1_relic` et `_step_t1_3b` insérées).
  - **Non fait / laissé pour Julien** : l'offset de la flèche au-dessus de l'ennemi (`_enemy_arrow_point()`, un `Vector2(0,-90)` codé en dur) reste une estimation jamais mesurée précisément à l'écran. Le différenciateur visuel "no-reset" (§9.3 du doc, Reinforce/Scout 3) reste une décision de design ouverte, non traitée ici (le tuto enseigne le comportement par le texte, jamais le mot "Support").

- **Session du 2026-07-11 (conversation distincte) : animation complète du flow Scout (ouverture → réveil des options → vol du dé choisi vers le slot "next roll" → fermeture) — NON PLAYTESTÉ.** Contexte : le starter deck contient maintenant une carte Scout 3 (`card_scout3_no_exhaust.tres`, ajoutée par Julien lui-même avant la session — il y a aussi mis All In). Avant cette passe, tout le flow était instantané (panel snap-in avec toutes les faces déjà visibles, clic = tout disparaît d'un coup, le panneau "next roll" apparaissait de nulle part). Tout vit dans `battle.gd` (section scout, constantes `SCOUT_*` regroupées) + un hook dans `dice.gd::_on_next_roll_determined()` :
  - **Ouverture** : le panel se déplie vers le haut depuis la zone des dés (pivot bottom-center posé dans `_resize_scout_panel`, scale 0.7→1 TRANS_BACK + fade), pendant que des motes lumineuses (même recette gradient-radial + blend additif que les power orbs, dupliquée dans battle.gd car les caches de dice.gd sont de l'état d'instance) montent du dé actif vers le panel, teintées `DicePalette.accent(Global.dice_type)` — "le dé projette ses futurs", une mote par face révélée, timée pour arriver pile au pop de sa face. **Cibles des motes calculées arithmétiquement** (même formule que `_resize_scout_panel`), PAS lues sur les enfants du HBox — le layout d'un container ne settle qu'en fin de frame, les positions seraient stale juste après un resize.
  - **Réveil des options — 3 passes de tuning sur retours successifs de Julien avant convergence.** Passe 1 (stagger 0.07s, pop instantané) : trop rapide/simultané. Passe 2 (flicker "come on come on" + traitement spécial "payoff" plus long sur la DERNIÈRE option révélée — pause supplémentaire, flickers en plus, punch, hit-stop, carillon 2-notes) : jugé "too much... especially on the last roll", retiré. Passe 3, entièrement retirée à son tour car un pop-in simple sans aucun flicker s'est révélé lire comme "un orbe qui frappe un dé statique" plutôt que "le dé se matérialise" une fois testé en jeu. **Version finale retenue : chaque face flicker brièvement (2 cycles scale 0.45↔0.82 + alpha 0.3↔0.75, TRANS_SINE) avant de se figer (pop overbright 1.9×→blanc, TRANS_BACK) — le flicker est gardé, mais TOUTES les options reçoivent exactement le même traitement, aucune n'a de délai/pop/effet supplémentaire.** `SCOUT_FACE_REVEAL_STAGGER = 0.3`, `SCOUT_FACE_TEASE_COUNT = 2`, `SCOUT_FACE_TEASE_STEP = 0.055`, `SCOUT_FACE_REVEAL_TIME = 0.14` — un Scout 3 (cas courant) pop pile à ~1s du début à la fin, cadence régulière, sans traiter la dernière option différemment. **`mouse_filter = IGNORE` jusqu'à la fin complète du flicker+lock-in de chaque face** (pas juste le pop initial) : un TextureRect en cours de flicker reçoit quand même gui_input, donc sans ça un clic pouvait choisir une option encore incertaine.
  - **Choix** : l'état est posé IMMÉDIATEMENT au clic (`Global.next_guaranteed_roll` — même principe que le nombre de Power qui se met à jour instantanément pendant que les orbes volent) ; le dé cliqué punch (1.22×) puis vole en arc bezier propre (bow latéral déterministe, PAS le wobble aléatoire des orbes — un dé seul qui serpente lirait "saoul") vers le slot next-roll, laissant une traînée de motes accent espacées le long du CHEMIN (le t easé du tween_method pilote aussi le gating de la traînée). Clone sur `ui_layer` z=150 (même convention que refuel/All In). **`Events.next_roll_determined` n'est émis qu'à l'atterrissage** (`_land_scout_pick`), gardé par `next_guaranteed_roll != -1` — un roll ou un changement de dé actif pendant le vol consomme/annule la garantie, auquel cas pas de pop d'un panel périmé.
  - **Fermeture** : la face choisie passe `modulate.a = 0` (PAS `hide()` — ça re-flowerait le HBoxContainer et décalerait les voisines en pleine fermeture), les autres shrink/fade en léger stagger, le panel attend 0.08s (le dé sort d'abord) puis se replie vers le bas. Bouton Exit → même fermeture animée. Tous les tweens scout trackés dans `_scout_tweens`, tués à chaque réouverture/fermeture — un second Scout joué panel ouvert ré-initialise proprement.
  - **Atterrissage (dice.gd)** : `_on_next_roll_determined()` joue maintenant un pop de livraison sur `next_roll_panel` (scale 0.55→1.14→1 + flash surbrillant ; le `modulate` du panel se propage à la texture du dé dedans) — **profite aussi à Focus/Focus+** qui émettent le même signal sans vol. Tween tracké (`_next_roll_pop_tween`) pour que deux émissions rapprochées redémarrent le punch au lieu d'empiler.
  - **Piège de vérif à retenir** : le `--check-only` headless de Godot ne marche PAS sur ce projet (les autoloads Events/Global ne sont pas enregistrés dans ce mode, TOUS les scripts "échouent" faussement sur "Identifier not found: Events") — syntaxe vérifiée via `gdtoolkit` (`python -m gdtoolkit.parser <fichier>`, pip install gdtoolkit) à la place.

- **Session du 2026-07-11 (troisième conversation distincte) : repasse du positionnement d'ennemis, follow-up de l'audit géométrique du 07-10.** Julien a signalé sur screenshot que le premier ennemi de certains combats reste trop proche du panneau de dés central malgré l'audit précédent (98 violations → 0, basé sur bbox alpha réelle contre 4 zones : panel de dés, bouton End Turn, bords du viewport, sprites voisins). **Root cause non couverte par cet audit : `IntentUI` (l'icône+nombre d'intent au-dessus de la tête, ex. le "10" épée) a un offset LOCAL FIXE (63 à 177px depuis l'origine de l'Enemy, `scenes/enemy/enemy.tscn`), indépendant de la sprite elle-même — l'audit précédent vérifiait la bbox de la sprite (art réel), jamais celle de l'IntentUI.** Le vrai coupable : le label `CurrentPower` (le nombre de Power à côté du gros dé ROLL, `scenes/dices/dice.tscn`) est ancré au bord DROIT de son parent avec un offset qui le fait déborder jusqu'à x=779 en coordonnées monde (bien au-delà de la boîte du dé elle-même, x521-665) — un premier ennemi positionné sous x≈750 voit forcément son IntentUI chevaucher ce nombre. Vérifié précisément sur les 2 combats des screenshots : `tier_2_defender_satyr.tscn` (Defender/Marauder, floor 14) et `tier_1_crab_satyr.tscn` (Crab/Skeleton, floor 6) — les deux avaient leur ennemi de front sous x=705, confirmé chevauchement mathématique avant tout playtest. **Audit complet refait par script (position + scale de chaque nœud ennemi dans les 40 fichiers `battles/*.tscn`, croisé contre `battle_stats_pool.tres` pour ne fixer que le contenu réellement atteignable en jeu)** : 13 combats en pool avaient ce chevauchement (`tier_0_bigger_octopus_2`, `tier_0_bigger_satyr_octopus`, `tier_0_octopus_1_satyrs_2`, `tier_0_octopus_2_satyr_1`, `tier_0_octopus_3`, `tier_0_satyrs_1_octopus_2`, `tier_0_satyrs_2_octopus_1`, `tier_0_satyrs_3`, `tier_1_crab_satyr`, `tier_1_machopeur_octopus`, `tier_1_octopus_2_satyrs_2`, `tier_2_defender_satyr`, `tier_2_machopeur_octopus`) — premier ennemi de chacun poussé à x=750 (scale 1.0) ou x=755 (scale 0.75), sauf `tier_1_octopus_2_satyrs_2` (x=743, composition à 4 corps déjà dense, marge réduite pour ne pas écraser le gap vers son voisin `B Octopus`). **4 fichiers exclus à dessein** (`tier_0_crab_satyr`, `tier_0_satyrs_octopus_3`, `tier_1_bat_crab`, `tier_1_bats3`) : confirmés hors `battle_stats_pool.tres` donc jamais joués — `balance_analysis_2026-07.md` §"Built but UNREACHABLE" les documente déjà comme doublons/leftovers mal nommés (bat_crab/bats3 utilisent en fait Crab/Minotaur, pas de bat). Script d'audit non committé (scratchpad de session). **Piège méthodologique à retenir pour tout futur audit de positionnement d'ennemi : vérifier aussi la bbox de l'IntentUI (offset fixe scalé par `Enemy.scale`, PAS lié à la taille réelle de la sprite/art) en plus de la bbox de la sprite elle-même — un ennemi avec une petite sprite bien cadrée peut quand même avoir son icône d'intent qui déborde loin au-dessus/à côté.**

- **Suite immédiate de la repasse ci-dessus, même conversation : 3 nouveaux problèmes signalés par Julien sur 5 nouveaux screenshots (ennemis flottant au-dessus de leur barre de vie, statut du Lurker caché par les cartes, Defender qui chevauche encore Satyr1 malgré le fix x=750) + une demande de garde-fou anti-redondance.** Le point le plus dur à diagnostiquer (le "floating") a nécessité d'abandonner l'analyse statique (PIL sur les PNG sources) au profit d'un **rendu Godot headless réel** — méthode nouvelle, à réutiliser pour tout futur doute de positionnement visuel qui résiste au calcul à la main :
  - **Nouvelle méthode de vérification : `Godot_v4.3-stable_win64_console.exe --path . --script res://debug_xxx.gd --rendering-driver opengl3 --position 2000,2000`** (PAS `--headless` seul, qui force le driver "dummy" et ne peut produire aucun pixel réel — `Image.save_png()` échoue silencieusement sur une texture nulle). Un script `extends SceneTree` avec `_initialize()` async instancie un `SubViewport` (1280×720, `UPDATE_ALWAYS`), y ajoute le `Background`+`Camera2D` de `battle.tscn` (détachés de la vraie scène Battle pour éviter d'avoir à câbler tout le combat) puis la scène de combat testée (`battles/xxx.tscn`, qui contient déjà les `Enemy` avec leurs vraies `stats` assignées — le setter `set_enemy_stats` se déclenche automatiquement à l'instanciation), attend quelques `process_frame`, puis `viewport.get_texture().get_image().save_png(...)`. La fenêtre réelle (positionnée hors-écran via `--position 2000,2000` plutôt que masquée, Godot n'a pas de vrai mode "headless GPU" sur Windows) apparaît brièvement puis se ferme d'elle-même en fin de script — négligeable/non-gênant en pratique. **Cette méthode a immédiatement reproduit les 3 bugs signalés à l'identique des screenshots de Julien**, alors qu'un premier essai de diagnostic par calcul à la main (formule de `enemy.gd::update_enemy()` + bbox alpha des PNG sources) avait donné des résultats plats/peu concluants — les PNG sources de Hound/Machopeur n'ont en réalité PAS de gros padding transparent en bas (vérifié aussi par overlay de grille en pixels sur les PNG), donc l'écart venait bien de la formule (voir point suivant), pas de l'art.
  - **Root cause du "floating"** : `stats_ui_y_offset`/`status_handler_y_offset` (champs `@export` sur `Enemy`, per-instance dans les fichiers `battles/*.tscn`) ne sont réglés que sur une poignée d'ennemis (seuls `tier_2_hound.tscn` et `tier_2_vortex.tscn` en avaient AVANT cette session) — la formule par défaut (`tex_size.y * final_scale / 2`) suppose que l'art remplit sa boîte de rendu jusqu'en bas, ce qui n'est vrai que par coïncidence pour la plupart des ennemis déjà réglés à la main (crab, plant, temple defender, sigil slug, medusa, dragonpriest, gargantua, leviathan — tous vérifiés OK par rendu). Seuls **Machopeur/Marauder** (`tier_0_machopeur.tscn`, ajout `stats_ui_y_offset=-24`/`status_handler_y_offset=-24`), **Hound** (`tier_2_hound.tscn`, l'offset existant `+16` allait dans le mauvais sens — probablement réglé pour une version antérieure de l'art avant remplacement par `hound_perfect_v2.png` — corrigé à `+1`) et **Lich** (`tier_elite_lich.tscn`, `-15`/`-15`, robe qui traîne un peu haut) avaient un vrai écart. Corrections calibrées par mesure de pixels directe sur les rendus (pas par calcul théorique, qui s'est avéré peu fiable pour cette mécanique précise).
  - **Statut du Lurker caché par les cartes** (`tier_1_lurker.tscn` solo + `tier_1_lurker_crab.tscn`) : le Lurker démarre avec le statut Flux (`initial_statuses`), dont l'icône se positionne juste sous la barre de vie via `status_handler_y_offset` (formule : `stats_ui_bottom + status_handler_y_offset - 8`) — jamais un problème pour les autres ennemis testés, mais le Lurker est bas/accroupi et sa barre de vie tombe déjà assez bas à l'écran, laissant l'icône de statut à ~19px seulement du bord haut de la main de cartes (`Hand` dans `battle.tscn`, ancrée en bas, `offset_top=-139` → y=581 en coordonnées design 1280×720). Fix : `status_handler_y_offset=-40` (solo) / `-45` (pairé avec Crab, scale 0.75 donc le même offset local produit un déplacement écran plus petit) — l'icône se retrouve à côté/juste au-dessus de la barre plutôt que dessous, largement dégagée des cartes.
  - **Defender/Satyr1 toujours proches malgré x=750** : le fix précédent (basé sur l'offset fixe d'IntentUI, sans rapport avec la largeur réelle des sprites) ne garantissait qu'une chose — plus de chevauchement avec le nombre de Power — pas l'absence de chevauchement entre ennemis voisins. Le bouclier du Defender (qui dépasse largement à gauche de son sprite) touchait le premier Satyr. Fix : Satyr1/Satyr2 rétrécis 0.75→0.68 scale + repoussés (943→985, 1086→1115) — même schéma pour `tier_1_crab_satyr.tscn` (les 2 Satyr là-bas se touchaient quasiment, 0.75→0.65 + espacement 930/1069→915/1090). **Confirmation explicite de Julien : réduire l'échelle des ennemis squishies/faible HP dans les combats à plusieurs corps est une vraie option de fix, pas juste un pis-aller** — reste modéré (0.65-0.68, pas plus petit) pour ne pas tomber dans l'excès inverse.
  - **Audit complet des 23 fichiers multi-ennemis atteignables** (rendu réel + inspection visuelle un par un, même méthode) : SEULS `tier_2_defender_satyr` et `tier_1_crab_satyr` avaient un vrai chevauchement de sprites — tous les 21 autres déjà corrects (les tentacules d'Octopus qui se touchent légèrement dans `tier_0_bigger_octopus_2` jugées cosmétiques/acceptables, pas un vrai bug). Screenshots de vérification (avant/après) pas committés, générés puis supprimés en fin de session (scratchpad de rendu, voir méthode ci-dessus).
  - **Suite immédiate, retour de Julien après avoir vu le fix en jeu (même conversation) : le fix du statut Lurker (`status_handler_y_offset` négatif) faisait maintenant chevaucher l'icône PAR-DESSUS la barre de vie sur certains combats scalés (Act 2), et le Skeleton/Crab touchait encore un Satyr dans `tier_1_crab_satyr.tscn` malgré le fix de scale précédent.** Règle explicite de Julien à retenir pour tout futur ajustement de statut : **un statut ne doit JAMAIS chevaucher la barre de vie — toujours en dessous, comme dans Slay the Spire.** Root cause du chevauchement : `status_handler_y_offset` négatif déplace l'icône INDÉPENDAMMENT de la barre (pas de la même quantité), donc au-delà d'un certain seuil ça la fait remonter AU-DESSUS d'elle plutôt que de juste réduire l'écart avec les cartes. **Fix correct : utiliser `sprite_y_offset` (négatif) plutôt que `status_handler_y_offset`** — ce champ décale sprite + stats_ui + intent_ui + status_handler TOUS ENSEMBLE (même terme additif dans les 4 formules de `enemy.gd::update_enemy()`), donc l'ordre relatif (sprite → barre → statut) ne peut jamais s'inverser, seul le groupe entier remonte un peu à l'écran pour dégager les cartes. Appliqué : `tier_1_lurker.tscn` (`status_handler_y_offset` retiré, `sprite_y_offset = -25`), `tier_1_lurker_crab.tscn` (idem, `sprite_y_offset = -35` sur le nœud Lurker, scale 0.75 donc un offset local plus grand pour un déplacement écran équivalent). Le Lurker est une créature "flottante" sans pattes distinctes donc ce léger décalage vertical ne casse pas son ancrage au sol (contrairement à un ennemi avec des griffes nettes). Pour `tier_1_crab_satyr.tscn` : Satyr2 (le plus proche du Crab) poussé une seconde fois (915→965) — le bouclier du Skeleton dépasse plus loin à droite que prévu par le fix précédent. **Piège à retenir : toujours re-vérifier par rendu réel après un fix de ce genre plutôt que de supposer qu'un seul passage suffit — un ajustement de spacing/offset peut résoudre un problème tout en en créant un autre (ici : chevauchement statut/barre) si on ne raisonne que sur l'axe qui posait problème au départ.**

  - **Garde-fou anti-redondance ("pas 3 combats de suite octopus/satyr en début de run")** : **le mécanisme demandé par Julien EXISTAIT DÉJÀ** dans `run.gd::_get_unique_battle_for_tier()` (lignes ~427-430) — un champ `BattleStats.group: String`, câblé pour que choisir un combat d'un groupe donné marque IMMÉDIATEMENT tous les autres combats du même groupe comme "déjà utilisés" pour le reste du run (jusqu'au reset de ce tier). Un groupe `"slimes"` existait déjà sur 6 des 9 combats octopus/satyr de tier 0, mais PAS sur les 3 variantes "bigger" (`tier_0_bigger_octopus_2`, `tier_0_bigger_satyrs_2`, `tier_0_bigger_satyrs_octopus`) — un joueur pouvait donc tirer un combat du groupe "slimes" ET un des 3 "bigger" non couverts sur les 3 premiers étages, exactement la redondance décrite par Julien. **Fix = 3 lignes changées** (`group = ""` → `group = "slimes"` sur ces 3 `.tres`), aucune nouvelle logique de code — les 9 combats octopus/satyr de tier 0 partagent maintenant le même groupe, garantissant au plus 1 combat "critter" sur les étages 1-3 (les 3 seuls combats non-critter de tier 0 — crab, plant, machopeur — couvrent alors forcément les 2 autres étages). **Mécanisme réutilisable tel quel pour toute future demande de variété similaire sur tier 1/2** (pas fait cette session, scope resté sur "les 3 premiers combats" comme demandé).

- **Session du 2026-07-11 (menée en parallèle de l'overhaul dés ci-dessous, conversation distincte) : batch de fixes UI/contenu — icônes de relique, shop de cartes, cartes longues, 4 demandes de gameplay/UX.** Ordre chronologique de la session, chaque point traité sur signalement direct de Julien :
  - **Fond blanc sur les 10 icônes de relique du 2026-07-10 (cartographers_quill, snake_eyes_charm, house_money, echo_chamber, obsidian_scale, metronome, overflow_valve, prayer_beads, flywheel, trick_scale)** : aucune des 10 n'avait de canal alpha (RGB plat, fond blanc cuit en dur), contrairement à toutes les autres icônes de relique du jeu — pipeline `rembg` + trim sur bbox alpha (seuil >10, padding 6px) appliqué, même traitement que les icônes de statut du 07-10.
  - **`house_money.png` "coupée en deux" en jeu après ce premier fix** : la boîte d'icône de relique (`relic_ui.tscn::Icon`, réutilisée par la top bar ET le shop) est un carré 56/64px en `stretch_mode = 6` (KEEP_ASPECT_COVERED, zoom-crop) — le crop serré sur le contenu réel avait donné un canvas très vertical (528×801, aspect 0.66), et House Money est une composition en 2 parties empilées (bouclier en haut, pile de jetons en bas) : le crop carré coupait la pointe du bouclier ET la base de la pile, les deux extrémités identifiantes. **Repadding en canvas carré (pas de retour à un stretch "contain")** pour que le crop carré n'ait plus rien à couper. **Root cause du crop carré générique documentée** : `stretch_mode = 6` a été choisi à l'origine pour Dice Bag (image très verticale avec beaucoup de marge peinte) et zoome pour combler la boîte — mais zoome aussi bêtement sur tout le monde, donc toute icône avec un aspect loin de 1:1 perd du contenu sur les bords opposés à l'axe long.
  - **Re-signalé le 2026-07-11 : toujours des icônes coupées, et la barre de reliques "commence trop à gauche".** Audit d'aspect ratio sur les 26 reliques du pool (`treasure_relic_pool.tres`) : 4 outliers trouvés et repaddées en carré — `obsidian_scale.png`/`trick_scale.png` (aspect ~1.5, forme diagonale/horizontale dont les pointes/plateaux se faisaient couper sur les côtés) et deux reliques ANCIENNES jamais retouchées avant, `magic_sleeve.png`/`hunting_bow.png` (aspect 0.667, exactement le même défaut que House Money originale). Les icônes à aspect proche de 1.5 mais "increvables" visuellement (crown, war_drum, runic_bones) laissées telles quelles — décision au cas par cas par inspection visuelle, pas un seuil d'aspect ratio automatique appliqué en masse. **Marge gauche de `TopBar/RelicBar`** (`scenes/run/run.tscn`) montée de 28→44px (offset_right 478→494 en conséquence) — les icônes sont passées à 64px depuis la session du 07-07 qui avait tuné ce 28px sur une taille plus petite, jamais réajusté depuis.
  - **Cartes shop/récompense manquent les 10 nouvelles reliques** : `card_shop.gd`/`card_shop.tscn` maintenaient leur PROPRE array `shop_relics` (16 relics figées en dur dans le `.tscn`, jamais synchronisée avec `treasure_relic_pool.tres` qui en a 26) au lieu de référencer le pool partagé. Fix : nouveau `shop_relic_pool.tres` (ressource `RelicPool`, mêmes 26 relics que le pool trésor pour l'instant, peut diverger plus tard), `card_shop.gd` prend maintenant `@export var shop_relic_pool: RelicPool` au lieu d'un `Array[Relic]` brut. **Piège rencontré en éditant `card_shop.tscn`** : Godot avait le projet ouvert en live et a re-sauvegardé la scène entre deux `Edit` successifs dès qu'il a détecté une référence `ext_resource` cassée (état intermédiaire), effaçant silencieusement la property `shop_relics` en cours d'écriture — corrigé en refaisant l'édition en un seul `Write` atomique plutôt qu'en plusieurs `Edit` successifs. **Toujours vrai/à surveiller : toute édition de `.tscn` en plusieurs passes avec l'éditeur Godot ouvert risque de se faire écraser par son file-watcher si un état intermédiaire a une référence cassée.**
  - **Titres/descriptions de carte trop longs coupés/débordants** (ex. "Perpetual Motion" wrap sur 2 lignes, description de Critical Edge qui dépasse son panneau) : step-down de taille de police par longueur de texte ajouté dans **les deux implémentations dupliquées** (`card_ui.gd` ET `card_menu_ui.gd`, même piège de duplication que d'habitude) — titre 15pt→12pt au-delà de 13 caractères→10pt au-delà de 17 (le nom le plus long du pool fait 16 caractères) ; description 12pt→11pt au-delà de 60 caractères→10pt au-delà de 90. Le titre duplique maintenant TOUJOURS `TITLE_LABEL_SETTINGS` (plus seulement pour la couleur verte "upgraded") puisque `font_size` doit aussi varier par carte ; la description ré-applique son `theme_override_font_sizes/normal_font_size` à chaque appel de `_apply_description()`, donc une description dynamique qui s'allonge en cours de partie (`get_dynamic_description`) se re-dimensionne aussi.
  - **Metronome (relique) sans compteur visible** : ajout d'un `owner.counter` (même pattern que Crown, `RelicUI.counter`) qui cycle 1→2→3 (déclenchement) →1... au lieu d'écrire `dice_amount_rolled_this_turn % 3` brut (qui aurait affiché "0" pile sur le roll qui déclenche le bonus). **Bug de syntaxe rencontré et corrigé** : `var n := Global.dice_amount_rolled_this_turn` plantait au parsing ("Cannot infer the type") parce que `Global.dice_amount_rolled_this_turn` (dans `global.gd`) est déclaré SANS type statique — `:=` ne peut pas inférer un type depuis une variable Global elle-même non typée. Fix : type explicite `var n: int = ...`. **Piège générique à garder en tête : ne jamais utiliser `:=` sur une valeur lue depuis une variable `Global` non typée, toujours mettre le type explicitement.**
  - **Act 2 pouvait re-proposer des events déjà vus en act 1** : `_enter_act_2()` (`run.gd`) réinitialisait `event_stats_pool.pool` depuis le snapshot `initial_event_pool` pris au début du run — undoing volontairement la déplétion (les events tirés sont retirés du pool à chaque pioche, voir le bloc `Room.Type.EVENT`). Fix : cette ligne de reset supprimée, le pool continue simplement de se vider across la transition d'act. `initial_event_pool` reste dans le code/la sauvegarde (dead weight harmless) plutôt que de toucher au format de sauvegarde pour un fix aussi scopé.
  - **The Fickle Broker (event) : trois bugs/manques.** (1) Aucun tooltip au survol des boutons de trade — ajout de 2 tooltips empilés par bouton (relique donnée en rouge, relique reçue en vert), même système que `relic_ui.gd`/`card_menu_ui.gd` (`TooltipScene`, cleanup sur `_exit_tree`). (2) Les deux boutons de trade pouvaient offrir la MÊME relique reçue — `RelicPool.get_random_relic()` a gagné un paramètre optionnel `exclude: Array[Relic] = []` (rétrocompatible avec les ~9 autres call sites existants), le trade B exclut maintenant le "get" du trade A. **Bug de typage rencontré en cours de route** : `var get_exclude: Array[Relic] = [_trade_a_get] if _trade_a_get else []` plantait au runtime ("Trying to assign an array of type Array to Array[Relic]") — un ternaire sur deux littéraux de tableau ne préserve PAS le typage statique du côté choisi, Godot voit un `Array` générique des deux côtés. Fix : déclaration typée + `.append()` conditionnel plutôt qu'un ternaire. (3) Les tooltips étaient positionnés "à droite du bouton" (convention habituelle) mais les boutons de cet event sont quasi pleine largeur → tooltip poussé hors écran, illisible. Fix : bascule à gauche du bouton si pas de place à droite + clamp horizontal dur. **Les deux pièges de typage (`:=` sur Global non typé, ternaire de tableau typé) sontà surveiller dans tout futur code GDScript touchant des collections/Global.**
  - **Campfire soignait un montant fixe (22 HP)** au lieu d'un pourcentage du Max HP — sans intérêt pour un joueur qui a gagné du Max HP en cours de run (Hollow Idol, Patient Monk...). Fix : `CAMPFIRE_HEAL_PERCENT := 0.33` (33%, choix arbitraire proche de l'ancien ratio 22/66 HP de départ — à retuner si Julien veut un chiffre différent), tooltip de survol affiche maintenant le vrai montant calculé ("Heal 22 HP (33% of Max HP)") au lieu du texte générique statique de `tooltip.gd::get_tooltip_content("REST")`.
  - **Dice Shop : les 9 bandeaux de titre ("EVIL DICE", "GIANT DICE"...) étaient tous le même Panel vert foncé uniforme**, jamais retouché depuis le passage aux textures de dés finales (2026-07-10) — repris avec un fond/bordure/texte dérivés de `DicePalette` par type (couleurs codées en dur dans le `.tscn`, `DicePalette` étant une classe GDScript non appelable depuis un fichier de scène) : fond = teinte OUTLINE, bordure 2px + texte = teinte ACCENT, ombre portée + coins arrondis pour matcher le style des boutons Buy déjà en place.

- **Suite de l'overhaul dés du 2026-07-11 (retours de Julien après avoir vu la passe du 07-10 en jeu) — NON ENCORE PLAYTESTÉ.** Trois demandes traitées : particules d'impact des cartes refaites, orbes dice→power poussées plus loin sur l'individualité, animation "dés consommés" pour All In.
  - **Particules d'impact de carte entièrement refaites** (`scenes/card_ui/card_particles.gd`) : rendaient des carrés magenta plats "montent puis fade" (texture/material jamais définis sur le `CPUParticles2D`, retombait sur le point 1×1 par défaut du moteur — carré à bords durs). Fix : même langage visuel que les power orbs (texture radiale douce en cache statique + `CanvasItemMaterial` blend additif en cache statique, couleur `DicePalette.accent(type) * 1.6`) pour que "l'impact soit visiblement la même magie qui charge les dés, relâchée sur l'ennemi". **Mouvement repensé** : spread passé de 42° (jet étroit vers le haut) à 165° (vraie explosion omnidirectionnelle) ; ajout de `orbit_velocity` (±0.9 tour/s, signe aléatoire par particule) + `tangential_accel` (±150) pour un vrai swirl magique au lieu d'une trajectoire balistique plate ; `radial_accel` toujours négatif (-70 à -20) pour un rappel doux vers le centre après l'explosion (« les particules sont récupérées », cohérent avec la métaphore power-orb) ; gravité allégée (500→180, le swirl fait maintenant le travail de "garder du mouvement") ; lifetime légèrement allongée (0.34+0.015×roll → 0.42+0.02×roll) pour laisser le temps au swirl de se lire. **Comptage/taille de particules INCHANGÉS** (déjà retunés plusieurs fois par le passé, formule `75+15×roll` gardée telle quelle). Validé par une simulation Python approximative de la physique CPUParticles2D avant application (deux itérations : swirl initial trop "spirographe"/géométrique avec orbit=±1.4 + tangential=±220, réduit à ±0.9/±150 pour des arcs plus organiques).
  - **Orbes dice→power poussées plus loin sur l'individualité** (`dice.gd::_spawn_power_orbs`/`_orb_bezier_step`) : Julien trouvait encore un effet "train" malgré le fix du 07-09 (jitter sur mid_x/stagger). Root cause restante identifiée : (1) le stagger indexé (`STAGGER*i + jitter`) préservait quasi toujours l'ORDRE de lancement même avec du jitter (jitter ±0.028 vs espacement de base 0.035 — dépassait rarement l'espacement) ; (2) TOUTES les orbes utilisaient exactement le même couple trans/ease (`TRANS_SINE`/`EASE_IN`) sur leur vol — même avec des vitesses/chemins différents, un profil d'accélération identique se lit comme "tout bouge pareil". **Fix double** : (1) le délai de lancement de chaque orbe est maintenant tiré indépendamment au hasard dans toute la fenêtre de la salve (`POWER_ORB_LAUNCH_WINDOW_PER_ORB * count`) plutôt que d'avoir un "créneau" par index — l'ordre de lancement réel est maintenant complètement mélangé (l'orbe #7 peut partir avant l'orbe #2) ; (2) chaque orbe pioche aléatoirement parmi 5 couples trans/ease (`POWER_ORB_EASE_PROFILES`, tous "EASE_IN"-ish pour garder l'histoire "attiré vers le nombre" mais avec des courbes d'accélération différentes) ; (3) **wobble perpendiculaire ajouté** sur la courbe bezier elle-même (`_orb_bezier_step` reçoit maintenant fréquence/amplitude/phase par orbe, offset sinusoïdal perpendiculaire à la tangente du chemin, enveloppe `sin(t·π)` = 0 aux deux extrémités donc le lancement/atterrissage reste garanti) — la trajectoire elle-même serpente au lieu d'être une courbe bezier propre. **Piège corrigé en même temps** : `_play_power_orb_arrival_reaction` (le flash "livraison" sur le nombre) se déclenchait sur l'orbe `i==0` en supposant qu'elle atterrirait en premier — plus vrai du tout une fois délai ET durée de vol indépendamment aléatoires. Fix : pré-calcul de tous les délais/durées AVANT la boucle de spawn, l'orbe avec le temps d'arrivée (délai+vol) le plus court est identifiée d'abord, c'est ELLE qui reçoit le callback de réaction peu importe son index.
  - **Nouvelle animation "dés consommés" pour All In** (`characters/warrior/cards/all_in.gd` + `dice.gd::_spawn_all_in_consumed`) : la carte consommait tous les dés restants de tous types en silence — le joueur ne voyait que le nombre de dégâts final "sans pouvoir deviner ce qui s'est passé". Nouveau signal dédié `Events.all_in_dice_consumed(consumed: Array[Dictionary])` (⚠️ **volontairement séparé de `Events.refuel_happened`** — ce dernier est écouté par des reliques comme Fuel-o-meter/Flywheel qui réagissent à un RAVITAILLEMENT de dés ; All In fait l'inverse, les détruit, donc réutiliser ce signal aurait déclenché ces reliques à tort). Le calcul de dégâts réel (`randi_range(1,6)` par dé, inchangé, pas de rebalance) reste séparé d'un second tirage purement visuel (`DISPLAY_FACE_VALUES`, une vraie face valide par type — évite par exemple d'essayer de charger une texture "evil3.png" qui n'existe pas, puisque Evil n'a que des faces 0 et 6) utilisé uniquement pour choisir quelle texture afficher sur chaque icône. **Comportement du flourish, réutilise le pattern déjà existant de `_spawn_refuel_return` (Recombobulate etc.) mais avec un dernier acte différent** : pop hors de la carte → montée → **hover nettement plus long** (0.9s vs 0.4s pour refuel, demande explicite "make them visible a bit longer") → au lieu de voler dans un dé actif (n'a pas de sens ici, les dés consommés viennent de plusieurs pools différents), elles **"brûlent" sur place** (flash surbrillant, petite montée finale, effondrement d'échelle à zéro) — lit comme "dépensé", pas "retourné".

- **Overhaul couleurs/feel de tout ce qui touche aux dés en combat (2026-07-10 soir, une fois les 9 textures de dés finales verrouillées) — NON ENCORE PLAYTESTÉ PAR JULIEN.** Palette entière re-dérivée des vraies couleurs de l'art final (couleurs dominantes extraites par script des PNG), au lieu des valeurs historiques choisies avant que l'art existe.
  - **Nouvelle classe `DicePalette` (`custom_resources/dice_palette.gd`, statique comme `KeywordColorizer`) = source unique des couleurs par type de dé** : `ACCENT` (couleur d'identité vive — texte/orbes/particules), `OUTLINE` (ombre profonde même teinte), `burst()` (accent tiré vers blanc chaud pour les célébrations). Consommateurs branchés dessus : `dice.gd` (nombre de Power + outline, roll history, popup "+X", orbes, teinte des bursts), `card_particles.gd`, `damage_popup.gd`, `dice_interface.gd` (highlight du slot sélectionné), `card_ui.gd` (glow "jouable" des cartes, qui référençait le `DICE_TYPE_COLOR` supprimé de dice_interface — **penser à ce call site si on renomme encore**). **Sync À LA MAIN requis (pas d'accès possible au runtime)** : `keyword_colorizer.gd::DICE_KEYWORD_COLORS` (hex strings dans un const dict) et les 9 `scenes/dices/*_dice_shader.tres` (magic_color/accent_color) — les trois endroits sont commentés en ce sens. Changements de teinte notables voulus : green mint→vert feuille, odd citron→laiton, giant lime néon→vert mousse, red orangé→cramoisi sang, mech gris foncé 0.35→acier clair (le 0.35 était illisible en nombre de Power).
  - **Bug corrigé : l'outline par-type du nombre de Power n'a JAMAIS été appliqué** — `update_dice_display()` faisait `add_theme_color_override("font_outline_color", ...)` sur un Label qui a un `label_settings` : **un LabelSettings a priorité totale sur les theme overrides, l'appel était silencieusement ignoré** (outline resté brun pour tous les types depuis toujours). Fix : muter `current_power.label_settings.outline_color` (sûr : dice.tscn n'est instancié qu'une fois, et `_set_power_text` mutait déjà `font_size` sur cette même ressource). **Piège générique : sur un Label avec LabelSettings, les `add_theme_*_override` de police/couleur ne font RIEN.**
  - **Bug corrigé : l'animation "charge" de dice.tscn corrompait les auras des autres types** — ses tracks bezier écrivaient l'`accent_color` du shader BLEU (valeurs en dur) dans le material ACTIF au moment de l'anim ; les 9 ShaderMaterial `.tres` étant des ressources préchargées partagées, charger un dé pendant que magma/evil/etc. était actif repeignait l'accent de CE type en bleu-violet jusqu'au redémarrage. Fix : tracks accent_color/material retirés de "charge" ET de RESET (ne gardent que les tracks de scale), remplacés par un pulse de `charge_heat` piloté en code dans `_on_charge_dice_animation()` (monte à 1.0 puis `_update_dice_aura_charge()` restaure le niveau lié au power banké).
  - **9 shaders d'aura re-colorés sur l'art final** (`*_dice_shader.tres`) : mech passait d'un **vert marécage foncé** (copié-collé jamais ajusté ?) à acier/rouille (matche les rivets rouillés du dé) ; evil perd son accent **bleu électrique** hors-sujet pour un violet profond (matche les veines magenta) ; green mint→feuille/forêt ; odd citron/rouge→laiton/bronze ; even orange pâle/rouge→orange saturé/brûlé ; magma accent 0.68→orange lave pleine intensité ; blue accent violet-magenta→indigo royal ; red/giant retouches légères (déjà proches de l'art).
  - **Orbes de power en blend additif** (`CanvasItemMaterial BLEND_MODE_ADD` partagé, construit une fois comme la texture) — elles lisent maintenant comme de la lumière au lieu de sprites teintés plats. Le flash d'arrivée sur le nombre utilise `accent.lightened(0.35)` (l'ancien flash prenait la couleur d'orbe ≈ couleur du nombre lui-même, quasi invisible).
  - **Bursts de particules teintés par type** : le burst radial max-roll ET l'anneau convergent de charge prennent `DicePalette.burst(type)` (accent lerpé vers blanc chaud — garde le cœur doré "succès", identité du dé en plus). Le flash blanc-or du max roll reste neutre volontairement.
  - **Pop d'atterrissage au changement de type de dé actif** (`_on_active_dice_changed`) : le swap de texture était instantané sans aucun feedback — petit scale punch 1.12→1.0.
  - **Fond de combat : nouveau `scenes/battle/battle_background.gdshader`** sur le Sprite2D Background de battle.tscn — remplace le `modulate = 0.73` plat par des **paramètres éditables sur le material** : brightness 0.74 (≈ même exposition qu'avant), saturation 1.08 + contrast 1.05 (compensent le gris-délavé du dim), vignette 0.4/0.55/0.55 (assombrit les coins, focus le centre — les ennemis/HUD ressortent pour les screenshots). `position = (0,1)` corrigé en (0,0) au passage (rangée de 1px de clear color visible en haut depuis toujours).
  - **Bouton ROLL : PAS de teinte par type de dé (demande explicite de Julien)** — seulement du polish neutre : texte "ROLL" passé crème avec un vrai outline brun foncé (n'avait qu'une ombre), shadow du stylebox 1→3. Fill plat conservé (préférence établie de Julien, jamais de gradient dessus).
  - **Bug corrigé le 2026-07-11 (remonté par Julien après playtest de cette passe) : scouter/forcer la face 0 (crack) du dé Evil ne faisait RIEN — le roll suivant restait aléatoire.** Root cause : `Global.next_guaranteed_roll` utilisait **`0` comme sentinelle "aucun roll garanti"**, mais `0` est aussi la valeur légale de la face crack d'Evil (scoutée via `battle.gd::_on_scout_dice_clicked` ou forcée par le statut Unlucky sur Evil via `values.min()`) — `dice.gd::roll_dice()` testait `if Global.next_guaranteed_roll != 0:`, donc un `0` fraîchement assigné était indiscernable de "rien n'est garanti" et toute la branche (y compris `next_roll_panel.hide()`) était sautée : le roll restait aléatoire ET le panel "next roll" restait affiché indéfiniment avec la face 0 dessus jusqu'au prochain changement de type de dé. **Fix : sentinelle changée de `0` à `-1`** (aucune face de dé du jeu n'est négative) sur les 5 sites concernés (`global.gd` déclaration + `reset_run_state`/reset de combat, `dice.gd` check/reset/reset-au-changement-de-dé) ; les sites d'écriture (`lucky.gd`, `unlucky.gd`, `battle.gd` scout click, `focus.gd`/`focus_plus.gd`) n'ont pas eu besoin de changer, ils assignaient déjà la vraie valeur. **Corrige aussi silencieusement Unlucky sur le dé Evil**, qui souffrait exactement du même bug (`values.min()` = 0 sur Evil) et n'avait donc jamais forcé la face crack non plus. **Son ajouté sur la face crack** (`dice.gd::play_crack_sound()`, déclenché dans `_apply_roll_result()` sur `dice_type == "evil" and Global.last_roll == 0`) — placeholder `res://whipsound.mp3` (asset inutilisé ailleurs, son "crack" net qui convient par hasard), à remplacer si Julien trouve un vrai SFX de pierre/verre qui craque.
  - **Bug préexistant trouvé et corrigé au passage (pas lié aux dés, remonté par Julien via un crash log pendant le playtest) : `CardUI.targets` pouvait contenir une référence à un ennemi mort.** `targets` (`scenes/card_ui/card_ui.gd`) est peuplé par `area_entered`/`area_exited` sur l'`Area2D` de détection quand on drag/aim une carte au-dessus d'un ennemi — mais `area_exited` ne se déclenche QUE si le node quitte physiquement la zone, jamais s'il est libéré (`queue_free`, ex. un ennemi tué par un roll Magma AoE pendant qu'on visait une carte dessus). La référence morte restait alors dans `targets` indéfiniment (la carte reste en main plusieurs tours). Au roll de dé suivant, `_on_dice_rolled_update_description()` lisait `targets[0]` sans vérifier sa validité et passait l'objet libéré à `get_dynamic_description(modifiers, aimed_target: Node)` — Godot plante avec "previously freed" plutôt que de traiter silencieusement comme null, puisque le typage statique du paramètre échoue sur un objet dont la classe n'est plus valide. **Fix : nouveau helper `_prune_stale_targets()`** (retire les entrées `not is_instance_valid()` de `targets`) appelé aux 3 points de lecture (`play()`, le second `card.play()` du chemin dé-rouge dans `_on_red_dice_rolled()`, et `_on_dice_rolled_update_description()` — ce dernier est aussi appelé depuis `card_target_selector.gd` pendant l'aiming, donc couvert par le même fix). **Piège générique à retenir : `area_exited` (et tout signal "sortie de zone" basé sur la physique) ne se déclenche jamais pour un node libéré pendant qu'il est encore dans la zone — toujours `is_instance_valid()` avant de lire un array peuplé par `area_entered`/`body_entered` si les objets qu'il contient peuvent mourir en cours de partie.**
  - **Statuts qui se chevauchaient en combat corrigé** : `theme_override_constants` `h_separation`/`v_separation` de `status_handler.tscn` étaient **négatifs** (-10/-20, donc les icônes se recouvraient quasi systématiquement) — passés à +6/+6 uniformes (l'override `h_separation=4` propre à `enemy.tscn` a été supprimé, plus nécessaire). `StatusHandler` contre-scale maintenant sa propre échelle contre celle de l'Enemy racine (`scenes/enemy/enemy.gd::update_enemy()`) — les combats multi-ennemis (`scale=0.75` sur le noeud Enemy pour tenir plusieurs corps à l'écran) rétrécissaient aussi les icônes de statut ; elles gardent maintenant une taille constante peu importe la composition du combat.
  - **Audit géométrique complet des 40 fichiers `battles/*.tscn`** : script Python (non committé, vivait dans le scratchpad de session) calculant la vraie bounding box de chaque sprite à partir du crop alpha réel des assets (pas juste la boîte `width`/`height` nominale — certains arts ont jusqu'à 45% de padding transparent), croisée contre 4 zones fixes (panel de dés, bouton End Turn, bords du viewport, sprites voisins). 98 violations trouvées initialement → 0 après 3 passes de convergence (une correction pouvait légèrement décaler un voisin, donc ré-audit après chaque application) → 77 repositionnements sur 24 fichiers. Julien a retrouvé 2 cas supplémentaires en playtest que l'audit avait laissés passer de justesse (Machopeur trop près des dés dans `tier_2_machopeur_octopus.tscn`, re-fixé une seconde fois avec une marge plus généreuse) — **leçon : préférer une marge large (50px+) à une marge qui "passe" tout juste le seuil du check.**
  - **Bug réel trouvé et corrigé : `sprite_y_offset`** (utilisé par 13 ennemis — Satyr, Machopeur, Crab, Hound, Lich... — pour ajuster leur art verticalement) **déplaçait le sprite mais pas le reste de la UI** (HP bar/nom/statuts/intent) dans `enemy.gd::update_enemy()` — un ennemi avec `sprite_y_offset` positif pouvait avoir ses jambes qui chevauchaient sa propre barre de vie. Fix : les 4 calculs de position Y (`intent_ui`, `stats_ui`, `status_handler`, plus `name_label` qui en dérive) incluent maintenant `+ sprite_y_offset`. A rouvert 6 chevauchements End Turn qui étaient masqués par le bug (la barre était placée "trop haut" par accident, évitant coïncidemment la zone du bouton) — re-corrigés en Y sur les mêmes 6 fichiers.
  - **Nom d'ennemi au survol entièrement reconstruit** (`scenes/enemy/enemy.tscn`/`enemy.gd`) : vivait comme un `Label` enfant direct de l'Enemy, donc dans le canvas de base — toujours SOUS n'importe quel `CanvasLayer`, y compris `BattleUI` où vit la main de cartes ; aucun `z_index` ne pouvait le faire passer au-dessus (une frontière de `CanvasLayer` bat toujours le `z_index`, voir mémoire dédiée). Déplacé dans un nouveau `NameLabelLayer` (`CanvasLayer`, `layer=10`), repositionné à la volée dans `_on_mouse_entered()` via `global_position` de l'ennemi + son échelle, puisqu'un `CanvasLayer` ignore entièrement la transform de son parent `Node2D`. Police corrigée au passage : n'avait aucun `font` défini, retombait silencieusement sur `CinzelDecorative-Bold` (police décorative du thème par défaut du projet, illisible à 14px) au lieu de `LuckiestGuy-Regular` (déjà utilisée pour Floor/Gold/HP en top bar) — nouveau `scenes/enemy/enemy_name_label_settings.tres` dédié. Centrage recalé sur x=124 (centre local réel du sprite) au lieu de 120 (ancienne approximation).
  - **`StatsUI` (barre de vie ennemie) avait un ancrage différent de ses 3 voisins** (`anchors_preset=7`/`anchor_top=anchor_bottom=1.0`/`grow_vertical=0`, contre l'ancrage "point" top-left par défaut de `NameLabel`/`StatusHandler`/`IntentUI`, qui eux se positionnent correctement) — normalisé au même ancrage (override supprimé, hérite du preset CENTER_TOP de la scène de base `stats_ui.tscn`). **⚠️ N'a pas entièrement résolu l'écart signalé par Julien entre le sprite et la barre de vie** ("still got a weird space but forget about it") — cause racine exacte non confirmée, Julien a choisi de laisser tomber plutôt que de continuer à creuser. À reprendre si le sujet revient.
  - **20 icônes de statut normalisées** (crop sur bbox alpha réelle + repad centré à ~86% de remplissage du nouveau canvas carré, ratio d'aspect du contenu préservé) : corrige des tailles visuellement incohérentes entre icônes dues à des marges de padding très variables sur les assets sources (ex. l'icône d'épée/Muscle ne remplissait que 76-79% de son canvas contre 92-96% pour Exposed). `eclipse.png`/`serenity_perfect.png` volontairement **pas touchés** — ce sont des illustrations pleines (paysage/scène), pas des icônes cadrées ; `rembg` n'a rien trouvé à détourer (pas de fond séparable). Nécessitent un vrai choix créatif de recadrage ou un nouvel asset, pas une normalisation automatique.
  - **Bug de highlight au survol du Goblin corrigé** : son PNG (`goblin_perfect_v2.png`) avait 11 îlots de pixels alpha déconnectés du corps principal (50-700px chacun, contre ~408 000px pour le personnage), invisibles à l'oeil mais tracés individuellement par le shader de contour au survol — donnait l'impression de pixels fantômes surlignés autour du personnage. Nettoyé à un seul îlot connecté, 2727px de bruit retirés, aucune perte visuelle.
  - **5 renommages d'ennemis** (`enemy_name` sur toutes les variantes tier1/tier2 concernées) : Machopeur→**Marauder**, Octopus→**Kraken**, Plant→**Venom Bloom**, Hound→**Lava Hound**, Vortex→**Maelstrom** — Oculus/Skeleton (Crab)/Temple Defender/Satyr/Lurker jugés déjà corrects, conservés tels quels.
  - **Bug systémique trouvé sur la preview de dégâts des cartes contre un ennemi Exposed** (ou tout futur statut `DMG_TAKEN`) : `get_dynamic_description()` (le texte de carte qui résout "X" en un vrai chiffre pendant qu'on vise) n'appliquait QUE les modifiers du joueur (Strength...), jamais ceux de la cible visée — le vrai calcul de dégâts (`DamageEffect`/`Enemy.take_damage()`) applique bien les deux passes (joueur puis cible), donc les dégâts réels étaient corrects mais la preview les sous-estimait contre un ennemi Exposed. Fix : nouveau helper `Card.apply_target_modifier()`, `card_ui.gd` transmet maintenant l'ennemi actuellement visé (`targets[0]`) à `get_dynamic_description()`, et `card_target_selector.gd` rafraîchit la description en direct pendant qu'on vise (avant, seul un roll de dé la rafraîchissait). Signature mise à jour sur les 80 cartes ayant une description dynamique (patch scripté), 53 d'entre elles enveloppées avec le nouveau helper. **Piège rencontré et corrigé avant commit** : le patch scripté (regex sur `var total := modifiers.get_modified_value(..., DMG_DEALT)`) a accidentellement touché le VRAI calcul de dégâts de `resonance.gd` (qui utilise la même syntaxe dans `apply_effects()`, pas seulement dans la preview) — repéré en vérifiant qu'aucun `apply_target_modifier` ne fuitait hors de `get_dynamic_description()` sur les 80 fichiers, corrigé avant de committer.
  - **Campfire retravaillée pour que Heal matche la structure d'Upgrade** (`scenes/campfire/campfire.tscn`) : la hiérarchie de `HealTextureButton`/`HealButton` ne correspondait pas à celle d'`UpgradeTextureButton`/`UpgradeButton` (bouton Heal enfant d'un wrapper `HealZone` séparé, ancré à un point fractionnaire visiblement placé à la main dans l'éditeur, plutôt qu'enfant direct de sa propre texture comme Upgrade) — restructurée pour un miroir exact (même taille de boîte 200×200, symétrique par rapport au centre écran, même police sur le label), gap texture→bouton resserré de 30 à 10px sur les deux. *(Les textures `heal.jpg`/`blacksmith.jpg` utilisées pendant cette passe ont depuis été remplacées par `campfire_rest_icon.png`/`campfire_upgrade_icon.png` — voir la passe séparée juste au-dessus, seule la structure/le spacing documentés ici viennent de cette passe-ci.)*
  - **Label "TREASURE" recoloré pour matcher "CAMPFIRE"** (`scenes/treasure/treasure.tscn`) : même police déjà utilisée (`CinzelDecorative-Bold`, taille 30), il manquait juste la couleur or/outline/shadow du `LabelSettings` de Campfire — copiés à l'identique.
- **Passe séparée du 2026-07-10 (menée en parallèle, indépendante de la review ci-dessous) : 8 icônes de statut générées + un vrai bug de code trouvé sur le dé Evil, plus 3 bugs d'event.**
  - **8 icônes de statut Blessing manquantes (Foresight, Perpetual Motion, Stockpile, Cogwork, Critical Edge, Hardened Grip, Opening Gambit, Steady Hand) générées et branchées** (`foresight_status_icon.png`, etc., remplaçant le placeholder `scroll.jpg` dans leurs `.tres` respectifs) — Critical Edge+/Steady Hand+ réutilisent l'icône de leur version de base (même effet visuel, pas de nouvel asset). Dark Pact confirmé jamais ajouté à `warrior_draftable_cards.tres` (vérifié, pas juste supposé) — fichiers laissés sur disque non-draftables, pas d'icône faite pour lui.
  - **Pipeline de nettoyage appliqué aux 8 icônes** : (1) `rembg` pour retirer le fond blanc opaque que Firefly avait cuit en dur (aucune vraie transparence à la génération, contrairement à ce qu'on pensait) ; (2) normalisation du taux de remplissage de chaque icône dans son canvas carré (crop sur bbox alpha avec seuil >10 pour ignorer le bruit quasi-transparent en bord d'image, puis rescale centré à ~0.84 du canvas) — les 8 générations variaient de 0.83 à 1.00 (edge-to-edge), rendues cohérentes avec la moyenne des icônes historiques (~0.81-0.86). Script `normalize_icon_fill.py`, pas committé dans le repo (vivait dans le scratchpad de la session).
  - **`marionette_status_icon.png` normalisé au même passage** — fill ratio 0.68x0.86 avant (contenu décentré, pas assez cadré). ⚠️ **Incident en cours de route** : un premier essai de normalisation sur `marionette_status_icon_v2.png` (fichier NON utilisé par le jeu — `status_marionette.tres`/`_plus.tres` pointent tous les deux vers `marionette_status_icon.png` sans suffixe) a donné un résultat cassé (bbox `getbbox()` polluée par du bruit alpha quasi-transparent en bord d'image) ; `git checkout --` a été utilisé pour "reset" les deux fichiers avant de comprendre le bug, **sans stash préalable** — ça a fait perdre une version 538×538 non-committée de `marionette_status_icon.png` qui existait avant cette session (contenu réel différent du 500×500 committé, pas juste une recompression). Version 500×500 (committée) re-normalisée proprement ensuite avec un bbox à seuil alpha (>10) au lieu du `getbbox()` brut. **Si le rendu de Marionette semble "différent d'avant" un jour, c'est probablement cette version 538×538 perdue — aucune copie connue.**
  - **Bug de code trouvé sur la face 0 du dé Evil** (pas un problème d'asset) : `scenes/dices/dice.gd` (2 emplacements, `evil_faces` + `_get_dice_face_texture()`) et `scenes/battle/battle.gd` chargeaient en dur `blue0.png` (un vieux placeholder plat, jamais l'art `evil0.png` réellement destiné à cette face) pour la face 0 du dé Evil — donc remplacer `evil0.png` sur disque n'avait auparavant AUCUN effet en jeu. Les 3 références corrigées pour charger `evil0.png`.
  - **3 bugs trouvés/corrigés dans les events narratifs** : `event_gold_dice.tscn` affichait le dé à sa taille native (`stretch_mode = 2` sans `expand_mode`, contrairement à `event_fountain_heal.tscn` qui a le bon `stretch_mode = 5` + `expand_mode = 1`) — corrigé, plus de dé géant qui débordait de l'écran. `event_fountain_heal.tscn` avait une icône goblin+dé (`events_gold_dice.png`) collée sur le bouton ROLL, copiée-collée du template gold_dice sans rapport avec une fontaine de soin — retirée (ext_resource devenue inutile aussi supprimée). `event_russian_dice.gd` : le bouton Stop restait cliquable après une défaite (`loser_panel.show()` sans désactiver `stop_button`, qui restait actif depuis le premier succès) — permettait de contourner une perte en cliquant Stop juste avant Continue ; fix = `stop_button.disabled = true` ajouté dans les 5 branches de défaite (`replace_all`, les 5 blocs étaient identiques). Crash `character_stats.health` sur `Nil` retrouvé dans `_on_winner_exit_button_pressed` (seulement en lançant la scène directement depuis l'éditeur, pas `setup()` appelé) — même garde ajoutée que le fix déjà en place dans `event_fountain_heal.gd` (`push_error` + skip au lieu de planter).
  - **Icônes Rest/Upgrade de la campfire refaites** (`campfire_rest_icon.png`, `campfire_upgrade_icon.png`, remplaçant `heal.jpg`/`blacksmith.jpg`) : les deux anciennes textures ne matchaient ni le style ni le ratio du jeu (`heal.jpg` = illustration vectorielle plate sans rapport, `blacksmith.jpg` = portrait peint réaliste en format portrait écrasé dans une boîte carrée 200×200 avec `stretch_mode = 0`). Nouvelles icônes générées en format carré, style volontairement plus simple/lisible (inspiré de la clarté des icônes de campfire de Slay the Spire, sans copier son look) plutôt que le style "comic book" à fond éclaté utilisé pour les cartes.
- **Review de Julien sur les 18 cartes + 10 designs de reliques du 2026-07-06 (§ "Card pool depth expansion"), traitée le 2026-07-10** : verdicts appliqués directement au code, voir [card_pool_expansion_2026-07.md](card_pool_expansion_2026-07.md) pour les designs d'origine (les verdicts eux-mêmes ne sont PAS reportés dans ce fichier .md, seulement ici et dans le code).
  - **Cartes coupées du pool draftable** : Cascade et Dark Pact n'avaient en fait jamais été ajoutées à `warrior_draftable_cards.tres` (trouvé en vérifiant, pas une action de cette session) — Executioner retirée explicitement (ext_resource + entrée array supprimées). Les 3 fichiers restent sur disque, juste non-draftables.
  - **Cartes gardées telles quelles** : Crescendo, Foresight (déjà "Charge 2" confirmé, pas "Refuel 2"), Bulwark, Rigged, Momentum.
  - **Cartes reworkées** : Resonance (le pair-check porte maintenant sur TOUT `roll_history`, pas juste les 2 derniers rolls ; le bonus devient "inflige les dégâts une seconde fois" au lieu de "pas de reset de Power") ; Jackpot (même généralisation du triple-check sur tout l'historique, dégâts base = X au lieu de 6 fixe, bonus = X×5 au lieu de 30 fixe) ; All In (dépense maintenant TOUS les types de dés restants, pas seulement Red) ; Adrenaline (passée Celestial, `can_play_without_dice = true`) ; Breach (Min 9 → Min 6) ; Stockpile (cap 5 → 8 Power, dans `status_stockpile.gd` ET son tooltip) ; Transmutation (ajout "for this turn only" + Exhaust — en réalité déjà implicitement vrai puisque `dice_interface.gd::_on_player_turn_started()` réécrit `current_amount` depuis `max_amount` à chaque tour, donc aucune persistance possible même sans le flag Exhaust) ; Second Wind (nouveau gate Max 12, `requirement = 2`/`requirement_number = 12`, apply_effects no-op au-delà comme Breach) ; From Nothing (texte reformulé seulement — **la logique gérait DÉJÀ le cas Weak** : `dice.gd` applique `Global.roll_value = max(0, roll_value + next_roll_modifier)`, donc un roll ramené à 0 par Weak déclenchait déjà la carte avant même cette session, pas juste la face 0 du dé Evil).
  - **10 reliques implémentées** (`relics/cartographers_quill.gd`, `snake_eyes_charm.gd`, `house_money.gd`, `echo_chamber.gd`, `obsidian_scale.gd`, `metronome.gd`, `overflow_valve.gd`, `prayer_beads.gd`, `flywheel.gd`, `trick_scale.gd`, toutes enregistrées dans `treasure_relic_pool.tres`), plusieurs avec un effet modifié par rapport au design d'origine du doc : House Money inflige maintenant 5 dégâts à tous les ennemis (au lieu de donner 3 Block) sur un Red Dice à 5/6 ; Echo Chamber charge un Blue Dice (au lieu de Boost 2) sur une paire consécutive ; Obsidian Scale et Trick Scale sont devenues des triggers de tour fixe (`Global.fight_turn == 1`/`== 2`, même pattern que `relics/volcanic_rock.gd`) chargeant respectivement 1 Evil/1 Mech Dice, remplaçant entièrement leurs designs d'origine (0-face → pioche, Mech ±2) ; Cartographer's Quill ajoute +1 face de Scout (`Global.scout_bonus_amount`, lu directement par `battle.gd::_on_scout_effect`) au lieu de forcer 5 — **le panel de scout a été élargi à 6 slots (`ScoutDice6` ajouté dans `battle.tscn` + `battle.gd::scout_faces`) spécifiquement pour que Quill reste utile sur un Scout 5 déjà existant**, plafond relevé de 5 à 6 en conséquence côté code (le tooltip lui-même reste volontairement `"Your Scout cards show 1 more roll outcome"`, sans mentionner le chiffre 6 — retiré sur demande de Julien le 2026-07-10) ; Flywheel donne une carte Scout 2 (`card_scout2.tres`, déjà existante, jamais utilisée nulle part avant) sur `Events.refuel_happened` (signal déjà émis par Recombobulate/Catalyst/Voodoo).
  - **Prayer Beads a nécessité de toucher 24 fichiers Blessing** (`characters/warrior/cards/*.gd`, base + versions "+") : chaque script Blessing vérifie son seuil de roll en dur (`if Global.roll_value >= 6:` pour 16 d'entre elles, `== 6`/`== 8`/`>= 4`/`% 6 == 0` pour les autres) — nouveau flag global `Global.blessing_cast_any_roll` ajouté en `or` à CHACUNE de ces conditions. **Pattern à reproduire pour toute future carte Blessing** : sa condition de cast doit inclure `or Global.blessing_cast_any_roll` dès l'écriture, sinon Prayer Beads ne la couvrira pas.
  - **Icônes des 10 nouvelles reliques = vrai art dédié depuis le 2026-07-10 soir** (au départ des placeholders réutilisés, remplacés une fois que Julien a fourni `C:\Users\julie\Downloads\daiso july art`) : `cartographers_quill.png`, `snake_eyes_charm.png`, `house_money.png`, `echo_chamber.png`, `obsidian_scale.png`, `metronome.png`, `overflow_valve.png`, `prayer_beads.png`, `flywheel.png`, `trick_scale.png`, tous copiés à la racine du projet et branchés dans leurs `.tres` respectifs. Matching fait par nom de prompt IA tronqué dans le nom de fichier (ex. "An ornate glowing quill pen hove..." → Cartographer's Quill), pas par nom de fichier littéral — plusieurs variantes existaient pour certaines reliques dans ce dossier, une seule choisie par relique, les autres laissées sur disque si Julien préfère une variante différente.
  - **⚠️ Pas encore traité dans cette review** : les 6 designs de dés (§4 du doc) — Julien a dit "will do later". Tout le reste (events narratifs, debuff-applier, big-power) a été traité dans la foulée, voir bullet suivant.
- **Suite de la review le même jour (2026-07-10, deuxième partie de session)** : debuff-applier (§2c) et big-power-payoff (§2d) tranchés, plus une repasse complète sur les 15 events narratifs du 2026-07-09.
  - **Debuff-applier** : **Sigil** (carte) avait été retenue parmi les 8 proposées, avec **Requirement ODD** ajouté (gate non prévu dans le design d'origine) — réutilisait **le statut `sigil.tres` existant tel quel** (celui du Sigil Slug) plutôt qu'un nouveau statut, la vérification réactive ("`Global.roll_value == sigil.stacks` → +1 Blue Dice") vivant déjà dans `dice.gd::_check_sigil_trigger()`. **Coupée du pool le 2026-07-10 (soir même)** : Julien "pas convaincu" une fois vue en contexte — retirée de `warrior_draftable_cards.tres`, fichiers (`sigil_card.gd`/`card_sigil.tres`) laissés sur disque comme tout le reste du contenu coupé cette session, art `sigil.png` inutilisé pour l'instant. Les 7 autres designs (Crimson Brand, Corrode, Hex, Tuning Fork, Enfeeble, Siphon, Shatter) restent non-implémentés.
  - **Big-power-payoff** : Tidal Force et Juggernaut implémentées telles que proposées ; Dominance implémentée avec **Min 10** (au lieu de Min 15) ; Earthquake, Obliterate et High Roller coupés (jamais implémentés). Les 4 nouvelles cartes (Sigil, Tidal Force, Juggernaut, Dominance) avaient déjà leur art généré et posé sur le disque par Julien (`sigil.png`, `tidal_force.png`, `juggernaut.png`, `dominance.png`, tous non référencés avant) — trouvés et branchés directement, pas de placeholder recyclé cette fois.
  - **5 des 15 events narratifs coupés** (retirés de `events_stats_pool.tres`, fichiers laissés sur disque comme d'habitude) : The Hungry Altar, The Bargaining Skeleton, The Dice Forge, The Twin Vaults, The Traveling Healer. Pool à 23 events désormais (13 pré-existants + 10 restants du lot du 07-09).
  - **5 events reworkés** : Hollow Idol (coût 4→6 Max HP, bouton "Leave" retiré entièrement — seulement 2 choix maintenant) ; Patient Monk (gain 5→8 Max HP) ; Whetstone Shrine (le refus ne donne plus 20 or, ouvre maintenant un vrai reward screen de carte) ; Wandering Merchant (le browse donne maintenant des cartes **upgradées** via un nouveau flag one-shot `Global.force_upgraded_card_rewards` consommé dans `battle_reward.gd::_show_card_rewards()` ; le refus devient "vendre du bric-à-brac" pour 50 or au lieu de 15) ; Crimson Eclipse (le pari coûte maintenant 2 reliques aléatoires au lieu de 50 or ; refuser coûte maintenant 6 HP au lieu d'être gratuit) ; Healing Spring (passé de 2 à 3 options : payer 25 or pour 25 HP / 10 HP gratuit / saigner 10 HP contre 40 or).
  - **Fickle Broker entièrement repensé** (le plus gros morceau) : au lieu d'un swap à l'aveugle, l'event génère maintenant **2 propositions d'échange concrètes et nommées** au moment du `setup()` (avant que le joueur ne clique quoi que ce soit) — chaque bouton affiche en direct "Trade [Relic X] for [Relic Y]" via `RelicUI.relic.relic_name`, plus un 3e bouton "Refuse" toujours présent. `relic_handler`/`treasure_relic_pool` sont déjà assignés par `run.gd` avant l'appel à `setup()` (ordre confirmé dans le bloc de dispatch d'event), donc safe d'y construire les offres directement dedans plutôt que dans `_ready()`.
  - **Nouveau slot Scout ajouté au moteur** (`ScoutDice6` dans `battle.tscn`, `scout_faces` dans `battle.gd`) — voir bullet Cartographer's Quill ci-dessus, c'est un changement transversal qui touche `_on_scout_effect()` au-delà du seul contexte relique.
  - **Images d'events branchées le 2026-07-10 soir** une fois Julien a pointé vers `C:\Users\julie\Downloads\events art` : les 10 events narratifs restants (Hollow Idol, Fickle Broker, Whetstone Shrine, Wandering Merchant, Patient Monk, Crimson Eclipse, Twin Shrines, Wayside Shrine, Healing Spring, Golden Die Shrine) ont chacun leur vraie illustration maintenant, copiée à la racine sous le nom de l'event et branchée dans le `TextureRect` principal de leur `.tscn` (remplace les anciens placeholders recyclés type `oswald.png`/`sledgehammer.png`/`unity.png`). Le dossier contenait aussi de l'art pour les 5 events coupés (Dice Forge, Bargaining Skeleton, Hungry Altar, Traveling Healer, Twin Vaults) — laissé de côté, pas copié.
- **⚠️ NON RÉSOLU — crash "health on Nil" dans `event_fountain_heal.gd` après avoir roll le dé (2026-07-09 soir)** : `character_stats.health += health_reward` plante parce que `character_stats` est `null`, alors que la relecture statique du dispatch (`run.gd::_on_map_exited`, cas `Room.Type.EVENT`) montre `event_scene.setup(character, stats)` bien appelé, avec `character` non-null en amont. Cause exacte NON confirmée (pas pu relancer le jeu pour déboguer en live) — si ça revient, vérifier via l'onglet Remote de l'éditeur au moment du crash si `character_stats` est vraiment `null` sur l'instance réelle. Fix appliqué en attendant : garde `if character_stats == null` dans `event_fountain_heal.gd` (n'écrase plus, juste `push_error` et skip le heal au lieu de planter) + un filet de sécurité redondant dans `run.gd` (`if "character_stats" in event_scene: event_scene.character_stats = character`, même pattern duck-typé que le `char_stats` déjà présent pour d'autres events). Le jeu ne devrait plus crasher dessus, mais la cause racine reste à élucider.
- **Textures des 9 types de dés uniformisées (2026-07-09 soir)** : les 8 types travaillés plus tôt (red/magma/mech/green/even/odd/evil/giant) ET le dé bleu (fait manuellement par la copine de Julien, pips irrégulières par endroits) sont maintenant tous régénérés via le même pipeline (`rembg` pour le fond transparent + script Python de placement de pips en pourcentages fixes, voir section dédiée plus haut) — toutes les faces de tous les types ont maintenant un espacement/une taille de pip strictement cohérents. Le dé evil est passé de sa version `_BEST` (jugée trop sombre) à sa version `_FALLBACK` (plus lisible). `blue1_reference_style.png` (la référence de style d'origine) n'est plus utilisée nulle part dans le jeu, gardée seulement comme référence externe.
- **Main menu entièrement retravaillé (2026-07-09 soir)** : nouveau fond (`main_menu_v4.png`, généré par IA à partir de l'ancien comme référence, SANS bouton peint dedans cette fois — contrairement à `main_menu_v3.jpg` où "Start New Run" faisait partie de l'image). Les deux boutons (`NewRun`/`LoadRun`) sont maintenant de vrais boutons Godot stylés identiquement : fond bordeaux/vin **identique à la couleur des cartes normales** (`card_ui_normal.tres`, `Color(0.311214, 0, 0.0696221, 1)`) + bordure or universelle (`Color(0.788235, 0.635294, 0.152941, 1)`), coins arrondis à 12, ombre, glow doré au survol — repris après plusieurs itérations de retour de Julien (d'abord brun, puis prune/violet, puis ce bordeaux final "kinda like most of my cards"). Largeur réduite (~380px au lieu de ~540px). **Piège rencontré en éditant `main_menu.tscn` à la main** : dans le format `.tscn`, `parent=` est TOUJOURS relatif à la racine de la scène par nom de nœud, jamais préfixé par le nom de la racine elle-même (`parent="."` pour un enfant direct, PAS `parent="NomDeLaRacine"`) — une première tentative de restructuration a cassé `get_node("DiceTooltip")` en écriture `parent="CanvasLayer"` au lieu de `parent="."`, laissant le nœud enfant orphelin de fait malgré une indentation/lecture visuellement correcte du fichier texte.
- **Écran de confirmation "Load Run" (2026-07-09 soir)** : cliquer sur "Load Run" affiche maintenant un aperçu (Floor, Act, Gold, HP, nombre de cartes/reliques/dés) avant de charger réellement, avec boutons "Yes, that's my run!" / "Cancel" (mêmes styleboxes que le popup tutoriel existant, aucune nouvelle ressource visuelle). Si le fichier de sauvegarde est corrompu/version incompatible, l'écran de confirmation est sauté entièrement (fallback direct vers `run.gd::_load_run()`, qui a lui-même déjà un fallback vers un run neuf).
- **Bouton Map (consultation) converti en icône (2026-07-09 soir)** : `ConsultMapButton` n'est plus un `Button` avec texte "Map"/"Close Map" mais un `TextureButton` affichant une icône de parchemin (générée par IA, fond retiré via `rembg`), repositionné juste à côté du `FloorLabel` (au lieu d'être poussé tout à droite par `ExpandSpacer`). Highlight au survol : léger contour doré (`HoverGlow`, un `Panel` bordure-seule en `z_index = -1` derrière l'icône, fade in/out) + éclaircissement de l'icône elle-même, le tout compatible avec la pause du mode consultation (`Tween.TWEEN_PAUSE_PROCESS` explicite, sinon les tweens ne s'animent pas pendant que l'arbre est en pause).
- **Nouveau flag `EventStats.force_for_testing` pour tester un event rapidement (2026-07-09 soir)** : cocher la case sur un `.tres` d'event le fait gagner systématiquement le tirage (`EventStatsPool.get_random_event_for_tier` le retourne avant même de faire le tirage pondéré), en ignorant tout poids. **`event_tier` n'est PAS utilisable pour ça** — c'est un filtre strict à égalité exacte (0/1/2, un event à tier 77 sortirait simplement de tous les pools et ne serait jamais tiré, l'inverse de l'effet recherché). Piège à retenir : le flag est stocké sur une ressource `.tres` partagée/persistante — il reste actif sur TOUS les runs suivants tant qu'il n'est pas décoché manuellement.
- **Bug du roll-spam corrigé (2026-07-09 soir)** : cliquer très vite sur ROLL permettait de rollede plus de dés que le joueur n'en avait (ex. 2 Blue Dice → 5-6 rolls). Cause : le décompte réel (`Global.blue_dice_current_amount -= 1`, dans `dice_interface.gd::_on_dice_rolled`) ne se déclenche qu'à la FIN de l'animation de roll (~250ms), via le signal `dice_rolled` émis en fin de `_apply_roll_result()` — mais `roll_dice()` vérifie `can_roll` au DÉBUT, donc des clics rapides passaient tous la vérification contre un compteur pas encore décrémenté. Fix : nouveau flag `_roll_in_progress` dans `dice.gd`, posé dès qu'un roll est accepté, remis à `false` seulement à la fin de `_apply_roll_result()`.
- **Écran de victoire simplifié (2026-07-09 soir)** : plus de jingle ni d'écran "Victory!" à cliquer manuellement — `battle_over_panel.gd` attend juste 1 seconde (`WIN_AUTO_ADVANCE_DELAY`) puis enchaîne directement sur l'écran de récompense. Le bouton debug "Battle" (`run.gd::_on_debug_battle_button_pressed`) pose `Global.debug_battle_entry = true`, ce qui fait sauter entièrement le délai d'1s pour itérer plus vite en test — flag remis à `false` automatiquement après lecture, ne contamine jamais un vrai run suivant. L'écran "Game Over" (défaite) n'est pas concerné, inchangé.
- **Tooltips de la Dice Shop : trois bugs distincts corrigés (2026-07-09 soir)**, tous liés au fait que la Dice Shop peut maintenant s'ouvrir par-dessus une Map pleinement interactive (nouveauté du 07-07, jamais testée dans cette config avant) : (1) clics qui traversaient jusqu'à la map en dessous — `mouse_filter = 2` (IGNORE) sur le `Control` racine et son `Panel` de fond, remis à `STOP` (défaut) ; (2) tooltip qui restait bloqué à l'écran après un clic qui fait voyager vers une salle pendant un survol — ajout d'un `_exit_tree()` dans `shop.gd` qui nettoie les tooltips actifs, même pattern que `relic_ui.gd` déjà corrigé une fois ; (3) **tooltip invisible bien que créé et positionné correctement** — `dice_tooltip.tscn` avait sa racine en simple `Panel` (pas de `CanvasLayer`), donc rendu à la couche de base, SOUS le panneau opaque de la Dice Shop qui vit maintenant dans `TopBar` (`CanvasLayer layer=3`) ; fix : racine passée en `CanvasLayer layer=100` (même schéma que le tooltip de relique, déjà correct) — corrige aussi silencieusement le tooltip de dé EN COMBAT (`dice_interface.gd`), jamais remarqué comme cassé mais souffrant du même problème via `BattleUI` (`layer=1` par défaut).
- **Event "Russian Dice" restylé pour matcher le reste du jeu (2026-07-09 soir)** : le mini-jeu de gamble (`DicePanel` et ses sous-panneaux) utilisait des couleurs plates ad hoc (teal/orange/gris) sans rapport avec le langage visuel établi ailleurs — repris avec le même vocabulaire bordure-or/fond-teal-sombre que les boutons de shop (`shop_button_*.tres`). Piège trouvé en cours de route : les boutons Continue/Stop avaient reçu par erreur le thème `base_button.tres` (prévu pour les petits boutons ROLL superposés aux dés) plutôt que d'hériter du thème global par défaut du projet (`main_theme.tres`, déjà appliqué automatiquement à tout bouton sans style custom et déjà du bon style) — recevoir CE thème écrasait le bon rendu par défaut avec un look moutarde/olive plat ; fix = retirer l'override, pas en ajouter un nouveau.
- **⚠️ 15 nouveaux events narratifs ajoutés le 2026-07-09 — CONTENU CLAUDE, NON REVU PAR JULIEN.** Voir la section dédiée "Events narratifs" plus bas pour le détail complet (mécaniques, chiffres, art réutilisé) — Julien a dit explicitement vouloir les relire/ajuster/couper à la prochaine session, donc **traiter ce contenu comme provisoire** : ne pas supposer que les noms, chiffres ou mêmes la présence de ces events sont définitifs tant qu'il n'a pas fait sa passe de review. Même consigne que pour les designs de reliques/dés en attente dans `card_pool_expansion_2026-07.md` — contenu généré, pas encore validé.
- **Système de sauvegarde v1 shippé (2026-07-07)** — voir section dédiée "Système de sauvegarde" plus bas (checkpoints sur l'écran de map, slot unique, `var_to_str`).
- **Nouveau bouton "Map" dans la top bar pour consulter la carte sans perdre son combat/event/shop en cours (2026-07-07)**, distinct du `MapButton` de debug (qui reste destructif intentionnellement) — voir section "Map" plus bas. Trois bugs trouvés et corrigés au passage, tous réutilisables pour tout futur code qui cache/pause une vue temporairement : (1) un `CanvasLayer` niché dans la vue cachée (ex. `BattleUI`, `CardPileViews` dans `battle.tscn`) ignore le `hide()` du parent, même piège que `MapBackground` déjà documenté — fix générique `_set_nested_canvas_layers_visible()` dans `run.gd` qui recurse et bascule tout `CanvasLayer` trouvé ; (2) un `Camera2D` niché (`battle.tscn` en a un) n'est pas un `CanvasItem` et reste "current" même si son parent est caché — il faut le désactiver explicitement (`_set_nested_cameras_enabled()`) avant d'activer la caméra de la Map, sinon on scroll une caméra qui n'est pas celle affichée à l'écran ; (3) `relic_ui.gd` avait un tooltip principal SANS timeout de sécurité (contrairement à ses propres tooltips secondaires et à ceux de `card_ui.gd`/`intent_ui.gd`) — `get_tree().paused = true` empêche `mouse_exited` de jamais se déclencher, donc le tooltip restait affiché indéfiniment ; fix : timeout de secours ajouté + purge explicite des tooltips de relique à l'ouverture du mode consultation. La Dice Shop a aussi été déplacée de `current_view` (world-space, sujette à la caméra de la Map) vers un enfant de `TopBar` (CanvasLayer, screen-space) — elle se décentrait après avoir scrollé la map avant l'ouverture.
- **Particules "power orbs" (dé → Power) rendues plus individuelles + SFX d'impact ajouté (2026-07-09)** : `dice.gd::_spawn_power_orbs()` — Julien trouvait que les orbes se déplaçaient "en train", parfaitement alignés. Fix : `mid_x` (le ratio le long du chemin où l'orbe bombe) est maintenant randomisé par orbe (`POWER_ORB_MID_X_MIN/MAX`, était fixe à 0.55) + jitter du point de contrôle élargi (±25 au lieu de ±15) + plage d'apex élargie (40–130 au lieu de 60–110) + le délai de lancement (`POWER_ORB_STAGGER * i`) reçoit maintenant un jitter aléatoire (`POWER_ORB_STAGGER_JITTER`) plutôt que d'être parfaitement linéaire — c'est la cadence de lancement parfaitement régulière qui donnait l'effet "train", pas seulement la trajectoire. Nouveau : un petit son ("tick", placeholder `sfx/578807__nomiqbomi__pluck-1.mp3` — asset inutilisé ailleurs, à remplacer si Julien a mieux) joue à CHAQUE atterrissage d'orbe (`_play_power_orb_land_sfx`), pitch/volume jitterés par hit pour ne pas sonner comme une mitrailleuse sur un gros roll. Volume monté deux fois sur retour de Julien (-10dB → -4dB → +2dB). `sound_player.gd::play()` a gagné deux paramètres optionnels (`pitch_scale`, `volume_db`) pour ça, rétrocompatible avec tous les appels existants.
- **SFX de roll de dé retrouvé "en retard" — root cause = silence en tête du fichier audio, pas un bug de timing dans le code (2026-07-09)** : `play_dice_roll_sound()` était déjà appelé à la toute première ligne de `roll_dice()`, avant toute animation. Diagnostiqué via `ffmpeg silencedetect` : `dicerollsound1.mp3` avait 341ms de silence avant le premier son, `dicerollsound2.mp3` 143ms, `dicerollsound3.mp3` 54ms (négligeable) — les 3 sons sont tirés au hasard à chaque roll, donc le délai apparaissait sur 2 rolls sur 3. Fix : les 3 fichiers `.mp3` ont été retrimés avec `ffmpeg` (silence en tête coupé), remplacés directement sur disque. **Piège méthodologique à retenir** : avant de chercher un bug de timing/ordre d'appel dans le code, vérifier si l'asset audio lui-même a du silence en tête — `ffmpeg -i <file> -af silencedetect=noise=-30dB:d=0.05 -f null -` donne la réponse en une commande.
- **Reliques déplacées sous la top bar façon Slay the Spire (2026-07-07)** : `RelicHandlerContainer` retiré de `TopBar/BarItems` (la rangée horizontale HP/dés/reliques/deck) ; un nouveau `TopBar/RelicBar` héberge maintenant le même nœud `RelicHandler` (toujours `unique_name_in_owner`, toujours résolu via `%RelicHandler` dans `run.gd`). **Itération 1 (Panel avec fond teal) rejetée par Julien** — "je veux juste les reliques affichées là, comme Slay the Spire" — `RelicBar` est maintenant un `Control` totalement transparent (`mouse_filter = 2` pour ne pas bloquer les clics sur la map en dessous), les icônes flottent directement sur le fond du jeu, sans panneau.
  - **Composition de la top bar restaurée à l'identique sauf reliques** (régression du 1er passage, corrigée) : retirer `RelicHandlerContainer` avait aussi supprimé `VSeparator3` et l'unique enfant à `size_flags_horizontal = 3` (Expand) de `TopBar/BarItems` — sans lui, `DiceShop`/`DeckButton` n'étaient plus poussés à l'extrémité droite de l'écran (ils suivaient directement les dés). Fix : `VSeparator3` restauré, remplacé l'ancien `RelicHandlerContainer` par un nouveau `ExpandSpacer` (`Control` invisible, `size_flags_horizontal = 3`) qui reprend exactement son rôle de poussée. **Piège à retenir** : dans une `HBoxContainer` ancrée pleine largeur, retirer le SEUL enfant en `Expand` sans le remplacer casse silencieusement l'alignement de tout ce qui vient après lui — jamais juste "supprimer" un enfant Expand, toujours vérifier s'il jouait aussi un rôle structurel.
  - **Nouveau `FloorLabel`** (`unique_name_in_owner`, même `LabelSettings` que `HealthLabel` pour rester visuellement unifié) inséré juste après `VSeparator3`/les dés, avant le spacer. `run.gd::_update_floor_label()` l'affiche via `"Floor: %d" % min(map.floors_climbed + 1, MapGenerator.FLOORS)` (`floors_climbed` est 0 avant le premier choix, donc +1 donne bien le numéro de l'étage courant/en cours de sélection ; clampé à `MapGenerator.FLOORS` pour éviter un "Floor 16" cosmétique juste après avoir choisi le boss). Appelé dans `_start_run()`, `_enter_act_2()`, et `_on_map_exited()` (donc à chaque sélection de salle) — se réinitialise proprement à l'entrée de l'act 2 puisque `map.floors_climbed` repart de 0 avec la nouvelle map.
  - **Bug trouvé via le Remote Scene Tree de Godot : `Map/MapBackground` restait affiché derrière TOUS les écrans du jeu depuis toujours (2026-07-07)**. Signalé par Julien comme une "ligne dorée bizarre" qu'il pensait liée au déplacement des reliques — **aucun rapport en réalité**, coïncidence de timing pure. Root cause diagnostiquée en live par Julien via l'onglet Remote de l'éditeur (technique : cocher/décocher `Visible` sur un nœud pendant que le jeu tourne pour voir l'effet en direct — fonctionne même en fenêtre séparée, contrairement au surlignage de sélection qui ne marche qu'en jeu intégré à l'éditeur) : `MapBackground` est un `CanvasLayer` (`layer = -1`) enfant de `Map`, jamais référencé dans `map.gd`. `Map.hide_map()` faisait `hide()` sur `Map` lui-même (un `Node2D`) — **un `CanvasLayer` enfant n'hérite PAS de la visibilité de son parent `Node2D`/`Control`**, contrairement à un enfant normal (`map_legend`, lui aussi un `CanvasLayer`, était déjà explicitement caché à la ligne d'après — seul `map_background` avait été oublié). Résultat : le fond parchemin restait rendu en permanence à `layer = -1` derrière battle/shop/campfire/event depuis le tout début, invisible tant que l'écran du dessus couvrait 100% du viewport, mais visible dès qu'un fond de combat laissait le moindre interstice. **Fix** : `map.gd` référence maintenant `map_background` et l'ajoute explicitement à `hide_map()`/`show_map()`, au même endroit que `map_legend`. **Piège générique à retenir pour tout futur `CanvasLayer` imbriqué dans un nœud 2D/Control qu'on cache/montre** : ne jamais supposer qu'un `hide()`/`visible = false` sur le parent suffit — un `CanvasLayer` doit toujours être caché/montré explicitement, quel que soit son parent.
  - ⚠️ **Second liseré résiduel, plus petit, PAS ENCORE RÉSOLU (parqué par Julien pour plus tard)** : après le fix `MapBackground` ci-dessus, une toute petite bordure persiste sur le bord gauche de l'écran, y compris en plein écran à 1920×1080 (donc PAS un problème de pillarboxing/aspect ratio — `window/stretch/mode = "canvas_items"` sans `aspect` explicite a été écarté comme cause puisqu'il n'y a aucun mismatch d'aspect à cette résolution). Piste non vérifiée avant l'arrêt de la session : `scenes/battle/battle.tscn::Background` est un `Sprite2D` (`centered = false`, `position = Vector2(0, 1)`, pas de `scale`) — donc affiché à sa taille native, potentiellement pas parfaitement callé sur les bords du viewport contrairement à un `TextureRect` ancré. Prochaine étape suggérée : même technique Remote+`Visible` toggle sur `Battle/Background` pour confirmer si le liseré vient de là ou de plus profond (couleur de fond par défaut du viewport, ou `map_background` pas réellement caché sur CE chemin de code précis). Non prioritaire, cosmétique mineur.

- **Hover "flash" des reliques rogné par le clip de pagination, corrigé (2026-07-07)** : `mouse_entered` déclenche `flash()` (scale 1→1.2→1 sur `Icon`, `relic_ui.gd`) — la rangée `Relics`/`RelicsControl`/`RelicHandler` étant calée pile sur la taille de l'icône (64px), ce grossissement de 20% débordait dans les 6-7px que `RelicsControl.clip_contents = true` (nécessaire pour masquer les reliques hors de la page de scroll visible) coupait net en haut/bas. Fix : les 3 conteneurs passés à 80px (au lieu de 64), et `RelicHandler._resize_for_top_bar()` force `size_flags_vertical = SHRINK_CENTER` sur chaque `RelicUI` créé (sinon le `HBoxContainer` les étire par défaut pour remplir toute la nouvelle hauteur, écrasant l'icône) — l'icône reste à sa taille native de 64px, centrée, avec de la marge de chaque côté pour respirer pendant l'animation.
  - **Marge à gauche ajoutée** (`RelicBar.offset_left` 16→28) à la demande de Julien ("comme STS2 laisse un peu d'espace") — la rangée ne touche plus le bord gauche de l'écran.
  - **Dice Bag (relique de départ) rognée/petite corrigée (2026-07-07)** : son asset `dicebagv3.png` est en format portrait avec beaucoup de marge sombre peinte en haut/bas (contrairement aux autres reliques, plus carrées/cadrées serré) — `stretch_mode = 5` (KEEP_ASPECT_CENTERED, "fit inside", letterboxe l'image entière y compris ses marges) la faisait paraître minuscule dans sa boîte. Fix dans `relic_ui.tscn::Icon` : `stretch_mode` passé à **6** (KEEP_ASPECT_COVERED, "cover" façon `object-fit: cover` CSS — zoome pour remplir la boîte, rogne le surplus). **Safe pour toutes les reliques déjà carrées** (aucun changement visuel si l'image correspond déjà au ratio de la boîte) — contrairement au fix de taille de boîte plus haut, celui-ci ne touche PAS les dimensions de la boîte donc n'affecte pas le rendu déjà peaufiné de la boutique, seulement la façon dont chaque texture est recadrée à l'intérieur.
  - **Icônes de relique agrandies SANS toucher au fichier partagé `relic_ui.tscn`** : son `Icon` (`TextureRect`) a un bug/quirk préexistant où le rectangle réel de l'icône (32×32) est plus petit que la boîte cliquable du `RelicUI` (56×56) — corriger ça directement dans `relic_ui.tscn` aurait aussi fait grossir les reliques de la boutique (`shop_relic.tscn` scale ce même nœud ×3.2, taillé précisément pour ce ratio 32/56 ; le casser aurait fait déborder l'ombre/le cadre déjà peaufinés là-bas). **Fix scopé à `RelicHandler.add_relic()`** (`scenes/relic_handler/relic_handler.gd::_resize_for_top_bar()`) : chaque `RelicUI` instancié par le handler (donc seulement ceux de la top bar) est retaillé en code juste après `instantiate()` — boîte + icône interne passées à 64×64 (l'icône remplit maintenant toute la boîte, comblant l'espace mort). `relic_handler.tscn` a aussi eu ses hauteurs (`Relics`/`RelicsControl`/`RelicHandler` racine) bumpées à 64 et son `separation` réduit de 6 à 3 pour un rendu dense façon STS2 — ce fichier n'est utilisé QUE par `run.tscn`, donc sans risque pour la boutique. **Pattern à retenir pour toute future ressource `.tscn` réutilisée dans plusieurs contextes avec des besoins visuels différents** : ne jamais modifier le fichier partagé lui-même si un seul consommateur a besoin d'un rendu différent — retailler par code au point d'instanciation spécifique à ce consommateur.

- **Expansion de profondeur du pool de cartes (2026-07-06, troisième partie de session) — voir [card_pool_expansion_2026-07.md](card_pool_expansion_2026-07.md) pour l'analyse complète et les designs.** Sur brief explicite de Julien (profondeur > quantité, récompenser COMMENT le power a été généré, pas de cartes multiplicateurs) : notation systématique de chaque mécanique (générateurs/enablers/payoffs/finishers 1-10) → 5 vrais trous identifiés (contenu de la chaîne de rolls, throughput par tour, pilier Red quasi inconstructible, face 0 du dé Evil sans aucune interaction, zéro tech anti-block) → **8 nouvelles cartes implémentées et ajoutées au pool (60→68)** : Resonance (paire dans `roll_history` → pas de reset), Crescendo (dégâts = `power_generated_this_turn`, Exhaust), All In (finisher Red : dépense tous les dés Red restants), Adrenaline (Charge 2 Red — premier générateur Red du jeu), From Nothing (Celestial : roll de 0 → 12 Power), Breach (Min 9 : retire le Block avant les dégâts), Foresight (Blessing : Scout → Charge 1), Perpetual Motion (Blessing : première panne de dés du tour → Refuel 2). Zéro nouveau système — tout sur la plomberie existante (`roll_history`, `power_generated_this_turn`, `no_reset`, `scout_effect`, `dice_rolled`). Pas encore de versions "+" (specs Julien à venir). **+ 10 designs de reliques et 6 designs de dés dans le doc, NON implémentés, en attente de sélection par Julien.** Deux questions tranchées par Julien dans la foulée : Bullseye dans le starter deck = insert de test, laisser tel quel ; les 4 Blessings hors pool (Precision Engine, Counterweight, Guard Stance, Lucky Sevens) = coupe intentionnelle ("not a fan"), ne pas les remettre. **Batch 2 le même jour (validation enthousiaste de Julien) : 10 cartes "rare-feel" supplémentaires (pool 68→78)** — Bulwark (dégâts = Block, archétype block-payoff né), Cascade (suite de 3 valeurs consécutives → total de la chaîne en AoE), Jackpot (triple → 30, machine à sous Evil), Rigged (Celestial, Lucky 2), Dark Pact (Blessing : 0 → +2 Strength permanent), Stockpile (Blessing : garde jusqu'à 5 Power entre les tours, via `starting_power_next_turn` existant), Transmutation (Celestial : convertit tous les dés vers le type actif), Momentum (4 + 4/carte jouée ce tour — dégâts indépendants du Power), Second Wind (soin = moitié du Power, seul heal in-combat du pool), Executioner (kill → Power conservé, exécutions en chaîne). Détail + watch-list de balance dans le doc §2b.

- **Act 2 placeholder ajouté (2026-07-06, seconde partie de session) — voir section dédiée "Act 2 (placeholder)" plus bas pour le détail.** Après le boss d'act 1, le GGPanel de l'écran de récompense devient "Act 1 Complete!" avec un bouton "Continue to Act 2" (le bouton ne fait que fermer le panneau — la vraie transition est armée dans `run.gd::_on_battle_won` et se déclenche au prochain retour à la map, pour ne pas sauter les récompenses du boss). Transition = full heal (décision Julien : "act = campfire gratuite") + nouvelle map (même générateur, 15 étages) + pool d'events rechargé + `Global.current_act = 2`. **Aucun nouveau fichier `.tres`/`.tscn` de contenu** : l'act 2 recycle les combats d'act 1 (tier 0 ← pool tier 1, tiers 1-2 ← pool tier 2, élites/boss ← eux-mêmes, Leviathan re-sert de boss final scalé — décision Julien) et les scale à l'instanciation dans `battle.gd::_apply_act2_scaling()` (multiplicateur de HP + Muscle de départ, tout le tuning dans 2 tables const au même endroit). Or de combat ×1.5 en act 2. `Map.generate_new_map()` sait maintenant se ré-exécuter proprement (nettoyage des nœuds/lookups de l'ancienne map — c'était le seul vrai piège technique). **Premier playtest de Julien OK** (transition, heal, map, scaling fonctionnels) + 3 retours traités dans la foulée : barres de HP ennemies partiellement remplies (bug d'ordre value/max_value dans `stats_ui.gd`, latent depuis toujours), Muscle off-by-one (placeholder `CrabEnemy` de `battle.tscn` encore présent pendant la frame de scaling, `queue_free` différé), bannière "ACT 2: THE CATACOMBS" à l'arrivée sur la map (nouveau `act_banner.tscn` façon TurnBanner) — détails dans la section dédiée. Les 2 `weight = 10.0` reliquats (`tier_0_bigger_satyrs_2`, `tier_1_lurker_crab`) remis à 1.0 sur validation de Julien.

- **Session du 2026-07-06 : contenu complet des ~65 cartes upgrade + 2 bugs UI trouvés et corrigés — voir section dédiée sous "Cartes" pour le détail complet.** Toutes les cartes draftable (2-60) et les 5 cartes starter (Strike/Block/Low Blow/Reinforce/Recombobulate, #61-65) ont maintenant une version "+" réelle (script + `.tres` + `upgraded_version` câblé), en plus de Bullseye (#1, fait la session précédente) — le pool des ~60 cartes annoncé au tout début de la fonctionnalité est donc désormais entièrement rempli. Détails clés : carte "Scout 5" créée (le panel de scout supportait déjà 5 dés, juste jamais utilisé), 3 statuts dupliqués avec `class_name` propre pour Marionette+/Steady Hand+/Critical Edge+ (leur effet vit dans le statut, pas la carte), notation "X+3" introduite pour les upgrades à bonus additif plat. **Deux bugs UI réels trouvés en testant l'upgrade sur des cartes Celestial/Blessing, tous deux corrigés** : (1) `CardMenuUI.set_card()` ne réinitialisait jamais le style Celestial/Blessing d'un nœud réutilisé (le panel de confirmation avant/après réutilise ses 2 nœuds à chaque clic, contrairement aux grilles boutique/récompense qui recréent une instance à chaque fois) — une carte normale prévisualisée après une carte Celestial restait bleue ; (2) le fix initial de (1) utilisait `remove_theme_*_override()` en pensant révéler un style "par défaut" du `.tscn`, mais un `theme_override_styles/panel` posé dans le `.tscn` **est** le même stockage que `add_theme_stylebox_override()` en runtime — `remove_*` l'efface purement et simplement, cassant l'apparence de TOUTES les cartes normales (banner/description tombant au thème générique). Fix définitif : ré-appliquer explicitement les valeurs "normales" via des constantes dédiées plutôt que `remove_*_override()`. **Leçon générale à garder pour tout futur reset de style sur un nœud réutilisé** : ne jamais supposer qu'un `.tscn` a une couche "valeur par défaut" séparée du système d'override runtime — il n'y en a pas, il faut toujours réappliquer explicitement.

- **Système d'upgrade de carte à la campfire (façon Slay the Spire), plomberie complète ajoutée le 2026-07-05 — voir section dédiée sous "Cartes" pour le détail.** Julien a explicitement demandé la mécanique seule, PAS le contenu (les ~60 versions "+" des cartes draftable restent à créer, aucune n'existe encore). Testé en jeu par Julien avec une carte temporaire (Strike → "Strike+ (TEST)"), preview/confirm/cancel tous confirmés fonctionnels, puis nettoyé.
- **Session du 2026-07-04, essentiellement UI/polish + tweaks de cartes (pas de travail d'équilibrage entamé)** : renommage Rite→Blessing + recoloration prune `#482D4B` (voir section Cartes) ; renommage statut Blessed→Infused + bug de signal copié-collé corrigé (Chaos/Energized/Depleted/Emanation) ; passe de lisibilité sur la carte (dimming des lignes, anneau "disponible" tenté puis retiré, légende réduite — voir section Map) ; restylage top bar (bordure+ombre, badge "!" rouge→vert, police gold/HP unifiée) et shop (boutons partagés, reliques agrandies) ; polish flèches mech (hover/punch/disabled/teinte) ; pool de cartes : Nova/Slice retirées, Gang Up/Catalyst/Fortify/Dynamite simplifiées (plus de bonus_requirement) ; dimming des cartes injouables à deux niveaux (0.6 avec power / 0.75 sans power, Celestial toujours épargné) ; tutoriel Blessing ajouté sur l'écran de récompense (carte entière comme illustration, pas un artwork statique) ; **piège cross-worktree découvert** (une background task voit un worktree isolé au dernier commit, pas les modifs non-committées de la session — voir mémoire `feedback_background_task_worktree_blindspot`). Détails complets de chaque point dans leurs sections respectives plus bas.
- **✅ ÉQUILIBRAGE DES COMBATS — les 5 étapes du plan sont FAITES (session du 2026-07-04, suite du même jour) → voir [balance_analysis_2026-07.md](balance_analysis_2026-07.md) pour le détail complet, ce fichier ne résume que les points structurants.** Ordre suivi : (1) bug pass (gold 7575→75, kit Goblin réparé, poids Vortex, condition Oculus, spaghetti Dragonpriest supprimé, flags Hound/Gargantua réinitialisés par combat) ; (2) restructure en 3 tiers coupés au trésor de l'étage 8 (tier0 fl.1-3, tier1 fl.4-8, tier2 fl.9-13, `run.gd::_get_tier_for_room`) — pool passé de 25 à 34 fights sans créer aucun nouveau fichier, juste relabel + ajout au pool ; (3) micro-bumps tier 0 (Satyr/Octopus 8, Bigger 18, Crab 26 + spike telegraphié tous les 4 tours via `Global.fight_turn % 4 == 3`, Plant 32 + ramp adouci, Reinforce +1→+2) ; (4) checkpoint playtest Julien (voir bugs ci-dessous) ; (5) chiffres tier 2 (+30% sur les comps, calcul uniforme par membre pour éviter les conflits inter-fights), élites 85-95 HP (Canalize/Absorb intouchés, refusés en standing-rule/cap), boss Leviathan 140 HP + valve Ink 2→3 tours.
  - **⚠️ Décision de design à retenir pour toute future composition de pool** : ne jamais mettre un ennemi solo ET "ce même ennemi + un partenaire" dans le MÊME tier (trouvé sur Lurker solo + Lurker+Crab tous deux en tier 1 — Julien : *"the same tier had a fight that's literally the same, but harder! This is not good balance"*). Lurker+Crab déplacé en tier 2. Toute paire doit avoir sa propre raison d'exister (dynamique de composition, voir §4.5 du doc), pas juste être "le mode difficile" du solo.
  - **Chaque ennemi partagé entre plusieurs tiers (Plant, Crab, Machopeur, Satyr/Bigger Satyr, Octopus/Bigger Octopus, Defender, Lurker) a maintenant une ressource `EnemyStats` dédiée par tier** (`<enemy>_tier1.tres` / `_tier2.tres` à côté du fichier "maison" du tier le plus bas) — plus aucun risque qu'une modif de HP sur un tier fasse bouger silencieusement un autre tier. Convention à suivre pour tout futur ennemi réutilisé entre tiers.
  - **Deux bugs systémiques trouvés en playtest, corrigés le même jour, qui touchaient TOUS les ennemis (pas juste ceux testés)** : (a) l'intent d'un ennemi pouvait diverger de ce qu'il jouait réellement — un reset de block juste avant l'exécution redéclenchait le choix d'action avec le `Global.fight_turn` déjà incrémenté par le tour suivant (fix : `stats_changed` rebranché sur `update_intent` au lieu de `update_action` dans `scenes/enemy/enemy.gd`) ; (b) 19 des ~39 scripts d'attaque ennemis appliquaient Strength/Muscle au texte d'intent affiché mais PAS aux dégâts réellement infligés (`damage_effect.amount = damage` au lieu de `modifiers.get_modified_value(...)`) — tous corrigés.
  - Autres fixes playtest du même jour : `Global.ink_active` ne se réinitialisait jamais entre combats (pouvait rester coincé pour tout le run) — reset ajouté dans `battle.gd::start_battle()` ; dimming Ink pendant qu'on est encré changé en un vrai état neutre (`CardUI.PlayableGlow.NEUTRAL`, aucun signal visuel plutôt que le dimming "injouable" trompeur) ; espacement des icônes de statut standardisé (`status_ui.gd` donnait une taille différente selon qu'un badge de durée/stacks était affiché ou non) ; 6 tooltips de reliques recasés en majuscules pour matcher les cartes (Blood Sword, Fuel-o-meter, Runic Bones, Sledgehammer, Volcanic Rock, Crown) ; Lurker redimensionné dans Lurker+Crab (débordait sur la main, pas de `scale`/`width`/`height` alors que Crab dans la même scène en avait).
  - Nouvelle section **"Patterns de décision joueur en combat"** ajoutée juste avant Autoloads : capture des enchaînements concrets observés en playtest (Scout+Mech, Unlucky comme enabler Low Roll, Lucky+Giant pour du burst garanti, switch de dé en cours de tour, Recombobulate comme soupape anti-malchance) — utile pour juger si une future carte/mécanique crée une synergie réelle avec l'existant.

- **Coloration des mots-clés (le plus gros morceau, voir section dédiée sous "Cartes")** : `custom_resources/keyword_colorizer.gd` (classe `KeywordColorizer`) est la source unique partagée entre `Card.get_colorized_description()` et `Relic.get_colorized_description()`. Une seule couleur or pour tous les mots-clés (sauf types de dés, qui gardent leur propre couleur et n'ont PAS besoin d'être tagués). **Comportement de groupement affiné en fin de session** : si un mot-clé générique (Charge, Boost...) est directement suivi d'une mention de type de dé, toute la phrase ("Charge 2 Magma Dice") prend la couleur du dé plutôt que d'être coupée en deux couleurs — voir le detail (regex en 3 passes) dans la section dédiée, c'est le point le plus fragile du fichier si on veut retoucher le groupement à nouveau.
- **Conversion Description Label → RichTextLabel** (`card_ui.tscn`/`card_menu_ui.tscn`, y compris `BonusEffectLabel`) pour supporter le BBCode nécessaire à la coloration — a cassé pas mal de choses au passage (alignement, police/contour/ombre, un bug de survol "saccadé"), tout corrigé. **Piège à surveiller si un futur Label de carte/UI est converti en RichTextLabel** : son `mouse_filter` par défaut est **STOP** (Label est **IGNORE**) — sans repasser à `mouse_filter = 2` (+ `scroll_active = false`), le nœud avale les événements souris et tout ce qui est dessous devient saccadé au survol/drag.
- **Système de particules "power orbs"** (`dice.gd::_spawn_power_orbs()`) : des orbes volent du dé vers le nombre de Power à chaque roll (courbe de Bézier bombée, pas une ligne droite), colorés par type de dé, count/taille/luminosité montent avec le roll. Le nombre de Power reste mis à jour **immédiatement** au roll (`_apply_roll_result`) — retarder l'affichage a été essayé puis rejeté par Julien — mais une réaction visuelle secondaire plus petite (`_play_power_orb_arrival_reaction`) se déclenche en plus, synchronisée sur l'arrivée du premier orbe.
- **Highlight du bouton End Turn** (`battle_ui.gd::_update_end_turn_highlight`, pulse or lent) quand il ne reste plus aucune action possible ce tour : 0 dé restant (tous types), 0 power banké, ET aucune carte Celestial en main.
- **Refonte des hit-stops** (voir section dédiée sous "Power, dés..." pour les chiffres) : `Shaker.hit_stop()` est maintenant **reference-counted** (piège corrigé : deux appels concurrents se resettaient indépendamment, le plus court coupait le plus long en plein milieu — corrigeait aussi un bug latent sur les cartes AoE multi-cibles) ; hit-stop sur dégâts re-scalé pour que les gros coups (12+ tôt en run) se sentent nettement plus gros ; nouveau hit-stop sur les cartes support/power (scalé par le delta de power, ex. Reinforce vs Blaze) ; nouveau bonus hit-stop flat (0.16s) sur `card.requirement == EXACT` réussi, câblé dans `Card.play()`.
- **Pattern de bug "tooltip qui reste affiché pour toujours" trouvé et corrigé 2 fois ce sessions** (`scenes/ui/intent_ui.gd` et `scenes/relic_handler/relic_ui.gd` + `scenes/battle_reward/battle_reward.gd`) : les tooltips sont ajoutés sous `get_tree().root` (pas sous le node qui les affiche), donc rien ne les libère automatiquement si (a) un nouveau hover écrase la variable `tooltip_instance` sans libérer l'ancienne d'abord, ou (b) la scène qui les a fait apparaître est détruite pendant qu'on survole encore (le nœud est libéré avant que `mouse_exited` ait une chance de se déclencher, et une coroutine `await` en cours se fait annuler silencieusement avec lui — donc un éventuel timeout de sécurité en cours ne se déclenche jamais). **Fix générique à appliquer à TOUT futur système de tooltip similaire** : (1) toujours libérer l'instance existante en tout début de `_on_mouse_entered`, avant d'en créer une nouvelle, et (2) ajouter un `_exit_tree()` sur le nœud qui possède le tooltip pour le nettoyer explicitement quand ce nœud quitte l'arbre, en plus d'un timeout de secours (8s / 6s selon le fichier).
- **`battle_reward.gd` n'avait jamais eu les tooltips secondaires de mots-clés** (ex. "Scout" à côté de la description d'une relique en récompense) contrairement à `relic_ui.gd` qui les a — pas une régression, un gap qui existait depuis toujours sur cet écran spécifique, comblé cette session (même pattern que `relic_ui.gd`, dupliqué faute de classe de base commune entre les deux).
- **Nom d'ennemi au survol** (`Enemy._on_mouse_entered`, nouveau `NameLabel` sous `StatsUI`) : nouveau champ `EnemyStats.enemy_name` (optionnel, override le nom affiché). Le fallback "dérive le nom depuis le nom de fichier .tres" dans `_compute_display_name()` **ne fonctionne pas de façon fiable en pratique** (tous les ennemis affichaient juste "Enemy", suggérant que `resource_path` n'est pas accessible de façon fiable au moment où `set_enemy_stats()` tourne, malgré that l'ExtResource semble correctement référencé dans les scènes de combat — cause exacte non identifiée) → **`enemy_name` est maintenant rempli explicitement sur les 21 fichiers `.tres` d'ennemis** (19 dossiers + `bigger_octopus`/`bigger_satyr`), le fallback par nom de fichier reste dans le code par sécurité mais ne pas compter dessus pour un futur ennemi sans lui donner `enemy_name` explicitement. `crab_enemy.tres` a `enemy_name = "Skeleton"` (nom de design confirmé, pas le nom de fichier).
- **Campfire (`scenes/campfire/`)** : titre + bouton Rest restylés (même pattern gold-border/dark-teal que les boutons Buy du shop), tooltip du Rest maintenant instantané (le délai de 0.5s a été retiré des deux handlers de survol).
- **Dice shop (`scenes/shop/dice_shop.tscn`)** : tentative de restylage visuel large, **en grande partie annulée** à la demande de Julien (préfère l'ancien look pour le panel/les headers/la police) — seuls les styles normal/hover/pressed des boutons Buy/Reroll/Exit (qui n'avaient auparavant AUCUN style custom, juste le thème Godot par défaut) ont été gardés.
- **Card shop (`scenes/shop/card_shop.tscn`, `shop_card.tscn`, `shop_relic.tscn`) restylé le 2026-07-04** : les boutons Buy (cartes + reliques) et le bouton Leave n'avaient eux non plus AUCUN style custom (juste le thème Godot par défaut, contrairement à ce qu'on pourrait croire en les voyant à l'écran — ils empruntaient tous le thème global du projet) → reprennent maintenant exactement le style teal/bordure or déjà utilisé pour Buy/Reroll/Exit du dice shop, extrait dans 3 fichiers `.tres` partagés (`scenes/shop/shop_button_normal.tres`/`_hover.tres`/`_pressed.tres`) réutilisés dans les 3 scènes plutôt que dupliqués en `sub_resource` locaux. **Reliques agrandies** (`shop_relic.tscn` : `scale` du root passé de `2` à `2.6`, `custom_minimum_size` du `ShopRelic` ajusté en conséquence, séparation de la rangée `Relics` dans `card_shop.tscn` passée de 10 à 40 pour éviter le chevauchement) + **ombre de contact "posée sur la table"** ajoutée (nouveau nœud `Shadow` = `Panel` avec un `StyleBoxFlat` noir semi-transparent en forme de pilule, ancré sous l'icône). ⚠️ Piège rencontré : `RelicContainer` (le `CenterContainer` que `shop_relic.gd` référence via `@onready $RelicContainer` et dans lequel il fait `add_child()`/`get_children()` à chaque `set_relic()`) a dû être déplacé d'un niveau (`$RelicVisual/RelicContainer`) pour pouvoir empiler `Shadow` et `RelicContainer` l'un derrière l'autre dans un wrapper commun — **le chemin `@onready` dans `shop_relic.gd` a été mis à jour en conséquence**, sinon la relique ne s'affiche plus du tout (le script ajoutait l'instance au mauvais endroit).
- **Dice tooltips unifiés** (`tooltip.gd` du hover carte/relique + `dice_tooltip.gd` du shop) sur un seul texte partagé (`KeywordColorizer.DICE_TOOLTIP_TEXT`, format "Faces: X, Y, Z" — pas de phrase complète, préférence explicite de Julien), et ajout des 5 types de dés qui n'avaient encore jamais eu de tooltip nulle part (Blue/Red/Odd/Even/Mech — seuls Green/Magma/Giant/Evil en avaient un avant).
- **Petite amélioration de l'animation d'ouverture du trésor** (`treasure.tscn`, squash d'anticipation + pop d'échelle ajoutés à l'animation "open" existante, aucun nouveau nœud/script).
- **Piège à connaître avant de toucher à l'UI de carte** (toujours vrai) : il y a deux implémentations dupliquées (`card_ui.gd` et `card_menu_ui.gd`) sans classe de base commune — toute modif visuelle doit être faite dans les deux, y compris leur logique de tooltip au survol (`_on_card_frame_mouse_entered`), qui est elle aussi dupliquée mot pour mot entre les deux fichiers.
- **Découverte importante (session du 2026-07-02, toujours vraie)** : `card.requirement` (l'enum MIN/MAX/EXACT/etc.) était **purement cosmétique** avant cette session-là — uniquement utilisé pour le badge visuel et le tooltip, jamais vérifié pour bloquer le jeu d'une carte. `CharacterStats.can_play_card()` est un stub qui renvoie toujours `true`. Chaque carte gère elle-même sa condition via un `if` dans `apply_effects()`. `Card.meets_requirement()` est la première utilisation réelle de cet enum pour de la logique (dynamic description ET maintenant le bonus hit-stop EXACT), pas pour bloquer le jeu de la carte.
- **Décisions encore ouvertes** (héritées de sessions précédentes, voir section dédiée plus bas) : icône visuelle Blessing (en attente d'asset de Julien), 1-2 cartes "Keep" à couper jamais précisées, texture des 8 dés non-Blue.
- **Note technique** : les `.tres` créés par Claude ont des `uid` inventés (pas générés par l'éditeur Godot) ; Godot les corrige/retire automatiquement à l'ouverture du projet (comportement normal, pas un bug à corriger).
- **`Card.Type.RITE` renommé en `BLESSING` (2026-07-04), décision tranchée** : le nom "Rite"/"Glyph" est abandonné, remplacé par "Blessing" — voir section dédiée sous "Cartes" pour le détail (bannière/frame/description repensées avec une palette dédiée, pas juste la bannière comme avant ; **couleur finale = prune sombre `#482D4B`** après plusieurs itérations, voir palette exacte dans la section dédiée). **Le statut "Blessed" a aussi été renommé en "Infused"** (même session, changement de vocabulaire séparé du renommage de carte) — `statuses/blessed.gd/.tres` → `statuses/infused.gd/.tres`, classe `BlessedStatus` → `InfusedStatus`, signal `Events.check_blessed_status` → `check_infused_status`, tag/description "Blessed" → "Infused" partout (Gang Up, Rainbow). **Bug corrigé en cours de route** : `statuses/energized.gd`, `depleted.gd`, `chaos.gd` et `status_emanation.gd` se connectaient TOUS à ce même signal au lieu de leur propre signal dédié — copié-collé de Blessed sans renommer, même pattern que les bugs Goblin/Oculus déjà documentés plus bas. Fix appliqué : `chaos.gd` se connecte maintenant à son vrai signal dédié `Events.check_chaos_status` (déjà déclaré/émis dans `dice.gd`, jamais connecté avant) ; `energized.gd`/`depleted.gd`/`status_emanation.gd` ont eu leur `initialize_status()`/`consume_stack()` supprimés entièrement (code mort — leur vrai effet et cycle de vie passent déjà par `Status.Type` START_OF_TURN, pas par ce signal). **⚠️ Piège cross-worktree rencontré en corrigeant ce bug** : une tâche en arrière-plan lancée via `spawn_task` tourne dans un worktree git isolé (`.claude/worktrees/<nom>`) qui ne voit PAS les modifications non-committées de la session qui l'a lancée — elle avait initialement conclu à tort que ce renommage n'avait jamais été appliqué (elle regardait son propre worktree, resté au dernier commit, pas le répertoire de travail réel). Toujours vérifier dans quel checkout un rapport de background task a été produit avant de le croire sur parole s'il contredit un travail non-committé de la session en cours.

## Structure des dossiers

```
characters/warrior/cards/   - ~219 fichiers de cartes (.gd + .tres)
enemies/<type>/             - 1 dossier par type d'ennemi (19+), chacun avec .tres + ai .tscn + actions .gd
dices/                      - ressources .tres des dés (DiceData)
statuses/                   - statuts, paires .gd (logique) + .tres (data)
relics/                     - reliques, paires .gd (logique) + .tres (data)
scenes/events/              - events narratifs : .gd + .tres (EventStats) + .tscn
custom_resources/           - classes de base : Card, Status, Relic, DiceData, EnemyStats, Effect, EventStats...
effects/                    - Effect concrets : DamageEffect, StatusEffect, BlockEffect, SupportEffect
global/                     - autoloads : events.gd (signaux), shaker.gd, music/sfx players
global.gd                   - autoload Global, state machine du run (pas dans global/, à la racine)
scenes/battle/               - scène de combat principale
scenes/dices/                - UI + logique des dés (dice.gd, dice_interface.tscn)
scenes/card_ui/               - UI carte en main
scenes/modifier_handler/      - système Modifier/ModifierValue (dmg dealt/taken, card cost...)
scenes/status_handler/        - StatusHandler attaché à Player/Enemy
scenes/relic_handler/         - RelicUI, affichage + cycle de vie des relics
scenes/map/, shop/, campfire/, treasure/, battle_reward/, run/ - scènes méta-run
```

## Conventions de nommage

- Fichiers `.gd` et dossiers : `snake_case` (`weak.gd`, `hunting_bow.gd`, `enemies/temple_defender/`)
- `class_name` : `PascalCase` (`Card`, `WeakStatus`, `BowRelic`, `EnemyStats`)
- Ressources `.tres` : généralement `snake_case.tres`, mais **inconsistant** pour les cartes — certaines préfixées `card_xxx.tres`, d'autres juste `xxx.tres`. Ne pas supposer un préfixe systématique, vérifier au cas par cas.
- Attribut `id` interne : toujours `snake_case`, sert de clé logique indépendante du nom de fichier.
- Ennemis : dossier `enemies/<nom>/`, fichiers `<nom>_enemy.tres`, `<nom>_enemy_ai.tscn`, `<nom>_attack_action.gd` etc.
- Events : `event_<nom>.gd/.tres/.tscn`. Pools d'events pondérés : `events_<pool>.tres`.
- Variables globales dans `global.gd` : `snake_case`, booléens préfixés `is_/has_/can_`. Chaque type de dé a son triplet `<type>_dice_current_amount` / `_max_amount` / `_bonus_amount`.

## Cartes (`Card`, `custom_resources/card.gd`)

`extends Resource`. Une carte = un script `.gd extends Card` qui override `apply_effects(targets, modifiers)`, plus une ressource `.tres` qui porte les données (id, name, type, target, description, rarity, requirement, etc.) et référence le script.

Enums clés :
- `Type {ATTACK, SKILL, BLESSING}` — **renommé le 2026-06-24** (`POWER`→`RITE`) puis **de nouveau le 2026-07-04** (`RITE`→`BLESSING`, décision tranchée par Julien après plusieurs allers-retours sur "Rite"/"Enchantment"/"Glyph", voir section dédiée plus bas). "Blessing" = équivalent des Powers de Slay the Spire (effet permanent pour le reste du combat, généralement `exhausts = true`).
- `Target {SELF, SINGLE_ENEMY, ALL_ENEMIES, EVERYONE}`
- `Rarity {NORMAL, SUPPORT}`
- `Requirement {NONE, MIN, MAX, EVEN, ODD, RED, MULTIPLE, EXACT, PANDORA}` — condition de roll pour activer un bonus/effet de la carte. Il existe aussi `bonus_requirement` / `bonus_requirement_number` pour un second palier d'effet.

`play()` (dans `card.gd`) gère le routing tutoriel, les particules, et appelle `apply_effects()` avec les bonnes targets. Les cartes individuelles ne touchent presque jamais au targeting elles-mêmes.

Exemples de référence : `characters/warrior/cards/blaze.gd` (manipulation de Power simple + statut), `low_blow.gd` (archétype Low Roll), `berserker.gd` (archétype Red, maintenant type BLESSING), `calculations.gd` (archétype Exact/Scout), `recombobulate.gd` (mécanique de refuel/reset, voir section Power), `emanation.gd` (carte BLESSING, voir bug stacking ci-dessous).

### Différenciation visuelle des cartes Blessing (ajouté 2026-06-24, recoloré et étendu 2026-07-04)

Les cartes Blessing ont un tooltip auto-généré ("A lasting effect for the rest of the combat. Exhausts when played.") déclenché par `card.type == Card.Type.BLESSING` — **pas** par un tag, contrairement aux autres tooltips. C'est volontaire pour ne jamais dépendre d'un tag oublié.

**Recoloration complète du 2026-07-04** : la bannière violette d'origine (`card_banner_rite.tres`) a été remplacée par une palette dédiée (`card_banner_blessing.tres`, `card_ui_blessing.tres` pour le frame, `card_ui_description_panel_blessing.tres` pour le panneau de description, `blessing_card_description_label.tres` pour l'outline du texte) — même traitement que les cartes Celestial (`can_play_without_dice`), pas juste la bannière comme avant. **Couleur retenue après plusieurs itérations avec Julien (bronze rejeté, puis vert/olive/indigo/marron écartés pour risque de collision avec des couleurs déjà prises) : prune sourd `#482D4B`** = `bg_color = Color(0.282353, 0.176471, 0.294118)`, avec le `border_color` universel `Color(0.788235, 0.635294, 0.152941)` (le même doré que toutes les autres cartes — contrairement à une itération intermédiaire qui utilisait un or plus pâle/distinct, abandonnée pour rester cohérente avec le pattern Celestial où seul le bg change). Raison du choix : ne collisionne avec aucune couleur déjà utilisée ailleurs sur la carte (le ruban Min, très présent sur les cartes Blessing actuelles, est un olive-or ; le vert entrait en collision avec le badge MAX et l'accent des dés Green ; le marron répétait la toute première tentative déjà rejetée). Couleur de texte de description inchangée (crème, comme partout ailleurs) — seul l'`outline_color` est assombri (~0.7× le bg, même ratio que Celestial) pour rester lisible sur le nouveau fond, exactement le même principe que Celestial (teinte différente du frame + outline assorti, texte lui-même jamais retouché). **Contrairement à Celestial, le badge de requirement (MIN/MAX/EXACT ribbon) n'est PAS re-stylé** : plusieurs cartes Blessing ont un vrai `requirement` (ex. Berserk = MIN 6, Counterweight = MULTIPLE 8) contrairement aux cartes Celestial qui sont presque toujours NONE — forcer un badge "NONE" dessus casserait ces cartes.

**Piège du même genre que le bug de bordure Celestial déjà documenté plus bas** : `card_frame`/`panel` reçoit maintenant un override de stylebox pour Blessing dans `_set_card()`/`set_card()` — il faut resynchroniser `_base_frame_stylebox`/`_hot_frame_stylebox` (dans `card_ui.gd`) sinon le même bug "la carte redevient marron au repos" réapparaît. Les 3 endroits qui appliquent un style de frame selon le type de carte (`_set_card`/`set_card`, `card_base_state.gd::on_mouse_entered/on_mouse_exited`, `card_dragging_state.gd::enter`) suivent tous le même ordre de précédence : Celestial (`can_play_without_dice`) gagne si une carte est À LA FOIS Blessing et Celestial (cas extrêmement rare/inexistant aujourd'hui) — mêmes fichiers, même piège de duplication documenté juste en dessous.

**⚠️ Piège architectural important : il existe DEUX implémentations parallèles et dupliquées de l'affichage de carte**, sans classe de base commune :
- `scenes/card_ui/card_ui.gd` — carte en main pendant le combat
- `scenes/ui/card_menu_ui.gd` — carte en boutique, récompense de combat (`card_rewards.gd` instancie `CardMenuUI`), vue du deck

**Toute modification visuelle d'une carte (nouveau style, nouveau tooltip, nouvelle condition d'affichage) doit être appliquée dans CES DEUX fichiers séparément**, sinon un des deux contextes (typiquement boutique/récompense) ne reflète pas le changement. Le style Celestial (`can_play_without_dice`) souffre du même pattern dupliqué — c'est la norme ici, pas l'exception. Si une 3e vue de carte apparaît un jour, vérifier si elle a sa propre copie aussi avant de supposer qu'un fix est complet.

**Depuis le 2026-07-03, le node `Description` dans `card_ui.tscn` ET `card_menu_ui.tscn` est un `RichTextLabel`** (converti depuis `Label` pour supporter le BBCode nécessaire à la coloration des mots-clés, voir section dédiée plus bas), tout comme `BonusEffectLabel` dans les deux fichiers. **Piège à ne pas réintroduire si un autre Label de carte est converti un jour** : `RichTextLabel` a `mouse_filter` par défaut à `STOP` (Label est `IGNORE`) — sans repasser explicitement à `mouse_filter = 2` (+ `scroll_active = false`), le nœud avale les événements souris et le survol de la carte devient saccadé. `Description` est en plus enveloppé dans un `CenterContainer` (`DescriptionCenter`, avec `Description.fit_content = true`) plutôt que du pur anchoring, pour retrouver le centrage vertical que `Label.vertical_alignment` fournissait gratuitement (RichTextLabel n'a pas d'équivalent direct) — donc le chemin `@onready` est maintenant `.../DescriptionPanel/DescriptionCenter/Description`, pas `.../DescriptionPanel/Description`.

**Tooltips de statut sur les cartes** : le système de tooltip au survol (`_on_card_frame_mouse_entered` dans les deux fichiers ci-dessus) lit `card.tags` (string séparée par virgules) et cherche une correspondance dans `scenes/ui/tooltip.gd::get_tooltip_content()`. **Si une carte accorde un statut (Infused, Strength, Exposed, etc.) via son script, il faut ajouter le nom de ce statut dans le champ `tags` de la `.tres`, sinon son tooltip explicatif n'apparaît jamais** — ce n'est pas déduit automatiquement de l'effet du script. Confirmé par Julien sur Gang Up (qui accorde Infused via script mais nécessitait `tags = "Charge, Infused"` pour que le tooltip Infused s'affiche).

### Système d'upgrade de carte à la campfire (façon Slay the Spire), ajouté 2026-07-05

Plomberie complète pour upgrader une carte de son deck à la campfire — **volontairement sans contenu** : Julien a demandé la mécanique seule d'abord, aucune des ~60 versions "+" des cartes draftable n'existe encore. Tant qu'aucune carte n'a de version upgradée assignée, le bouton Upgrade fonctionne mais cliquer sur une carte du deck ne fait rien (comportement attendu, pas un bug — voir plus bas).

- **`Card` (`custom_resources/card.gd`)** a deux nouveaux champs : `upgraded: bool` (à cocher sur la ressource "+") et `upgraded_version: Card` (référence **directe** vers la ressource `.tres` upgradée — pas de convention de nommage/chemin à deviner, juste assigner la ressource dans l'inspecteur Godot) + un helper `can_be_upgraded() -> bool` (`upgraded_version != null and not upgraded`). Une upgrade purement numérique (comme Strike+ dans Slay the Spire) peut réutiliser le même script avec juste les champs `.tres` modifiés ; une upgrade qui change le comportement a besoin de son propre `.gd`, comme n'importe quelle autre carte.
- **`Global.upgrading_card: bool`** (nouveau, miroir de `removing_card` existant) : `CardMenuUI._on_card_frame_gui_input()` (`scenes/ui/card_menu_ui.gd`) branche dessus — en mode upgrade, un clic sur une carte du deck émet `Events.card_upgrade_requested(card)` au lieu de jouer l'animation de suppression utilisée par le mode remove.
- **`CardPileView` (`scenes/ui/card_pile_view.gd`/`.tscn`, déjà l'écran utilisé pour retirer une carte à la campfire) gère tout le flow lui-même**, sans repasser par `run.gd` : elle écoute `card_upgrade_requested`, affiche un nouveau panneau `UpgradeConfirmPanel` (avant/après côte à côte, deux instances `CardMenuUI` en lecture seule via un nouveau `@export var interactive: bool` sur `CardMenuUI` pour empêcher un clic sur la preview de redéclencher le flow, + boutons Upgrade/Cancel) et effectue le swap directement via `card_pile.replace_card(old, new)` (elle a déjà une référence à la `CardPile` du joueur, assignée par `run.gd` au démarrage).
- **`CardPile.replace_card(old_card, new_card)`** (nouvelle méthode dans `custom_resources/card_pile.gd`) : remplace l'objet `Card` entier à son index dans le tableau plutôt que de muter ses champs en place — évite de contaminer la ressource `.tres` de base si elle est encore référencée ailleurs (ex. le pool draftable), puisque les ressources Godot chargées via `load()`/`preload()` peuvent être partagées/cachées entre plusieurs instances.
- **Campfire (`scenes/campfire/campfire.gd`/`.tscn`)** : le bouton "Upgrade" existait déjà comme stub caché ("Coming soon!", jamais connecté) — maintenant visible, stylé comme le bouton Rest (mêmes 3 styleboxes teal/or), avec tooltip au survol ("UPGRADE" ajouté à `tooltip.gd::get_tooltip_content()`), câblé sur un nouveau signal `Events.open_deck_view_for_upgrade` → géré dans `run.gd::_on_open_deck_view_for_upgrade()` (symétrique à `_on_open_deck_view()` existant pour le remove), qui met `Global.upgrading_card = true` et ouvre `%DeckView` avec le titre "Upgrade a Card".
- **Bug préexistant corrigé au passage** : le bouton Back / la touche Echap de `CardPileView` faisaient juste `hide()` sans jamais reset `Global.removing_card` — un flag resté bloqué à `true` après avoir quitté l'écran sans retirer de carte pouvait fausser silencieusement le comportement au clic sur n'importe quel autre écran de carte ouvert ensuite (shop, récompense). `CardPileView._on_back_pressed()` reset maintenant les deux flags (`removing_card` ET `upgrading_card`), pas seulement en cas de succès.
- **Testé en jeu par Julien avec une carte upgrade temporaire** (Strike → "Strike+ (TEST)" assignée sur `warrior_axe_attack1.tres`, le fichier réellement utilisé dans le starting deck) : preview avant/après OK, confirm (swap réel dans le deck) OK, cancel OK. Fichier de test et référence supprimés après validation, `warrior_axe_attack1.tres` revenu à son état d'origine.
- ⚠️ **Non résolu, pas un bug de code** : le positionnement du bouton Upgrade à la campfire reste visuellement un peu bancal. Julien pense que c'est surtout dû à l'absence d'un vrai visuel pour Rest à gauche (actuellement seul Upgrade a une illustration de carte dédiée à côté du bouton ; Rest utilise juste `heal.jpg`, une texture plus générique) plutôt qu'un problème de layout à corriger côté code — en attente d'un asset de sa part, ne pas retoucher le positionnement sans nouvelle info.
- **Pas encore décidé** : aucune limite n'existe sur le nombre d'upgrades par visite de campfire (contrairement à certains deckbuilders qui limitent à une action par campfire) — chaque confirm referme l'écran mais rien n'empêche de rouvrir Upgrade à volonté à la même campfire. Pas tranché, à demander à Julien si ça devient pertinent une fois du contenu réel en place.
- **Polish ajouté le 2026-07-05 (suite du même jour), après le premier test réussi** : cartes preview (BeforeCard/AfterCard) agrandies à `scale = Vector2(1.5, 1.5)` avec `pivot_offset = Vector2(70, 105)` (même convention que l'ExampleCard du tutoriel Blessing, juste à l'envers) pour un agrandissement symétrique ; flèche entre les deux dotée de son propre `LabelSettings` dédié (`font_size = 96`) plutôt que de réutiliser le `LabelSettings` partagé par le titre "Discard pile"/"Upgrade this card?" de la même scène (sinon les deux auraient grossi aussi) ; son "cling" de forge (`sounds/blacksmithsound.wav`, le même que Reinforce et l'ajustement ±1 des dés Mech) joué via `SFXPlayer.play()` dans `CardPileView._on_confirm_upgrade_pressed()` ; titre de la carte coloré en vert (`Color(0.36, 0.85, 0.36)`, style STS2) quand `card.upgraded == true`, ajouté dans **les deux implémentations dupliquées** (`card_ui.gd::_apply_title_color()` et `card_menu_ui.gd::_apply_title_color()`, même piège de duplication que d'habitude) — **duplique le `LabelSettings` partagé (`card_title.tres`) avant de toucher `font_color` plutôt que de le muter en place**, sinon ça teinterait en vert le titre de TOUTES les cartes du jeu (sub-resource partagé entre toutes les instances de `CardUI`/`CardMenuUI` tant que `resource_local_to_scene` n'est pas coché, même piège que documenté dans la mémoire `feedback_modulate_vs_shader_gotcha`).
- **⚠️ Bug structurel trouvé et corrigé le 2026-07-05, à respecter pour CHAQUE future carte "+" créée** : la toute première carte de contenu réel (Bullseye+, `bullseye_plus.tres`) a vu son lien `upgraded_version` sur `bullseye.tres` disparaître **deux fois de suite** après un simple test en jeu (Julien n'avait pourtant pas touché ce fichier). Cause trouvée par comparaison : `bullseye_plus.tres` était le SEUL fichier `.tres` de tout `characters/warrior/cards/` sans `uid=` dans son en-tête `[gd_resource ...]` (confirmé par grep sur tout le dossier) — un `.tres` créé à la main sans uid semble se faire couper de toute référence entrante par Godot dès qu'un autre fichier qui le référence est resauvegardé (pas seulement à l'ouverture du projet comme documenté plus haut pour les uids *inventés* — ici il s'agissait d'une référence par `path=` seul, sans uid du tout des deux côtés). **Fix appliqué et à reproduire systématiquement** : toute nouvelle ressource `.tres` d'upgrade doit avoir un `uid="uid://xxxxxxxxxxxxx"` explicite dans son `[gd_resource ...]`, ET la ligne `ext_resource` qui la référence depuis la carte de base doit inclure `uid="uid://..."` **en plus de** `path=` (exactement le même format que tous les autres `ext_resource` du projet, jamais `path=` seul pour un type `Resource`). Un uid inventé à la main est accepté sans problème (Godot le corrige silencieusement si besoin, comportement déjà documenté) — c'est l'ABSENCE totale de uid qui casse la référence, pas sa validité.

### Contenu des cartes upgrade — pool complet, ajouté 2026-07-06

Toutes les cartes du pool ont désormais une version "+" réelle (Julien a donné les specs par lots de ~10-45, un card_id + `requirement` + `description` à la fois) : les 60 cartes draftable (#2-60, Bullseye #1 fait la veille) et les 5 cartes starter (#61-65 : Strike, Block, Low Blow, Reinforce, Recombobulate). Convention suivie pour chaque carte, identique à Bullseye+ : nouveau `<carte>_plus.gd` (dupliqué depuis le script de base avec le nombre modifié) + nouveau `card_<carte>_plus.tres` (mêmes icon/sound que la base, `upgraded = true`, `uid=` explicite dès la création) + `upgraded_version` câblé sur la `.tres` de base (avec `uid=` **et** `path=` sur la ligne `ext_resource`, cf. bug ci-dessous). **Strike et Block ont chacun 4 copies distinctes dans le starting deck** (`warrior_axe_attack1-4.tres` / `warrior_block1-4.tres`) — un seul `warrior_axe_attack_plus.tres`/`warrior_block_plus.tres` est partagé, câblé sur les 4 fichiers de base à la fois (`replace_card()` swap par référence, donc rien n'empêche plusieurs bases de pointer vers la même version upgradée).

**Cas particuliers qui ont demandé plus qu'un simple changement de nombre** :
- **Carte "Scout 5" créée** (`card_scout5.tres`/`oracle_scout5.gd`, copie de Scout 3 avec juste `Events.scout_effect.emit(5)`) : utilisée par Calculations+/Mindfulness+/Swipe+/Spark+. Bonne surprise en creusant `scenes/battle/battle.gd::_on_scout_effect()` : le panel de scout avait **déjà** 5 slots de dé prévus dans `battle.tscn` (`ScoutDice1`-`5`), jamais exploités puisque personne n'avait jamais émis autre chose que Scout 3. Seul vrai ajout de code : `_resize_scout_panel(visible_count)` (nouvelle fonction dans `battle.gd`), qui élargit dynamiquement le panel ET la rangée de dés en fonction du nombre d'options (`SCOUT_PANEL_MIN_WIDTH`/`SCOUT_DICE_SIZE`/`SCOUT_DICE_SEPARATION`), centré sur la position d'origine du panel (`SCOUT_PANEL_CENTER_X`, une constante figée plutôt que recalculée à chaque appel pour éviter toute dérive) — Scout 3 continue de rendre un panel identique à avant (le calcul retombe sur le même minimum), seul Scout 5 déclenche l'élargissement.
- **Marionette+, Steady Hand+, Critical Edge+** : leur effet numérique (Boost/dégâts) vit dans le **statut** qu'elles posent (`status_marionette.gd`, `status_steady_hand.gd`, `status_critical_edge.gd`), pas dans le script de la carte elle-même — et ces 3 statuts ont chacun un `class_name` propre (`MarionetteStatus`, etc.), donc impossible de dupliquer juste la ressource `.tres` avec un nombre changé. Chacun a eu droit à un statut "+" séparé (`status_marionette_plus.gd`/`.tres` avec `class_name MarionettePlusStatus`, etc. — nom de classe distinct obligatoire, Godot n'autorise pas deux scripts avec le même `class_name`), la carte "+" appliquant ce nouveau statut au lieu de l'original.
- **Notation "X+3" introduite** (Pinpoint+, Experiment+, Strike+, Block+) : aucune carte existante n'utilisait un bonus additif plat sur le nombre de base (toutes les notations existantes type "X3"/"X4" sont des *multiplicateurs*) — Julien a validé cette nouvelle notation lui-même en donnant les specs ("deal x+3 damage"), donc traité comme la convention officielle pour ce cas plutôt qu'une exception ad hoc.
- **Cogwork+** : changement de type de requirement, pas juste de nombre — Exact 6 devient Mult(iple) 6 (le badge passe de "Exact 6" à "Mult 6"), ce qui élargit réellement la condition si jamais un dé à faces plus hautes que 6 est actif (Exact 6 ne peut matcher qu'un 6 pile, Multiple 6 matche aussi 12).
- **Deviation+** : "Charge a random Dice" devient "Charge a Giant Dice" (un type fixe au lieu d'aléatoire) — vrai changement de comportement, pas juste un nombre.
- **Electrify+ et Occultism+** : suppriment leur downside (Depleted 1 / Unlucky 1 respectivement) suite aux specs de Julien qui ne les mentionnaient plus dans la description fournie — **pas explicitement confirmé avec un "(no more X)" comme Occultism l'avait précisé**, à vérifier si Julien playtest et trouve que ça n'était pas voulu pour Electrify+ spécifiquement.

**⚠️ Complément au bug uid documenté juste au-dessus (toujours d'actualité)** : même en respectant la règle (uid explicite sur la nouvelle ressource + `uid=` sur la ligne `ext_resource`), Godot a **quand même** périodiquement retiré le `uid=` de la ligne `ext_resource` (en gardant le `path=`) sur plusieurs fichiers pendant cette session — mais cette fois **sans casser la référence** (`upgraded_version` restait intact), contrairement au tout premier incident Bullseye où l'absence TOTALE de uid (des deux côtés) faisait disparaître la ligne entière. Interprétation : un uid explicite sur la ressource cible suffit à la rendre "stable" pour Godot, qui peut ensuite se permettre de nettoyer la référence redondante (`uid=` en trop sur l'`ext_resource`, puisqu'il peut retrouver l'uid via son propre cache) sans rien casser. Donc la règle reste : **toujours mettre un `uid=` sur toute nouvelle ressource `.tres`** — le `uid=` sur la ligne `ext_resource` qui la référence est une bonne pratique mais Godot peut le retirer sans conséquence tant que la ressource cible en a un.

### Bugs UI trouvés en testant l'upgrade sur des cartes Celestial/Blessing (2026-07-06)

Deux bugs réels dans `scenes/ui/card_menu_ui.gd::set_card()`, trouvés en prévisualisant l'upgrade d'une vraie carte Celestial/Blessing puis d'une carte normale juste après :

1. **Fuite de style Celestial/Blessing sur un nœud réutilisé** : `set_card()` n'appliquait un style spécial à `card_frame`/`card_banner`/`description_panel`/`description` (outline) QUE si la carte était Celestial ou Blessing — jamais de branche `else` pour revenir au style normal. Inoffensif tant que chaque `CardMenuUI` n'affiche qu'UNE carte dans sa vie (le cas des grilles boutique/récompense/deck, toujours fraîchement instanciées) — mais `UpgradeConfirmPanel.BeforeCard`/`AfterCard` (voir section Upgrade plus haut) sont deux nœuds **persistants réutilisés à chaque clic**. Prévisualiser une carte Celestial puis cliquer sur une carte normale laissait le style bleu Celestial collé sur le nœud. Fix : restructuré en `if Celestial / elif Blessing / else reset` explicite.
2. **Le fix du point 1 a lui-même introduit un second bug** : la branche `else` utilisait `remove_theme_stylebox_override()`/`remove_theme_color_override()` en supposant révéler une valeur "par défaut" posée dans le `.tscn`. **Ce n'est pas comme ça que fonctionne le système de thème de Godot** : un `theme_override_styles/panel` (ou `theme_override_colors/...`) posé directement sur un nœud dans le `.tscn` occupe le **même emplacement de stockage** que celui que `add_theme_stylebox_override()`/`add_theme_color_override()` modifient à l'exécution — il n'existe pas de couche "valeur de scène par défaut" séparée du système d'override runtime. Appeler `remove_*_override()` efface donc purement et simplement la valeur posée dans le `.tscn`, faisant retomber le nœud sur le thème générique du projet (résultat : toutes les cartes normales avaient un banner/description panel "bizarres", visible sur le pool de cartes de la capture d'écran de Julien). **Fix définitif** : ne jamais utiliser `remove_theme_*_override()` pour "revenir à la normale" sur un nœud qui a un style posé dans le `.tscn` — réappliquer explicitement la vraie valeur normale à la place. Concrètement : `NORMAL_BANNER_STYLEBOX` (réutilise le fichier existant `card_banner.tres`), `NORMAL_DESC_STYLEBOX` (nouveau fichier `card_ui_description_panel_normal.tres`, extrait d'un sub-resource qui vivait seulement inline dans le `.tscn` avant), et `NORMAL_DESC_OUTLINE_COLOR` (Color codée en dur, copiée depuis la valeur `.tscn` d'origine) — trois nouvelles constantes dans `card_menu_ui.gd`, appliquées explicitement dans la branche `else`. **Leçon générale pour tout futur reset de style sur un nœud Godot réutilisé : ne jamais supposer une couche "par défaut" séparée — toujours réappliquer la valeur normale explicitement, jamais `remove_*_override()`.**

### Hover renforcé + étendu à Celestial/Blessing (2026-07-06)

Julien : sur l'écran de récompense, seules les cartes normales avaient un vrai effet de survol visible — en creusant, `card_ui_hover_celestial.tres` était un **doublon octet-pour-octet** de son style au repos (`card_ui_celestial.tres`), et Blessing n'avait tout simplement pas de style de survol du tout (`_on_visuals_mouse_entered` réutilisait `BLESSING_STYLEBOX`, le même qu'au repos). Fix : `card_ui_hover_celestial.tres` différencié (bordure plus claire/épaisse, `expand_margin` augmenté, léger glow doré ajouté) : nouveau `card_ui_hover_blessing.tres` créé sur le même principe (fond prune de Blessing + même traitement de bordure/glow) et câblé dans `_on_visuals_mouse_entered`. Le survol "normal" (`card_menu_ui_hover_test.tres`) a reçu le même petit boost (bordure 2→3px, `expand_margin` 3→4, glow ajouté) pour que les trois se sentent cohérents entre eux, à la demande de Julien ("slightly increase the hover treatment").

### Tutoriels d'explication sur l'écran de récompense (`scenes/ui/card_rewards.gd`/`.tscn`, ajouté 2026-07-04)

Un panneau explicatif ponctuel (une seule fois par run, flag `Global.tutorial_*_explanation_needed`) peut apparaître sur l'écran de choix de carte quand une carte de la sélection correspond à une catégorie spéciale. Deux existaient déjà (`BonusExplanationBox` pour `bonus_requirement`, avec une illustration statique `slice_illustration_ok.png`/`focus_illustration.png` pour Celestial), un troisième a été ajouté pour Blessing (`BlessingExplanationBox`, `Global.tutorial_blessing_explanation_needed`) — même pattern exact : détection dans la boucle de `set_rewards()` (`has_blessing_card := card.type == Card.Type.BLESSING`), affichage une seule fois si le flag est encore `true`, bouton "Got it" dédié (`Button3` → `_on_button_3_pressed`) qui le referme. **Illustration = la vraie carte, pas un artwork statique** : plutôt qu'un `TextureRect` avec une image d'illustration (comme les deux tutoriels précédents), le nœud `ExampleCard` instancie directement `CardMenuUI` avec `card = card_emanation.tres`, à `scale = 0.8` (`pivot_offset` centré sur son propre centre 70/105 pour un rétrécissement symétrique) — Julien a explicitement demandé la carte entière (bannière, ruban de requirement, description, palette Blessing incluses) plutôt qu'un simple artwork découpé, pour que le joueur apprenne à reconnaître une Blessing par son habillage complet, pas juste par une image.

**⚠️ Tutoriel bonus-effect désactivé (pas supprimé) le 2026-07-04** : Julien prévoit de rendre plus de cartes bonus-effect draftable dans une session future (actuellement seulement 3-4 dans le pool) — le bloc `if has_bonus_requirement...` dans `set_rewards()` est commenté (pas effacé), la détection (`has_bonus_requirement`) et le nœud `BonusExplanationBox` restent intacts. **Repositionner `BlessingExplanationBox` si ce tutoriel est réactivé** : il occupe actuellement les mêmes coordonnées que `BonusExplanationBox` (slot gauche, `offset_left=21`) puisque ce slot était libre le temps que l'autre tutoriel reste désactivé — les deux se chevaucheraient visuellement s'ils étaient actifs en même temps.

### Descriptions dynamiques (`get_dynamic_description`, ajouté 2026-07-02)

Une carte peut optionnellement implémenter `func get_dynamic_description(modifiers: ModifierHandler) -> String` qui remplace le texte statique `card.description` par un texte résolu en direct (le "X" devient le vrai nombre). Détecté via duck-typing (`card.has_method("get_dynamic_description")`), appelé depuis `scenes/card_ui/card_ui.gd::_on_dice_rolled_update_description()`, câblé sur les signaux `Events.dice_rolled`, `dice_roll_reset`, `change_current_power`, `red_dice_rolled`.

**⚠️ Câblé uniquement dans `card_ui.gd`, pas dans `card_menu_ui.gd`** — encore un exemple du piège de duplication documenté plus haut. Volontaire pour l'instant (boutique/récompense n'ont pas de dé actif à résoudre), mais à refaire si Julien demande la même chose ailleurs.

Trois helpers sur `Card` (`custom_resources/card.gd`) à utiliser dans l'ordre, avant de résoudre quoi que ce soit :
1. **`is_inked()`** → `Global.ink_active`. Si vrai, remplacer le nombre par `"?"` (le splash d'encre cache le nombre de Power ailleurs dans l'UI, donc la carte ne doit pas le révéler non plus).
2. **`has_active_roll()`** → `not Global.roll_history.is_empty()`. `Global.roll_value == 0` est ambigu (reset OU vrai résultat sur un dé evil) ; `roll_history` est vidé à chaque reset et rempli à chaque vrai roll, donc fiable pour distinguer "pas encore roll" de "roll et obtenu 0".
3. **`meets_requirement()`** → compare `Global.roll_value`/`Global.dice_type` au `requirement`/`requirement_number` de la carte (MIN/MAX/EVEN/ODD/RED/MULTIPLE/EXACT). Si faux, garder le texte statique — sinon une carte "Max 12" afficherait un nombre absurde à 20 Power, alors qu'elle ne ferait rien si jouée. Règle validée par Julien : pour les cartes EXACT (roll doit être exactement N), ne résoudre QUE si l'exact est atteint ; pour MIN/MAX/MULTIPLE/EVEN/ODD, même principe (ne pas résoudre si hors-condition).

Format retenu (validé par Julien) : le nombre résolu passe **en premier**, la formule/règle reste en texte **ensuite entre parenthèses** — ex. Diceslap : `"Deal 11 damage (5 + 3 per consecutive dice rolled)"` plutôt que `"Deal 5 damage + 3 for each..."`. Objectif : le joueur voit le total sans calculer, sans perdre la règle du mécanisme.

**Cartes Block** : utilisent `Global.roll_value` brut, **jamais** `modifiers.get_modified_value(..., DMG_DEALT)` — il n'existe pas de `Modifier.Type` pour le Block dans le code actuel, et les cartes Block elles-mêmes n'appellent jamais ce modifier. Appliquer Strength (un modifier de dégâts) à un montant de Block serait mécaniquement faux.

**Carte socketée sur dé rouge** : l'affichage du dé chargé (`scenes/dices/dice.gd`, nodes `charged_card_texture`/`charged_card_description`) est une UI statique séparée, PAS le vrai `CardUI` (qui reste caché — `card_ui.hide()` — tant que la carte est socketée). A nécessité son propre hook de rafraîchissement, `dice.gd::_update_charged_card_description()`, câblé aux mêmes 4 signaux + `_on_card_charged`.

**Cas particulier confirmé avec Julien** : Kamikaze a `requirement = RED` mais son `apply_effects()` ne vérifie jamais le type de dé (seulement `roll_value == 1` pour la branche backfire) — ce badge RED semble ne rien bloquer réellement. Sa description ignore donc `meets_requirement()` volontairement (le gating aurait affiché "X3" alors que l'effet se résoudrait normalement sur dé non-rouge).

**Cartes qui ont le pattern** (~31, dont les starters Strike/`warrior_axe_attack.gd`, Block/`warrior_block.gd`, Low Blow/`low_blow.gd`) : grep `get_dynamic_description` dans `characters/warrior/cards/` pour la liste à jour plutôt que la garder synchronisée ici. **Cartes volontairement sans ce pattern** : tout ce qui n'a pas de "X" dans sa `description` (cartes Blessing passives, manipulation de dés/Power sans dégâts/block, `bonus_requirement`-only comme Catalyst) — pas la peine de le rajouter dessus.

### Descriptions colorées (mots-clés), ajouté 2026-07-03

`custom_resources/keyword_colorizer.gd` (classe `KeywordColorizer extends RefCounted`, tout `static`) colore en BBCode les mots-clés reconnus dans un texte de description — utilisé par `Card.get_colorized_description(text)` **et** `Relic.get_colorized_description(text)` (extrait en classe partagée dès que les reliques ont eu besoin de la même logique, pas dupliqué deux fois).

- **`KEYWORDS`** : liste des mots-clés reconnus, calquée sur les mêmes strings que `tooltip.gd::get_tooltip_content()` attend déjà dans `tags` (Charge, Refuel, Scout, Boost, Infused, Weak, Exposed, Lucky, Unlucky, Depleted, Energized, Strength, Muscle, Exhaust, Support, REST, + les 9 "X Dice"). Un mot-clé n'est coloré que s'il est listé dans le champ `tags` de la carte/relique — **sauf les types de dés** (voir plus bas).
- **Une seule couleur** (`KEYWORD_HIGHLIGHT_COLOR = "FFD700"`, or) pour tous les mots-clés, pas une couleur par mot-clé — décision explicite de Julien après avoir vu Slay the Spire 2 : STS2 garde le texte des mots-clés dans UNE seule couleur et différencie les ressources via des icônes inline, pas via la couleur du texte. Ce projet n'a pas encore d'icônes par mot-clé, donc pas de différenciation par icône pour l'instant — à ne pas confondre avec "il faudrait plus de couleurs".
- **Exception : les types de dés** (`DICE_KEYWORD_COLORS`) gardent chacun leur propre couleur (mêmes valeurs hex que `dice.gd::_get_power_orb_color()`, pour rester cohérent avec l'aura du dé/les orbes de Power) — teinter "Blue Dice" en bleu renforce une association déjà apprise par le joueur ailleurs dans le jeu, contrairement aux autres mots-clés qui sont arbitraires.
- **Les types de dés sont aussi la SEULE exception au tag obligatoire** : `colorize()` les cherche toujours dans le texte, qu'ils soient dans `tags` ou non. Trouvé en corrigeant un vrai bug : `tooltip.gd` n'avait historiquement de texte explicatif que pour 4 des 9 types (Green/Magma/Giant/Evil), donc personne n'avait jamais pensé à tagger Blue/Red/Odd/Even/Mech sur les cartes/reliques qui les mentionnent (le tag n'aurait rien affiché de toute façon avant cette session) — ex. la relique Dice Bag (`relics/coupons.tres`) a `tags = ""` mais mentionne "Blue Dice" dans son tooltip. Un nom de dé littéral n'a quasiment aucun risque de faux positif dans ce jeu, donc pas besoin du garde-fou du tag.
- **`LEADING_NUMBER_KEYWORDS = ["Strength", "Muscle"]`** : tous les mots-clés sont écrits "Keyword N" dans le texte (Charge 1, Boost 4, Scout 3...) sauf Strength/Muscle qui sont toujours écrits "N Strength" (ex. Bolster : "Gain 2 Strength"). `colorize()` capture le nombre adjacent du bon côté selon cette liste, pour que le nombre soit inclus dans la couleur plutôt que de rester en texte simple à côté.
- **`find_dice_keywords_in_text(text)`** : détecte quels types de dés sont mentionnés dans un texte, utilisé par les handlers de survol (`card_ui.gd`/`card_menu_ui.gd::_on_card_frame_mouse_entered`, `relic_ui.gd::_on_mouse_entered`) pour afficher le tooltip d'un type de dé même si la carte/relique n'a jamais été taggée avec — même logique que la coloration, réutilisée pour la popup de tooltip.
- **`DICE_TOOLTIP_TEXT`** : source unique pour "que fait ce type de dé", au format `"Faces: 2, 4, 6, 8"` (préférence explicite de Julien — pas de phrase complète du style "Even Dice can roll..."). Utilisé à la fois par `tooltip.gd` (tooltip carte/relique au survol) et `scenes/ui/dice_tooltip.gd` (tooltip du dice shop) — avant cette session chacun des deux fichiers avait son propre texte codé en dur, désynchronisé.
- **Limitation connue** : si un mot-clé taggé est un sous-mot d'un autre mot-clé taggé sur la même carte (ex. "Strength" et "True Strength"), le plus court peut se faire re-matcher une 2e fois à l'intérieur du plus long déjà coloré — inoffensif depuis le passage à une seule couleur (juste un tag `[color]` imbriqué redondant, pas de bug visuel), donc pas traité davantage.
- **Groupement "Charge N DiceType Dice" (affiné en fin de session 2026-07-03)** : `colorize()` fait 3 passes dans cet ordre précis, à ne pas réordonner sans comprendre pourquoi : (1) tous les types de dés d'abord, capturant un nombre PRÉCÉDENT ("2 Magma Dice" en un bloc) ; (2) pour chaque mot-clé générique tagué, une regex "absorbe" le mot-clé dans le tag `[color]` du type de dé qui le suit directement (transforme "Charge [color=#X]2 Magma Dice[/color]" en "[color=#X]Charge 2 Magma Dice[/color]") — décision explicite de Julien : toute la phrase dans la couleur du dé plutôt que deux couleurs (or + dé) dans une phrase aussi courte ; (3) coloration normale en or de ce qui reste de chaque mot-clé, avec un **negative lookbehind** pour ne pas re-matcher un mot-clé déjà absorbé en (2) (sinon il serait ré-enveloppé en or, imbriqué DANS le tag couleur du dé, et le tag interne gagnerait — bug réel rencontré et corrigé pendant le développement de cette fonctionnalité).

## Statuts (`Status`, `custom_resources/status.gd`)

`extends Resource`, paire `.gd`/`.tres` comme les cartes. Enums :
- `Type {START_OF_TURN, END_OF_TURN, EVENT_BASED}`
- `StackType {NONE, INTENSITY, DURATION}`

Les statuts EVENT_BASED se connectent à des signaux du autoload `Events` dans `initialize_status()`.

Statuts confirmés présents dans le code : Weak, Ink, Exposed, Lucky, Infused (ex-Blessed, renommé 2026-07-04 — fichiers `statuses/infused.gd/.tres`, classe `InfusedStatus`), Depleted, Energized, True Strength, Muscle, Berserk (status_berserk), Unlucky, Chaos, Canalize, Parasite, Flux, Sigil, Absorb, Greedy, Eclipse, Serenity, Emanation, Marionette.

**Statuts listés dans le contexte design mais introuvables dans le code actuel** : Vulnerable, Rune, Strict, Stuck, Gargantua (status), Red Sensitive. À considérer comme non implémentés, pas comme bugs.

**Boost existe bel et bien**, mais pas comme un `Status` — implémenté via `Global.next_roll_modifier` + l'UI `NextRollBonusPanel`/`NextRollBonusLabel` dans `scenes/dices/dice.gd`/`dice.tscn` (ajoute un bonus au prochain roll). Plusieurs cartes l'utilisent déjà (Dynamite, Finesse, Preparation, Catalyst, Fireflies, Snatch). Correction du 2026-06-22 : ce fichier disait par erreur que Boost était absent du code.

**Parasite** est le statut de départ (starting status) de l'ennemi **Oculus** : "Gains 3 Strength if you generate more than 15 Power in the same turn." Ce n'est pas un ennemi distinct — c'est un statut auto-buff sur Oculus qui se déclenche sur le power généré par le joueur dans le tour (confirme `Global.power_generated_this_turn > 15` trouvé dans le code).

**Strength = Muscle** : c'est le même concept, `muscle.gd`/`muscle.tres` est la statut Strength sous le capot. **True Strength** est un statut séparé qui fait gagner du Muscle/Strength chaque tour (un générateur de stacks, pas un alias).

**Piège du stacking de statuts (`StatusHandler.add_status()` dans `scenes/status_handler/status_handler.gd`)** : pour qu'un statut déjà présent voit son affichage de stacks incrémenter quand on le rapplique, il faut **`stack_type = 1` (INTENSITY)**. Avec `stack_type = 2` (DURATION) et `can_expire = false` simultanément, **aucune branche du code ne s'exécute** sur une réapplication (ni l'extension de durée, qui demande `can_expire = true`, ni l'addition de stacks, qui demande INTENSITY) — le statut reste visuellement figé au compte initial même si l'effet mécanique sous-jacent (souvent un compteur `Global` séparé, ex. `blue_dice_bonus_amount_fight` pour Emanation) continue bel et bien à s'additionner correctement derrière. Bug trouvé et corrigé sur `statuses/status_emanation.tres` le 2026-06-24 (était `stack_type=2`+`can_expire=false`, corrigé en `stack_type=1`). **Si un futur statut censé stacker n'affiche pas le bon nombre, vérifier cette combinaison de champs en premier.**

## Dés (`DiceData`, `custom_resources/dice_data.gd` + logique dans `scenes/dices/dice.gd`)

`DiceData extends Resource` : `name`, `texture`, `possible_rolls`, `min_roll`, `max_roll`, `color`, `type` (String, pas enum !), `description`, `special_effect`, `current_amount`, `max_amount`.

Le `type` est une **string libre** (`"blue"`, `"red"`, `"evil"`, `"giant"`, `"magma"`, `"even"`, `"odd"`, `"green"`, `"mech"`), pas un enum Godot — donc pas de vérification statique, attention aux typos lors de l'ajout de nouveaux types.

**Pas de classe `Dice` propre par type** : toute la logique de roll/affichage pour tous les types de dés vit dans un seul (long) `scenes/dices/dice.gd` — confirmé par Julien comme étant lui-même conscient que c'est spaghetti. Ne pas supposer un pattern Strategy/polymorphique : c'est probablement un gros `match`/`if` sur la string `type`.

Types de dés confirmés implémentés, avec sémantique confirmée par Julien :
- **blue** (d6) : roll AVANT la sélection de carte (planning).
- **red** : roll APRÈS la sélection de carte (gamble).
- **even / odd** (d6) : ne tombent que sur faces paires / impaires.
- **giant** (d12) : roll 1–12.
- **magma** : dégâts AoE à tous les ennemis au roll.
- **green** (d3) : roll 1–3, alimente l'archétype Low Roll.
- **evil** : faces **6, 6, 6, 0** — très swingy, forte chance de 6 mais risque de 0. (Le "evil" du code n'est pas explicitement le "Red" du doc de contexte — c'est un dé distinct avec ses propres faces.)
- **mech** (d6) : après chaque roll, le joueur peut ajuster le résultat de ±1 power via une petite flèche dans l'UI à côté du dé. **Polish 2026-07-04** (`scenes/dices/dice.gd`/`dice.tscn`, nœuds `MechSection/MechIncrease`/`MechDecrease`) : les deux flèches étaient jusque-là le même asset générique `tutorial_arrow_down.png` réutilisé ailleurs pour les callouts tutoriel (increase = juste ce même PNG flippé verticalement), sans hover/pressed/punch. Ajouté : `disabled` réellement posé sur les deux `TextureButton` une fois l'ajustement consommé (avant, seul un dim d'alpha à 0.3 empêchait visuellement le clic, le bouton restait techniquement cliquable, protégé seulement par un guard script) ; pop au survol (scale ×1.2, gated sur `disabled` pour ne pas pop une flèche déjà consommée) ; punch scale-squash au clic réussi (seul feedback avant = le son `blacksmithsound.wav`, rien sur le bouton lui-même) ; teinte `Color(0.75, 0.78, 0.82)` (gris-acier clair) via `modulate`, cohérent avec l'identité mech déjà établie ailleurs (gris neutre `0.35,0.35,0.35` sur le power/aura/glow — teinte plus claire ici pour ne pas assombrir l'asset générique jusqu'à l'illisibilité).

Dés "planifiés" du contexte (**Precise, Reroll, Casino, Sticky**) : **non trouvés dans le code**, considérer comme non implémentés.

## Ennemis (`EnemyStats`, `custom_resources/enemy_stats.gd extends Stats`)

Un ennemi = dossier `enemies/<nom>/` contenant la ressource stats (`max_health`, `art`, `ai` = PackedScene de l'IA), la scène d'IA `.tscn`, et des scripts d'action (`<nom>_attack_action.gd`, `<nom>_block_action.gd`...).

Ennemis présents : satyr, octopus, machopeur, medusa, dragonpriest, lich, leviathan, chimera, crab, gargantua, goblin, hound, lurker, minotaur, oculus, plant, sigil_slug, temple_defender, vortex.

**Précisions de Julien** :
- "Skeleton" du doc de contexte = **`crab`** dans le code (renommé, pas manquant).
- `temple_defender` et `hound` sont **deux ennemis distincts et réels**, pas une confusion à résoudre.
- "Gargantua" existe comme ennemi (`enemies/gargantua/`) ; le statut "Gargantua" du contexte design n'existe pas encore dans le code — pas de conflit actuel.
- `oculus` a pour starting status **Parasite** (voir section Statuts).

**Nom affiché au survol (ajouté 2026-07-03)** : `Enemy._on_mouse_entered()` affiche un `NameLabel` (nouveau nœud dans `enemy.tscn`, positionné dynamiquement juste sous `StatsUI` dans `update_enemy()`) sous la barre de vie. Source du nom, dans l'ordre : `EnemyStats.enemy_name` (nouveau champ, `@export`) s'il est rempli, sinon `Enemy._compute_display_name()` tente de le dériver du nom de fichier `.tres` (`goblin_enemy.tres` → "Goblin" via `String.capitalize()`). **Ce fallback par nom de fichier ne marchait pas de façon fiable en pratique** (tous les ennemis affichaient juste "Enemy" au premier essai, suggérant que `resource_path` sur la ressource `EnemyStats` reçue par `set_enemy_stats()` n'est pas fiable à ce moment précis malgré un `ExtResource` qui semble correctement référencé dans les scènes `battles/*.tscn` — cause exacte non identifiée, pas creusé plus loin). **Conséquence pratique : `enemy_name` est maintenant rempli explicitement sur les 21 fichiers `.tres` d'ennemis existants** (les 19 dossiers + `bigger_octopus_enemy.tres`/`bigger_satyr_enemy.tres`) — ne pas compter sur le fallback pour un futur ennemi, toujours lui donner un `enemy_name` explicite. `crab_enemy.tres` a `enemy_name = "Skeleton"` (le nom de design confirmé par Julien, pas "Crab").

## Relics (`Relic`, `custom_resources/relic.gd`)

`extends Resource`. Pattern standard EVENT_BASED :
```gdscript
func initialize_relic(owner: RelicUI) -> void:
    Events.dice_rolled.connect(_on_dice_rolled.bind(owner))

func _on_dice_rolled(...) -> void:
    # logique

func deactivate_relic(owner: RelicUI) -> void:
    if Events.dice_rolled.is_connected(_on_dice_rolled):
        Events.dice_rolled.disconnect(_on_dice_rolled)
```
Toujours déconnecter dans `deactivate_relic` pour éviter les doubles-connexions/fuites entre combats. Référence : `relics/crown.gd`, `relics/hunting_bow.gd`.

**Tooltips de relique (`relic_ui.gd`, et depuis 2026-07-03 aussi `battle_reward.gd`)** : deux implémentations séparées (pas de classe de base commune, même piège que les cartes) — `relic_ui.gd` gère le survol en combat/boutique (les deux réutilisent la même scène `RelicUI`, `shop_relic.gd` l'instancie directement), `battle_reward.gd` gère le survol sur l'écran de récompense avec sa propre logique de tooltip plus simple, qui **n'avait jamais eu les tooltips secondaires de mots-clés** (ex. "Scout" à côté du tooltip principal d'une relique) avant cette session — comblé en dupliquant le pattern de `relic_ui.gd`. **Piège de fuite de tooltip corrigé dans les deux fichiers** : les tooltips sont ajoutés sous `get_tree().root`, pas sous le nœud qui les affiche — si ce nœud (RelicUI ou l'écran de récompense) est détruit pendant qu'un tooltip est affiché (ex. quitter la boutique en survolant encore une relique), `mouse_exited` ne se déclenche jamais et une éventuelle coroutine de timeout de sécurité en cours se fait annuler silencieusement avec le nœud — le tooltip reste affiché pour toujours, y compris après un changement de scène. Fix : `_exit_tree()` sur les deux nœuds pour libérer explicitement les tooltips actifs. **Pattern à répliquer sur tout futur système de tooltip similaire** (voir aussi TL;DR en haut de fichier).

## Events narratifs (`EventStats`, `custom_resources/event_stats.gd`)

`scenes/events/event_<nom>.gd/.tres/.tscn`. `EventStats` porte `event_tier` (0-2), `weight` (pondération pour le tirage), `scene` (PackedScene affichée), `accumulated_weight` (utilisé par le pool de sélection pondérée).

### ⚠️ 15 nouveaux events ajoutés le 2026-07-09 — CONTENU CLAUDE, PAS ENCORE REVU

Julien a demandé une extension du pool d'events sur le vibe "dungeons, temples, treasures, magic, weird encounters, dice", en insistant sur deux points : (1) que chaque choix se sente comme une vraie décision (pas un "évidemment tu prends l'option A"), (2) qu'aucune paire d'events ne se ressemble trop (ex. pas deux fois "carte contre or", une fois gratuite et une fois payante). Il a explicitement dit vouloir les relire/corriger/en couper certains à la prochaine session — **traiter tout ce qui suit comme provisoire tant qu'il n'a pas fait cette passe** : ne pas s'étonner si des noms, des chiffres, ou des events entiers changent ou disparaissent.

Les 15 sont tous `event_tier = 0`, `weight = 1.0` (même poids que les 13 events pré-existants — le pool total passe donc de 13 à 28, chaque event a maintenant ~3.6% de chance au lieu de ~7.7%). Tous suivent le même squelette de scène que les events existants (`TextureRect` fond degradé → `MarginContainer` → `Panel` noir → bandeau-titre + illustration 500×500 + `RichTextLabel` de texte + colonne de boutons), avec de l'art **réutilisé** de la bibliothèque d'assets existante (pas de génération d'image possible côté Claude — chaque image a été visuellement vérifiée avant d'être choisie, mais Julien peut vouloir la changer).

**Lot 1 (10 events, mécaniques "moyennes" — relique/carte/dé/or/HP combinés)** :
- **The Hollow Idol** (`event_hollow_idol`) : relique + **perte permanente de 4 Max HP**, ou 65 or sans risque. Premier event du projet à toucher `max_health` en dehors du système de combat — a révélé un vrai gap (voir plus bas).
- **The Fickle Broker** (`event_fickle_broker`) : échange à l'aveugle une relique possédée au hasard contre une nouvelle relique aléatoire.
- **The Hungry Altar** (`event_hungry_altar`) : sacrifie une carte du deck → relique en retour. La récompense n'arrive QU'après suppression réelle de la carte (`Events.card_removed` connecté en one-shot au clic, pas juste à l'ouverture du deck view) — plus robuste que `event_remove_card.gd` existant, qui affiche son bouton Continue dès l'ouverture sans attendre la suppression réelle.
- **The Whetstone Shrine** (`event_whetstone_shrine`) : 45 or pour upgrader une carte au choix, ou 20 or de remboursement si on décline.
- **The Bargaining Skeleton** (`event_bargaining_skeleton`) : vend une carte du deck contre 40 or (même pattern que Hungry Altar, récompense sur suppression confirmée).
- **The Dice Forge** (`event_dice_forge`) : 35 or pour un nouveau type de dé mystère (privilégie un type pas encore possédé, sinon copie bonus d'un type exotique déjà possédé).
- **The Wandering Merchant** (`event_wandering_merchant`) : 25 or pour parcourir 2 cartes fraîches (`Global.pending_card_rewards = 2` + `Events.show_reward`), ou décliner pour un pourboire de 15 or.
- **The Patient Monk** (`event_patient_monk`) : pur upside sans coût — soigner 20 HP maintenant, ou +5 Max HP permanent. Voir le bug Max HP ci-dessous.
- **The Twin Vaults** (`event_twin_vaults`) : coffre gauche gratuit (+1 dé d'un type déjà possédé) vs coffre droit payant (55 or, relique aléatoire) — choix entre deux CATÉGORIES de récompense plutôt qu'une version sûre/risquée de la même récompense.
- **The Crimson Eclipse** (`event_crimson_eclipse`) : 50 or pour un Evil Dice spécifiquement (pas aléatoire, contrairement à Dice Forge).

**Lot 2 (5 events, mécaniques "simples", demandés explicitement par Julien : combos remove/upgrade/heal)** :
- **The Twin Shrines** (`event_twin_shrines`) : retirer une carte OU en upgrader une, les deux gratuits.
- **The Wayside Shrine** (`event_wayside_shrine`) : retirer une carte OU soigner 15 HP, les deux gratuits.
- **The Golden Die Shrine** (`event_golden_die_shrine`) : upgrader une carte OU soigner 15 HP, les deux gratuits (même paire que Twin Shrines/Wayside Shrine, complète le triangle remove/upgrade/heal).
- **The Healing Spring** (`event_healing_spring`) : soigner 25 HP maintenant, ou 10 HP + 25 or (heal pur avec un vrai choix de quantité/bonus).
- **The Traveling Healer** (`event_traveling_healer`) : payer 30 or pour soigner 30 HP, ou soin gratuit de 12 HP.

**Piège technique découvert et corrigé pendant ce lot** : pour les 3 boutons "gratuits" jamais cachés après clic (Twin Shrines/Wayside Shrine côté "remove", pour permettre d'annuler et réessayer l'autre option sans être bloqué), un clic → annulation → re-clic tentait de connecter deux fois le même `Callable` à `Events.card_removed`, ce que Godot refuse (erreur "already connected"). Fix : `if not Events.card_removed.is_connected(_on_card_removed): Events.card_removed.connect(_on_card_removed, CONNECT_ONE_SHOT)` avant de connecter. **À reproduire pour tout futur event qui laisse un bouton de suppression de carte cliquable plusieurs fois sans jamais le cacher.**

**Bug système découvert et corrigé au passage (The Hollow Idol / The Patient Monk, touchant `max_health`)** : `Global.player_max_hp` n'était jamais mis à jour au runtime — seul `character_stats.max_health`/`Stats.max_health` existait auparavant, et le TopBar affiche `Global.player_max_hp` (pas `max_health` directement, voir `run.gd::_on_hp_changed`). Avant cette session, RIEN dans le jeu ne modifiait `max_health` en cours de run (seulement au démarrage), donc ce gap était resté invisible. Les deux events qui touchent le Max HP mettent maintenant `Global.player_max_hp` à jour en même temps que `max_health`, et re-clampent `health` explicitement. **Si un futur système (relique, carte, event) touche `max_health`, ne pas oublier `Global.player_max_hp` en parallèle.**

## Power, dés, et système de combat — zone la plus enchevêtrée

Variables clés dans `global.gd` :
- `roll_value` : valeur courante affichée comme "power" — **réassignée** à chaque roll, pas un compteur cumulatif en soi.
- `power_generated_this_turn` : cumul réel du power sur le tour.
- `roll_history` : array des valeurs rollées ce tour (utilisé par Recombobulate et par le statut Flux).
- `last_roll`, `next_roll_modifier`, `starting_power_next_turn`.

Reset du power piloté par le signal `Events.dice_roll_reset`, émis par les cartes (ex. low_blow, berserker, recombobulate) et écouté dans `scenes/dices/dice.gd` pour remettre `roll_value` à 0.

Règles confirmées par Julien :
- Le power **s'accumule** quand on roll des dés du **même type consécutivement**.
- Il **reset** quand on joue une carte (la plupart des cartes), ou quand on **change de type de dé**.
- Les cartes taguées **support** ne déclenchent pas ce reset.
- Beaucoup de cartes utilisent le Power comme multiplicateur ou condition (ex. dégâts = X × Power).

**Recombobulate, comportement voulu et confirmé** (`characters/warrior/cards/recombobulate.gd`) : c'est une carte à jouer normalement (pas un trigger automatique sur enchaînement de 2 dés). Son but : si tu as mal roll (ex. 1 et 2 sur tes dés Blue), tu joues Recombobulate pour récupérer les dés rollés ce tour (`Global.roll_history.size()`) et les reroll en espérant mieux. Le comportement du code (refund = nombre de dés rollés ce tour, pas un nombre fixe de 2) correspond donc à l'intention réelle — la formulation du doc de contexte ("rolling 2 dice in a row") était trompeuse/imprécise, pas le code.

**Lien visuel dé → Power ("power orbs", ajouté 2026-07-03, individualité + SFX ajoutés 2026-07-09)** : à chaque roll, `dice.gd::_spawn_power_orbs()` fait voler quelques orbes du dé vers le nombre de Power affiché, le long d'une courbe de Bézier bombée vers le haut (contrôle randomisé, pas une ligne droite), colorés par type de dé actif. Le nombre de Power lui-même se met à jour **immédiatement** au roll (dans `_apply_roll_result`, comme avant) — retarder l'affichage jusqu'à l'arrivée du 1er orbe a été essayé puis rejeté par Julien (Global.roll_value doit rester lisible/cohérent tout de suite pour les autres systèmes, ex. `get_dynamic_description`). Une réaction visuelle secondaire plus petite (`_play_power_orb_arrival_reaction`, flash coloré + petit pop) se déclenche quand même à l'arrivée du 1er orbe, en plus du punch/flash blanc habituel du roll — les deux beats sont volontairement distincts. Roll à 0 (face 0 du dé evil) n'émet aucun orbe.

- **Fix "effet train" (2026-07-09)** : Julien trouvait que les orbes voyageaient "en train", parfaitement alignés. Root cause principale = la cadence de lancement parfaitement linéaire (`POWER_ORB_STAGGER * i`), pas seulement la trajectoire. Fix : `mid_x` (ratio le long du chemin où l'orbe bombe) randomisé par orbe au lieu d'être fixe à 0.55 (`POWER_ORB_MID_X_MIN/MAX`), jitter du point de contrôle élargi (±25 au lieu de ±15), plage d'apex élargie (40–130 au lieu de 60–110), ET le délai de lancement reçoit un jitter aléatoire (`POWER_ORB_STAGGER_JITTER`) plutôt que d'être parfaitement régulier.
- **SFX de roll retrouvé "en retard", root cause = silence en tête de fichier audio (2026-07-09)** : `dice.gd::play_dice_roll_sound()` était déjà appelé à la toute première ligne de `roll_dice()`, avant toute animation — pas un bug de timing/ordre d'appel dans le code. Diagnostiqué via `ffmpeg -i <file> -af silencedetect=noise=-30dB:d=0.05 -f null -` : `sounds/dicerollsound1.mp3` avait 341ms de silence avant le premier son, `dicerollsound2.mp3` 143ms, `dicerollsound3.mp3` 54ms (négligeable) — les 3 sont tirés au hasard à chaque roll donc le délai apparaissait sur 2 rolls sur 3. Fix : les 3 fichiers retrimés avec `ffmpeg` (silence en tête coupé), remplacés directement sur disque. **Piège méthodologique généralisable** : avant de chercher un bug de timing dans le code, vérifier si l'asset audio lui-même a du silence en tête.
- **SFX d'atterrissage ajouté (2026-07-09)** : un petit "tick" (`_play_power_orb_land_sfx`, placeholder `sfx/578807__nomiqbomi__pluck-1.mp3` — asset inutilisé ailleurs, à remplacer si Julien a mieux) joue à CHAQUE atterrissage d'orbe, pitch (0.85–1.25×) et volume jitterés par hit pour qu'un gros roll (jusqu'à ~15 orbes) ne sonne pas comme une mitrailleuse répétant la même note. Volume monté deux fois sur retour de Julien : -10dB → -4dB → +2dB (`POWER_ORB_LAND_VOLUME_DB`). A nécessité d'ajouter deux paramètres optionnels à `global/sound_player.gd::play()` (`pitch_scale`, `volume_db`, défauts 1.0/0.0) — rétrocompatible avec tous les ~40 appels existants dans le projet, et **toujours réassignés explicitement à chaque appel** (pas seulement quand non-défaut) puisque les `AudioStreamPlayer` du pool sont réutilisés entre appels sans rapport — sinon un pitch/volume seraient restés collés sur le lecteur pour le prochain son qui n'en demandait pas.

**Highlight du bouton End Turn ("plus rien à faire", ajouté 2026-07-03)** : `battle_ui.gd::_update_end_turn_highlight()` fait pulser le bouton End Turn (or lent) quand les 3 conditions sont réunies : 0 dé restant (tous types), `Global.roll_value <= 0`, et aucune carte avec `can_play_without_dice` en main. N'importe laquelle des trois encore disponible désactive le highlight. Réévalué sur `Events.hover_playable_cards` (déjà émis par `dice.gd` à chaque roll/reset/changement de dé) et `Events.card_played` (pour capter une carte Celestial qui quitte la main).

**Hit-stops (`Shaker.hit_stop()` dans `global/shaker.gd` + call sites, refonte 2026-07-04)** :
- **`Shaker.hit_stop()` est maintenant reference-counted** (`_hit_stop_active` compte les appels en cours) — avant, deux appels concurrents remettaient `Engine.time_scale` à 1.0 chacun sur leur propre timer indépendamment, donc le plus COURT des deux pouvait couper le plus long en plein milieu. Ça touchait déjà (silencieusement) les cartes AoE multi-cibles (un `hit_stop` par ennemi touché, dans une boucle). Maintenant seul le DERNIER appel encore actif à se terminer restaure `time_scale`.
- **Hit-stop sur dégâts** (`effects/damage_effect.gd`, `DamageEffect.execute()`, s'applique identiquement que ce soit le joueur OU un ennemi qui inflige les dégâts, même code path) : `clampf(amount * 0.014, 0.04, 0.24)` (était `* 0.008`, plafond `0.16`) — un coup de ~12+ dégâts (repère donné par Julien pour early-game) se rapproche maintenant nettement du hit-stop de max roll (0.2s, le plus perceptible du jeu selon Julien) sans forcément l'égaler ; au-delà de ~17 dégâts ça peut désormais dépasser le max-roll.
- **Hit-stop sur cartes support/power** (`dice.gd::_on_change_current_power()`) : nouveau, scalé par le delta de power réel (`clampf(power_delta * 0.01, 0.03, 0.1)`) plutôt qu'une valeur fixe — Reinforce (+1) à peine perceptible, Blaze (+5) un peu plus. Ces cartes n'avaient AUCUN hit-stop avant (seulement le "power clang" visuel).
- **Bonus hit-stop EXACT** (`custom_resources/card.gd::play()`, juste après `apply_effects()`) : 0.16s flat quand `card.requirement == Requirement.EXACT and meets_requirement()` — délibérément flat, pas scalé par les dégâts. Sur un coup modeste (Duo, ~8 dégâts, hit-stop dégâts natif ~0.11s) ce bonus domine grâce au ref-counting (seul le PLUS LONG des deux appels compte), donnant le "tu as pile réussi" recherché par Julien ; sur un gros coup (Doomsday) le hit-stop dégâts est déjà plus long, donc le bonus est absorbé sans rien ajouter — pas de double-compte nécessaire. S'applique aussi tel quel aux cartes EXACT sans dégâts (Eruption), qui n'avaient aucun hit-stop avant. Seul `EXACT` est concerné pour l'instant (pas MIN/MAX/MULTIPLE/EVEN/ODD).

**Setup de départ confirmé (corrigé 2026-07-04 après vérification code)** : **66 HP** (`warrior.tres`, pas 70), deck de départ = 4 Strike, 4 Block, 1 Recombobulate, 1 Reinforce, 1 Low Blow, dés de départ = 2 Blue + 1 Red + relique Dice Bag (+1 Blue au 1er tour de chaque combat). ⚠️ `warrior_starting_deck.tres` contient actuellement Dice Slap + Calculations à la place de 2 Strikes — inserts de TEST de Julien, pas le vrai deck. ⚠️ `global.gd: gold = 7575` = mode testing, vrai départ = 75.

### Patterns de décision joueur en combat (observés en playtest par Julien, 2026-07-04)

Julien a partagé un tour-par-tour détaillé d'un run complet (tier 0 → premiers elites) pour transmettre non pas des chiffres mais **la façon dont un joueur qui connaît le jeu enchaîne les systèmes entre eux**. Utile pour juger si une carte/mécanique future "a du sens" dans l'écosystème existant, ou pour évaluer si un futur ajout crée de nouvelles synergies intéressantes. ⚠️ Julien lui-même : *"new players will never play that clean, I just know my game"* — ce qui suit décrit du jeu **optimal/expert**, pas l'expérience d'un joueur moyen. Un combat qui "passe bien" dans ce genre de run peut rester un mur pour un nouveau joueur qui n'a aucun de ces combos en main (voir Lurker+Crab, section Bugs récurrents / balance, qui a semblé un mur même à un joueur expérimenté).

**Chaîne observée (event fight Machopeur+2×Octopus)** : Scout un dé **Mech** → force un "1" → joue Catapult (AoE, archétype Low Roll) → obtient Lucky 1 en bonus → **change de type de dé actif vers Red** (le changement de dé remet le power à 0 mais ne coûte rien d'autre) → Lucky garantit un 6 sur le prochain roll Red → joue Flurry sur ce 6 → la relique Blood Sword ("+2 bonus Power sur vos dés Red") s'ajoute automatiquement → 8 power total sur Flurry → 16 dégâts sur Machopeur. Ensuite, 3 Blue dice restants roll très mal (1, 2, 1) — au lieu de subir la mauvaise main, Recombobulate est en main et sert de **filet de sécurité** : refuel les dés rollés ce tour pour retenter, permet quand même de tuer un Octopus et bloquer l'attaque de Machopeur ce tour-là.

**Enseignements génériques à garder en tête pour le design futur** :
- **Scout + Mech est une synergie réelle et recherchée par les joueurs experts**, pas juste deux mécaniques indépendantes : Mech permet d'ajuster ±1 après roll, donc un Scout qui garantit "5 ou 6" devient aussi bon qu'un Scout garantissant "6 pile" pour une carte à condition MULTIPLE/EXACT (ex. Aegis, "mult 6") — le joueur peut se permettre de viser une fourchette plus large plutôt qu'un chiffre exact grâce à ce filet. Exemple concret cité : contre Dragonpriest, Scout posé sur Mech spécifiquement *parce que* ça sécurise le pari sur Aegis.
- **Unlucky peut être un enabler délibéré, pas seulement un debuff subi.** Unlucky force le prochain roll à la face minimale — mauvais pour un deck qui veut du power, mais *exactement ce que veut* une carte Low Roll comme Catapult (qui veut un 1). Julien a volontairement pris une carte qui donne Unlucky à lui-même pour garantir un 1 et enchaîner sur Catapult, plutôt que de la subir passivement.
- **Lucky + dé à grosse face (Giant, face max 12) = burst garanti.** Lucky garantit la face maximale du dé actif ; combiné à Giant (le dé au plus haut plafond du jeu), ça transforme un tour en dégâts fixes et prévisibles plutôt qu'un pari.
- **Changer de type de dé actif en cours de tour est un vrai outil tactique**, pas juste un choix fait une fois par combat : le changement reset le power (comportement confirmé ailleurs dans ce fichier) mais permet d'accéder à des cartes gated par `Requirement.RED` tout en gardant le bénéfice de relics passives liées au type de dé (Blood Sword ne bonifie QUE le Red, donc switcher vers Red au bon moment est aussi une façon de "activer" une relique normalement dormante sur Blue).
- **Recombobulate/refuel est perçu et utilisé comme une soupape anti-malchance**, pas comme une carte de power générique — le joueur la garde consciemment en réserve pour absorber un mauvais roll plutôt que de la jouer proactivement.
- **Lecture d'intent → priorisation de cible** : tuer en priorité l'ennemi qui s'apprête à attaquer plutôt que celui qui bloque/buff (ex. tuer l'Octopus qui allait attaquer avant de s'occuper de Machopeur qui se buffait, jugé "safe" pour l'instant).
- **Évaluation du risque sur les mécaniques anti-bank/anti-greed** (Canalize de Dragonpriest, scaling de Lich sur le dernier roll banké) : un joueur expérimenté calcule si "continuer à accumuler" reste rentable face à la menace grandissante, plutôt que de bank aveuglément — Lich en particulier a été perçu comme la mécanique la plus dangereuse du roster si on ne le tue pas à temps (jusqu'à 18 dégâts avec 7 Strength accumulée).
- **Achat de dé ciblé sur la main de cartes courante**, pas générique : Mech acheté spécifiquement parce que la main du moment (Clank, Catapult, Aegis) récompense des rolls précis/ajustables, pas parce que Mech est intrinsèquement le meilleur choix dans l'absolu.

## Autoloads (`project.godot`)

- `Events` (`global/events.gd`) — hub de signaux (~100+), tout le couplage inter-systèmes passe par là.
- `Global` (`global.gd`, racine du projet, pas dans `global/`) — state du run.
- `Shaker`, `MusicPlayer`, `SFXPlayer` — utilitaires audio/visuel.

Avant de modifier une mécanique, grep les signaux `Events.xxx` concernés pour voir tous les émetteurs/écouteurs — la logique est dispersée entre cartes, statuts, relics et UI plutôt que centralisée.

## Effects (`custom_resources/effect.gd`, `effects/`)

`Effect extends RefCounted`, méthode `execute(targets: Array[Node])`. Sous-classes : `DamageEffect`, `StatusEffect`, `BlockEffect`, `SupportEffect`. Les cartes/relics construisent un Effect, le configurent, puis appellent `.execute()`.

## Modifiers (`scenes/modifier_handler/`)

`Modifier` (Node) avec enum `Type {DMG_DEALT, DMG_TAKEN, CARD_COST, SHOP_COST, NO_MODIFIER}`, alimenté par des `ModifierValue` (FLAT ou PERCENT_BASED). Sert à appliquer les bonus/malus de statuts/relics sur les calculs (dégâts, coût de carte...).

## Bugs récurrents trouvés en session (pattern à surveiller)

Plusieurs ennemis et cartes contiennent du code **copié-collé d'un autre ennemi/carte sans renommer les identifiants internes** (`action_id`, noms de status, noms de dés). Ce n'est pas un cas isolé — c'est arrivé au moins 4 fois sur des fichiers différents. **Avant de faire confiance à une condition `if enemy.last_action == "..."` ou un nom de statut/dé en dur dans un script, vérifier qu'il correspond bien au contexte courant, pas à celui d'où le code a été copié.**

Cas confirmés et corrigés cette session :
- `enemies/goblin/goblin_attack_action.gd` et `goblin_attack_action_2.gd` référençaient `"defender_block"`/`"defender_single_attack"` (copié de Temple Defender) au lieu des `action_id` propres à Goblin — son combo ne se déclenchait jamais. Corrigé.
- `enemies/oculus/oculus_attack_action.gd` et `oculus_attack_action_2.gd` référençaient `"plant_attack"`/`"plant_buff"` (copié de Plant) au lieu de ses propres `action_id` — sa deuxième attaque ne se déclenchait jamais. Corrigé.
- `statuses/status_berserk.tres` avait le champ `tooltip` qui contenait le texte d'Emanation ("Gain one more Blue Dice...") au lieu de décrire son vrai effet (double dégâts Red). Confirmé mais pas corrigé (tooltip cosmétique, pas prioritaire).

**Bugs de bordure/couleur sur les cartes trouvés le 2026-07-02** (pattern à surveiller si un futur bug de bordure/couleur de carte apparaît) :
- `card_ui.gd::set_playable_visual()` cache lazily `_base_frame_stylebox` (le style "au repos" de `CardFrame`) au premier appel — si cet appel arrive avant que `_set_card()` applique le style céleste, le cache reste bloqué sur le style normal (maron) pour toujours, même après un `card_frame.add_theme_stylebox_override(...)` explicite ailleurs. D'où des cartes célestes qui redevenaient marron au repos. Fix : `_set_card()` resynchronise `_base_frame_stylebox`/`_hot_frame_stylebox` explicitement quand `can_play_without_dice` est vrai.
- `card_background.tres` (le fond statique derrière `CardFrame`) avait un `corner_radius` (5) différent de celui de `CardFrame` (6), et le glow "jouable" (`GLOW_BORDER_WIDTH_HOT/AVAILABLE`) changeait `border_width` sans changer `expand_margin` en conséquence — dans certains états (surtout le glow "hot"), la bordure intruse à l'intérieur du rect au lieu de border déborder vers l'extérieur, ce qui la faisait passer sous `RequirementPanel`/`DescriptionPanel`/`BonusEffect` (dessinés par-dessus). Fix définitif : `set_playable_visual()` fait maintenant `expand_margin = border_width` à chaque changement, donc la bordure ne rentre jamais dans le rect — pas besoin d'inset compensatoire sur les panels enfants (une tentative avec un inset de 3-5px a été essayée puis retirée, corriger à la source était la bonne approche).

**Dimming des cartes injouables, deux niveaux (`card_ui.gd::set_playable_visual()`, ajouté/affiné 2026-07-04)** : `PlayableGlow.NONE` assombrit la carte via un multiply RGB à alpha plein (pas un alpha-fade — voir commentaire au-dessus de la const, le alpha-fade bavait à travers les couches empilées de façon inégale). Deux teintes désormais, choisies selon `Global.roll_value` au moment de l'appel : `UNPLAYABLE_MODULATE_HAS_POWER` (`0.6`, plus sombre) si du power est déjà banké mais que cette carte précise ne remplit pas sa condition ; `UNPLAYABLE_MODULATE_NO_POWER` (`0.75`, plus clair) si rien n'a encore été roll (`roll_value <= 0`) — lu comme "pas encore" plutôt que "non". **Les cartes Celestial (`can_play_without_dice`) ne passent jamais par cette branche** : `hand.gd::_get_glow_state()` retourne `PlayableGlow.HOT` pour elles en tout premier, avant même de regarder `roll_value` — donc elles restent à pleine luminosité même à 0 power, déjà correct par construction, pas de fix nécessaire à ce sujet.

## Économie boutique (état au 2026-06-24)

`scenes/shop/shop.gd` : or de départ 75 (`global.gd`). Prix de base des dés (avant escalade ×1.35 par rachat du **même type** dans le run, mécanisme volontaire à garder) : Magma 270, Evil 240, Giant 240, Mech 200, Even 210, Odd 190, Blue 180, Red 180, Green 150. Hiérarchie de puissance qui justifie ces prix : Magma (dégâts AoE gratuits à chaque roll) et Evil (75% de chance de max value) sont les plus forts ; Red/Blue sont la référence basique sans avantage intrinsèque.

`scenes/shop/shop_card.gd` : prix carte `randi_range(30, 80)` (pas de système de rareté actuellement — choix volontaire de simplicité, à revisiter si les cartes Blessing doivent coûter plus cher que les cartes normales).
`scenes/shop/shop_relic.gd` : prix relique `randi_range(120, 170)`.

Voir aussi `card_pool_analysis.md` à la racine pour le détail complet de l'analyse des 65 cartes du pool warrior, les coupes effectuées, et le backlog de cartes Blessing proposées (le doc lui-même date d'avant le renommage et parle encore de "RITE" — historique, pas mis à jour).

## Main menu (`scenes/ui/main_menu.tscn` + `main_menu.gd`, nettoyé le 2026-07-02)

Scène volontairement minimale : un seul bouton visible ("Start New Run", peint dans l'image de fond `main_menu_v3.jpg`, le `Button` Godot par-dessus est transparent au repos et ne montre qu'une bordure dorée au survol), pas de Settings/Continue/Quit à ce stade. Cliquer dessus ouvre `EnableTutorialPanel` (popup "Enable tutorial?").

**"Load Run" ajouté le 2026-07-07 (placeholder de test pour le système de sauvegarde)** : nouveau `Button` (`LoadRun`, stylé avec les mêmes styleboxes partagées shop_button_normal/hover/pressed), caché par défaut et affiché seulement si `SaveManager.has_save()`. Pose `Global.load_run_requested = true` puis charge `run.tscn` — consommé par `run.gd::_late_init()`. Voir section "Système de sauvegarde" plus bas pour le détail complet.

Nettoyage fait cette session :
- Supprimé `@onready var new_run: Button = $VBoxContainer/NewRun` dans `main_menu.gd` — pointait vers un chemin inexistant (pas de `VBoxContainer` dans la scène, `NewRun` est un enfant direct de la racine) et n'était utilisé nulle part. Levait une erreur silencieuse à chaque chargement du menu pour rien.
- Supprimé un `Button`+`CheckBox` "tutorial: on/off" caché (`visible = false`) et totalement déconnecté (aucun signal câblé, aucune référence dans le script) — vestige d'une ancienne version du toggle, remplacé depuis par le popup `EnableTutorialPanel`.
- `main_menu.png`, `main_menu_v2.png`, `main_menu_resolution_ok.png` : anciennes itérations du visuel, plus référencées par aucune scène (seul `main_menu_v3.jpg` est utilisé). Laissées sur le disque, pas de nettoyage d'assets fait.
- `EnableTutorialPanel` restylé pour matcher le langage visuel établi cette session (bordure dorée au lieu de teal-vert, titre couleur or, boutons "Enable"/"Skip" avec vrai style au lieu du bouton Godot par défaut) + ajout d'un bouton "X" (Cancel) en haut à droite du popup — avant cette session, cliquer sur "Start New Run" par erreur forçait à choisir Enable/Skip, aucun moyen d'annuler.

## Campfire (`scenes/campfire/campfire.gd` + `campfire.tscn`, polish 2026-07-03)

Titre + bouton Rest (`HealButton`) restylés avec le même langage visuel que les boutons Buy du shop (bordure or, `corner_radius=8`, normal/hover/pressed). Le tooltip du Rest (et de la zone de heal) était volontairement retardé de 0.5s avant d'apparaître (`_on_heal_button_mouse_entered`/`_on_heal_zone_mouse_entered`) — ce délai a été retiré à la demande de Julien, le tooltip apparaît maintenant instantanément au survol.

## Map (`scenes/map/map.gd` + `map_room.gd`, lisibilité du chemin ajoutée le 2026-07-04)

Le graphe complet est révélé dès le début (philosophie "carte transparente" façon roguelike, pas de brouillard de guerre) — mais avant cette session, TOUTES les lignes de connexion (`_connect_lines`, dessinées une seule fois à la génération de la carte) restaient à pleine opacité en permanence, créant un treillis dense de lignes qui se croisent sans distinction entre "ton chemin", "tes choix actuels" et "tout le reste".

- **Dimming des lignes de connexion** (`Map._refresh_line_visibility()`) : chaque ligne connait ses deux `Room` d'origine (`_line_edges`, un tableau de `{line, from, to}` rempli dans `_connect_lines`, plus `_room_lookup` qui mappe `Room → MapRoom` pour lire l'état `available` en direct). Une ligne reste à pleine opacité si `from.selected && to.selected` (c'est un segment de ton chemin réellement emprunté — passe au **doré** `#f0c040`, assorti à l'anneau de sélection déjà existant sur les salles) OU si **`to`** (pas `from`) est une salle actuellement `available` (le trait mène VERS ton choix vif, couleur normale). Tout le reste retombe à `LINE_DIM_COLOR` (`#4A3210`, plus sombre que `LINE_DEFAULT_COLOR`) à `alpha=0.45`. ⚠️ **Couleur dim ajustée le jour même** : la version initiale réutilisait `LINE_DEFAULT_COLOR` (`#8B5E1A`, plus clair) à `alpha=0.28` — quasi invisible contre les zones plus claires du parchemin (Julien : "barely visible against lighter parchment"). Fix : couleur dédiée plus sombre + alpha remonté, pour que le contraste tienne même sur les patchs clairs plutôt que de compter uniquement sur l'alpha. **Anneau de sélection (`show_selected()`, orange/or) : proposé un restyle (double-anneau/pointillé/arcs), Julien a préféré garder l'existant tel quel — ne pas re-proposer.** **Marqueur persistant pour l'état "disponible" : essayé puis retiré la même session.** Julien avait signalé que certaines lignes semblaient plus claires que d'autres "sans raison" ("am i tripping or some lines are lighter than others for no reason") — root cause identifiée (les lignes brillantes mènent vers des salles `available`, qui n'ont qu'un pulse d'échelle comme indice visuel, quasi invisible sur une capture figée), et un anneau sarcelle persistant avait été ajouté sur `set_available()` pour rendre la corrélation évidente. **Julien a jugé ça une sur-réaction à une remarque mineure et a demandé de revenir en arrière** — remarque sur la variation de luminosité des lignes traitée comme mineure/cosmétique, pas comme un vrai problème à corriger avec un nouveau marqueur visuel. Retiré entièrement (`AVAILABLE_RING_COLOR` et les lignes correspondantes dans `set_available()`). Levier appris : ne pas systématiquement transformer une observation en fix — évaluer d'abord si Julien demande une correction ou fait juste une remarque en passant. ⚠️ **Bug corrigé le jour même** : la première version vérifiait `from.available` au lieu de `to.available` — ça allumait les lignes SORTANT des salles disponibles (soit une rangée trop loin) plutôt que les lignes MENANT VERS elles. Signalé par Julien : "i am on floor 1 and it highlights lines from floor 2 to floor 3". Toujours vérifier `to`, pas `from`, si ce calcul est retouché. **Piège évité par construction** (toujours valide) : sur un losange où une salle a deux parents possibles, la règle `from.selected && to.selected` désambiguïse correctement sans stocker l'historique du chemin, parce qu'au plus une salle par ligne peut être `selected` dans une run donnée (`_on_map_room_selected` verrouille explicitement toutes les autres salles de la même ligne dès qu'une est choisie). Rafraîchi à chaque fois que la disponibilité change (`unlock_floor()`, `unlock_next_rooms()`), pas seulement à la génération.
- **Marqueur "tu es ici" : ajouté puis retiré dans la même session.** Un anneau bleu-glacé pulsant sur `last_room.position` a été essayé, mais rendait la lisibilité PIRE plutôt que meilleure dès qu'il coïncidait avec l'anneau orange de disponibilité déjà existant (deux anneaux concentriques de couleurs différentes sur le même nœud = confus, pas clair). **Retiré entièrement** (plus de `CurrentMarker` dans `map.tscn`, plus de `_setup_current_marker()`/`_update_current_marker()` dans `map.gd`) — ne pas réintroduire un anneau supplémentaire sans repenser comment il cohabiterait avec l'anneau de disponibilité déjà présent.
- **Médaillon derrière les icônes : essayé puis entièrement retiré.** Deux itérations tentées (médaillon brun avec contour net, puis version basse-opacité sans contour) — **toutes deux rejetées par Julien** : "still have circles on every node". Même une version très discrète (alpha 0.3, rayon ×0.7) restait visible comme un disque derrière chaque icône, et avec 20+ salles à l'écran ça lit comme "tout est cerclé" plutôt que comme un détail de contraste. **Retiré complètement** (plus de nœuds `Visuals/Backing`/`Visuals/BackingBorder` dans `map_room.tscn`, plus de `_update_backing()` dans `map_room.gd`) — si le contraste icône/parchemin doit être retravaillé un jour, ne pas repartir sur un disque plein derrière l'icône, cette direction a été testée deux fois et rejetée les deux fois.
- **Pop au survol** (`MapRoom._on_mouse_entered/_on_mouse_exited`, gated sur `available`) : léger scale-up (1.0→1.12) du nœud RACINE de la salle (pas `Visuals`, qui est déjà animé en boucle par `AnimationPlayer::highlight` — scaler un nœud différent évite que les deux animations se battent). Nécessite les connexions `mouse_entered`/`mouse_exited` sur l'`Area2D` (ajoutées dans `map_room.tscn`), qui fonctionnent gratuitement puisque le picking souris est déjà actif pour `_on_input_event`. Seule des 5 nouveautés visuelles de cette session à avoir survécu sans retouche.
- **Légende corrigée puis largement rétrécie** (`MapLegend` dans `map.tscn`) : elle listait Fight/Elite/Event/Shop/Treasure mais **oubliait complètement Campfire** alors que l'icône apparaît sur la carte — gap réel, pas juste cosmétique. Campfire ajouté. **Boss explicitement PAS ajouté** — Julien : "no need to include boss" (il est unique et évident en jeu, pas besoin d'entrée dédiée). ⚠️ **Trois tentatives de taille de panneau avant la bonne** : 1ère (200→280) laissait déborder l'entrée Boss (depuis retirée) ; 2e (130×180) jugée "way too big" ET laissait un grand vide sous la dernière entrée. Cause du vide : `Panel` étendait son rect de 60px au-delà de `Control` (`offset_top=-62`/`offset_bottom=-2`, un vestige de template) alors que le `VBoxContainer` à l'intérieur ne remplissait qu'une fraction de cet espace (alignement par défaut = haut, donc tout l'espace en trop restait vide en bas). **Fix définitif** : `Panel` colle maintenant exactement au rect de `Control` (offsets verticaux à 0), taille calculée précisément (6 lignes × 26px + 5 séparations × 2px = 166, + 16px de padding via des offsets explicites sur le `VBoxContainer` = 182, `Control.custom_minimum_size` fixé à **(130, 195)** pour une marge confortable sans vide visible) plutôt qu'un delta ajouté au jugé. **Si une future entrée de légende est ajoutée, refaire ce calcul (hauteur de ligne × nombre de lignes + séparations + padding) plutôt que d'agrandir au pif — ça a raté deux fois de suite avant cette version.**
- **Non traité cette session (parqué pour plus tard)** : le fond parchemin (`MapBackground`, `CanvasLayer` avec `layer=-1`) reste fixe à l'écran alors que la caméra scrolle le contenu du monde — effet "cutout glissant sur papier peint statique" plutôt qu'un vrai défilement continu sur un même rouleau de parchemin. Changement plus structurel, volontairement pas fait dans cette passe.

### Bouton "Map" de consultation non-destructive (`run.gd`, ajouté 2026-07-07)

Julien voulait pouvoir consulter la carte en plein combat/event/shop **sans perdre sa progression**, contrairement au `MapButton` de debug existant (`DebugButtons/MapButton` dans `run.tscn`, wired sur `_show_map()` qui `queue_free()` la vue courante — resté intentionnellement destructif, c'est un outil de dev, pas touché).

- **Nouveau `ConsultMapButton`** (`TopBar/BarItems`, stylé avec les mêmes styleboxes partagées shop) → `_on_consult_map_button_pressed()` → `_open_map_consult()` : `current_view.get_child(0).hide()` (PAS `queue_free()`) + `map.show_map()` + `get_tree().paused = true` (même pattern que `battle_over_panel.gd`) → fige tout ce qui est cachée (tours ennemis, timers d'event) pendant que la map est affichée. `_close_map_consult()` fait l'inverse. Un flag `map_consult_mode` guarde `run.gd::_on_map_exited()` (`if map_consult_mode: return`) pour qu'un clic sur une salle pendant la consultation ne puisse jamais déclencher une vraie sélection.
- **`Map` (l'instance dans `run.tscn`) a `process_mode = 3` (ALWAYS)** — sans ça, `map.gd::_process()`/`_input()` (scroll clavier/molette/drag) sont eux aussi gelés par la pause et la carte reste bloquée là où elle était. Effet de bord accepté : les salles héritent aussi de ALWAYS (deviennent "cliquables" pendant la pause), neutralisé par le flag `map_consult_mode` ci-dessus.
- **Trois bugs trouvés en testant, tous des pièges génériques réutilisables pour tout futur "cacher/pauser une vue temporairement"** :
  1. **CanvasLayer niché ignore le `hide()` du parent** (même piège que `MapBackground`, voir plus haut) — `battle.tscn` a 5 `CanvasLayer` (`BattleUI`, `CardPileViews`, `BattleOverLayer`, `RedFlash`, un sans nom) qui restaient visibles (cartes en main, piles, End Turn) même après `hide()` sur la racine de Battle. Fix générique : `run.gd::_set_nested_canvas_layers_visible(node, visible)` — recurse tout le sous-arbre et bascule `.visible` sur chaque `CanvasLayer` trouvé, en plus du `hide()`/`show()` normal sur la racine.
  2. **`Camera2D` niché n'est pas un `CanvasItem`** — `battle.tscn` a son propre `Camera2D`, qui restait "current" (actif) même avec son parent caché, entrant en concurrence avec la caméra de la Map. Résultat observé : scroller la map ne faisait RIEN visuellement, puisque la caméra de Battle restait celle qui pilotait réellement le viewport. Fix générique : `run.gd::_set_nested_cameras_enabled(node, enabled)` — désactive tout `Camera2D` niché dans la vue avant d'activer celle de la Map, réactive à la fermeture.
  3. **Tooltip de relique qui restait affiché pour toujours** — `relic_ui.gd` avait un tooltip PRINCIPAL sans aucun timeout de secours (contrairement à ses propres tooltips secondaires de tags, à 6s, et contrairement à `card_ui.gd`/`intent_ui.gd` qui ont chacun leur propre garde-fou) : `mouse_exited` ne se déclenche jamais sur un nœud en pause non-`ALWAYS`, donc un tooltip ouvert juste avant la pause restait figé à l'écran indéfiniment, y compris après avoir refermé la consultation. Fix double : (a) timeout de 8s ajouté au tooltip principal (même pattern que `intent_ui.gd::_start_tooltip_safety_timeout`), (b) `_open_map_consult()` purge explicitement tous les tooltips de relique actifs (`relic_ui._cleanup_tooltips()`) avant de pauser, pour un nettoyage immédiat plutôt que d'attendre le timeout.
- **Dice Shop déplacée hors de `current_view` vers `TopBar` (CanvasLayer)** : elle ne couvre pas tout l'écran et est censée flotter par-dessus la map visible (comme `DeckView`, jamais dans `current_view` non plus) — mais tant qu'elle vivait dans `current_view` (world-space, soumis à la caméra de la Map), elle se décentrait dès qu'on l'ouvrait après avoir scrollé loin de l'étage 0. Fix : instanciée directement sous `TopBar` (`run.gd::_on_dice_shop_pressed`), suivie via `dice_shop_instance` (nettoyée dans `_show_map()`).

## Act 2 (placeholder, ajouté 2026-07-06)

But explicite de Julien : voir comment un run se déroule au-delà de l'act 1, avec du contenu recyclé mais scalé "selon nos observations d'équilibrage" — PAS du contenu final. Décisions prises par Julien avant implémentation : vrai rebalance D/P (pas un simple duplicate), Leviathan re-sert de boss final scalé, act 2 = même forme que l'act 1 (15 étages, 3 tiers, mêmes poids de salles), full heal à l'entrée d'act 2.

**Architecture — tout en runtime, zéro duplication de fichiers** (contrairement au pattern `_tier1`/`_tier2` de l'équilibrage act 1, volontairement : ~40 fichiers évités, et Julien peut tuner tout l'act depuis 2 tables) :
- **`Global.current_act`** (nouveau, `:= 1`) : remis à 1 dans `run.gd::_start_run()`, passé à 2 dans `run.gd::_enter_act_2()`. Tout le reste s'y réfère.
- **Sélection des combats** (`run.gd::_get_unique_battle_for_tier`) : en act 2, le tier act-local est remappé vers un pool source d'act 1 via `ACT2_SOURCE_TIER = {0:1, 1:2, 2:2, 3:3, 4:4}` — les étages 1-3 d'act 2 servent les combats tier-1 d'act 1, tout le reste du hallway sert le pool tier-2, élites/boss se resservent eux-mêmes. Tiers 1 et 2 d'act 2 puisent dans le MÊME pool source (tier 2 d'act 1, 9 combats) avec des multiplicateurs différents — le no-repeat (`used_battles`, comparé sur le `source_tier` maintenant) garantit qu'un combat vu en début d'act 2 ne revient pas plus profond. `used_battles` est vidé à la transition (le boss redevient éligible).
- **Scaling + reskin à l'instanciation** (`battle.gd::_apply_act2_scaling()`, appelé dans `start_battle()` ENTRE `setup_enemies()` et `reset_enemy_actions()` — l'ordre compte : les stats sont déjà des instances dupliquées (`create_instance()`), donc mutables sans contaminer les `.tres` partagés, et le premier intent affiché intègre déjà le bonus) : multiplicateur de HP (`ACT2_HP_MULT = {0:1.55, 1:1.3, 2:1.75, 3:1.75, 4:1.6}`) + **flat damage baké** (`ACT2_DAMAGE_BASE = {0:2, 1:3, 2:4, 3:5, 4:4}`) + **reskin art/nom** (voir le bullet dédié en tête de fichier, 2026-07-19). **⚠️ Changé le 2026-07-19 : le flat damage n'est PLUS un statut Muscle visible mais une `ModifierValue` FLAT invisible (`_bake_bonus_damage()`, source `"act2_power"`) sur le modifier `DMG_DEALT`** — mêmes chiffres, nourrit dégâts réels ET intent exactement comme Muscle, mais sans icône Strength (qui trahissait l'ennemi recyclé). Depuis le fix systémique du 2026-07-04, toutes les attaques ennemies appliquent `DMG_DEALT` aux dégâts réels ET à l'intent, donc ce seul modifier monte chaque coup honnêtement et visiblement. C'est un **budget par combat, pas par corps** : `max(1, base - (nb_ennemis - 1))` — un swarm multiplie déjà ses dégâts par le nombre de corps.
- **Tier act-local** : `run.gd` passe `battle_scene.act_tier = tier` à l'entrée de salle (un combat d'étage 1 d'act 2 recyclé du pool tier-1 doit être scalé comme du tier 0 d'act 2, pas comme du tier 1 — le `battle_tier` du `.tres` est le tier SOURCE, plus le tier de scaling). Fallback `battle_stats.battle_tier` si `act_tier` reste à -1 (lancements debug).
- **Transition** (`run.gd`) : `_on_battle_won` arme `act_transition_pending` si salle BOSS et act 1 ; `_show_map()` exécute `_enter_act_2()` au prochain affichage de la map (donc APRÈS l'écran de récompense — les rewards du boss ne sont pas sautées). `_enter_act_2()` : full heal, `used_battles.clear()`, pool d'events restauré depuis un snapshot pris dans `_start_run()` (`initial_event_pool` — les events consommés en act 1 re-servent en act 2), `map.generate_new_map()` + `unlock_floor(0)`.
- **`Map.generate_new_map()` est maintenant ré-exécutable** (`map.gd::_clear_map()` : libère les nœuds rooms/lines et vide `_line_edges`/`_room_lookup`) — avant, un 2e appel doublait les nœuds et laissait `_refresh_line_visibility` lire des edges libérés. Seul vrai piège technique de la fonctionnalité.
- **GGPanel act-aware** (`battle_reward.gd/.tscn`) : après le boss d'act 1 (`is_final_boss_fight` && `current_act == 1`), le panneau devient "Act 1 Complete!" (titre + texte réécrits en code, Discord caché, nouveau bouton `ContinueAct2Button` stylé avec les styleboxes partagées du shop) ; après le boss d'act 2, panneau final "thanks for playing" d'origine inchangé (defaults du `.tscn`). Le détecteur `resource_path.contains("leviathan")` de `battle.gd` fonctionne pour les deux bosses puisque c'est la même ressource.
- **Or ×1.5 en act 2** (`ACT2_GOLD_MULT`, appliqué dans `_on_battle_won`) — pour que les achats de dés #2/#3 restent atteignables sous l'escalade globale ×1.4.

**Cibles d'équilibrage utilisées** (modèle D/P de `balance_analysis_2026-07.md` §3, joueur post-act-1 estimé P≈26-30 en début d'act 2 → ~40-45 pré-boss) : t0 ~55-90 EHP / D 11-15, t1 ~75-125 / D 14-18, t2 ~100-165 / D 17-24, élites ~150-165, boss Leviathan 224 HP (coups 22+Ink3 / 19+Weak2). Contrainte clé qui a façonné les chiffres : **les HP joueur ne montent pas entre les acts (66 fixes)** — la difficulté d'act 2 passe par l'EHP (combats plus longs = plus d'attrition) et non par l'inflation des coups par hit, plafonnés vers ~35-45 % des HP courants conformément au principe §4.4 du doc d'équilibrage.

**Limites connues / à surveiller au playtest (placeholder assumé)** :
- Les montants de **block ennemis ne scalent pas** (codés en dur dans les actions, Muscle ne touche que les dégâts) — les murs de block d'act 1 deviennent relativement plus faibles. Accepté.
- Les stacks de débuffs (Weak/Ink/Exposed) ne scalent pas non plus. Accepté.
- Le multiplicateur uniforme préserve la dispersion interne des pools sources : Plant+Goblin (~88 EHP) est lourd pour du t0 d'act 2, Machopeur+2×Octopus (~112) est lourd pour du t1. Si un combat précis se révèle un mur, le levier fin = revenir au pattern `.tres` par tier pour CE combat-là.
- La campfire à +22 HP flat et les events (tous tier 0) perdent de la valeur relative en act 2 — déjà parqué comme connu dans le doc d'équilibrage.
- Après le boss d'act 2, le run se termine sur l'écran de récompense/map comme avant (pas d'act 3, comportement d'origine).
- ~~`weight = 10.0` sur `tier_0_bigger_satyrs_2.tres` et `tier_1_lurker_crab.tres`~~ — **corrigés à 1.0 (2026-07-06, validé par Julien)**, reliquats de la passe §5 du doc d'équilibrage qui les avait ratés.

**Fixes issus du premier playtest act 2 de Julien (2026-07-06, même session)** :
- **Barres de HP ennemies partiellement remplies en act 2** (label "149/149" mais barre à ~57 %) : bug d'ordre latent dans `scenes/ui/stats_ui.gd::update_stats()` — `health_bar.value` était assigné AVANT `health_bar.max_value`, et un `ProgressBar` clampe `value` contre le max COURANT au moment de l'assignation. Invisible tant que `max_health` ne bougeait jamais en cours de combat — le scaling act 2 est le premier système à le faire. Fix : `max_value` d'abord. **À retenir pour tout futur code qui augmente un max + une valeur en même temps sur un ProgressBar : toujours assigner le max en premier.**
- **Muscle off-by-one sur tous les combats act 2** (élite solo à 4 stacks au lieu de 5, repéré sur le screenshot de Julien) : `battle.tscn` contient un `CrabEnemy` placeholder posé dans l'éditeur sous `EnemyHandler` ; `setup_enemies()` le retire via `queue_free()` qui est **différé** — il est encore enfant pendant la frame où `_apply_act2_scaling()` compte les corps, gonflant le compte de 1. Fix : filtre `is_queued_for_deletion()` sur la liste. **Piège générique : tout code qui compte les enfants d'`EnemyHandler` dans la même frame que `setup_enemies()` doit exclure les nœuds en cours de libération.**
- **Bannière d'annonce d'act** (`scenes/ui/act_banner.gd`/`.tscn`, demandée par Julien façon STS) : "ACT 2: THE CATACOMBS" centré à l'écran à l'arrivée sur la map d'act 2 — même langage visuel que le `TurnBanner` de combat (même police MinionPro-Bold or/outline, punch-in + hold + fade, hold plus long à 1.4s), mais déclenchée par appel de méthode (`announce(text)`) depuis `run.gd::_enter_act_2()` plutôt que par signal (one-shot par transition). Root = `CanvasLayer` `layer = 10` pour passer au-dessus de la map/top bar/légende, instanciée dans `run.tscn`. Le nom "The Catacombs" est un placeholder choisi par Claude — à renommer quand l'act 2 aura un vrai thème.

## Game feel ajouté en session (2026-06-23/24)

- `global/shaker.gd` : nouvelle fonction `hit_stop(duration, time_scale)` (freeze bref via `Engine.time_scale`), appelée depuis `effects/damage_effect.gd` proportionnellement aux dégâts.
- Popup de dégâts (`damage_popup.gd`) repensé façon Slay the Spire (chute accélérée avec déviation latérale, plutôt qu'un simple fade sur place).
- Nouveau popup de block (`block_popup.gd` + `scenes/ui/block_popup.tscn`), branché dans `effects/block_effect.gd`.
- Couleur du popup de dégâts désormais liée au type de dé actif (`damage_popup.gd::DICE_TYPE_COLORS`), seulement quand le joueur inflige les dégâts (pas sur les attaques ennemies).
- Fix de deux bugs ennemis (Goblin, Oculus) — voir section "Bugs récurrents" plus haut.
- Arc de visée de carte (`scenes/card_target_selector/`) : dégradé de couleur + flash doré au survol d'une cible valide.
- ⚠️ Une tentative d'ajouter un cadre visuel autour du bloc de dés actif (`scenes/dices/dice.tscn`) a été essayée puis **annulée** (rendu jugé moche) — ne pas réessayer la même approche (un second Panel "FrameBackdrop" plus grand que l'Aura) sans una nouvelle validation visuelle d'abord.

## Points clarifiés par Julien (2026-06-22) — ne plus re-poser ces questions

- "Skeleton" du design = `crab` dans le code.
- `temple_defender` et `hound` sont deux ennemis réels distincts.
- Parasite = starting status d'Oculus ("Gains 3 Strength if you generate more than 15 Power in the same turn"), pas un ennemi.
- Muscle = Strength (même chose). True Strength = génère du Muscle/Strength chaque tour.
- evil dice = faces 6/6/6/0. mech dice = d6 avec ajustement ±1 power post-roll via une flèche UI.
- Pas de classe `Dice` par type : toute la logique de roll vit dans un seul `scenes/dices/dice.gd` (spaghetti reconnu par Julien).
- Recombobulate : carte normale, refuel = nombre de dés rollés ce tour (`roll_history.size()`), comportement voulu. La formulation "2 dés en rafale" du doc de contexte était imprécise, pas un bug du code.
- Statuts/dés absents du code (Vulnerable, Rune, Strict, Stuck, Gargantua-statut, Red Sensitive, Precise, Reroll, Casino, Sticky) : non implémentés, à considérer comme idées de design futures plutôt que bugs. Boost EXISTE (voir section Statuts) — ne pas le lister comme absent.
- Changer le type de dé actif en combat **existe déjà** (clic sur le type voulu dans le pool de dés, `scenes/dices/dice_interface.gd`) — ce n'est pas une fonctionnalité à construire, seulement du polish visuel éventuel.
- Le pool de cartes draftable warrior (`characters/warrior/warrior_draftable_cards.tres`) a été nettoyé le 2026-06-24 : 8 cartes retirées (toujours sur disque, juste non-draftables), plusieurs reworkées, plein de tweaks numériques. Détail complet dans `card_pool_analysis.md`.
- **Nouveau passage le 2026-07-04** : Nova et Slice retirées du pool draftable (toujours sur disque, `card_nova.tres`/`card_slice.tres` intacts, juste plus référencées dans `warrior_draftable_cards.tres`). Quatre cartes bonus-effect simplifiées en cartes à effet unique (Julien prépare une future passe pour rendre plus de cartes bonus-effect draftable — voir `tutorial_blessing_explanation_needed` / le tutoriel bonus-effect désactivé plus haut dans ce fichier) :
  - **Gang Up** : ne donne plus Infused — juste `Charge 4 Blue Dice` (était 3 + Infused 1). Requirement EXACT 6 inchangé.
  - **Catalyst** : le "Charge 1" (dé actif) qui n'était qu'un bonus (`bonus_requirement = MIN 6`) devient inconditionnel ; Boost 3 supprimé entièrement. Nouvelle desc : "Refuel your active Dice. Charge 1", `bonus_requirement` remis à NONE.
  - **Fortify** : le Gain 2 Strength qui n'était qu'un bonus (`bonus_requirement = EVEN`, coché en dur via `roll_value % 2 == 0` dans le script, indépendamment du Block qui, lui, s'appliquait toujours) devient le **requirement primaire mandatory** (`requirement = EVEN`) — Block ET Strength sont maintenant TOUS LES DEUX gatés par la même condition paire (`meets_requirement()` check ajouté en tête d'`apply_effects()`, la carte ne fait plus rien du tout sur un roll impair). Nouvelle desc : "Block X. Gain 2 Strength".
  - **Dynamite** : le +2 Boost supplémentaire sur dé rouge (`bonus_requirement = RED`, +2 en plus des 4 de base) supprimé — Boost fixe à **5** dans tous les cas, `bonus_requirement` remis à NONE. Nouvelle desc : "Boost 5".
- Philosophie de design confirmée par Julien (2026-06-24) : privilégier le fun et les "build-defining cards" plutôt que l'équilibrage strict à ce stade du développement — accepter des cartes excitantes mais peut-être trop fortes, à nerfer plus tard si besoin, plutôt que des cartes "safe" mais ternes.

## Décisions en attente (ne pas re-proposer, juste rappeler si pertinent)

- **Icône de différenciation visuelle des cartes Blessing** : le slot existe (`SupportIcon` dans `card_ui.tscn`/`card_menu_ui.tscn`, actuellement caché), en attente que Julien fournisse une icône (pas de génération d'image possible côté Claude).
- 1-2 cartes supplémentaires à couper de la liste "Keep" du pool de cartes — jamais précisées par Julien.
- Texture des dés : seul Blue est en cours (généré par la copine de Julien, itération manuelle sur les pips). Les 8 autres types restent sur l'ancienne texture plate.

## Système de sauvegarde — v1 IMPLÉMENTÉE (2026-07-07)

Sauvegarde de run à slot unique, scopée comme recommandé par la recherche du 2026-07-02 : **checkpoints sur l'écran de map uniquement, jamais en plein combat**. Quitter en plein combat/event/shop ⇒ on reprend au dernier checkpoint map (la salle n'est pas re-marquée comme choisie — techniquement ça permet de re-roll la salle en quittant, accepté pour la v1, STS1 l'empêche en sauvegardant à l'entrée de salle).

### Architecture
- **`global/save_manager.gd`** (`class_name SaveManager`, tout statique, pas un autoload) : `has_save()`/`write_save()`/`read_save()`/`delete_save()` sur `user://run_save.save`. Format = Dictionary sérialisé **`var_to_str`** (PAS JSON : JSON convertit tous les ints en floats, poison silencieux pour les index de `shop_dice_selection` ; PAS ResourceSaver : aucun risque uid/.tres, cf. incident Bullseye+, et `str_to_var` ne peut pas exécuter de code). Marche tel quel sur l'export web itch.io (`user://` → IndexedDB).
- **Cartes/reliques/battles/events sauvegardés par `resource_path`** — vérifié : tout est ressource partagée chargée par fichier à runtime (le `cards.duplicate(true)` de l'écran de récompense ne deep-copie que le TABLEAU, pas les objets Card). Les cartes ont en plus un fallback par `id` (`_build_card_id_lookup()` dans run.gd) si un futur code path duplique une Card (les duplicates perdent leur `resource_path`). Bonus : l'égalité par référence tient au chargement grâce au cache de ressources (`load(path)` re-renvoie la même instance que celle des pools — le no-repeat `used_battles.has()` continue de marcher).
- **`Map.get_save_data()`/`load_from_save_data()`** (map.gd) : le graphe est sérialisé en données plates — liens `next_rooms` stockés comme index de colonne de la rangée suivante (toujours row+1), positions en Vector2 natif via var_to_str. Seules les salles on-path (`next_rooms > 0`) + le boss sont sauvegardées. La reconstruction passe par le même `_clear_map()`/`create_map()` que la régénération act 2.
- **`run.gd`** : `_save_checkpoint()` appelé en fin de `_show_map()` (chaque retour à la map) + fin de `_start_run()` (commencer un nouveau run écrase la sauvegarde = abandon, convention roguelike). `_load_run()` (miroir de `_start_run`) déclenché par `Global.load_run_requested`, posé par le bouton **Load Run du main menu** (placeholder stylé shop-buttons, visible seulement si une sauvegarde existe). Sauvegardé : or, HP/max, deck, reliques, dés max par type, inventaire/compteurs d'achat/état du dice shop, flags tutoriel cross-room (`SAVED_TUTORIAL_FLAGS`), `used_battles`, pools d'events (restant + initial), RunStats (card_rewards/weights), act, map complète.
- **Suppression** : mort (`battle.gd::_on_player_died`) et victoire finale (boss act 2, `_on_battle_won` + flag `run_finished` qui bloque tout checkpoint ultérieur).
- **`Global.reset_run_state()`** (global.gd), appelé en tête de `_late_init` : remet TOUT l'état run-scopé aux défauts. **Corrige au passage un bug latent** : le bouton Restart de l'écran de mort rechargeait la scène Run mais l'autoload Global survivait — or/dés/act/flags du run mort fuyaient dans le "nouveau" run.
- **Ordre de restauration qui compte** : `Global.gold` AVANT `_setup_top_bar()` (gold_ui le lit à l'assignation) ; `character.max_health` AVANT `character.health` (le setter clampe) ; deck restauré AVANT `_setup_top_bar()` (qui câble deck_button/deck_view) ; la relique de départ ajoutée par `_setup_top_bar` est dédupliquée par `has_relic(id)` quand la liste sauvegardée la re-contient.

### Limites connues v1 (assumées)
- Sauvegarde web = par navigateur/appareil, perdue si le joueur vide ses données de site (Safari peut évincer l'IndexedDB) — pas de cloud save.
- Quitter en plein combat = reprendre à la map d'avant (re-roll de salle possible, voir plus haut).
- Compteurs internes de reliques (`Relic.counter`) non sauvegardés — leur cycle de vie est per-combat via `initialize_relic`, pas d'état cross-salle aujourd'hui.
- Le pool d'events partagé (`events_stats_pool.tres` muté en mémoire) reste dépendant du cache de ressources sur un restart mi-session sans passer par une sauvegarde — bug préexistant, hors scope.

Petit bug latent noté lors de la recherche, toujours vrai : `Stats._ready()`/`CharacterStats._ready()` se connectent à `Events.event_damage`, mais `Resource` n'a pas de `_ready()` — code mort depuis toujours.
