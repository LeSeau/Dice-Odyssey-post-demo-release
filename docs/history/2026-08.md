# Historique des chantiers : 2026-08

Entrées déplacées mot pour mot depuis le « TL;DR » de CLAUDE.md le 2026-09-25. Les titres `### H-###` ont été ajoutés à ce moment-là.
Ce fichier n'est pas chargé automatiquement. Chercher `H-###` ou un mot-clé, puis lire seulement la section utile.
Les statuts écrits dans les entrées (NON PLAYTESTÉ, NON COMMITTÉ...) datent du jour de l'entrée.
Une nouvelle entrée va en haut du fichier de son mois (règles : section « Où écrire les comptes-rendus » de CLAUDE.md).

### H-024 (2026-08-29) Summon du panneau de Scout (comète + dépliage)

- **🔮 SUMMON DU PANNEAU DE SCOUT (2026-08-29, demande de Julien : « something from the moment you play the card and the panel appears ; the panel apparition itself should also be animated ») — plan : [scout_panel_animation_plan_2026-08.md](scout_panel_animation_plan_2026-08.md) — VÉRIFIÉ PAR HARNAIS (`debug_scout_summon.gd`/`.tscn` racine, non committé : 17 checks 0 fail, CONTRÔLE NÉGATIF FAIT) + STRIP MOVIE MAKER. NON PLAYTESTÉ.**
  - **Le constat : RIEN ne reliait la CARTE au panneau.** Les seules motes montaient du DÉ, et le panneau lui-même faisait un fade+pop générique de 0.18s — la carte la plus cérémonielle du jeu se résolvait en « un élément d'UI apparaît ». **La lecture retenue = « la carte pose la question, le dé répond »** : une comète part du point de RELÂCHÉ de la carte et ACCÉLÈRE (EASE_IN — elle est TIRÉE vers le futur, miroir exact du pick flight tiré vers le bas), frappe, et le panneau **se déplie depuis une couture de lumière horizontale**. **Les motes dé→panneau et le reveal flicker/lock ne bougent PAS d'un octet** (c'est la moitié que Julien aime déjà) — ils sont juste CRÉÉS à l'impact, donc zéro comptabilité d'offset.
  - **⚠️⚠️ 3 RATÉS QUE SEUL LE STRIP POUVAIT ATTRAPER, tous shippés au 1er jet** : (1) **le dépliage était INVISIBLE** — `TRANS_BACK/EASE_OUT` mettait le panneau à pleine hauteur en **2 frames** (back-out front-loade tout son trajet, même piège que l'EXPO de l'onde de charge) → il ne restait qu'un fade. **Fix : `TRANS_CUBIC/EASE_OUT` vers un overshoot 1.07 sur 0.22s PUIS un settle SINE de 0.09s** — le trajet devient lisible (~5 frames) et le snap vit dans le settle. **Règle : pour un mouvement qu'on doit VOIR voyager, jamais BACK/EXPO sur le trajet lui-même ; l'overshoot s'achète avec une 2e étape.** (2) **le bloom d'impact TENAIT sa crête** (`EASE_IN` sur un fade-out garde l'alpha à ~1 puis chute tard) → une boule blanche de 96px posée ~5 frames sur le panneau, cachant le titre et la 1re face — **répétition exacte du bug de crête tenue du ghost de charge-delivery** ; fix = alpha sur sa PROPRE courbe plus courte (`EASE_OUT`, `durée × 0.6`) pendant que le scale continue, + bloom 80px (sous les 104px du panneau, donc jamais un couvercle). (3) **comète trop fine et trop blanche** — 30px + traînée clairsemée = point pâle sur la pierre claire d'act 1, et `accent.lerp(WHITE, 0.45)` saturait en additif vers un spark blanc générique qui jetait l'identité du dé. **Fix mesuré sur le strip : 42px, spacing de traînée 0.14→0.09, lerp blanc 0.45→0.32** (la traînée lit enfin bleue sur un dé Blue). **Leçon MASSE > FORME, encore une fois.**
  - **⚠️ FUITE PRÉEXISTANTE DURCIE AU PASSAGE (elle n'est PAS de cette passe)** : `_kill_scout_tweens()` tuait les tweens des motes d'ouverture, or **tuer un tween SAUTE son `tween_callback(queue_free)`** → un pick rapide (ou un 2e scout en vol) laissait des nœuds à l'écran POUR TOUJOURS. Invisible jusqu'ici seulement parce que les motes meurent d'elles-mêmes en ~1s. Nouveau registre **`_scout_fx_nodes`** vidé par `_kill_scout_tweens`. ⚠️ **Le flyer et la traînée du PICK n'y sont surtout PAS enregistrés** : ce vol doit délibérément survivre à `_close_scout_panel` (son tween n'est d'ailleurs pas dans `_scout_tweens` pour la même raison).
  - **Origine = la règle des power orbs, copiée telle quelle** : point de relâché (`Global.last_played_card_position`) si `card_played` est tombé sur LA MÊME FRAME (nouveau stamp `_last_card_played_frame`, `Card.play()` émet `card_played` en 1re ligne puis appelle `apply_effects` synchronement) ET pas `playing_red_card` ; sinon centre du dé actif ; **sinon (pas de dé du tout) on retourne la destination**, ce qui fait échouer le check de distance et ouvre instantanément. **2 fallbacks vers l'ouverture immédiate** : pas d'`ui_layer` (harnais/boot) et relâché à moins de 80px du panneau (un vol de 3 frames lit comme un glitch). **Le panneau se déplie dans TOUS les cas** — seul le voyage devant est conditionnel.
  - **⚠️ Le pivot du panneau est emprunté puis RENDU** : `_resize_scout_panel` l'écrit bottom-center (le fold de fermeture en dépend), le dépliage a besoin du CENTRE → `_restore_scout_panel_pivot()` en fin de tween d'ouverture, ET dans `_finish_scout_close` (filet pour le cas « Exit cliqué pendant les 0.2s du dépliage », qui tue le tween qui aurait rendu le pivot ; fait après le `hide()` donc jamais de saut visible).
  - **⚠️ BUG ATTRAPÉ AU 1er PLAYTEST (Julien : « le halo reste dans le scout panel, en plein centre, un peu dans le dé du milieu ») : LA COMÈTE N'ÉTAIT JAMAIS LIBÉRÉE.** Aucun `tween_callback(queue_free)` en fin de vol — elle se garait donc au centre du panneau à pleine luminosité jusqu'au prochain `_kill_scout_tweens` (pick / close / re-scout). Le vol du PICK, lui, freeait son flyer depuis toujours. Fix : `flight.tween_callback(comet.queue_free)` avant le callback d'ouverture, le bloom d'impact né la même frame couvre le handoff.
  - **⚠⚠ POURQUOI LE HARNAIS NE L'A PAS VU (trou de méthode à retenir) : la section D ne comptait les FX qu'APRÈS UN PICK**, or un pick appelle `_kill_scout_tweens` qui free tout à la main — donc un nœud qui ne se libère jamais LUI-MÊME y était structurellement invisible. **Nouvelle section F : ouvrir un scout, laisser tout se poser SANS pick ni close, asserter 0 nœud vivant.** Contrôle négatif fait (free retiré ⇒ F2 échoue à exactement 1 nœud). **Règle : tout test de cleanup doit avoir un cas qui n'appelle PAS le chemin de cleanup manuel.** J'avais en plus VU le blob sur le strip et je l'ai expliqué (« la queue du bloom ») au lieu de le chasser — ne jamais rationaliser une anomalie sur un rendu.
  - **Coût : la 1re face cliquable passe de ~0.55s à ~1.13s après le play.** Compresseurs si ça traîne au playtest : `SCOUT_SUMMON_FLIGHT 0.35→0.28` et démarrer le 1er flicker à la couture au lieu de la fin du dépliage. Tous les leviers sont dans le bloc `SCOUT_SUMMON_*`/`SCOUT_UNFURL_*` en tête de la section scout de `battle.gd`.
  - **⚠️ `debug_layout_probe.gd` a dû passer d'un COMPTE DE FRAMES à un timer de temps de jeu** : il mesure le rect du panneau, et un compte de frames vaut un temps totalement différent selon la vitesse de rendu. Re-passé : panneau inchangé à `(401,88) 408×104`, topGap 8 / rowGap 10.
  - **Son** : le pizzicato de cast part maintenant AU LANCEMENT (t=0 ne doit pas être muet pendant que la comète voyage) ; le dépliage prend le **pizzicato de VIOLA** (`sfx/374388__sgossner__...`, asset déjà importé et JAMAIS utilisé — le frère du violoncelle du cast, donc les 2 beats riment sans nouvel asset). **PLACEHOLDER**, à trancher par Julien.
  - **⚠️ Tutoriel (T3.2→T3.3) à surveiller** : le director `_wait` sur `scout_effect` et avance instantanément, donc son texte « Three possible rolls… » s'affiche ~0.5s avant que les faces existent. Inoffensif mécaniquement (le gate passe par `tutorial_scout_allowed_index`, appliqué avant les callbacks de clickabilité — harnais E1 le vérifie ; et le stuck-guard est à 2.5s ≫ 0.55s). Si ça lit sale : gater les beats 0-2 sur `Global.tutorial_on`.
  - **Effet de bord connu** : avec la Blessing **Foresight** (qui écoute `scout_effect` → Charge 1), un scout lance la comète vers le HAUT pendant qu'une livraison de charge descend vers la rangée. Rare, directions opposées, assumé. ⚠️ **REDÉMARRER L'ÉDITEUR avant de jouer** (`battle.gd` édité hors éditeur ; pas de nouveau `class_name`/`@export`, donc pas de risque de strip).

### H-025 (2026-08-29) Pulse de charge round 3 : wind-up + shockwave (+ sample de charge rogné)

- **💥 PULSE DE CHARGE — ROUND 3 : WIND-UP + VRAI SHOCKWAVE (2026-08-29, retour de Julien sur le round 2 : « ça semble rushed, on a un pulse très rapide instantanément après que les dés chargés arrivent dans la dice interface. Je veux plus : les dés arrivent, très petite anticipation, BOOM ! pulse. Et plus un shockwave FORT qu'un pulse rapide ») — VÉRIFIÉ PAR HARNAIS (`debug_charge_pulse` **18 checks 0 fail**, dont 3 nouveaux + CONTRÔLE NÉGATIF : avec l'ancien timing E1/E2/E3 échouent tous les 3) + STRIP MOVIE MAKER + COMPARATIF DE FORME EN DIFF DE PIXELS. NON PLAYTESTÉ.**
  - **La phrase est maintenant : atterrissage (clack + kick de panneau + flash de slot) → 0.22s de WIND-UP → BOOM.** Pendant le wind-up, l'anneau de particules convergentes se referme sur le dé ET **l'aura INSPIRE** (`CHARGE_AURA_INHALE` 0.78 : `power_intensity` et `glow_reach` baissent en SINE EASE_IN) — un trou silencieux se lirait comme du lag, un trou pendant lequel la lumière se RÉTRACTE se lit comme un armement. Puis tout part sur UNE frame : gust + flash d'aura + surge d'emanation + punch d'absorption + hit-stop.
  - **⚠️⚠️ LA VRAIE CAUSE DU « RUSHED » N'ÉTAIT PAS UN RÉGLAGE : le gust partait à t=0 et le punch d'absorption à t=0.30**, chacun sur sa propre constante — deux événements sans rapport dont le premier était fini avant que le second commence. **`CHARGE_PULSE_ANTICIPATION` est maintenant LA constante partagée** (le gust ET `converge_time` de la cérémonie d'absorption en dérivent). Mesuré : gust à 264ms et punch à 248ms après l'atterrissage, soit **16ms d'écart** (contre 39ms vs 405ms sur le contrôle négatif). **Ne jamais re-forker ces deux timings.**
  - **Les particules sont TIME-SCALÉES pour atterrir sur le boom** (`gpu_particles_2d.speed_scale = CHARGE_PARTICLE_CONVERGE / CHARGE_PULSE_ANTICIPATION`) — l'anneau met ~0.33s à converger à speed_scale 1 (rayon d'émission 85px sous ~1500px/s² d'accel rentrante), donc sans ça il finirait de tomber APRÈS l'explosion. `speed_scale` et pas un édit de `lifetime` : la lifetime vit dans le sub-resource authoré du `.tscn` et l'émetteur est réutilisé à chaque volée.
  - **« Punchy » (mode 2, celui qui SHIPPE) ne veut plus dire PLUS RAPIDE mais PLUS LOURD** — c'était littéralement la qualité dont Julien se plaignait (l'ancien mode 2 multipliait la durée par **0.78**). Nouveau : travel **0.34 → 0.50s**, reach **69 → 87px**, `GUST_WIDTH` **20 → 25**, cap de peak **1.25 → 1.35**, et surtout un **HOLD de 0.10s au peak** (`CHARGE_GUST_HOLD`) avant que la décroissance démarre, pendant que le rayon continue d'avancer sur son tween séparé. C'est le hold qui transforme un clignotement en front d'onde.
  - **⚠️ LE HIT-STOP DE VOLLEY DÉMÉNAGE de `dice_interface.gd` vers `dice.gd`** : le freeze doit ponctuer la frame la plus forte, et ce n'est plus l'atterrissage mais la détonation 0.22s plus tard ; deux freezes si rapprochés se lisent comme du stutter. L'atterrissage garde clack + kick + flash de slot + motes. Effet de bord VOULU : le freeze tombe pile sur le front, donc sa montée et son hold jouent en slow-mo (plateau mesuré **238ms réels**). ⚠️ **À surveiller au playtest : l'atterrissage n'a plus AUCUN freeze** — si ça lit trop léger, le 1er levier est d'y remettre un mini hit-stop (~0.04s), pas d'annuler le déplacement.
  - **⚠️ L'AUDIO DU BANG DÉMÉNAGE AUSSI (2026-08-29, retour de playtest de Julien : « much better animation but the sfx should trigger when the dice pulse happens »).** `chargedicesound.mp3` partait au LANCEMENT à **+4 dB** — le moment le plus fort de toute la phrase, ~1s avant que quoi que ce soit n'explose — pendant que la détonation n'avait RIEN et l'atterrissage un simple clack à -8 dB : **la plus grosse image avait le plus petit son.** Maintenant : le lancement garde un cue léger (pitch 1.12, **-7 dB**), et la détonation joue le boom à **+4 dB** avec un **pitch qui BAISSE selon le count** (`CHARGE_BOOM_PITCH_*`, Charge 4 plus lourd qu'un Charge 1 — le même ladder que l'amplitude du front), **UN SEUL sample.** ⚠️ Un clack grave avait été ajouté en couche de corps et a dû ressortir immédiatement (Julien, même soirée : « there is also a dice roll sfx when the pulse happens, not sure why ») — le seul clack grave du projet est `dicerollsound3`, et le ralentir ne l'empêche pas de se lire comme un dé qu'on lance. **Toute future couche ici doit être un sample qui n'est PAS un dé.** **Les plinks par dé ne bougent PAS** : ils SONT le wind-up, pas le payoff. `CHARGE_FINAL_SOUND` disparaît de `dice_interface.gd` (plus aucun usage).
  - **⚠️ LE COLORECT `Emanation` GRANDIT 320×300 → 380×340** (offsets -113/-123/267/217, `die_center_uv` 0.5/0.588235) — c'était LE plafond du reach : avec 86px de dégagement latéral, un front à 87px se faisait TRANCHER par le bord droit du rect. **Le glow au repos est identique au pixel près** : tout le shader travaille en px die-centered dérivés de `rect_size` + `die_center_uv`, donc le rect ne gagne que du headroom. **⚠️ Si quelqu'un rebouge ce nœud, les 2 uniformes DOIVENT suivre** : centre du dé = `(77 - offset_left, 77 - offset_top)` en local ActiveDice (le Panel fait 154×154 à l'origine, centre 77,77).
  - **⚠️ Nouveau fade de bord dans le shader (`EDGE_FADE_PX = 18`)** : une lumière encore vive au bord du ColorRect se fait couper par un rectangle parfaitement droit — invisible sur les langues ambiantes, criant sur un front lumineux (même famille d'échec que l'encoche `row_side` du round 2). C'est ce fade qui rend l'agrandissement du reach SÛR. Vérifié au strip : brillance résiduelle 0.0 sur les bords gauche/droite/haut, 13 max en bas (et le bas est derrière le bouton ROLL, z 12).
  - **⚠️ Nouveau : le front S'ARRONDIT en voyageant** (`GUST_ROUND_PX 70` / `GUST_ROUND_MAX 0.85`, blend de la distance SDF vers une distance RADIALE au fur et à mesure que `gust_radius` grandit). **Trouvé au rendu, pas au code** : garder l'isoline de la boîte arrondie jusqu'à 87px faisait lire l'onde comme **DEUX DALLES VERTICALES** flanquant le dé — le row clearance cache le haut de l'anneau et le bouton ROLL le bas, donc les côtés DROITS de la boîte étaient la seule partie visible. En blendant vers le radial, la naissance épouse toujours la silhouette (le read « c'est sorti de CET objet ») et le voyage redevient un arc. **Prouvé par diff de pixels amplifié côte à côte** — la version boîte rend un rectangle à coins arrondis, la version arrondie un vrai ovale.
  - **⚠️ EFFET DE BORD ASSUMÉ : `_play_overcharge_escalation` (franchissement T2/T3) réutilise `CHARGE_GUST_REACH`/`CHARGE_GUST_TIME`**, donc son onde grossit aussi (+26% de reach, +14% de durée). C'est cohérent avec son propre commentaire (« le même wavefront qu'une charge ») mais ça n'a pas été rendu — à regarder au playtest en overcharge. Le gust spontané des fuites d'overcharge, lui, a ses propres constantes (`OVERCHARGE_GUST_*`) et ne bouge pas.
  - **⚠️⚠️ PIÈGE DE HARNAIS ATTRAPÉ (un check qui ne pouvait pas échouer) : la 1re version de E3 mesurait le plateau du front en HORLOGE MURALE et PASSAIT contre le contrôle négatif** — le hit-stop étire tellement la décroissance qu'un front SANS hold traîne quand même 167ms réels près du peak. Mesuré en **TEMPS DE JEU** (`get_process_delta_time()` accumulé, donc l'horloge sur laquelle tournent les tweens) le plateau est sans ambiguïté : **118-132ms avec hold contre 33ms sans**. Seuil à 0.995×peak et pas 0.9 : une décroissance QUAD ease-in reste à moins de 10% du peak pendant ~0.14s toute seule.
  - **⚠️ C1 du harnais RENDU ROBUSTE (il était devenu FLAKY, mesuré 1 run sur 3)** : il comptait « un front = un saut en arrière de `gust_radius` », or **la distance déjà parcourue par le 1er front au moment où le 2e part dépend maintenant de combien de temps de jeu le hit-stop a mangé** — donc d'une durée RÉELLE qui varie d'un run à l'autre. Il compte désormais les **changements de `gust_color`** (une couleur par `_fire_gust`, et un charge multi-type = 2 types différents) : ça teste exactement ce que le check prétend tester (le bam-bam en 2 couleurs) sans dépendre du timing. Le compte de resets de rayon reste imprimé en diagnostic. **Un harnais flaky crie au loup — c'est le même piège que `debug_card_batch_check`.**
  - **Mesures avant/après** : bande extérieure à l'arrivée du front **0.586 contre 0.131** au round 2 (≈4.5× plus lumineux à distance), trajet **-14 → 87px**, lumière ajoutée au pic **53k px** sur un Charge 3 (contre 40k sur un Charge 1 — le ladder de count se voit), pic de lumière **~230ms après l'atterrissage** (contre ~0ms avant).
  - **⚠⚠ « LE SON ARRIVE TROP TARD APRÈS LE PULSE » — C'ÉTAIT LE FICHIER, PAS LE CODE (2026-08-29, 2e retour de playtest).** `SFXPlayer.play` est appelé dans la FRAME MÊME du gust, mais `chargedicesound.mp3` avait **273ms de silence numérique pur en tête** et ne montait à son pic qu'à **~390ms**. **C'est EXACTEMENT le piège déjà vécu le 2026-07-09 sur les sons de roll** (dicerollsound1 avait 341ms de silence, même diagnostic, même fix). Aggravant : le boom joue à **pitch 0.82-0.94** (le ladder de count), et un pitch bas RALENTIT la lecture — donc le retard s'étirait à ~476ms sur un Charge 4. **Fix : le fichier est trimmé en place à 0.345s + fade-in de 5ms** (l'original intact est archivé dans `daiso_archive_2026-07-31\original_audio\chargedicesound_ORIGINAL_untrimmed.mp3`, ré-encodé au même 256k/48kHz/stéréo). Mesuré après : audible à **0ms**, pic plein à **50ms** (contre 390). ⚠️ **MÉTHODO À RÉUTILISER : avant de chercher un bug d'ordre/timing dans le code, mesurer l'ENVELOPPE du sample** (`ffmpeg -af silencedetect` puis un profil RMS/peak par tranches de 10ms) — un son « en retard » dont le code tire au bon moment est presque toujours un fichier à attaque lente. ⚠️ Le même fichier sert AUSSI de cue de lancement (`dice_interface.gd`, -7 dB) : il devient plus sec là aussi, ce qui est souhaitable.
  - **🔊 SON DE MAX ROLL : `dicecrush2` → `detonationsound.mp3`** (2026-08-29, Julien : « my dice crush sfx is too aggressive »). ⚠️ **Les deux samples ne sont PAS interchangeables au même gain, mesuré** : dicecrush2 = mean **-8.1 dB** / peak **0.0 dB** sur **3.24s** (un crunch chaud, clippé, qui traîne) ; detonationsound = mean **-16.2 dB** / peak **-1.0 dB** sur **2.16s**. Un swap sec aurait fait TOMBER la célébration de ~8 dB et l'aurait enterrée — le gain au site d'appel passe donc de **-2.0 à +4.0 dB** (`dice.gd`, branche `is_max_roll`), (posé à +2.0 d'abord, Julien en a demandé plus le même soir) : **~2 dB plus doux qu'avant**, et c'est l'ENVELOPPE qui fait le reste du travail « moins agressif », pas le volume. ⚠️ **`detonationsound.mp3` est AUSSI le `HIT_SOUND` de `status_earthquake.gd`** — les deux se croisent rarement (un roll chanceux vs un payout de début de tour) mais c'est désormais un boom PARTAGÉ, plus un son qui ne veut dire qu'une chose. L'override debug (`Global.debug_high_roll_sound`) continue de passer par-dessus, `impact1.ogg` reste en place (son de carte sur ~40 `.tres`).
  - **Leviers de tuning** : `CHARGE_PULSE_ANTICIPATION` (le gap — le baisser si ça lit comme du lag), `CHARGE_GUST_*` et `CHARGE_AURA_INHALE` en tête de `dice.gd` ; `GUST_WIDTH`/`GUST_ROUND_*`/`EDGE_FADE_PX` dans `dice_emanation.gdshader`.
  - ⚠️ **Le worktree a dû refaire un `--headless --import`** (cache périmé : `status_strength_icon.png` sans `.ctex`, le harnais hangeait). Ça churne ~700 `.import` — **ne PAS les committer**. ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** (`dice.gd`, `dice_interface.gd`, `dice.tscn` et le shader édités hors éditeur).

### H-026 (2026-08-29) Throw ≠ roll : séparation complète (+ double emit du dé rouge)

- **🎯 THROW ≠ ROLL — SÉPARATION COMPLÈTE (2026-08-29, demande de Julien : « differentiate throw from roll ; strength does not affect it, throw does not count towards dice roll (dice slap, relics, ...) ») — VÉRIFIÉ PAR HARNAIS (`debug_throw_connections.gd` INVERSÉ EN PLACE, committé : 62 checks 0 fail, **DEUX CONTRÔLES NÉGATIFS**) + régressions (relic_batch 228/228, golem 12/12, ricochet 26/26, attack_anim 30/30). NON PLAYTESTÉ. Plan + as-built : [throw_roll_separation_plan_2026-08.md](throw_roll_separation_plan_2026-08.md).**
  - **Le contrat, en une ligne : un dé JETÉ n'est PAS un dé roulé.** Il inflige sa face BRUTE, plus le bonus plat de Trebuchet, multiplié par l'Exposed de LA CIBLE — et ne touche rien d'autre.
  - **⚠️ LA MOITIÉ DE LA DEMANDE ÉTAIT DÉJÀ LIVRÉE : la Strength ne s'applique plus aux dés jetés depuis le 2026-08-20.** Seuls 2 commentaires périmés (fastball, dice_avalanche) affirmaient encore le contraire — supprimés. **`all_in.gd` garde volontairement la Strength** : son lump est le dégât DE LA CARTE (Power + somme des faces dépensées en UN hit), pas le dégât d'un dé.
  - **TOUT passait par UNE fonction, `global.gd::report_thrown_die_landed()`** — elle bumpait 5 compteurs puis émettait. Gutée : elle ne fait plus qu'émettre. Les 5 compteurs que les throws ne touchent plus : `fight_dice_rolled` (Tsunami, Crown, Metronome, Sixth Gear, Greedy), `dice_amount_rolled_this_turn` (Assault, Stampede, Turbo Mode), `dice_types_rolled_this_turn` (Spectrum, Prismatic Lens), `sixes_rolled_this_fight` (Jackpot), `run_stat_dice_rolled` (scoreboard de fin de run). **Dice Slap était DÉJÀ correct** (il lit `roll_history`, que les throws n'ont jamais touché — c'est le modèle que le reste rejoint).
  - **18 opt-ins débranchés** (13 reliques : consolation_chip, crown, giants_signet, house_money, hunting_bow, jackpot_pin, metronome, needle_die, prismatic_lens, sixth_gear, snake_eyes_charm, the_one, underdog_ring ; 4 statuts : effigy, greedy, ruptured, status_hardened_grip ; + le refresh de description de `card_ui.gd`). **`Events.dice_thrown_landed` SURVIT avec ZÉRO listener** — c'est le crochet pour du contenu délibérément *de throw* (comme Trebuchet). Le commentaire de contrat dans `events.gd` le dit explicitement : n'y brancher qu'un payoff de THROW, jamais un payoff de ROLL.
  - **⚠️ 2 VRAIS BUGS RÉPARÉS GRATUITEMENT PAR LA SÉPARATION.** (a) **Artillery tuait le bonus « premier roll » d'Assault** : son dé de début de tour atterrissait comme « dé n°1 du tour », donc ton vrai premier roll comptait comme le n°2 — et il pré-remplissait le set arc-en-ciel de Spectrum/Prismatic Lens. Pire, c'était une **RACE** (le dé atterrit ~0.95s après, avant ou après ton premier roll selon l'horloge réelle). (b) **Dice Avalanche offrait Spectrum et Prismatic Lens** : il conjure un dé de CHAQUE type possédé, donc une carte maxait Spectrum et satisfaisait « 4 types ce tour » sans rouler un seul dé.
  - **⚠️⚠️ CORRECTION FACTUELLE — NE PAS RE-PROPAGER : mon affirmation « les rolls Rouges ne comptent pas comme des rolls » était FAUSSE.** Le plan d'origine (§6) prétendait que Metronome/Sixth Gear rataient les rolls Red parce que `dice.gd` émet `red_dice_rolled` au lieu de `dice_rolled`. **Mesuré en moteur (harnais `debug_red_roll_counts.gd`/`.tscn`, racine, non committé) : FAUX.** Le `dice_rolled` est bien émis, juste DIFFÉRÉ — par `card_ui.gd::_on_red_dice_rolled` quand la carte socketée résout, ou par `_fire_socketless_red` pour Armageddon ; et `roll_dice()` refuse un roll Red sans l'un des deux, donc il n'y a pas de 3e chemin. **Un roll Red bump `fight_dice_rolled` de 1 et émet exactement un `dice_rolled`, identique à un Blue. Leçon : j'avais lu la moitié du chemin et conclu — mesurer avant d'affirmer.**
  - **⚠️ EN REVANCHE le harnais a trouvé un VRAI bug à côté, et il est réparé : avec DEUX cartes socketées (Dual Cannon, `red_socket_capacity = 2`, ET il est dans le pool draftable), UN roll Red émettait `dice_rolled` DEUX FOIS** pendant que `fight_dice_rolled` ne bougeait que de 1 — chaque `CardUI` dont l'id est dans `charged_card_instance_ids` émettait le sien. Mesuré `[["red",2],["red",2]]`. Conséquences live : Hunting Bow 6 dégâts au lieu de 3, Snake Eyes 2 Strength au lieu de 1, Needle Die 6 au lieu de 3, Underdog Ring 4 Block au lieu de 2, Sixth Gear +12 Power au lieu de +6, Metronome tirant son AoE de 20 deux fois. **⚠️ C'ÉTAIT DÉJÀ CONNU MAIS PATCHÉ AU MAUVAIS ENDROIT** : le `_last_roll_token` d'`effigy.gd`/`ruptured.gd` existe précisément à cause du rapport de Julien du 2026-08-16 (« it triggers twice after I roll red ») — ces deux-là étaient donc immunisés, et Greedy aussi, mais aucune relique ne l'était. **Fix à la SOURCE : nouveau one-shot `Global.red_roll_pending_report`**, armé par `dice.gd` juste avant l'emit de `red_dice_rolled`, consommé par le premier émetteur (card_ui ou `_fire_socketless_red`), reset par combat dans `battle.gd`. **⚠️ Ne change PAS le MOMENT de l'emit** — le différé est porteur (`dice_interface` décrémente le dé Red sur `dice_rolled`, et ça doit rester après la résolution de la carte). Vérifié 13/13.
  - **⚠️ TEXTE JOUEUR : le tooltip du mot-clé « Throw » enseignait littéralement la règle supprimée** (« Rolls a bonus Dice without using any of your own. ») — c'est la seule ligne de copy qui l'apprenait, réécrite en « An extra Dice that resolves on its own. It builds no Power and never counts as a roll. » **⚠️ Le panneau de tooltip est à HAUTEUR FIXE et CLIPPE en silence au-delà de 3 lignes** (~90 caractères à cette largeur) — un premier jet de 178 caractères a été raccourci pour cette raison, ne pas rallonger sans re-rendre. **Aucune description de carte n'a bougé** : Tsunami (« for each Dice rolled this combat »), Stampede, Spectrum, Jackpot deviennent tous *strictement plus vrais* — c'est le signe que la règle est la bonne.
  - **⚠️ LE HARNAIS A ÉTÉ INVERSÉ EN PLACE, PAS RÉÉCRIT** : `debug_throw_connections.gd` (committé) assertait l'ancienne règle depuis le 2026-07-23 ; chaque check est devenu son inverse, **plus la moitié positive que l'ancien ne couvrait pas** (un throw inflige bien sa face brute / Trebuchet s'applique / l'Exposed de la cible s'applique / la Strength NON). **DEUX contrôles négatifs faits** : (1) restaurer les bumps de compteurs → 6 checks rouges ; (2) rebrancher un listener sur Hunting Bow → le check de source ROUGE **et** le check comportemental « no enemy damage » rouge (40 → 32 HP). **⚠️ Ce 2e contrôle a révélé un trou dans mon propre harnais** : Hunting Bow frappe un ennemi AU HASARD, donc lire `_enemies()[0]` laissait passer un listener réintroduit une fois sur deux — la section D somme maintenant les HP de TOUS les ennemis.
  - **⚠️ Le pool est à 85 cartes dans ce worktree, pas 87** : `debug_card_review_batch` échoue 14 checks « resource missing » sur 5 `.tres` (hoard, weighted_dice, second_socket, red_edge_plus, socketless_red_plus) **absents du git HEAD de cette branche** — préexistant, aucun `.tres` n'a été touché par cette passe. `debug_relic_rework.tscn` n'existe pas ici non plus.
  - **⚠️ BALANCE À SURVEILLER — c'est un VRAI nerf de l'archétype Throw** : Dice Avalanche / Meteor / Pixie Volley / Rampart / Windfall / **Artillery** perdent TOUTE leur surface de synergie et deviennent des dégâts bruts scalés par une seule chose. **Trebuchet devient le SEUL scaler de throw du jeu** (Uncommon, +3 / +4 par dé) — si Throw doit rester un archétype, il lui faut 1-2 payoffs dédiés ; sinon c'est assumé que les throws sont un plancher de dégâts non-buildable. Aussi : Gargantua devient plus facile pour un deck throw (Greedy ne mange plus les jets), Hardened Grip + throws disparaît, et Crown/Metronome/Sixth Gear se remplissent plus lentement (leurs seuils 10/20/8 ont été tunés dans un monde où les throws comptaient).
  - **Décisions prises seul, à retourner si besoin (§10 du plan)** : throws exclus du scoreboard « Dice Rolled » **sans ajouter de ligne « Dice Thrown »** (1 ligne dans `run_stats_panel.gd` si voulu) ; **Turbo Mode laissé à 8 dés/tour** (Dice Avalanche ne le débloque plus gratuitement, il devient un vrai achievement — et nettement plus dur) ; **aucun nouveau payoff de throw ajouté**. Pas de renommage : `report_thrown_die_landed` et `dice_thrown_landed` disent déjà « thrown », jamais « rolled » — seuls leurs commentaires mentaient.
  - ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** (~25 `.gd` édités hors éditeur dont l'autoload `global.gd`, qui gagne une variable). ⚠️ Ce worktree a eu besoin d'un `--headless --import` (cache périmé) : les `.import` et `.godot/` churnés sont des **artefacts de build locaux, à NE PAS committer** ; les `.gdshader` marqués `M` par `git status` sont du bruit de mtime, `git diff` dessus est vide.

### H-027 (2026-08-29) 3 coupes, Ringer en Blessing, Parasite sur tout le Power, odds des reliques, skip du tuto

- **🧪 PASSE DU 2026-08-29 : 3 COUPES DE CARTES + RINGER EN BLESSING + PARASITE COMPTE TOUT LE POWER + AUDIT DES ODDS DE RELIQUES — VÉRIFIÉ PAR HARNAIS (`debug_parasite_power` 12/12 avec CONTRÔLE NÉGATIF, `debug_ringer_check` 16/16, `debug_relic_odds` 8/8) + RÉGRESSIONS (ricochet 26/26, golem 12/12). NON PLAYTESTÉ.**
  - **Coupées du pool draftable (fichiers laissés sur disque, convention) : Critical Edge, Bulletproof, Experiment.** Pool **85 → 82**. Vérifié : aucune des 3 n'était référencée ailleurs (pas d'`upgraded_version`, pas d'autre pool).
  - **Ringer passe SKILL → BLESSING, Min 5 → Min 6, `exhausts = true`** (le gate Min 6 + exhaust est la forme standard de TOUTES les Blessings du pool, vérifié sur les 42 `.tres` de type 2). **Ringer+ : Min 6 et Surge 2.** ⚠️ **Ringer+ a dû recevoir son PROPRE script `ringer_plus.gd`** : le montant vit dans `const SURGE_AMOUNT` et une Card n'a pas de champ payload équivalent au `stacks` d'un Status — le pattern « stacks-comme-payload » ne s'applique qu'aux statuts. Corps identique à `ringer.gd`, seule la const diffère : **les modifier ensemble.** Gains gratuits du passage en Blessing : tooltip Blessing auto (généré depuis `type`, pas depuis un tag) et bypass Prayer Beads (`_requirement_bypassed()` est scopé sur `type == BLESSING`).
  - **⚠️ VRAI BUG : `Global.power_generated_this_turn` n'était crédité QUE par les rolls** (`dice.gd::_apply_roll_result`, `+= last_roll`, seul écrivain). Donc Reinforce/Blaze/mech +1/relique-qui-paie ne le bougeaient PAS, et **Parasite (Oculus) ne voyait pas un tour à gros Power de cartes** alors que son tooltip promet depuis toujours « generate more than 15 Power in a turn » — le code ne matchait pas son propre texte. **Fix à la SOURCE dans `dice.gd::_on_change_current_power` : `if roll_value > old_power: power_generated_this_turn += delta`.** ⚠️ **Le non-double-comptage est structurel, pas une coïncidence** : `_apply_roll_result` crédite lui-même PUIS appelle `_set_power_text` (qui resynchronise `_last_shown_power`) et **n'émet jamais ce signal** — donc toute hausse encore visible ici est par construction du Power non-roll. Seules les HAUSSES comptent (un reset / switch de type / rewind Ricochet ne doit jamais soustraire ; le rewind resynchronise le texte AVANT d'émettre, donc il arrive ici en delta zéro).
  - **`parasite.gd` écoute maintenant AUSSI `Events.change_current_power`** (les 2 hooks partagent un `_evaluate()`, le badge ne ré-émet que si le nombre bouge). Ordre garanti par construction : `dice.gd` connecte dans son `_ready()` (instanciation de battle.tscn) donc strictement AVANT que `start_battle()` initialise les statuts d'ennemis ; le hook `dice_rolled` reste en ceinture-bretelles.
  - **⚠️ EFFET DE BORD VOULU : Crescendo (« Deal damage equal to all Power generated this turn ») voit maintenant le Power des cartes lui aussi.** C'est une mise en conformité avec son propre texte, pas un buff accidentel — mais c'est un vrai buff, à surveiller au playtest. `AchievementManager.add_stat("power_generated")` n'a PAS été touché (stat lifetime, hors sujet).
  - **⚠️ PIÈGE DE HARNAIS NOUVEAU ET COÛTEUX : la ressource Muscle/Strength a l'id `"strength"`, PAS `"muscle"`** (`statuses/muscle.tres`). Un `_has_status("muscle")` rend silencieusement 0 et transforme chaque assertion Parasite en FAUX ÉCHEC — c'est ce qui a fait croire que le fix ne marchait pas. + le piège déjà documenté `var x := Global.<membre non typé>` = parse error → harnais qui PEND à l'infini (gdtoolkit dit PROPRE).
  - **🎲 ODDS DE RELIQUES : LES ODDS SONT EXACTES, LE PROBLÈME EST LA FORME DU ROSTER — ne pas « corriger » les poids.** Mesuré sur 60 000 tirages à travers le VRAI `RelicPool.get_random_relic` : **C 50.1 / U 33.0 / R 16.8 %**, et le décompilé STS2 (`Core/Factories/RelicFactory.cs::RollRarity`) fait `num < 0.5f -> Common ; num < 0.83f -> Uncommon ; else Rare` = **exactement 50/33/17**. Notre `RARITY_WEIGHTS` est donc leur table au chiffre près.
  - **La vraie différence est la TAILLE DES LISTES, et elle est INVERSÉE par rapport à STS2.** Notre pool : **20 Common / 15 Uncommon / 7 Rare** (42). STS2 (compté sur les 300 `Core/Models/Relics/*.cs`, en ne gardant que les raretés que `RollRarity` peut sortir) : **31 Common / 41 Uncommon / 51 Rare** (123) — leurs autres catégories (Ancient 102, Event 35, Shop 30, Starter 12) sont tirées ailleurs. **Conséquence : chez eux une Common précise est ~5× plus probable qu'une Rare précise (1.61 % vs 0.33 % par tirage) ; chez nous c'est PLAT** (Common 2.50 % / Uncommon 2.20 % / **Rare 2.43 %** par relique) — la rareté n'achète aujourd'hui aucune rareté réelle, elle ne change que le label et la bande de prix. Avec 7 Rares seulement, on en croise une bonne partie à chaque run et les mêmes reviennent : c'est ça qui se lit « je reçois plein de rares », pas le taux.
  - **Volume mesuré (200 maps marchées × 2 acts + 3 000 runs simulés)** : ~2.3 élites + 2.0 trésors = **~4 reliques GARANTIES/run** (les événements en ajoutent ~1.7 offres, 6 des 20 events du pool touchent une relique ; le shop en MONTRE 3 par visite, ~1.4 visite/run), soit **0.71 Rare reçue/run** et **~1.4 Rare vue/run** — 15.3 % des runs reçoivent 2+ Rares, 1.9 % en reçoivent 3+. **Le taux est donc plutôt BAS, pas haut.** Levier si Julien veut que « Rare » redevienne un mot : **allonger la liste des Rares** (forme STS2), pas baisser le poids — baisser le poids rendrait les 7 mêmes Rares encore plus statiques. Les 7 actuelles : blood_chalice, golem_heart, magic_sleeve, metronome, overflow_valve, prayer_beads, runic_shield.
  - **⚠️ SKIP DU TUTORIEL : le coup à 35 du Skeleton est DÉSARMÉ** (rapport de Julien : « if you skip tutorial during battle, skeleton attacks you for 35 dmg »). `tutorial_skeleton_action.gd` monte à `big_hit_damage` à sa **3e** attaque ; ce coup n'existe que comme payoff du tour 3 SCRIPTÉ, où le tuto vient de mettre le kill exact à portée. Le garde qui existait (`== big_hit_after_attacks` et pas `>=`) empêchait seulement qu'il se RÉPÈTE, pas qu'il tombe une fois sur un joueur qui n'a plus ni le kill ni de quoi bloquer. **Fix : `_current_damage()` rend `damage` dès que `Global.tutorial_on` est faux** — lu en direct, pas snapshotté, donc ça prend effet à l'instant du skip, en plein combat, le seul moment où ça compte.
  - **⚠️ Le fix a une 2e moitié obligatoire : `TutorialDirector._release_tutorial()` REDESSINE les intents** (`enemy.update_intent()` sur le groupe `enemies`, juste après `tutorial_on = false`). Sans ça, un intent de tour 3 calculé pendant que le script tournait continuerait d'afficher **35** pour un coup qui ne peut plus tomber — exactement la divergence intent-vs-réalité que l'en-tête de ce script dénonce déjà, ici en faveur du joueur. `update_intent()` relit l'action COURANTE (il n'en retire pas une nouvelle), donc l'ennemi fait toujours ce qu'il télégraphiait, seul le nombre change. **Toute future action d'ennemi dont les dégâts dépendent de `tutorial_on` hérite du redraw gratuitement.**
  - **⚠️ Les dégâts sont figés au DÉBUT de `perform_action()`** (`damage_effect.amount` est calculé avant le tween de 0.4s). Skipper PENDANT l'anim de l'ennemi laisse donc le 35 partir — c'est correct : le coup était déjà engagé et l'intent affiché disait bien 35. Ne pas « corriger ».
  - **Le chemin normal du tuto est intact** : `tutorial_on` ne passe à faux qu'après la victoire (`_on_victory_continue_pressed`), donc le tour 3 scripté garde son finale. Épinglé par la section D du harnais. **`debug_tutorial_skip_bighit.gd`/`.tscn` (racine, non committé) : 9 checks 0 fail, boote le VRAI `tutorial_fight` avec le VRAI director et appuie sur le VRAI `_on_skip_pressed()` ; la section A est un CONTRÔLE (avant le skip l'intent DOIT lire 35, sinon tout le reste passerait à vide sur un Skeleton inoffensif).**
  - **Harnais laissés à la racine (non committés)** : `debug_relic_odds.gd`/`.tscn` (A odds par tirage vs STS2, B forme du roster, C volume par run), `debug_parasite_power.gd`/`.tscn`, `debug_ringer_check.gd`/`.tscn`.
  - **⚠️ `debug_card_batch_check` échoue 7 checks — 6 ÉTAIENT DÉJÀ PÉRIMÉS** (il épingle l'état du batch de juillet : Cursed Toss avant son passage en Evil, Kickstart avant son Exhaust, « pool a encore Windfall » alors que Windfall est coupée depuis). Le 7e (« pool size 85 ») est ma coupe. **Harnais à rafraîchir en bloc un jour, il crie au loup.**
  - ⚠️ **Le worktree a dû faire un `--headless --import`** (cache périmé : les `.tres` de reliques/statuts ne chargeaient pas). Ça churne ~700 `.import` — **ne PAS les committer**, artefacts locaux. ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** (dice.gd, parasite.gd + 3 `.tres` édités hors éditeur, et un nouveau script `ringer_plus.gd`).

### H-028 (2026-08-28) Pulse de charge recalé sur la livraison (dice_charge_delivered)

- **⏱️ PULSE DE CHARGE — RETIMED SUR LA LIVRAISON (2026-08-28, diagnostic de Julien : « le pulse devrait se déclencher APRÈS que les dés chargés arrivent dans la dice interface ») — VÉRIFIÉ PAR HARNAIS (`debug_charge_pulse` 15 checks 0 fail, CONTRÔLE NÉGATIF FAIT) + STRIP MOVIE MAKER. NON PLAYTESTÉ.**
  - **C'EST LE FIX DE FOND DES 3 ROUNDS PRÉCÉDENTS, ET CE N'ÉTAIT PAS UN PROBLÈME DE TUNING.** Toute la réponse du gros dé (gust + flash d'aura + anneau convergent + cérémonie d'absorption) partait à l'ÉMISSION de `dice_charged`, donc à l'instant du play — **le seul effet de taille « impact » du jeu qui se déclenchait au LANCEMENT et pas à l'ARRIVÉE** (les power orbs réagissent à l'atterrissage, le bash de dés jetés au slam). Résultat : une annonce sans payoff, finie avant que l'œil ait suivi les icônes jusqu'à la rangée, suivie d'un decrescendo de plinks. Retimé sur l'atterrissage, la phrase devient un **crescendo** : flare de départ → vol → plinks montants → **clack + kick + hit-stop + GUST sur UN seul beat**. **Mesuré : la lumière ajoutée culmine à 38k px à l'arrivée contre 14k pendant le vol — le plus gros moment visuel est enfin à la FIN.**
  - **Nouveau signal `Events.dice_charge_delivered(dice_type, count)`** émis par `dice_interface.gd`, consommé par `dice.gd` (`_on_dice_charged` → **`_on_charge_delivered`**). `dice_charged` garde ses 2 autres listeners INTACTS (la livraison elle-même + Runic Bones). **La chorégraphie interne du bloc déplacé n'a PAS bougé d'un octet** — seul le déclencheur change, les valeurs `CHARGE_GUST_*` et le mode 2 shippent tels quels.
  - **⚠️ EXACTEMENT-UNE-FOIS PAR VOLLEY, garanti par un token** (`_begin_charge_volley_token` / `_pending_charge_volleys` / `_emit_charge_delivered`) : **3 chemins peuvent annoncer la livraison** — la dernière arrivée (`index == total - 1`), le **timer failsafe** (`total + 1.0s`, calqué sur celui de `_finish_slot_materialize`, désormais armé INCONDITIONNELLEMENT et plus seulement sous `reveal_slot`), et le **fallback sans `ui_layer`** (harnais/boot : rien ne peut voler, donc le payoff est immédiat). Un double emit doublerait le pulse de CHAQUE charge, un emit perdu tuerait le pulse — d'où le check D2.
  - **⚠️ L'émission est placée AVANT le `hit_stop` dans `_on_charge_die_arrived`** : les listeners sont synchrones, donc les tweens du gust sont construits dans la frame même où le freeze commence — **la montée du gust joue DANS le hit-stop, c'est voulu** (c'est exactement la freeze frame que le slash ratait avant le rework CRESCENT). Ne pas « corriger » si un strip montre le début du trajet étiré.
  - **⚠️ Le gate d'ownership (`charged_type != dice_type`) s'évalue maintenant À L'ATTERRISSAGE**, donc changer de dé actif en plein vol change quelle affirmation part. **C'est plus honnête, pas un bug** : la cérémonie d'absorption dit « le dé que tu regardes vient d'avaler ça ». Ne pas snapshotter le type à l'émission.
  - **⚠️ C1 DU HARNAIS A CHANGÉ DE SENS — ne pas le « réparer » à l'envers.** Avant, les 2 charges d'un multi-type partaient à 1 frame d'écart et le cooldown 110ms en avalait une (1 front = correct). Maintenant les volleys sont séquencées (≥ `CHARGE_STAGGER` = 0.16s), donc **chacune a légitimement son propre front teinté** — c'est le bam-bam en 2 couleurs voulu. C1 compte désormais les **resets de `gust_radius`** (tweené `.from(GUST_START)` à chaque pulse, donc un saut en arrière = un nouveau front, détectable même pendant que le précédent décroît). Le cooldown ne sert plus qu'aux livraisons dégénérées same-frame.
  - **⚠️ LES BLOCS A/B DU HARNAIS TOURNENT VOLONTAIREMENT SANS `ui_layer`** (fallback → livraison immédiate) : les dés en vol, leurs traînées de motes et le flare de départ atterrissent PILE dans les couloirs que le profil radial A4/A5 échantillonne. **Ne PAS « corriger » en ajoutant un ui_layer dans `_build_stage` — ça détruit A4/A5.** Les blocs C/D l'activent via `_set_flights_enabled()` pour tester le vrai chemin shippé.
  - **CONTRÔLE NÉGATIF FAIT (méthodo maison)** : en repointant `dice.gd` sur `dice_charged`, **D1 tombe à 8ms au lieu de 1047ms** et C1 retombe à 1 front — les 2 nouveaux checks sont donc bien porteurs. **D1 mesuré en normal : premier gust à 1047ms**, ce qui colle au calcul (`stagger×2 + birth + flight ≈ 0.95s`).
  - **⚠️ `get_shader_parameter` rend `null` tant qu'un uniform n'a jamais été ASSIGNÉ**, même quand le shader déclare un défaut — le lire dans un `var: float` throw avant le 1er pulse d'un run. D'où `_param_f()` dans le harnais.
  - **⚠️ LE CACHE D'IMPORT DE CE WORKTREE ÉTAIT PÉRIMÉ** (antérieur à la passe WebP du 07-31) : un `--headless --import` a régénéré **238 fichiers `.godot/` TRACKÉS** (les `.ctex` repèsent leur vraie taille compressée, ex. `cartographers_quill` 157k→4k). **C'est ce qui explique le `debug_ricochet_reroll` 12/15 documenté : il passe maintenant 26/26.** Ce bruit est correct mais il pollue `git status` — à committer séparément ou à ignorer sciemment.
  - Régressions : `debug_golem_carryover` 12/12, `debug_ricochet_reroll` 26/26. **Aucun autre harnais ne touche le chemin de charge** (vérifié par grep sur les 29 `debug_*.gd`). ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** (3 `.gd` édités hors éditeur dont l'autoload `events.gd`, qui gagne un signal).

### H-029 (2026-08-28) Renommage Loaded → Surge

- **🏷️ RENOMMAGE « LOADED » → « SURGE » (2026-08-28, demande de Julien : « i don't like the word loaded for what it does ») — VÉRIFIÉ PAR gdtoolkit (18 scripts touchés, 0 erreur) + intégrité du pool draftable (87 ext_resources). NON PLAYTESTÉ, NON RE-IMPORTÉ (éditeur de Julien ouvert pendant la passe).**
  - **Le motif, à retenir si le nom revient sur la table : « loaded dice » veut dire TRUQUÉ (résultat prédéterminé) — c'est ce que fait déjà `Lucky`. La mécanique, elle, est un bonus PLAT par roll, pas une face forcée.** Le nom mentait donc sur la mécanique ET chevauchait un mot-clé existant. Écartés au passage : `Radiant` (le langage « plus de power = plus de lumière » appartient déjà à Emanation/l'aura/les braises d'Overcharge — deux systèmes sans rapport auraient voulu dire « dé qui brille »), `Laden`/`Ballast` (registre poids), `Dividend`/`Favor` (registres économie/chance).
  - **Renommés** : `LoadedStatus` → `SurgeStatus`, `statuses/loaded.gd`/`.tres` → `surge.gd`/`.tres` (id `"loaded"` → `"surge"`, **nouvel uid** `c5urge5tatu55` — l'ancien était `cl0aded5tatu5`, personne ne le référençait par uid), `Global.loaded_amount`/`loaded_expiring` → `surge_amount`/`surge_expiring`, les 20 consts `LOADED_MOTE_*` de dice.gd → `SURGE_MOTE_*` (+ groupe `surge_mote`, nœud `SurgeMote`), le mot-clé `KeywordColorizer` + les 2 tooltips, les 7 descriptions/tags de cartes, `debug_loaded_motes.gd`/`.tscn` → `debug_surge_motes`.
  - **⚠️ Reliquats DÉLIBÉRÉMENT laissés, ne pas les « corriger » sans raison** : (a) **l'achievement `"loaded"` / « Loaded » (« Hold 300 Gold at once ») garde son nom** — c'est le sens « riche », sans rapport, et il devient justement NON ambigu maintenant que le statut ne s'appelle plus comme ça ; (b) **`loaded_dice.png`** (l'art de Ringer) garde son nom de fichier — le renommer exige de toucher 2 `.tres` + le `.import` + un reimport, or l'éditeur était ouvert (règle « jamais d'import CLI concurrent », et un `path=` cassé sur un `.tres` est exactement ce qui a vidé le pool draftable une fois) ; (c) les uids leetspeak `cl0ad3ddic350`/`cl04d3dd1c3p1` de Ringer ne contiennent pas littéralement « loaded » et sont référencés par le pool — laissés tels quels.
  - **Ids de carte legacy nettoyés au passage** : `card_loaded_dice`/`_plus` → `card_ringer`/`card_ringer_plus` (la carte s'appelle « Ringer » depuis son rename ; l'id datait de l'ancien nom), + l'ext_resource id `301_loadeddice` → `301_ringer` dans le pool. `debug_card_review_batch.gd` mis à jour. ⚠️ Une save en cours retrouve la carte par `resource_path` (l'id n'est qu'un fallback), donc pas de casse — mais un run en cours au moment du swap n'a pas été testé.
  - ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** : un `class_name` a disparu (`LoadedStatus`) et un autre est apparu (`SurgeStatus`), `.godot/global_script_class_cache.cfg` est périmé — c'est exactement la configuration de l'incident de strip/vidage de pool. Le premier lancement doit aussi ré-importer.

### H-030 (2026-08-28) Clic droit pour inspecter une carte + aperçu de l'upgrade

- **🔍 RIGHT-CLICK POUR INSPECTER UNE CARTE + APERCU DE SA VERSION UPGRADEE (2026-08-28, demande de Julien « should be able to see upgraded versions when picking a card in the rewards, just like sts2 » puis « do the shop too ») — VÉRIFIÉ PAR HARNAIS (`debug_card_inspect.gd`/`.tscn` racine, **committé** : 56 checks 0 fail, CONTRÔLES NÉGATIFS INCLUS) + RENDUS (10 états) + régressions `debug_endscreens` et `debug_shop_rework` re-passées. NON PLAYTESTÉ. Mergé sur `main` en 2 temps (`01b0d89` rewards+shop, `ac3d44f` deck view) — merges auto propres, 0 conflit.**
  - **Modèle = `NInspectCardScreen` du décompilé STS2 (vérifié dans `Desktop\sts2_ref`) : clic droit (`AltPressed`) → overlay plein écran, UNE carte agrandie sur un backstop assombri, tickbox « Upgrade » qui re-rend LA MÊME carte upgradée, flèches pour parcourir l'offre, Esc/backstop pour fermer. ⚠⚠ **Leur before/after côte à côte (`NUpgradePreview`) est le REST SITE, pas l'écran de reward** — c'est notre UpgradeConfirmPanel de campfire, une autre feature. Ne pas confondre les deux si on retouche ça.**
  - **`scenes/ui/card_inspect_overlay.gd` (nouveau) : ZERO `.tscn`, ZERO `class_name`, tout construit en code** (pattern `run_stats_panel.gd`/`achievement_toast.gd`) — donc aucun rescan d'éditeur, aucun import, et aucun fichier de scène qu'une session parallèle puisse re-sauver depuis un buffer périmé. Les consommateurs `preload()` le `.gd` et font `.new()`.
  - **⚠⚠ `layer = 99` EST PORTEUR, ne pas monter la valeur** : les tooltips de survol (`tooltip.tscn`) sont leur propre `CanvasLayer` à **100** parenté à la racine — un overlay AU-DESSUS enterrerait justement les tooltips de mots-clés qu'on vient lire. 99 couvre quand même l'écran de reward et le shop (Control/ColorRect au canvas de base). Le seul truc qui nous dépasse = les popups tutoriel de `card_rewards` à **101**, donc `_open_inspect` REFUSE de s'ouvrir tant qu'une de ces boîtes est visible plutôt que d'empiler 2 modales.
  - **⚠ La carte vit dans un slot `mouse_filter = STOP` dimensionné à son empreinte SCALÉE.** `Visuals` ET `CardFrame` de `card_menu_ui.tscn` sont tous les deux en PASS, donc sans ce slot un clic sur la carte traverse jusqu'au backstop et FERME l'overlay qu'on vient d'ouvrir.
  - **⚠⚠ CORRECTION FACTUELLE à ne pas re-propager : `Control.get_global_rect()` EST scale-aware en Godot 4.3** (mesuré en moteur : une carte 140x210 à scale 2.4 rend `S: (336, 504)`). J'avais d'abord « corrigé » 3 sites de hit-test sur la théorie inverse — revertés. **Le VRAI bug était ailleurs** : la colonne de tooltips de mots-clés mélangeait `global_position` avec `size` (NON scalé), donc sur une carte agrandie elle atterrissait SUR la carte au lieu d'à côté. Fix = tout lire depuis `get_global_rect()`. Byte-identique à scale 1, où vivent tous les autres consommateurs.
  - **3 points d'entrée, même hook `Visuals.gui_input` déjà partagé** : `card_rewards.gd` (branche clic droit AVANT la branche de pick — le `Card` pické est **bindé à la connexion**, donc on pick TOUJOURS la carte de base, jamais l'aperçu) ; `shop_card.gd` émet un nouveau signal **local** `inspect_requested(card)` au lieu d'acheter, et `card_shop.gd` fournit la liste filtrée par `is_sold()` (⚠ un slot acheté GARDE son nœud ShopCard — seuls l'art et le prix sont free — pour figer l'empreinte de la rangée, donc il faut le filtrer explicitement).
  - **⚠⚠ 3e point d'entrée = `card_pile_view.gd`, et il apporte 2 pièges que les 2 autres écrans n'ont PAS.** Il couvre d'un coup le deck, les piles pioche/défausse/exhaust en combat ET le picker d'upgrade de campfire (même scène partagée) — c'est l'écran où « ça devient quoi ? » est littéralement la question posée. **(a) Cet écran se CACHE au lieu de se free, et un `CanvasLayer` enfant IGNORE le `hide()` de son parent Control** (le piège déjà documenté qui a laissé `MapBackground` peint derrière tous les écrans pendant des mois) — sans démontage explicite l'overlay resterait peint par-dessus la campfire ou le combat d'où on l'a ouvert ; `_dismiss_inspect()` est donc appelé sur les 2 sorties (Back ET upgrade confirmé), et le harnais ASSERTE ce cas précis. **(b) Il possède sa PROPRE modale (`UpgradeConfirmPanel`) qui rend DANS ce Control**, donc SOUS le CanvasLayer 99 de l'overlay — `_open_inspect` refuse tant qu'elle est visible plutôt que d'enterrer le dialogue qui attend une réponse ; son `_input` return aussi tôt quand l'overlay est ouvert pour que la propriété d'Esc ne dépende pas de l'ordre de dispatch. ⚠ Et ici on bind l'**INDEX**, pas la `Card` : un deck peut légitimement contenir 2 fois la MÊME ressource, et un `find()` paginerait toujours depuis la 1re copie. Une carte déjà « + » dans la pile cache le toggle via `can_be_upgraded()` et s'affiche telle quelle (couvert par le harnais, qui stocke exprès une « + »).**
  - **Nouveaux `CardMenuUI.display_card()` / `clear_hover_tooltips()`** : `set_card()` seul laisserait la colonne de tooltips de la carte PRÉCÉDENTE en place, et comme le curseur ne quitte jamais le cadre pendant un swap, `mouse_entered` ne peut pas refire — d'où le rendu explicite du hover (même pattern que `_on_rarity_gem_mouse_exited`).
  - **⚠ Le hint de découvrabilité (« Right-click a card to preview its upgrade ») est PLACÉ PAR MESURE, pas à l'œil, et les 2 écrans n'ont PAS la même unité : `card_rewards.tscn` a sa racine authorée 857x480 puis SCALÉE 1.5x** (donc font 12 rend ~18px, et un offset de 50 vaut 75 à l'écran — mon 1er jet chevauchait le bouton Skip pour exactement cette raison), tandis que `card_shop.tscn` est un Control full-rect à scale 1 (pixels écran directs, insets asymétriques pour centrer sur la rangée de cartes à x595 tout en dégageant le bouton Leave à x1024). Le harnais ASSERTE la non-intersection des rects plutôt que de faire confiance aux nombres.
  - **⚠ 2 pièges GDScript coûteux rencontrés dans le harnais (tous deux passent gdtoolkit)** : (a) **les lambdas capturent les locales PAR VALEUR** — `connect(func(c): picked_card = c)` écrit dans la COPIE de la lambda, l'assertion testait donc une variable que rien ne pouvait remplir (fix : membre de classe) ; (b) **`POOL.cards` est typé par CHEMIN DE SCRIPT, pas par le class_name `Card`** — `for card: Card in POOL.cards` et même `var card: Card = entry` sont REFUSÉS au parse (« Cannot assign a value of type res://custom_resources/card.gd to variable with specified type Card »), ce qui empêche le script de charger et fait **HANGER le harnais à l'infini**. Toucher les entrées de pool en NON typé et laisser `append()` sur un `Array[Card]` faire la validation runtime.
  - **Choix de design à trancher au playtest** : le toggle est **collant** entre les cartes (STS2 le remet à zéro par carte — collant est meilleur pour comparer 3 upgrades) ; les flèches **se désactivent** aux extrémités au lieu de disparaître (STS2 les cache ; désactiver évite un layout qui saute) ; **clic gauche sur la carte dans l'overlay ne fait RIEN** (fidèle à STS2 : on ferme puis on pick/achète) ; le hint est UN label, supprimable en retirant `_build_inspect_hint()`. **Non couvert par le harnais (à confirmer en jeu) : le routage souris réel et les tooltips de survol DANS l'overlay.**

### H-031 (2026-08-28) Animations d'attaque du héros (lunge, punch, strike du dé, finisher)

- **🥋 ANIMATIONS D'ATTAQUE DU HÉROS — BATCHES 1-4 LIVRÉS (2026-08-28, plan : [attack_animations_plan_2026-08.md](attack_animations_plan_2026-08.md)) — VÉRIFIÉ PAR HARNAIS (`debug_attack_anim.gd`/`.tscn` racine, **COMMITTÉ** contrairement à l'habitude — il épingle des invariants de gameplay, pas juste du feel : 30 checks 0 fail, CONTRÔLE NÉGATIF fait — strike désactivé ⇒ 5 fails ciblés) + RENDU MOVIE MAKER (les 3 beats) + RÉGRESSIONS (held_die 10/10, golem 12/12, hitbox 33/33, ricochet 26/26). NON PLAYTESTÉ.**
  - **Le constat de départ, vérifié dans le code : le corps du héros ne bougeait JAMAIS en attaquant** — seul combattant du jeu dans ce cas (le commentaire d'en-tête de `player.gd` le disait déjà : « enemies lunge, thrown dice fly, the big die hops »), et le punch du dé était à force PLATE 1.0 qu'on tape 2 ou 40, alors que slash/hit-stop/shake/camera laddéraient tous déjà. C'était le dernier élément de feel non laddéré du jeu.
  - **(1) LUNGE DU CORPS** (`LUNGE_OFFSET (14,-4)`, out 0.07 / back 0.30). **⚠️ Le knockback de dégâts écrivait DÉJÀ `self.position` : les deux partagent maintenant UN SEUL tween (`_body_move_tween`) et UNE SEULE position de repos canonique (`_body_rest_position`, capturée à la 1re utilisation).** L'ancien code re-lisait `position` à chaque hit pour définir « chez soi » — être touché en plein lunge aurait adopté un point mi-course comme repos et le héros aurait dérivé à travers l'arène, un hit à la fois. Épinglé par un check (lunge + hit simultanés → retour à ±0.000px).
  - **(2) PUNCH LADDERÉ** `DIE_PUNCH_TIER_STRENGTH = [1.0, 1.0, 1.15, 1.4, 1.7]` indexé par `Shaker.Impact` (mesuré : 18.4 / 21.2 / 31.6px) + **frame d'anticipation** (pull-back 0.045s — ce qui fait lire « lancé » plutôt que « tressauté ») + **ghost smear** sur STRONG+ + **bob d'idle** (4px, 3s). **⚠️ Le bob vit sur un NOUVEAU nœud `HeldDieBob` inséré entre SpriteRoot et `HeldDiePivot`** : le punch snappe le PIVOT à son repos à chaque interruption, donc un bob partageant ce transform se ferait écraser. Trois canaux, trois nœuds : racine = lunge/knockback, bob = lévitation, pivot = punch/strike.
  - **⚠️⚠️ LE FILTRE CENTRAL = UNE COMPARAISON DE FRAME, et il fait tout le travail : `Global.last_attack_card_played_frame`** (posé dans `Card.play()`, qui émet `card_played` en 1re ligne puis appelle `apply_effects` synchronement — même astuce que `_last_card_played_frame` de dice.gd pour les power orbs). Exclut GRATUITEMENT et par construction : attaques ennemies, AoE magma, statuts (Earthquake), thorns, et atterrissages de dés jetés (~0.95s plus tard). Épinglé par 2 contrôles négatifs.
  - **(3) STRIKE DU DÉ** — hit **mono-cible** ET (tier ≥ STRONG **OU** létal), 1× par play. Le dé se cache, un CLONE (`_die_pivot.duplicate(0)`, donc il hérite de l'offset de centre d'encre) vole en bezier 0.26s vers `Card.thrown_impact_pos`, s'enfonce, puis **blink-back** : dissolution + rematérialisation dans la paume avec le langage de retune (flash blanc → settle). Failsafe idempotent + jeton de génération : la paume ne peut jamais rester vide. **Dégâts DIFFÉRÉS à l'impact** — donc le freeze ponctue l'atterrissage au lieu d'échouer le vol en plein vol.
  - **⚠️⚠️ 2 CHANGEMENTS DE DESIGN FORCÉS PAR LE CODE (le plan ne les avait pas vus) : (a) TROIS CARTES LISENT L'ÉTAT DE LEUR CIBLE APRÈS L'AVOIR FRAPPÉE** — `executioner.gd` teste `health <= 0` (son propre commentaire dit « right after the (synchronous) damage »), `clank(_plus).gd` filtre « encore vivant entre 2 hits ». Différer leur aurait mangé leur payoff EXACTEMENT sur les kills pour lesquels elles existent. **Nouveau contrat déclaré `Card.observes_post_damage()`** (virtuel, override sur ces 3 cartes, scopé comme `playing_card_requirement`) : elles gardent le chemin instantané et ne strikent jamais. **Toute future carte qui lit sa cible après damage doit l'override** — c'est greppable et à côté du code qui en dépend, plutôt qu'une liste d'ids dans damage_effect. **(b) La fenêtre Kaboom est ouverte ET fermée synchronement dans `Card.play()`** : un hit différé tomberait hors d'elle, sur précisément les plus gros hits — les seuls qui peuvent la gagner. Le bundle différé rouvre donc une fenêtre autour de lui.
  - **⚠️ `_resolve_hit()` = la queue d'`execute()` EXTRAITE, appelée par les 2 chemins** (immédiat et différé) — jamais recopiée, c'est comme ça que deux chemins divergent. Son ordre est porteur : `take_damage` écrit `Global.damage_to_display`/`blocked_to_display` que tous les reporters et le popup relisent.
  - **⚠️ Le strike n'est NI un roll NI un throw** : aucun `report_thrown_die_landed`, aucun emit `dice_thrown*` — sinon Hunting Bow / Snake Eyes Charm / Metronome / Greedy et les compteurs de dés se déclencheraient. Épinglé (compteur d'écoute reste à 0).
  - **(4) FINISHER DE KILL** : sur un hit létal le clone **s'encastre et tient 0.1s** avant de se dissoudre. **Vérifié au rendu** : le dé est visiblement planté dans le corps blanc-flashé pendant 3 frames, PUIS le corps éclate en éclats de dés — la chaîne causale « son dé les a brisés » se lit. L'anim de mort elle-même n'est pas touchée.
  - **(5.2) FLINCH** (seule garniture prise) : prendre un coup recule aussi le dé — `punch_held_die(-0.35)`, une force NÉGATIVE étant exactement un recul (même tween à l'envers). Mesuré 6.5px, nettement séparé du plus petit thrust (18.4px). **5.1 (drumming multi-hit), 5.3 (chaleur overcharge) et 5.4 (triggers v2) NON faits** — les deux premiers demandent de la plomberie neuve, le dernier un verdict de Julien.
  - **⚠️ PIÈGE DE HARNAIS RETOMBÉ (§7.14, dans MON propre harnais) : `var target := _enemies()[0]`** — `_enemies()` rend un `Array` non typé, donc l'indexer donne Variant, donc `:=` ne peut pas inférer → **parse error qui tue tout le fichier, gdtoolkit le déclare PROPRE, et le harnais HANG à l'infini** (timeout 240s). Typer explicitement. **Et 2e piège : échantillonner sur un NOMBRE DE FRAMES** — en headless la boucle tourne bien plus vite que 60fps, donc 24 frames couvraient ~24ms et je mesurais l'anticipation en croyant mesurer le punch (12dmg lisait 7.5px au lieu de 21.2). Échantillonner en TEMPS DE JEU.
  - **Leviers de tuning** : consts `LUNGE_*`, `DIE_PUNCH_TIER_STRENGTH`, `DIE_ANTICIPATION_*`, `DIE_SMEAR_*`, `DIE_BOB_*`, `STRIKE_*`, `DIE_FLINCH_STRENGTH` en tête de `player.gd` ; `STRIKE_MIN_TIER` (3 = STRONG) dans `damage_effect.gd`. **⚠️ `debug_held_die.gd` a dû être repointé sur le nouveau chemin `SpriteRoot/HeldDieBob/HeldDiePivot/HeldDie`** — tout futur harnais qui vise le dé tenu doit passer par le bob. ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** (7 scripts édités hors éditeur). **À surveiller au playtest** : la paume reste vide ~0.5s sur un kill (embed hold inclus) ; le dé traverse visuellement les ennemis non ciblés en chemin ; et le son de lancement est un PLACEHOLDER (`whipsound.mp3`, celui des dés jetés).

### H-032 (2026-08-28) Carte mono-cible ne touche plus 2 ennemis, Trebuchet 3/4, Sixth Gear 8/6

- **🎯 UNE CARTE MONO-CIBLE NE PEUT PLUS TOUCHER 2 ENNEMIS + TREBUCHET 3/4 + SIXTH GEAR 8 DÉS / 6 POWER (2026-08-28, branche `claude/hitbox-overlap-balance-b4339f`) — VÉRIFIÉ PAR HARNAIS (`debug_single_target_hitbox.gd`/`.tscn` racine, non committé : 33 checks 0 fail, CONTRÔLE NÉGATIF INCLUS) + régressions `debug_relic_batch` 228/228 et `debug_golem_carryover` 12/12 et `debug_relic_rework` 67/67. NON PLAYTESTÉ.**
  - **Root cause du double hit** : les hitboxes d'ennemis sont un `CollisionShape2D` de TAILLE FIXE dans `enemy.tscn` (jamais retaillé par combat ni par art), donc deux corps assez proches se CHEVAUCHENT — et tant que le curseur est dans la bande commune, la sonde 4×4 de `card_target_selector` entre dans les DEUX et les laisse tous les deux dans `card_ui.targets`. `Card.play()` faisait alors `resolved_targets := targets` tel quel pour une carte SINGLE_ENEMY : **un seul play résolvait contre deux ennemis et les frappait les deux**. Ce n'est donc PAS un problème de placement par combat — aucun `battles/*.tscn` n'a été touché, et il n'y a rien à auditer fight par fight.
  - **Fix = nouveau `Card.pick_single_target(targets)`, appelé DANS `Card.play()`** (et pas dans le sélecteur de visée) : **tous les chemins de play y passent** — drag-release, l'aim forcé après un roll du socket Red, et tout futur chemin. Tie-break = **le hitbox dont le CENTRE est le plus proche du curseur** (« le corps sur lequel tu es le plus »). ⚠️ Le centre est celui du `CollisionShape2D`, PAS `enemy.global_position` : le `Sprite2D` est baké à x=124 dans le template, donc la position du root ment sur « où est le corps ». Les entrées freed sont filtrées (même famille que `_prune_stale_targets`).
  - **Le highlight suit le pick, sinon l'UI ment** : `card_target_selector.gd` garde `_highlighted_target` et **n'allume qu'UN corps** ; le pick est recalculé **dans `_process`** parce que glisser d'un corps à l'autre DANS la zone de chevauchement ne déclenche AUCUN `area_entered`/`area_exited` — sans ça le contour resterait sur l'ancien. La preview de dégâts (`card_ui.gd::_on_dice_rolled_update_description`) passe par le MÊME `pick_single_target` au lieu de `targets[0]`, donc nombre prévisualisé = ennemi réellement frappé.
  - **⚠️ Piège de harnais : on ne peut PAS asséner un objet déjà freed dans un `Array[Node]` typé** (Godot refuse l'assignation et rend un tableau VIDE, donc le test ne testait rien) — il faut appender pendant que le nœud est vivant PUIS le `free()`. Et les corps de `tier_1_crab_satyr` ont 8 HP : sans gonfler leurs HP d'abord, le contrôle négatif TUE un ennemi et le check AoE suivant compare contre un cadavre (l'ennemi mort quitte le groupe `enemies`).
  - **Trebuchet +2 → +3, Trebuchet+ +3 → +4** : 6 fichiers (les 2 `.gd`, les 2 `card_*.tres` description, les 2 `status_trebuchet*.tres` tooltip). Rappel : `Global.thrown_dice_bonus_fight` est LE seul scaler de dé jeté depuis que la Strength ne s'y applique plus, et il paie **par dé** — sur Dice Avalanche (jusqu'à 9 dés) ça fait +27/+36 sur un tour, à surveiller au playtest.
  - **Sixth Gear : « tous les 6 dés → 4 Power » devient « tous les 8 dés → 6 Power »** (`EVERY`/`POWER_BONUS` en tête de `relics/sixth_gear.gd` + tooltip du `.tres`). Le compteur affiché cycle maintenant 1..8. **Les 2 harnais de régression qui épinglaient les anciens chiffres (`debug_relic_batch`, `debug_relic_rework`) sont mis à jour** — sinon ils auraient crié au loup au prochain run. `debug_relic_rework.tscn` n'existait pas sur disque, créé pour pouvoir le relancer.
  - ⚠️ **Le worktree a dû faire un `--headless --import`** (cache périmé, symptôme habituel : « Failed loading resource » sur warrior.tres et 1900 erreurs). Ça a churné ~240 fichiers `.godot/` — **ne PAS les committer**, ce sont des artefacts de build locaux au worktree.

### H-033 (2026-08-27) Dé tenu par le héros : teinte par type + punch d'attaque

- **🎲 DÉ TENU PAR LE HÉROS : TEINTE PAR TYPE ACTIF + PUNCH D'ATTAQUE (2026-08-27, prépa trailer — idée de Julien : « le dé que le héros tient pourrait changer de couleur selon le type actif, et bouger sur les attaques ») — VÉRIFIÉ PAR RENDU MOVIE MAKER (composition de combat réelle) + HARNAIS (`debug_held_die.gd`/`.tscn` racine, mode `dim` : 9 checks 0 fail) + RÉGRESSIONS (golem 12/12, ricochet 26/26). NON PLAYTESTÉ.**
  - **Le héros lévite un dé BLANC au-dessus d'une paume ouverte** — c'est ce qui rend l'idée possible : le blanc est la cible de teinte idéale (`modulate` MULTIPLIE, donc les faces blanches prennent l'accent à fond pendant que le contour et les pips quasi-noirs restent noirs), et le dé est **spatialement isolé** (composant alpha unique de 1843px, x269-322 y42-89, aucun contact avec le gant). Mesuré : 39% de faces quasi-blanches (238,238,238), 43% de contour/pips sombres.
  - **`split_hero_die.py` (racine) coupe l'art en 2 PNG sur un canvas IDENTIQUE** — `main_character_chibi.png` (corps, die retiré, écrasé en place donc warrior.tres/uid/scène intacts) + `main_character_die.png`. Canvas identique = non négociable : la même transform fait retomber le dé PILE dans son trou, et **le shader de sway déforme en UV pondéré par la hauteur DANS LA TEXTURE**, donc un canvas identique déformerait le dé exactement comme quand il était baké. Combined source archivé dans `daiso_archive_2026-07-31\hero_art_2026-08-27\`. **Re-run le script à chaque nouvelle art de héros, puis `--headless --import`.**
  - **⚠️⚠️ LE PIÈGE CENTRAL : `scenes/enemy/enemy.gdshader` (le sway, partagé héros + TOUS les ennemis) finit sur `COLOR = tex_color;` — un sample de texture BRUT qui ÉCRASE le COLOR entrant, donc il DÉTRUIT `modulate`. Un sprite portant ce shader est INTEINTABLE.** C'est exactement la mémoire `feedback_modulate_vs_shader_gotcha`. Fix retenu : **le dé n'a AUCUN material** (éditer le shader toucherait chaque ennemi du jeu pour un accessoire de héros). Coût : il ne sway pas avec le corps — ce qui est probablement le MEILLEUR read (le dé est lévité, pas tenu) et la main qu'il survole ne parcourt que ~2.5px écran, sous le plancher de perceptibilité. Conséquence : le flash blanc de `take_damage` restaure le dé à **`null`**, jamais à `_sway_material` (ça tuerait sa teinte en silence).
  - **⚠️ Pivot sur le CENTRE D'ENCRE du dé, pas sur le nœud** : `Sprite2D` se centre sur sa TEXTURE, et l'encre du dé est à ~127px à droite / 122px au-dessus de ce centre — scaler le sprite le catapulterait en arc à travers l'écran. D'où `HeldDiePivot` (Node2D) posé sur le centre d'encre, dérivé au RUNTIME de `Enemy._get_content_rect(die_tex)` : re-splitter une nouvelle art ne demande donc AUCUN nombre calé à la main.
  - **Construit 100% en CODE** (pattern de l'ombre au sol) : zéro édit de `player.tscn`, donc zéro surface ext_resource/uid/load_steps, et le retrait = supprimer un bloc. Teinte = `DicePalette.accent(type).lerp(WHITE, 0.15)` — **infusion-aware gratuitement**, et un type inconnu rend blanc = l'art d'origine. Le lift de 15% existe parce qu'à sa taille réelle (**~38px écran**) un cobalt ou un fuchsia saturé devient de la boue sur la cape navy. Switch de type = flash vers le blanc puis settle dans le nouvel accent + punch fractionnaire (`DIE_RETUNE_PUNCH`). Attaque = poussée **droite-et-haut vers les ennemis** (15px = 40% de sa propre largeur) + rotation flick + pop d'échelle, gaté sur `card.type == ATTACK` — **le MÊME gate que le slash directionnel**, donc poussée et coupe (qui tombe 0.055s plus tard) lisent comme UN seul beat. Leviers : consts `DIE_*` en tête de `player.gd`.
  - **⚠️ `.parallel()` s'applique au tweener SUIVANT, jamais à celui qu'on vient de créer** — l'appeler après coup fait QUEUER les legs au lieu de les grouper (attrapé sur le dim ci-dessous avant tout rendu).
  - **⚠️⚠️ « DIM + DROP DE LA MAIN PENDANT LE TOUR ENNEMI » (item Batch 1 jamais shippé) EST UN NO-OP — NE PAS LE RE-PLANIFIER.** `end_turn()` lance les statuts END_OF_TURN → `discard_cards()`, et `player_hand_discarded` (qui DÉMARRE le tour ennemi) n'est émis qu'une fois la DERNIÈRE carte envolée : **la main est déjà vide avant qu'un ennemi n'agisse.** Le plan avait été écrit sans vérifier l'ordre. **La vraie cible = le CLUSTER DE DÉS** (aura, emanation, ROLL, rangée de slots) : l'objet le plus lumineux de l'écran, totalement inerte pendant qu'on se fait taper. `battle.gd` dimme donc les 2 ROOTS `ActiveDice`/`DiceInterface` à 0.5 en multiply de luminosité (jamais de l'alpha) sur `player_turn_ended`, restaure sur `player_turn_started`. **Sûr : les 13 écritures de `modulate` de dice.gd visent toutes un ENFANT**, donc zéro collision avec le highlight de slot, le nudge ou un flash.
  - **⚠️ Si l'art du héros change (design final en cours)** : re-run `split_hero_die.py`, sinon `main_character_die.png` reste l'ancien dé posé sur un nouveau corps. Garde-fou en place : texture manquante ⇒ overlay absent (comportement d'avant), et un swap A/B debug **cache** l'overlay (les candidats portent leur propre dé baké). **Spec à donner à Jenya : garder le dé DÉTACHÉ de la main (sinon le split auto ne marche plus) et BLANC/grisaille (une couleur bakée rend la teinte boueuse) ; livrer le source EN CALQUES, ou 2 PNG sur canvas identique — un dé sur son propre canvas serré oblige à redeviner l'alignement.**
  - **⚠️⚠️ BUG RÉEL CORRIGÉ AU 1er PLAYTEST (Julien : « je dois cliquer une 2e fois sur le slot pour que le dé du perso prenne la bonne couleur, il est toujours 1 en retard ») : `Global.dice_type` est assigné DANS le listener de `active_dice_changed` de dice.gd (`dice.gd:2540`)** — donc tout AUTRE listener du même signal court une race décidée par l'ordre de connexion, et `Player` est plus haut dans `battle.tscn` (l.239 vs 591) donc il gagnait la course et lisait la valeur PRÉCÉDENTE. **Fix : `_update_die_tint(animate, type_override)` prend le type DEPUIS L'ARGUMENT DU SIGNAL et ne lit plus jamais le global sur ce chemin.** ⚠️ **Le piège attend n'importe quel futur listener qui a besoin du type actif au moment du switch — toujours faire confiance à l'argument, jamais à `Global.dice_type`.** Régression épinglée dans `debug_held_die` (mode `dim`, check 10) **avec CONTRÔLE NÉGATIF fait** : en retirant le fix, le dé reste bleu (0.3533,0.56,1) sur un switch vers magma — exactement le symptôme rapporté.
  - **Son de max roll : `dicecrush3` → `dicecrush2`** (`Global.DEFAULT_HIGH_ROLL_SOUND`, pick de Julien sur l'A/B in-game du 2026-08-27). Consommé par le getter `Global.high_roll_sound()` (que `dice.gd:1578` joue sur le max roll), donc l'override debug F9 continue de marcher par-dessus.

### H-034 (2026-08-28) Rareté des récompenses de cartes sur le modèle STS2 exact

- **🎴 RARETÉ DES RÉCOMPENSES DE CARTES REPORTÉE SUR LE MODÈLE STS2 EXACT (2026-08-28, constat de Julien : « je me fais offrir BEAUCOUP de rares, et très souvent 1 rare = TOUTES les cartes sont rares ») — VÉRIFIÉ PAR HARNAIS (`debug_rarity_odds.gd`/`.tscn` racine, non committé : **36 checks, 0 fail**, CONTRÔLE NÉGATIF = l'ANCIEN algorithme rejoué à l'identique). NON PLAYTESTÉ.**
  - **⚠️⚠️ CORRECTION FACTUELLE, NE PAS RE-PROPAGER L'ERREUR : j'ai d'abord affirmé que « STS2 ne donne pas 3 rares au boss, c'est notre bug ». C'EST FAUX.** Le décompilé (`Desktop\sts2_ref\pck\src\Core\Odds\CardRarityOdds.cs`) a `bossRareOdds = 1f` et `BossEncounter => Rare 1.0` : **chaque récompense de carte de boss EST 100% rare dans STS2**. Julien avait raison et l'a dit avec certitude — j'avais répondu de mémoire au lieu de lire la référence QUI EST SUR LE DISQUE. **Leçon : `sts2_ref` existe, l'ouvrir AVANT d'affirmer quoi que ce soit sur STS2.** Le comportement boss est donc CONSERVÉ, et le tooltip Rare (« Every boss reward is Rare. ») reste vrai — rien à réécrire.
  - **⚠️ LE VRAI BUG ÉTAIT AILLEURS : notre pity était PAR ÉCRAN, STS2 l'avance PAR CARTE.** C'est TOUTE l'explication du « 1 rare = toutes rares » hors boss : nos 3 slots partageaient un même poids gonflé (donc corrélés), alors que chez eux l'offset retombe au plancher À L'INSTANT où le slot 1 sort Rare, ce qui met les slots 2-3 sous l'eau (0.03 − 0.05 < 0). Mesuré : écrans élite à 2 rares **11.8% → 2.1% (÷5.7)**, écrans normaux à 2 rares **1.3% → 0.00%**, rares hors-boss par run **3.8 → 2.7**.
  - **MODÈLE PORTÉ (tout dans `CardRarityDraw`, le foyer partagé déjà existant)** : `enum Source {NORMAL, ELITE, BOSS, SHOP}` + `BASE_UNCOMMON_ODDS`/`BASE_RARE_ODDS` + `roll_rarity(source, offset)` + `advance_offset(offset, rolled)`. **UN seul float par carte** : Rare si `x < base_rare + offset`, sinon Uncommon si `x < base_uncommon + ça`, sinon Common. Tables A0 de la référence : **normal .60/.37/.03, élite .50/.40/.10, boss 0/0/1, shop .54/.37/.09**. ⚠️ **L'Uncommon est une part FIXE — l'offset ne mange QUE la part de Common**, jamais celle d'Uncommon (notre ancien modèle multiplicatif faisait bouger les deux).
  - **Pity = un seul float persisté, `RunStats.rare_offset`** (remplace `common_weight`/`uncommon_weight`/`rare_weight`) : plancher **−0.05**, croissance **+0.01 par CARTE non-rare**, cap **+0.40**, reset au plancher dès qu'une Rare sort. Le plancher NÉGATIF **subsume exactement l'ancien cas spécial `RUN_START_RARE_WEIGHT` « pas de jackpot au floor 1 »** (0.03 − 0.05 < 0 = Rare littéralement impossible en début de run), donc ce hack maison disparaît sans rien perdre.
  - **La branche boss dédiée est SUPPRIMÉE et c'est un gain, pas une perte** : sa ligne d'odds étant un 1.0 plat, la boucle générique remplit les 3 slots en Rare toute seule — ET comme chaque Rare reset l'offset, un écran de boss **remet enfin le pity au plancher** (l'ancienne branche sautait `_update_rare_pity` entièrement, donc la pity traversait le boss).
  - **`ELITE_UNCOMMON_MULT`/`ELITE_RARE_MULT` SUPPRIMÉS.** ⚠️ Le ×4.0 multipliait le poids **déjà gonflé par la pity** : le commentaire promettait ~10% de rare par slot en élite, la mesure réelle donnait **21.8%** (et 41.7% au cap). STS2 n'a pas de multiplicateur du tout, juste une LIGNE d'odds distincte.
  - **CARTES UPGRADÉES EN RÉCOMPENSE (port de `CardFactory.RollForUpgrade`)** : un tirage par carte APRÈS la rareté, dans `_resolve_reward_card()` (le chokepoint qui existait déjà pour Wandering Merchant). Chance = `act_index × 0.25` → **act 1 = 0%, act 2 = 25%** (l'act 3 = 50% de la référence n'a pas d'équivalent ici). ⚠️ **Les RARES ne sont JAMAIS pré-upgradées** — dans la référence le bonus d'act n'est ajouté que si `card.Rarity != Rare` —, ce qui implique que **les écrans de boss (100% rares) n'en donnent jamais**. Les cartes de boutique non plus (la référence y passe une base négative énorme ; nous, on ne tire simplement pas). `force_upgraded_card_rewards` (Wandering Merchant) reste un override inconditionnel qui upgrade même les Rares. **Zéro travail de data : les 85 cartes du pool ont déjà un `upgraded_version`.**
  - **⚠️ BOUTIQUE : la garantie « 1 rare par visite » est SUPPRIMÉE** (`SHOP_COMPOSITION` → `SHOP_CARD_SLOTS = 5` + un roll par slot). STS2 tire chaque slot indépendamment et **LIT l'offset sans jamais l'avancer** (`RollWithoutChangingFutureOdds`), donc parcourir une boutique ne peut plus affamer la récompense de combat suivante. Conséquence de gameplay MESURÉE : une visite contient au moins une Rare **~100% → ~18%** au plancher d'offset (plus en période sèche). C'est le changement le plus sensible du lot pour le playtest.
  - **Sauvegarde migrée** : clé `rare_weight` → `rare_offset`, avec `.get(..., RARE_OFFSET_FLOOR)` — couvre d'un coup les saves pré-rareté (`normal_weight`/`support_weight`) ET les saves du modèle à poids (dont le `rare_weight` 0.3-2.0 n'a aucun sens sur l'échelle d'offset, donc on retombe volontairement au plancher plutôt que d'importer un nombre absurde).
  - **⚠️⚠️ PIÈGE DE HARNAIS NOUVEAU ET COÛTEUX : `load()` un script `class_name` PAR CHEMIN avant sa scène fait perdre son script à la scène au parse suivant.** Ma section « les scènes chargent toujours » chargeait `run.gd` d'abord (pour forcer un compile) et rapportait ensuite `run.tscn` **sans script** + `Parse Error: [ext_resource] referenced non-existent resource` — un faux positif terrifiant sur la scène principale du jeu. Prouvé inoffensif par une sonde isolée (run.tscn charge, garde `res://scenes/run/run.gd`, s'instancie en vrai `Run` avec `_start_run`). **Le jeu ne peut pas tomber dedans** (rien ne charge run.gd par chemin avant run.tscn), c'est un hasard propre aux harnais → sonder les SCÈNES en PREMIER.
  - **⚠️ Piège documenté retombé (dans le harnais) : `var x := source == CardRarityDraw.Source.ELITE` où `source` est une var de boucle NON TYPÉE = « Cannot infer the type » → le script ne charge pas → `_ready` ne tourne jamais → `quit()` non plus → LE HARNAIS PEND À L'INFINI** (attrapé en lisant la sortie, pas en attendant). gdtoolkit dit PROPRE. Typer explicitement `var x: bool = ...`.
  - **⚠️ Le projet est en 4 ESPACES, zéro ligne indentée par tabulation** (vérifié : 0 sur les 3 gros fichiers touchés). Un premier jet écrit en tabulations a dû être reconverti — un `.gd` à indentation mixte se fait refuser par gdtoolkit.
  - **Chiffres finaux mesurés (20 000 runs × 18 écrans)** : per-slot **C 50.9 / U 33.0 / R 16.1%**, **8.7 rares offertes par run** dont **6 viennent des 2 boss** (donc ~69% — avec seulement 2 acts, les écrans de boss dominent la part de rares par construction ; si le VOLUME global gêne encore au playtest, le levier est le nombre d'acts/boss, pas les odds). Boss 3/3 rares 100%, élite 2 rares 2.1% / 3 rares 0.02%, normal 2 rares 0.00%.
  - **Fichiers** : `custom_resources/card_rarity_draw.gd` (modèle), `custom_resources/run_stats.gd` (état), `scenes/battle_reward/battle_reward.gd` (tirage + upgrade), `scenes/run/run.gd` (save/load), `scenes/shop/card_shop.gd` (boutique). ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** : `RunStats` perd 3 `@export` et en gagne un (`rare_offset`) — configuration exacte de l'incident de strip par l'éditeur live, même si RunStats n'a aucun `.tres` sur disque (vérifié) donc rien à stripper ici.

### H-035 (2026-08-26) Slash de dégâts CRESCENT (remplacé par DIE_IMPACT, H-002)

- **🗡️ SLASH DE DÉGÂTS — CRESCENT RETENU ET SHIPPÉ (2026-08-26). Julien a playtesté la planche de 4 variantes et choisi CRESCENT (« I like it! »), puis demandé une passe « more everything » — `Enemy.slash_style` est donc sur `CRESCENT` par DÉFAUT et MERGÉ sur `main` (`175a296` + la passe de tuning). CURRENT/WOUND/FLURRY restent derrière le même switch, code intact. Régression `debug_golem_carryover` 12/12 ALL PASS. Planche d'origine : https://claude.ai/code/artifact/0b8c7857-ec8f-4ee7-ae21-664216dcce60**
  - **⚠️⚠️ VRAI BUG TROUVÉ AU PLAYTEST (Julien : « sur un 1 damage au dé rouge je ne vois LITTÉRALEMENT rien même en fixant ») : `HIT_SMEAR_MIN_DAMAGE` valait 3, donc tout coup sous 3 dégâts ne spawnait AUCUN slash** — pas un problème de tuning, un early-return. Passé à **1** (seul un vrai zéro est skippé). Le gate existait depuis le slash d'origine, donc **le bug est antérieur au rework** et touchait aussi CURRENT.
  - **Passe « more everything » (2026-08-26, après playtest)** : les DEUX bouts du ladder ont bougé, mais **le plancher est monté plus que le plafond** (les rungs bas passaient sous le seuil de perceptibilité ~4px/10%) — `CRESCENT_HOLD` [0.13-0.44] → **[0.26, 0.32, 0.40, 0.50, 0.62]**, `CRESCENT_LENGTH` [110-260] → **[150, 180, 220, 265, 310]**, `CRESCENT_DISSOLVE` 0.26 → 0.30, corps de lame accent×1.15 → **×1.4**, cœur 1.55 → **1.72**, cône 40+dmg×3 (cap 90) → **52+dmg×4 (cap 130)**.
  - **⚠️ Nouveau `CRESCENT_THICKNESS` = [1.30, 1.18, 1.06, 0.99, 0.96], DÉLIBÉRÉMENT inversé par rapport au ladder de longueur** : une lame courte scalée proportionnellement est fine DEUX FOIS (courte ET mince) et disparaît, donc les petits coups reçoivent une lame plus trapue et les gros restent élégants. C'est ce qui tient le bas du ladder au-dessus du seuil de visibilité sans aplatir le scaling que Julien veut garder.
  - **⚠️ PIÈGE DE HARNAIS (a fait juger la couleur sur la mauvaise teinte) : le cluster de dés ÉCRIT `Global.dice_type` pendant son propre init, APRÈS le `_ready()` du harnais** — poser `SLASH_DICE` une seule fois au démarrage est silencieusement perdu et TOUTES les lames rendent en bleu par défaut. `debug_slash_variants.gd` ré-assigne donc `Global.dice_type` avant CHAQUE coup et l'imprime. **Attrapé en MESURANT la teinte de la lumière ajoutée (diff contre une frame calme, bande moyenne pour éviter le cœur additif cramé), pas à l'œil** — l'œil voyait « blanc » et n'aurait jamais distingué rouge-raté de blanc-normal. Vérifié après fix : lumière ajoutée (94, 27, 36) sur dé rouge, et on voit le bloom blanc→accent (+6 quasi neutre → +14 rouge saturé).
  - **DIAGNOSTIC (3 causes cumulées, aucune n'est « les nombres sont trop bas » — c'est pour ça qu'éclaircir n'a jamais marché)** : (1) **le slash RATE sa propre freeze frame** — `DamageEffect` appelle `take_damage()` (qui spawn le slash) AVANT `hit_stop_impact()`, et le style CURRENT diffère ensuite de 0.055s de GAME time pour esquiver le flash blanc → sous un freeze à 0.1× ça s'étire à ~0.3-0.5s de temps réel, donc l'instant le plus photographiable de chaque coup montre flash + nombre et AUCUN slash, puis le streak joue son mouvement le plus rapide pendant la remontée ; (2) **il n'y a aucun hold** — le commentaire de `_spawn_smear_streak` promet sweep-puis-HOLD mais son tween de fade démarre à 35% de la vie pendant que le sweep court jusqu'à 45%, donc l'alpha tombe déjà avant la fin du mouvement ; (3) **la « lame » est un blob radial étiré** — elle ne lit comme une coupe QUE pendant qu'elle bouge, d'où le tuning rapide : ralentir seul laisserait une bavure lumineuse.
  - **4 styles derrière `enum SlashStyle` + `static var slash_style` (var, PAS @export — évite l'incident de strip par l'éditeur live)** : **CURRENT** (le shippé, code INTACT sous le dispatch) ; **CRESCENT** = vraie lame en croissant (bord convexe net, sillage concave doux, pointes effilées) qui apparaît À l'instant d'impact, blooms blanc-chaud → accent du dé pendant que le flash décroît, **HOLD immobile** (0.13→0.44s par palier), puis se dissout **queue-en-premier le long de sa propre longueur** ; **WOUND** = entaille gravée SUR le sprite (enfant de `Sprite2D`, donc elle hérite squash/knockback/scale/fade de mort gratuitement) qui PERSISTE 0.30→0.86s puis se referme — « la vitesse d'arrivée cesse de compter » ; **FLURRY** = le ladder de dégâts en CHORÉGRAPHIE (1 lame / 2 croisées / flurry de 3 dont la finisher est la plus longue, tient le plus longtemps et porte seule le cône de particules).
  - **⚠️⚠️ PIÈGE TWEEN LE PLUS COÛTEUX (a fait « disparaître » la wound 6 frames après l'impact alors que son hold avait encore 0.7s à courir) : `set_parallel(true)` APRÈS un `tween_interval()` ajoute dans l'étape COURANTE — celle de l'intervalle — donc le fade tournait EN PARALLÈLE DE SON PROPRE DÉLAI.** Utiliser `.parallel()` sur le 2e tweener, jamais `set_parallel(true)` derrière un intervalle. Diagnostic trouvé en éliminant pruning/z-index/bloom un par un.
  - **⚠️ `Array.filter()` retourne un `Array` NON typé** → l'assigner à un `Array[Node]` throw au runtime et **AVORTE toute la fonction** (donc aucune wound après la première). gdtoolkit parse ça sans broncher — seul le run le montre. Reconstruire avec une boucle explicite.
  - **⚠️ Dimensionner une bande contre la HAUTEUR DU CANVAS au lieu de sa BANDE VISIBLE sous-dimensionne d'environ 3×** (le croissant occupe ~58px d'un canvas de 176). D'où `CRESCENT_BAND_PX` / `WOUND_BAND_PX` : convertir une épaisseur ÉCRAN cible en scale passe par la bande, jamais par le canvas. Coûté un rendu.
  - **⚠️ « Bloomer » vers `accent * N` est PLUS SOMBRE que la naissance en blanc** — le bleu cobalt est (0.24, 0.48, 1.0), donc une marque née à 2.2 et « bloomée » vers accent×1.6 PERD sa luminance et coule dans l'art du corps. D'où `_hot_accent(accent, vers_blanc, gain, alpha)`. Symétriquement, un glow ADDITIF né trop chaud sature en blanc et **tue l'identité couleur du dé** (la partie que Julien aime) — chaud sur la frame d'impact, saturé le reste de sa vie.
  - **⚠️ Deux textures pour deux épaisseurs, jamais un scale.y** : amincir le même bitmap aplatit AUSSI son arc, et un cœur droit dans une lame courbe lit comme deux formes sans rapport. `_build_crescent(sigma_lead, sigma_trail, ...)` partage la géométrie d'arc entre les 2 couches.
  - **Le shader de wipe est construit depuis une STRING de code** (`Shader.new()` + `.code`), pas un `.gdshader` : zéro étape d'import, donc un cache de worktree périmé ne peut pas le transformer en parse error qui tue tout `enemy.gd`. Il ne touche que `COLOR.a` (en fragment canvas_item Godot 4, `COLOR` arrive déjà = texture × modulate).
  - **Les 2 blend modes du slash shippé sont CONSERVÉS et pour les mêmes 2 modes d'échec** : couche large en blend NORMAL accent saturé (l'additif délave en pastel et meurt sur les fonds clairs act-1 — c'est ce qui rend la coupe franchement BLEUE/ORANGE), cœur fin en additif blanc-chaud (ce qui survit sur les fonds sombres act-2).
  - **Harnais `debug_slash_variants.gd`/`.tscn` (racine, non committé)** : boote un VRAI combat et fait passer de VRAIS dégâts par `DamageEffect` (donc hit-stop, shake caméra, punch_zoom, popups et SFX réels — le freeze étant la moitié du problème jugé, le simuler invaliderait la planche), Movie Maker 30fps. Env `SLASH_STYLE|SLASH_DICE|SLASH_FIGHT|SLASH_DAMAGES|SLASH_GAP`. ⚠️ Il faut ajouter la Camera2D au groupe `"camera"` AVEC son script, sinon shake et punch_zoom no-opent en silence et on juge une scène plus calme que le jeu.
  - **⚠️ 2 points ouverts si un style est retenu** : FLURRY a besoin d'un cap pour les cartes multi-hit (Flurry, Stampede enchaînent déjà des hits → un flurry PAR hit) ; WOUND est la variante à surveiller pour l'encombrement (cap à 3 marques simultanées en place, mais un long tour sur un seul ennemi est le cas à regarder).

### H-036 (2026-08-26) Overcharge : paliers d'escalade au-dessus de ~18 Power

- **🔥 OVERCHARGE — PALIERS D'ESCALADE AU-DESSUS DE ~18 POWER (2026-08-26, demande de Julien : « j'aime le glow actuel qui monte avec le power, mais ça pourrait devenir DINGUE à très haut power, façon Balatro — sans bouffer tout l'écran, un feeling "mon power est incontrôlable" ») — VÉRIFIÉ PAR HARNAIS (`debug_overcharge.gd`/`.tscn` racine, non committé : 18 checks 0 fail, 3 CONTRÔLES NÉGATIFS) + RENDUS des 4 paliers + RÉGRESSIONS (charge_pulse 12/12, surge_motes 23/23, golem_carryover 12/12). NON PLAYTESTÉ.**
  - **Le constat de départ : le glow existant PLATEAUE.** Le gain d'énergie de l'emanation sature vers 12-16 power, donc un tour à 45 et un tour à 18 se ressemblent. Tout ce qui suit vit dans ce headroom inexploité. **4 lettres choisies par Julien sur un menu de 7 : B (le nombre s'embrase), A (gusts spontanés), E (le monde s'assombrit), F (simmer audio).** Écartées et NON à re-proposer : les fissures sur la face du dé (C) et la heat haze (D) — gardées en réserve pour un éventuel palier T3 plus tard.
  - **UN SEUL PILOTE : `_update_overcharge()` appelé en QUEUE de `_update_dice_aura_charge()`** — la fonction que TOUS les chemins de power appellent déjà (roll, reset de carte, début de tour, switch de type, keep Reservoir, restore ricochet, `_ready`). Donc le palier monte tout seul ET **se démonte tout seul sur chaque reset sans qu'aucun de ces chemins ne sache que le système existe**. Paliers : T1 = `POWER_TIER_THRESHOLD` (18, sa cérémonie EXISTE DÉJÀ — le flare d'ignition ; ne pas en ajouter une 2e), T2 = 30, T3 = 46. Un niveau continu 0..1 (`OVERCHARGE_LEVEL_FLOOR` 0.25 au franchissement de T1) alimente heat/hum/vignette ensemble ; les gusts et les braises sont GATÉS sur le palier.
  - **⚠️⚠️ LE PIÈGE CENTRAL, ET IL EST GÉNÉRAL À TOUT NOUVEL UNIFORM : un `ShaderMaterial` n'expose `shader_parameter/<nom>` comme VRAIE propriété qu'une fois ce paramètre ASSIGNÉ au moins une fois.** Déclarer l'uniform dans le `.gdshader` ne suffit pas — le material de dice.tscn n'écrit que amplitude/speed. Sans un `set_shader_parameter("heat", 0.0)` de seeding : `tween_property()` erreure (« does not exist in object ») et no-op en silence, `get_shader_parameter()` rend **null**, donc heat reste au défaut du shader POUR TOUJOURS et **la feature entière est morte en ayant l'air vivante**. La sonde « le shader expose-t-il un uniform heat ? » PASSAIT pendant tout ce temps. C'est aussi pour ça que `_update_power_float` utilise `set_shader_parameter` direct et pas un tween. **Corollaire harnais : affecter un `get_shader_parameter()` non assigné dans un `float` typé = « Nil to float » → la coroutine `_ready` meurt → `quit()` jamais appelé → HANG infini** (c'est comme ça que le 1er run a timeout).
  - **⚠️ LE LERP BLEU→ORANGE PASSE PAR LE MAGENTA : à T2 le nombre rendait SAUMON/ROSE.** Même famille que le lerp or→bleu qui atterrit dans la boue olive. **Fix : vider la teinte du dé vers le NEUTRE d'abord, puis teinter en braise** (accent → gris → ember ne croise aucune teinte). Check `B5` épingle l'ordonnancement `r > g > b` au MILIEU du ladder — ni B2 (« des pixels ont changé ») ni B3 (mesuré au sommet) ne voyaient ce raté.
  - **⚠️ Séparer le CORPS du glyphe de son CONTOUR se fait sur le CANAL MAX, pas sur la luminance.** Tous les accents de dés ont un canal ≈1.0 mais des luminances très différentes (mech 0.75, blue 0.47) → une porte en luminance n'embrasait le bleu qu'à **68%**, et les 32% de bleu restant ANNULAIENT l'orange : ça mesurait « plus chaud que le bleu » et ça rendait **blanc**. Le contour = ce même accent × un brun sombre, donc son canal max reste sous 0.22 même quand le nombre est flashé en `lightened(0.6)` — écart franc de part et d'autre de la rampe.
  - **⚠️ MÉTHODO : la moyenne de chaleur (r−b) sur TOUT le rect du nombre est inutilisable** — le glow bleu du dé déborde dedans et a tiré la lecture à 0.04 sur une frame dont les chiffres étaient franchement orange. **Mesurer la chaleur sur les pixels QUI ONT CHANGÉ uniquement** (0.24 réel). Et le seuil du check est **calibré sur une frame validée à l'œil**, pas inventé : les 2 versions rejetées mesuraient 0.09 (blanc) et 0.11 (à moitié embrasé), donc il discrimine encore les deux ratés.
  - **Ce que chaque lettre fait** : **B** = `heat` dans `power_float.gdshader` (nouveau `fragment()` — la LUMINOSITÉ suit heat directement, la COULEUR arrive tard via `smoothstep(0.25, 0.85, heat)`, donc T1 tremble/brille mais reste BLEU et seuls T2/T3 s'embrasent ; le cœur blanc-chaud est atteint par les PICS de flicker, pas par le niveau — le brancher sur heat rendait un nombre blanc uni) + braises additives montant du nombre à partir de T2 (groupe `overcharge_ember`, z 2, warmth 0.82 — un ember dans l'accent du dé est invisible dans le champ de lumière de ce dé). **A** = `_tick_overcharge_gust` dans `_process` rejoue le front de charge SHIPPÉ à amplitude réduite (0.40 T2 / 0.58 T3), toutes les 1.9-3.4s (T2) ou 1.1-2.1s (T3). **⚠️ Asymétrie VOLONTAIRE : une vraie charge tue toujours le gust spontané en vol, mais un gust spontané ne touche JAMAIS l'horloge `_last_charge_pulse_ms` et refuse de partir dans les 700ms qui suivent une vraie charge** — sinon une fuite tombant 100ms avant un Charge 4 lui mangerait son front. **E** = vignette +0.16 / brightness −0.08 sur le fond de combat (groupe **`battle_background`** ajouté sur le Sprite2D de battle.tscn). ⚠️ **Le material est DUPLIQUÉ puis réassigné** : c'est un SubResource potentiellement partagé entre instanciations, et une baseline capturée sur une copie déjà assombrie noircirait le jeu un peu plus À CHAQUE COMBAT sans que rien n'erreure (check `E3`). Effet de bord heureux : seul le DÉCOR s'assombrit, les ennemis gardent leur luminosité, donc intents/HP restent lisibles. **F** = `AudioStreamPlayer` dédié (bus SFX) sur le dé, volume −34→−13 dB + pitch 1.0→1.16 selon le niveau, coupé par `battle_over_screen_requested` (2 args → handler 2 args) et `_exit_tree` ; + le plafond du ladder de pitch du thud (`LAND_THUD_CHAIN_PITCH_CAP` 6) monte de 2 par palier.
  - **⚠️ HUM = ASSET PLACEHOLDER GÉNÉRÉ (`sounds/overcharge_hum.wav`, 2s / 22050 Hz / mono / 86 KB, script `build_overcharge_hum.py` au scratchpad).** Bouclage STRUCTUREL, pas un crossfade : toutes les fréquences sont des multiples de 1/durée et la couche de bruit est synthétisée en DOMAINE FRÉQUENTIEL (une irFFT d'un spectre harmonique est périodique par construction). **⚠️ Il n'est PAS ENCORE IMPORTÉ** — l'éditeur de Julien tournait, donc pas d'`--headless --import` concurrent (règle du projet). Le `load()` est null-gardé (donc zéro crash), et **le redémarrage d'éditeur requis de toute façon l'importera**. Le `.import` porte `edit/loop_mode=1` ET le code ré-affirme la boucle sur un `duplicate()` (`_looping_copy_of`) : si un rescan d'éditeur remet loop_mode à Disabled, le drone jouerait une fois et s'arrêterait, ce qui se lit comme un bug plutôt que comme une boucle manquante.
  - **⚠️ INK : l'escalade est GELÉE (elle ne se démonte pas — elle arrête de MONTER, et reprend au retrait de l'encre).** Ink existe pour cacher le nombre de Power ; un dé qui rugit + une pièce qui s'assombrit annonceraient l'info que l'ennemi vient de payer un tour pour dissimuler. **Un seul bool `OVERCHARGE_DURING_INK` pour prendre l'autre parti** (« tu peux cacher le nombre, pas l'énergie ») — rien d'autre à changer. À trancher par Julien au playtest.
  - **Leviers de tuning** : tout est en constantes `OVERCHARGE_*` en tête de la section (bas de dice.gd). Les seuils 30/46 sont les plus susceptibles de bouger — le plus gros hit enregistré d'un run est 40, donc T3 reste volontairement un spectacle de build, pas un mardi. ⚠️ **À surveiller au playtest** : la vignette qui mange la lisibilité des ennemis de bord d'écran, la densité sonore du hum sous les autres SFX, et la fréquence des gusts spontanés (s'ils deviennent du papier peint, allonger `OVERCHARGE_GUST_INTERVAL_*` avant de baisser l'amplitude). ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** (dice.gd + power_float.gdshader + battle.tscn édités hors éditeur, + 1 nouveau .wav à importer).

### H-037 (2026-08-25) Motes de Surge sur le dé + socket rouge « armé » par Armageddon

- **✨ MOTES DE SURGE SUR LE DÉ CENTRAL + SOCKET ROUGE « ARMÉ » PAR ARMAGEDDON (2026-08-25, demande de Julien : « j'aime les motes de l'infusion/shop, on les met en combat ? » + « rappeler au joueur qu'il peut roll sans carte dans le socket ») — VÉRIFIÉ PAR HARNAIS (`debug_surge_motes.gd`/`.tscn` racine, non committé : 24 checks 0 fail, CONTRÔLE NÉGATIF inclus) + RENDUS. NON PLAYTESTÉ.**
  - **Motes GATÉES sur Surge, PAS permanentes — c'est la décision de design centrale, ne pas la « corriger » en always-on.** L'infusion/shop utilisent les motes pour animer une image STATIQUE ; le dé de combat a déjà 3 systèmes continus qui disent « vivant » (emanation, aura, power orbs), donc une 4e couche permanente devient du papier peint en 3 tours et dépense de l'encombrement dans la seule zone qu'on se bat à préserver (la rangée de slots). **Surge, lui, n'avait AUCUNE présence sur le dé** alors que toute sa fantasy est « ce dé est plombé, chaque roll paie en plus » — le seul indice était un badge côté joueur, loin du dé. Densité scalée par `Global.surge_amount` (0.55s à Surge 1 → plancher 0.26s), donc monter l'échelle (Sleight → Ringer → Dead Weight en main) se VOIT sur le dé.
  - **⚠️⚠️ LE PIÈGE QUI A COÛTÉ 3 RUNS : `Panel/DiceDisplay` a `z_index = 1` dans dice.tscn — être un frère PLUS TARD ne suffit pas, z_index bat l'ordre d'arbre.** Les motes à z=0 rendaient parfaitement… DERRIÈRE la face opaque du dé. Toutes les sondes de propriétés étaient bonnes (`visible=true`, alpha 0.79, texture 256×256, rect global correct) et le pixel sous le centre de la mote était **identique au bit près** à la frame calme. `SURGE_MOTE_Z_INDEX = 2` (devant la face, sous DiceInk 4 / panneaux socket 8 / bouton ROLL 10 — donc un dé encré cache toujours les motes). **Avant d'ajouter le moindre enfant au cluster du dé : grep `z_index` dans dice.tscn, 6 nœuds en posent un.**
  - **⚠️ Piège de comptage : Godot renomme les frères homonymes en `@SurgeMote@2`** → un `name.begins_with("SurgeMote")` ne voyait qu'UNE mote sur N, ce qui a fait passer un check de densité sur du bruit (2 vs 3). Les motes sont dans le **groupe `surge_mote`**, jamais retrouvées par nom.
  - **⚠️ Géométrie mesurée, pas supposée : l'art du dé va jusqu'à y=173 en root-local mais le bouton ROLL (frère, z 10) couvre y≥148** — la moitié basse de la bande de spawn naissait invisible derrière le bouton. `_mote_spawn_base_y` est dérivé du rect RÉEL du bouton. Et la montée est **clampée** (`SURGE_MOTE_HEADROOM`) au lieu d'être juste « tunée pour ne pas dépasser », donc retuner la bande ne peut pas rouvrir le trou (23px de garde mesurés sous la rangée).
  - **⚠️ Tuning fait PAR MESURE, pas à l'œil** : aux valeurs du shop (11-22px / 0.38-0.62) les motes éclairaient **99px échantillonnés contre un plancher de bruit de 84px** — elles rendaient et n'existaient pas. Final : **14-26px, alpha 0.50-0.75, `burst(type, 0.78)`** → 415-551px. **Le warmth à 0.5 était encore bleu-sur-bleu** (bouillie pâle sur le dé Blue) ; à 0.78 le cœur lit comme une étincelle. Elles restent pâles et pas dorées parce que **l'additif sur un fond bleu saturé plafonne le canal B** — c'est correct pour la doctrine « lumière, pas matière » de ce dé. ⚠️ **La moyenne de région est inutilisable ici** (elle ne bougeait pas d'un iota quand je multipliais la taille par 1,5) : mesurer en AIRE ÉCLAIRÉE par diff contre une frame calme.
  - **SOCKET ARMÉ — le vrai constat n'est pas « il manque un rappel », c'est que le socket MENT.** Avec Armageddon en jeu, « ? / Drop a card / Place a card here » est FAUX, et c'est la seule chose que le socket dit jamais. Socket vide + Red + `Global.socketless_red` ⇒ titre **« Armageddon »**, art de la carte, ruban rouge **« No card needed »**, description **« Roll to deal ✦ damage to ALL enemies »** (+ « and gain Strength » si `socketless_red_strength > 0`), le tout dé-dimmé, plus un flash chaud one-shot **à la TRANSITION seulement** (un pulse permanent serait à l'écran la moitié du combat → papier peint).
  - **⚠️⚠️ NE JAMAIS remplir `charged_card_texture` pour y mettre l'art d'Armageddon** : `roll_dice()` ET `_fire_socketless_red()` s'en servent comme test « une carte est socketée ». Une face de carte là-dedans tuerait la bénédiction en silence tout en donnant un screenshot parfait. L'art passe par un **nœud overlay séparé** (`ArmedSocketIcon`, créé à la volée dans `panel`). Le harnais épingle l'invariant (B3).
  - **⚠️ `preload()` de l'art = PARSE ERROR qui tue TOUT `dice.gd`** quand le `.ctex` manque (cache de worktree périmé) — passé en `load()` runtime null-gardé, comme `_reskin_enemy`. gdtoolkit ne l'attrape pas.
  - Repeint aussi au **switch vers Red** (`_refresh_empty_socket_look`) : Armageddon est Min 6 et PAS `red_only`, donc il est presque toujours joué depuis un AUTRE dé, longtemps après le dernier `_set_socket_empty()`.
  - **⚠️ Bug PRÉEXISTANT repéré au passage, NON corrigé** : `_on_active_dice_changed` teste `if dice_type == "red" and new_dice_type != "red"` **après** avoir déjà fait `dice_type = new_dice_type` — les deux sont la même valeur, donc la branche « annuler la carte socketée en quittant Red » est morte depuis toujours. À trancher par Julien (la réparer changerait un comportement joué depuis longtemps).
  - **⚠️ Régressions : `debug_ricochet_reroll` échoue 12/15 DANS CE WORKTREE — vérifié identique sur HEAD sans mes changements** (844 erreurs « Failed loading resource », cache d'import périmé). `debug_golem_carryover` 12/12 ALL PASS. Rien à voir avec cette passe.

### H-038 (2026-08-25) Merge du pulse de charge sur main (implémentation concurrente archivée)

- **✅ MERGÉ SUR `main` le 2026-08-25 (`0ef2306`, merge propre depuis `claude/dice-charging-pulse-08e68c`, 0 conflit — zéro chevauchement de fichiers avec les 3 commits qu'avait pris `main` entre-temps).** ⚠️⚠️ **AU MOMENT DU MERGE, LE CHECKOUT PRINCIPAL CONTENAIT UNE 3e IMPLÉMENTATION NON COMMITTÉE DU MÊME EFFET** (celle du chat précédent, celui que Julien a abandonné en disant « we keep running in circles ») : elle scalait le NŒUD aura (1.34→1.46) + poussait `power_intensity` à 5.0 — approche **frontalement opposée** à celle retenue (le diagnostic round 2 est justement que l'inflation de l'aura NOYAIT l'onde). **Verdict de Julien : garder celle-ci + porter 2 fixes.** L'implémentation écartée est **archivée en patch** → `Desktop\daiso_archive_2026-07-31\superseded_aura_scale_pulse_2026-08-25.patch` (205 lignes ; contient AUSSI ses édits non liés de cartes/`main_character_chibi.png`, qui eux sont restés non committés et intacts dans le checkout). **LEÇON : toujours `git status` le CHECKOUT PRINCIPAL avant un merge, pas seulement le worktree — git refuse de merger sur un fichier modifié non committé, mais surtout ça révèle une session parallèle sur le MÊME sujet.**
  - **Les 2 fixes portés (orthogonaux au design retenu, valables quelle que soit la variante)** : (a) **VRAI BUG DE COLLISION : une charge et un atterrissage de roll écrivent TOUS DEUX `aura.scale` sur la même frame** — un dé Green infusé Gnome charge un Blue **sur un 1 naturel**, donc pile sur cet atterrissage — et le petit punch d'atterrissage coupait le beat de charge en plein vol. Garde « le plus long gagne » (même règle que le hit-stop ref-counté). ⚠️ **Adapté au mécanisme de CETTE branche** : ici le beat de charge est le kick de l'`AnimationPlayer` « charge », pas un tween, donc la garde teste `animation_player.is_playing() and current_animation == "charge"` au lieu d'un tween tracké. (b) **`dice_interface.gd::_ready()` pose `z_index = 5`** → la rangée de slots dessine AU-DESSUS de l'aura/emanation du gros dé (les deux étaient à z 0 et l'ordre d'arbre mettait le dé devant), donc le pulse ne lave plus les compteurs — **c'est ce qui plafonnait la luminosité autorisée du pulse**. Posé dans `_ready` et pas sur l'instance de `battle.tscn` pour que tous les harnais qui construisent la rangée à la main en profitent. Sûr : icônes de livraison à z 150 (ui_layer), bouton ROLL à z 10.

### H-039 (2026-08-25) Pulse de charge round 2 : le gust

- **💨 PULSE DE CHARGE — ROUND 2, « LE GUST » (2026-08-25, même jour : le round 1 ci-dessous a été REJETÉ par Julien « still not satisfied », et il a demandé le gust) — VÉRIFIÉ PAR HARNAIS (`debug_charge_pulse.gd` réécrit : 12 checks 0 fail, re-passés SUR `main` après merge) + PLANCHE DE 4 VARIANTES + DIFF COMPARATIF round1/round2. NON PLAYTESTÉ.**
  - **⚠️⚠️ ROOT CAUSE DU REJET DU ROUND 1, MESURÉE (ne PAS re-tenter un band sprite scalé) : le front ne VOYAGEAIT pas.** Diff amplifié frame par frame : le bandeau orange fait la MÊME taille à +66ms et à +333ms, il enfle sur place puis s'éteint = une RESPIRATION, pas une onde. Cause structurelle : corps du band ~35px pour un trajet total de 40px → **aucun écart ne s'ouvre jamais entre le dé et le front**. Et un sprite SCALÉ ne peut pas corriger ça : sa largeur grossit exactement au rythme où il avance. Aggravant : le flash d'aura poussait `glow_reach` à MAX **dans la bande même que le front traverse, au moment où il la traverse** → les deux fusionnaient en un gros halo. **Règle : une onde n'est lisible que si son épaisseur reste très inférieure à la distance parcourue.**
  - **LE FIX = l'onde est faite du CHAMP DE LUMIÈRE DU DÉ lui-même, pas d'un sprite par-dessus** (`dice_emanation.gdshader`, nouveaux uniforms `gust` / `gust_radius` / `gust_color`) : front à **LARGEUR CONSTANTE en px** (`GUST_WIDTH 20`, jamais scalé) qui balaie de −10 à +62px **au-delà de la silhouette** (`d` du SDF, donc il épouse la forme du dé) ; au passage il **penche les langues vers l'extérieur** (`q -= dirn * gust_band * GUST_LEAN_PX`, on échantillonne le bruit EN ARRIÈRE = vent dans le feu), les **étire** (`reach += gust_band*34`), les **allume** (`lum *= 1+gust_band*1.5`) et rend le `dir_budget` momentanément ISOTROPE (l'événement souffle dans toutes les directions — c'est ça qui le distingue de « la flamme est plus haute »). Front légèrement plus net côté extérieur (facteur 2.6 vs 0.9) pour qu'il ait un SENS. `gust_color` = `burst(type, 0.55)` : **tiré vers le blanc chaud parce qu'un front bleu dans un champ bleu est invisible à n'importe quelle luminosité** (c'était le cas du round 1 sur une charge Blue). 2 tweens SÉPARÉS (rayon / amplitude) — les chaîner ferait attendre la décroissance jusqu'à la fin du trajet (un step de Tween attend son tweener le plus long) et produirait un pop.
  - **Preuve mesurée que ça voyage (le check qui aurait fait ÉCHOUER le round 1)** : profil radial du diff dans les couloirs dégagés à gauche/droite du dé — à +90ms l'énergie est concentrée près du dé (inner 0.255 / outer 0.041), à +300ms **la bande extérieure a DÉPASSÉ l'intérieure** (0.054 / 0.131). L'énergie quitte réellement le dé.
  - **⚠️ VRAI BUG TROUVÉ PAR LE DIFF, invisible avant : `row_side` utilisait `step()`**, donc un bord vertical DUR au flanc de la rangée de slots → **encoche rectangulaire découpée dans le haut du front** dès que la lumière devient assez vive (invisible sur les langues ambiantes ternes, criante sur l'onde). Passé en `smoothstep` feathered sur 20px. **Règle : tout masque sur cette couche doit être doux.**
  - **`ROW_HALF_WIDTH`/`ROW_BOTTOM_Y` ne sont plus des CONSTANTES du shader mais des uniforms** alimentés par `dice.gd::_sync_emanation_row_clearance()` depuis le **rect réel** de la DiceInterface (groupe `dice_interface`) — une copie en dur cesse de correspondre dès que quelqu'un bouge la rangée dans `battle.tscn` et **RIEN n'erreure quand ça dérive** (exactement le mode de panne du clamp END_TURN_LEFT/TOP périmé). Fallback sur les défauts authored hors combat.
  - **Flash d'aura : `glow_reach` ne saute plus à MAX** mais à `courant + 0.05` (`AURA_CHARGE_FLASH_REACH_BUMP`) — le flash reste un événement de LUMINOSITÉ à la SOURCE, l'étalement appartient au front. `power_intensity` va toujours au max (ça, ça marche).
  - **`charge_pulse_mode` (var, pas @export)** = variante de pulse. **Julien a choisi le mode 2 « punchier » sur la planche de bake-off → c'est LUI qui SHIPPE** (trajet 0.34s au lieu de 0.44, reach 69px, amplitude ×1.15). Les autres valeurs existent pour que la planche reste reproductible : 0 gust standard, 1 gust + fin anneau sprite, 3 aucun (baseline pré-pulse). Le texture d'anneau de `DicePalette` est **affinée** (σ 9/15 → 5.5/8) par la même leçon d'épaisseur.
  - **⚠️ BUG DÉMASQUÉ PAR LE PASSAGE EN MODE 2 : l'échelle par count était MORTE.** `gust` était `hint_range(0, 1)` et le peak `minf(..., 1.0)` → en punchy, 0.95×1.15 = 1.09 → clampé à 1.0 pour TOUS les counts, donc Charge 1 et Charge 4 rendaient un front identique. Range élargie à 1.5, cap à 1.25 (mesuré : c1 1.08 / c3 1.19). **Règle : quand un multiplicateur de variante pousse une valeur contre le plafond de son uniform, tout ce qui s'additionnait en dessous disparaît en silence.**
  - **⚠️ PIÈGE DE HARNAIS (2 checks en faux négatif) : échantillonner une onde sur une HORLOGE MURALE ne marche pas entre variantes.** Les samples early/late étaient à 90ms/300ms, calibrés sur les 0.44s du mode 0 ; en mode 2 (0.34s) le sample « late » tombait APRÈS la fin de l'onde → « l'énergie n'est jamais partie », faux échec. Les samples sont maintenant keyés sur la POSITION du front (`gust_radius >= 2` puis `>= 55`), donc auto-calibrés quelle que soit la vitesse. + check A0 qui échoue bruyamment si un sample n'a jamais été capturé (sinon `_band_mean` rend 0.0 et satisfait la comparaison en silence).
  - **⚠️ VERDICT INVERSÉ LE 2026-08-27 — la rangée EST remontée, de 12px** (`DiceInterface` 214→**202** / 286→**274**, `ScoutPanel.offset_bottom` 218→**206** pour garder EXACTEMENT le même recouvrement de 4px qu'avant, prélevé sur les 26px de padding mort sous ses faces). Demande explicite de Julien (« lets slightly put higher the dice interface »), donc le ⛔ ci-dessous ne vaut plus comme interdiction — il reste utile comme HISTORIQUE (un premier essai à 20px avait été rejeté après coup). **12 et pas 20 exprès.** Rien d'autre à resynchroniser : `dice.gd::_sync_emanation_row_clearance()` lit le rect RÉEL de la rangée via le groupe `dice_interface`, et l'écart au gros dé (ActiveDice reste à 294) passe simplement de 8 à 20px. ⚠️ Les harnais qui composent la rangée à la main (`debug_slash_variants.gd`, `debug_held_die.gd`) codent `Vector2(514, 214)` en dur — mis à jour dans `debug_held_die`, **pas** dans `debug_slash_variants`.
  - **RECALIBRAGE QUI SUIT LA REMONTÉE (2026-08-27, 2 allers-retours de playtest) — VÉRIFIÉ PAR RENDU + rects imprimés, régressions held_die 10/10, golem 12/12, ricochet 26/26.** Le couloir utilisable ne fait que **122px** (bas de la top bar 80 → haut de la rangée 202), et deux éléments y étaient calés à la main contre l'ANCIENNE hauteur.
    - **ScoutPanel** : 90..206 → **88..192** (h 116→104), titre font 28→**24**, faces **50→48px**, séparation 15→**12**. Mesuré sur 6 faces : `(401,88) 408×104` = **8px d'air en haut, 10px en bas**. ⚠️ **Trajectoire à ne pas refaire : un premier essai à 42px de face a été jugé « dice textures a bit too small »** — 48 est le plancher acceptable, donc l'air se prend sur les MARGES, pas sur les dés. ⚠️ **La taille des faces vit à DEUX endroits** : les 6 `ScoutDice*` du `.tscn` ET `SCOUT_DICE_SIZE`/`SCOUT_DICE_SEPARATION` de `battle.gd` (qui calcule la largeur du panneau pour les Scouts 4-6) — les bouger ensemble.
    - **Tooltip de dé : ANCRÉ PAR LE BAS, centré au-dessus de la rangée, LARGEUR D'ORIGINE 204** (`show_tooltip_above(bottom, center_x, min_top)` dans `dice_tooltip.gd` ; `DICE_TOOLTIP_BOTTOM 196` / `CENTER_X 594` / `MIN_TOP 56` dans `dice_interface.gd`). ⚠️⚠️ **Le fond du problème : sa hauteur SUIT SON TEXTE et il grandissait VERS LE BAS depuis un coin haut-gauche codé en dur `(498,123)`** — donc un tooltip long enterrait déjà la rangée AVANT la remontée. Ancré par le bas il grandit vers le HAUT : **TOUS les tooltips se posent à 6px au-dessus de la rangée**, et seul le plus haut (Magma infusé, 139px) déborde de 23px sur le bas de la top bar. **C'est l'arbitrage retenu : protéger la RANGÉE (ce avec quoi on interagit) plutôt que le bas de la top bar (passif), qu'un popup de survol a le droit de couvrir.** ⚠️ **Deux passes REJETÉES par Julien, ne pas y revenir** : le mettre À GAUCHE du cluster (« should be right above the dice interface like it was before »), et l'élargir à 280 pour raccourcir le pire cas (« tooltips are too large, that looks bad, was way better before »). La largeur reste 204.
    - **⚠️ `dice_tooltip.tscn` : le `MarginContainer` était en `layout_mode = 0` avec des offsets FIXES (`offset_right = 202`), donc le texte wrappait à 200px QUOI QU'IL ARRIVE et élargir le panneau ne changeait RIEN à sa hauteur** (mesuré : 139px identique à 204, 240, 280 et 320 de large). Corrigé en ancrage plein-rect (insets 2px) + VBox `size_flags_horizontal = 3`. **Le bug est réparé mais la largeur reste 204** — donc aucun changement visuel aujourd'hui ; l'intérêt est qu'un futur élargissement fonctionnera vraiment.
    - **⚠️⚠️ Z-ORDER DU CLUSTER (2026-08-27, Julien : « the dice jumping when rolled should be IN FRONT OF the dice interface, not go behind it. Same for the +3 ») : `dice_interface.gd::_ready()` pose `z_index = 5` pour que la rangée passe au-dessus de l'AURA/EMANATION du gros dé — mais le CORPS du dé bondit dans cette bande à chaque roll et passait derrière.** Fix : **`Panel` (dice.tscn) passe à `z_index = 6`**, ce qui **décale tout son sous-arbre d'un bloc** et préserve donc chaque ordre interne (face → DiceInk). Ce qui doit rester SOUS la rangée (Emanation, aura) sont des enfants de la RACINE à z 0, intouchés. Conséquences à garder synchronisées : **`SURGE_MOTE_Z_INDEX` 2 → 8** (les motes sont des enfants de la RACINE, pas du Panel — elles seraient passées derrière la face sinon ; l'invariant « devant la face, sous DiceInk » est conservé : 8 entre DiceDisplay 7 et DiceInk 11), **bouton ROLL 10 → 12**, et le popup « +N » de `_spawn_roll_popup` (enfant racine, z 0 par défaut, il monte depuis le nombre de Power à travers la bande de la rangée) reçoit `z_index = 12`. Régressions z-sensibles re-passées : **surge_motes 24/24, charge_pulse 12/12**.
    - **⚠️ MÉTHODO : en `--headless` les hauteurs de RichTextLabel sont FAUSSES** — le même tooltip mesurait 246px headless contre 139px dans une vraie fenêtre. **Toute mesure de texte doit se faire FENÊTRÉE** (`--rendering-driver opengl3 --position 2000,2000`), jamais en headless.
    - ⚠️ Restent tolérés (transitoires, non signalés) : le tooltip recouvre le ScoutPanel si on survole un slot pendant un Scout, et le ScoutPanel passe SOUS la RelicBar (y 98..144, CanvasLayer 3 contre 0) au-delà de ~7 reliques.
  - **⛔ [HISTORIQUE, verdict renversé ci-dessus] RANGÉE DE DÉS : REMONTÉE PUIS REMISE EN PLACE.** Elle a été remontée de 20px (`DiceInterface` 214→194) pour donner de l'air au gust, PUIS **Julien a demandé de la remettre où elle était après avoir vu le résultat en jeu** (« charge animation is better but you can put dice interface back to where it was », 2026-08-25) → `battle.tscn` re-mis à **214/286**, et le trim du `ScoutPanel` qui n'existait que pour l'accompagner est **reverté aussi** (`offset_bottom` 198→218). État = celui d'avant, playtesté. **Le gust fonctionne très bien avec les 8px d'écart d'origine** (12/12 checks re-passés à 214, l'onde part toujours : outer 0.323 vs inner 0.100). Le verdict porte sur le LAYOUT, pas sur l'effet.
  - **Ce qu'il faut retenir du détour (utile si la rangée doit bouger un jour pour une AUTRE raison)** : le `ScoutPanel` descend à y=218 et **frôle déjà le haut de la rangée (4px de recouvrement, état d'origine)**, donc remonter la rangée l'enterre sous le panneau de scout — mais le ScoutPanel a **26px de padding MORT sous ses faces de dés** (son HBox finit à +102 pour un panneau de 128 de haut), donc il y a de la marge à récupérer sans rien changer visuellement. `_resize_scout_panel` lit `size.y` au runtime pour son pivot → rien à retoucher côté anim. Et **aucune constante shader à resynchroniser** : la clearance de la rangée est dérivée du rect réel (uniforms) depuis cette session, donc bouger la rangée suffit.

### H-040 (2026-08-25) Pulse de charge round 1 (REJETÉ, gardé pour la leçon)

- **💥 [ROUND 1, REJETÉ — gardé pour la leçon, voir le round 2 ci-dessus] PULSE DE CHARGE CONTENU AU DÉ (2026-08-25, demande de Julien après le revert du shockwave : « I want the player to feel the impact that something happened / came from the dice » — branche `claude/dice-charging-pulse-08e68c`) — VÉRIFIÉ PAR HARNAIS (`debug_charge_pulse.gd`/`.tscn` racine, committé : 14 checks 0 fail, CONTRÔLE NÉGATIF inclus — cooldown forcé → 0 ring, et le diff de pixels tombe de 21k à 4.9k, preuve que la métrique mesure bien l'anneau) + STRIPS MOVIE MAKER 30fps (mode `CHARGE_PULSE_MOVIE=1`) + régressions golem 13/13 et ricochet 27/27. NON PLAYTESTÉ.** Le beat universel de `_on_dice_charged` gagne UN front d'onde discret : le constat était que tout l'existant (flare d'aura, surge, particules convergentes) est de la modulation de glow CONTINUE — rien de discret ne SORT jamais du dé, donc pas d'impact perçu. (1) **`DicePalette.die_ring_texture()`** : annulus carré-arrondi partageant la géométrie SDF de `die_halo_texture` (bande au même 52% via `DIE_RING_BAND_FRACTION`), gaussienne asymétrique GRASSE (σ 9/15 — un front fin étalé sur une circonférence disparaît, leçon du revert) + légère irrégularité angulaire pour lire « énergie » et pas « stroke d'UI ». (2) **`dice.gd::_spawn_charge_pulse()`** : bande teintée `burst(charged_type, 0.18)`, spawn juste hors du panel (échelle DÉRIVÉE du rect réel — un resize du dé ne peut pas ré-enterrer le front), trajet 81→121px en 0.42s **QUAD** ease-out (jamais EXPO), alpha snap 0.05s puis fonte EASE_IN sur son propre tween (jamais de peak tenu pendant l'expansion) ; sous le cluster par ORDRE D'ARBRE (`move_child(ring, 1)`, jamais z_index négatif) ; échelle par count (+10%/dé, cap 4) + écho unique à 50% pour Charge 2+ ; **cooldown 110ms** = une volée multi-type same-frame ne stacke pas N fronts additifs. (3) **L'anim « charge » de l'aura retimée en KICK** (dice.tscn : pic 1.2@0.5s → 1.16@0.1s, settle ease-out 0.55s) pour que le canal scale tombe sur le MÊME beat que le flare de brightness au lieu de le traîner. Gate d'absorption INTACT (check A5 : déviation `dice_display.scale` = 0.000 sur charge non-active) ; sur charge active le front meurt PILE quand le punch d'absorb tombe (~0.3s) — annonce puis absorption, jamais en compétition. Leviers : consts `CHARGE_PULSE_*` en tête de dice.gd. ⚠️ REDÉMARRER L'ÉDITEUR avant de jouer (dice.tscn + 2 .gd édités hors éditeur, pas de nouvel @export/class_name).

### H-041 (2026-08-23) Rareté des reliques + reliques shop-only

- **🎖️ SYSTÈME DE RARETÉ DE RELIQUES + RELIQUES SHOP-EXCLUSIVES (2026-08-23, demandé par Julien juste après le batch : « might wanna establish rarity first, and implement the rarity system btw, and shop-exclusive relics ») — VÉRIFIÉ PAR HARNAIS (`debug_relic_batch` étendu : **250 checks, 0 fail**) + régressions (golem 12/12, ricochet 26/26). NON PLAYTESTÉ.**
  - **`Relic.RarityTier {COMMON, UNCOMMON, RARE}` + `@export var rarity_tier` + `@export var shop_only: bool`.** Rareté et exclusivité sont **volontairement ORTHOGONALES** (contrairement à STS qui fait de « Shop » une 4e rareté) : une relique shop-exclusive a quand même besoin d'un niveau de puissance et d'un prix. COMMON = valeur par défaut de l'enum → **seules les Uncommon/Rare ont une ligne dans leur `.tres`** (même convention que les cartes, et ça minimise la surface d'un strip d'éditeur).
  - **⚠️⚠️ LE PIÈGE CENTRAL DU TIRAGE, à ne PAS re-introduire : pondérer CHAQUE RELIQUE par le poids de son tier n'est PAS équivalent à tirer le TIER puis une relique dedans.** La 1re version faisait ça et les odds dépendaient alors du NOMBRE de reliques par tier : avec le roster actuel (15/27/6) un nominal 50/33/17 sortait à **~43/51/6** — donc ajouter des Uncommons rendait les Uncommons PLUS probables et les Rares plus rares. `_weighted_pick()` choisit maintenant le tier d'abord (poids 50/33/17), puis uniformément dedans → odds stables quelle que soit la croissance du roster (mesuré 45/36/20 sur 400 tirages). Les tiers épuisés (toutes les Rares déjà possédées) sont retirés et les poids renormalisent, donc jamais de reward qui disparaît.
  - **`shop_only` REMPLACE l'appartenance au pool comme mécanisme d'exclusivité.** Avant, « shop-only » voulait dire « pense à éditer 2 fichiers » — une 2e source de vérité qui dérive en silence. **Les 2 pools (`treasure_relic_pool.tres` et `shop_relic_pool.tres`) contiennent maintenant EXACTEMENT le même roster (48)** et c'est le flag qui filtre : `get_random_relic()` exclut les shop_only, le nouveau **`get_random_shop_relic()`** les inclut (seul chemin qui peut les sortir). ⚠️ **`card_shop.gd` lisait `shop_relic_pool.pool` EN DIRECT** (shuffle + slice) et contournait donc tout filtre — il passe par le tirage pondéré maintenant, une relique à la fois avec la liste courante en exclude (pas de doublon entre les 3 slots). Vérifié : plus aucun `.pool` de relique lu en direct ailleurs.
  - **⚠️ `elite_relic_pool` pointe sur LA MÊME ressource que treasure** (`run.tscn` ligne 169 → `treasure_relic_pool.tres`) : il n'existe pas de fichier de pool élite. Trésor, élites et les 9 events tirent donc tous du même pool. Si on veut un jour des reliques d'élite distinctes, c'est un vrai fichier à créer, pas juste un tri.
  - **RÈGLE D'EXCLUSIVITÉ (plus étroite que « non blue/red »)** : shop_only = **les reliques MORTES sans un type de dé qu'on POSSÈDE**. Giant's Signet (paie sur un roll Giant) oui ; **Volcanic Rock / Trick Scale / Obsidian Scale NON** — elles DONNENT leurs dés exotiques, donc elles marchent pour tout le monde et restent au pool normal. C'est la distinction **payoff vs générateur**. Actuellement 2 membres : `giants_signet` + `hagglers_loupe` (celle-ci par saveur : une remise sur les dés, achetée là où elle s'applique — pattern Membership Card de STS).
  - **PRIX PAR RARETÉ** (`shop_relic.gd::PRICE_BY_RARITY`) : Common 70-95, Uncommon 95-125, Rare 125-160 — le `@onready var gold_cost` est devenu une assignation dans `set_relic()` (un @onready tournait avant que la relique soit posée). **Le plafond du contrat de juillet tient : 160 reste clairement sous n'importe quel dé** (115-220).
  - **⚠️ RÉPARTITION ACTUELLE DÉSÉQUILIBRÉE, à trancher par Julien : 15 common / 27 uncommon / 6 rare.** Les odds sont bonnes (le tier est tiré avant la relique) mais **6 Rares c'est mince** — un tirage Rare se répète vite — et le tier Uncommon est un fourre-tout. Piste : promouvoir 3-4 Uncommons vers Rare (Runic Bones, Blood Sword, Prismatic Lens sont les candidats les plus « build ») et redescendre 4-5 vers Common (Cartographer's Quill, Dice Chip, Philosopher's Stone, Arcane Hat). **C'est de la DATA** : une ligne par `.tres`, aucun code à toucher.
  - **Récap visuel des 21 (icône + effet + rareté, groupé par archétype) : https://claude.ai/code/artifact/4955b223-4df7-49c8-b48a-fc0b075d0f97**
  - **MERGÉ SUR `main` le 2026-08-23** (`3b56da1` depuis `claude/relics-design-framework-1bb8fa`, merge auto propre de `global.gd` et `dice.gd`). ⚠️ **Le travail non committé de Julien a été CHECKPOINTÉ AVANT le merge** (`ce83cdb` : rework de Compound, `player.gd`, le commentaire + reset fight-scoped de `starting_power_next_turn`, un print de debug retiré) — rien d'écrasé, mais ce checkpoint n'est pas relu. ⚠️ **Trouvaille au passage : `battle.gd:174` reset `starting_power_next_turn` AVANT `battle_started.emit()` (l.182), donc le hook de Pilot Light tombe APRÈS le reset — il marche, et ne peut pas fuiter d'un combat à l'autre.**
  - ⚠️⚠️ **APRÈS LE MERGE, `main` NE TOURNE PAS TANT QUE LES 21 PNG NE SONT PAS IMPORTÉS** : le harnais y échoue à 46 checks avec « resource failed to load » sur CHAQUE relique — signature exacte du cache d'import périmé (les `.tres` référencent des textures sans `.ctex`). **Aucun `--headless --import` CLI n'a été lancé : l'éditeur de Julien était OUVERT** (règle « éditeur ouvert = jamais d'import CLI concurrent »). Redémarrer l'éditeur importe les 21 PNG et résout tout. Le MÊME code passe 250/250 dans le worktree, dont le cache est à jour — c'est le cache, pas le code.
  - ⚠️ **`relic.gd` a gagné 2 `@export`** → configuration exacte de l'incident de strip par l'éditeur live : **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR** avant de rouvrir le projet, sinon un re-save peut effacer `rarity_tier`/`shop_only` des 35 `.tres` concernés (tout retomberait silencieusement en Common non-exclusif).

### H-042 (2026-08-23) Batch de 21 reliques

- **💎 BATCH DE 21 RELIQUES (2026-08-23, designs approuvés par Julien sur la grille condition × effet, art généré par lui le même jour) — VÉRIFIÉ PAR HARNAIS (`debug_relic_batch.gd`/`.tscn` racine non committé : **241 checks, 0 fail**, CONTRÔLE NÉGATIF fait) + RÉGRESSIONS (golem_carryover 12/12, ricochet_reroll 26/26) + RENDU MOTEUR de la rangée à 46px. NON PLAYTESTÉ. Pool trésor 27 → 47, pool shop 27 → 48.**
  - **Les 21** : Underdog Ring (roll 1-2 → 2 Block), Needle Die (roll 1 → 3 dmg), Worm's Eye Lens (cartes **Max** +3 dmg), Sixth Gear (6e dé du tour → 6 Power), Conductor's Baton (chaîne de 4 → Charge 1, 1×/tour), Giant's Signet (Giant ≥10 → 1 Str), Pilot Light (+2 Power à chaque début de tour), Marked Deck (1er roll Red du combat = face max), Consolation Chip (Red ≤2 → Charge 1 Red), Jackpot Pin (Red 6 → 2 Str), Blood Chalice (carte jouée depuis le socket Red → Exposed 1), Whetstone Pendant (2 Str par combat), Mortar Trowel (garde 5 Block entre les tours), Thorned Plate (block PARFAIT → 4 dmg en retour), Fuel Gauge (Refuel → 2 Block), Wayfinder Compass / Diplomat's Seal (switch de type **avec du Power en banque** → +2 next roll / 3 Block), Stray Die (1 dé bonus d'un type NON possédé par combat), Alms Box (Blessing → 5 Block), Haggler's Loupe (dés −15%), Golem Heart (tous les dés non dépensés se reportent).
  - **⚠️ AMENDEMENTS DE JULIEN À RESPECTER** : Consolation Chip rend **un dé Red** (pas du Block) ; les 2 relics de switch exigent **« with Power left »** — sans ce gate elles se farment en tapotant entre types à 0 Power sur un tour mort. Et son idée de **shelf** est implémentée a minima : **Giant's Signet est SHOP-ONLY** (dans `shop_relic_pool.tres` mais PAS dans `treasure_relic_pool.tres`) — une relique type-spécifique dans un coffre = coffre mort pour qui n'a pas ce dé. Étendre la shelf = déplacer d'autres entrées entre les 2 pools, rien d'autre à coder.
  - **7 HOOKS CŒUR AJOUTÉS (tous par défaut inertes, donc zéro changement de comportement sans la relique)** : `global.gd` (9 vars « relic-owned » + reset dans `reset_run_state()` + **discount appliqué APRÈS l'escalation** dans `current_dice_price`) ; `card.gd::play()` (scope `playing_card_requirement` + `last_played_card_targets` résolus **AVANT** l'emit `card_played`, sinon un listener lit les cibles de la carte PRÉCÉDENTE) ; `modifier_handler.gd` (bonus Max plié dans `base`, comme Dead Weight+) ; `dice.gd` (branche Marked Deck, skippée si une garantie Scout/Lucky est en attente → la relique ne peut jamais écraser une face choisie par le joueur et **reste armée**) ; `dice_interface.gd` (`keep_all_dice_always` en OR sur le stash existant + capture `power_at_last_switch` sur les 9 sites d'emit) ; `player_handler.start_turn()` (capture du Block **avant** le wipe) ; `enemy.gd::do_turn()` (`Global.acting_enemy` pour que le thorns sache qui a frappé) ; nouveau signal `Events.player_fully_blocked(attacker)` émis par `damage_effect.gd` **sur le même test que le popup « Blocked »** (la relique ne peut pas contredire ce que le joueur voit).
  - **⚠️⚠️ PIÈGES D'ORDONNANCEMENT, la partie la plus coûteuse — ne PAS les re-découvrir** : (a) **`player_handler.start_turn()` active les relics AVANT d'émettre `player_turn_started`**, et `dice.gd` **ASSIGNE** `roll_value` sur ce signal → toute relique qui ajoute du Power directement se fait **écraser en silence**. Pilot Light passe donc par `Global.starting_power_next_turn` (le champ de Stockpile, consommé par dice.gd), alimenté sur `battle_started` et `player_turn_ended` — les 2 sont strictement AVANT la consommation. Le `+=` fait qu'il **stacke** avec Stockpile au lieu de s'écraser. (b) **`activate_relics_by_type()` est TWEEN-DIFFÉRÉ** (0.5s entre chaque relique) : un relic START_OF_COMBAT finit d'agir AVANT que `start_battle → start_turn` ne fasse le refill, qui **ÉCRASE** `current_amount` par `max + bonus` → Stray Die écrit dans **`bonus_amount`** (mécanisme de Dice Bag), jamais dans `current_amount`. (c) `active_dice_changed` est écouté par le handler qui remet le Power à 0 → lire `roll_value` depuis une relique serait une **race décidée par l'ordre de connexion** ; d'où la capture côté ÉMETTEUR.
  - **⚠️ VRAI BUG ATTRAPÉ PAR LE MOTEUR, PAS PAR gdtoolkit** : `var price := DICE_BASE_PRICES[type] * pow(...)` = subscript de Dictionary → **Variant** → `:=` ne peut pas inférer → **tout `global.gd` cesse de parser**, et avec lui TOUT ce qui touche Global. Les logs d'import sont passés de **1272 → 791 → 2 erreurs** une fois typé `: float`. **gdtoolkit dit « propre » sur ce fichier** (parser syntaxique, zéro inférence). Même piège 2× de plus dans le harnais (`relic.icon.get_width()` — `icon` est typé `Texture`, la BASE, qui n'a pas `get_width()`), et un parse error dans un harnais le fait **HANGER à l'infini** (script non chargé → `_ready` jamais appelé → pas de `quit()`). **Règle : après toute édition, lancer un `--headless --import` et compter les erreurs — c'est le seul filet contre cette classe de bug.**
  - **ART** : 21 icônes générées par Julien (zip `Adobe_2026-08-23 (1)`), keyées par `key_relic_icons.py` (scratchpad) au pipeline maison (rampe de distance → plus gros blob → remplissage intérieur → de-fringe depuis un cœur non teinté → resize PRÉMULTIPLIÉ → plancher d'alpha). **⚠️ Sorties CARRÉES à 512×512, remplissage 0.94 du canvas** : `relic_ui.tscn` dessine en `stretch_mode = 6` (KEEP_ASPECT_COVERED) donc **toute icône non carrée perd son axe long** (le bug « House Money coupée en deux » du 07-11) — et 0.94 plutôt que 0.84 parce que les reliques DÉJÀ en jeu sont croppées serré (~1.00), donc une marge plus large ferait paraître toutes les nouvelles plus petites que leurs voisines. Overrides par icône pour les formes FINES (baton, needle, whetstone, pilot light) : normaliser sur l'axe long sous-pondère une diagonale — comparer la **surface peinte** (leçon des icônes de map). Le zip contient **16 autres icônes de relique (série `664121`, prompts de Julien) NON utilisées**, plus de l'art d'ennemis act-2 (2e zip entier) — rien touché.
  - **⚠️ 2 POINTS À TRANCHER PAR JULIEN** : (1) **« Marked Deck » montre un DÉ, pas un jeu de cartes** — le nom vient de l'argot du tricheur mais l'art (correctement généré depuis mon prompt) est un dé encoché ; « Marked Die » réglerait la dissonance, non renommé sans son accord. (2) `prismatic_lens_REGEN.png` (regen de l'icône d'une relique DÉJÀ en jeu) est **gardé au scratchpad, non intégré** — swap = 1 copie + repointage.
  - **⚠️ REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** : 21 `.tres` + 2 pools + 8 fichiers cœur édités hors éditeur, et un éditeur resté ouvert re-sauve sa copie périmée (incident de vidage du pool documenté 3×). Les `.tres` ont tous un `uid=` explicite (incident Bullseye+).

### H-043 (2026-08-20) Review de ~30 cartes + 8 coupes (throw sans Strength, passifs en main)

- **🃏 PASSE DE REVIEW DE CARTES (2026-08-20, verdicts de Julien sur ~30 cartes + 8 coupes) — VÉRIFIÉ PAR HARNAIS (`debug_card_review_batch.gd`/`.tscn` racine, non committé) + gdtoolkit sur les 44 scripts touchés, NON PLAYTESTÉ. Pool 95 → 87 draftables, et 16 cartes « + » CRÉÉES (le batch d'art des 19 nouvelles cartes n'en avait AUCUNE).**
  - **⚙️ CHANGEMENT MÉCANIQUE GLOBAL : les dés JETÉS ne prennent plus la Strength.** Ça INVERSE la décision du 07-21 (« la Strength s'applique aux dés jetés, cohérence ») — Julien : « too OP ». La cause était déjà mesurée à l'époque et se confirme : la Strength s'applique **une fois PAR DÉ**, donc Dice Avalanche (jusqu'à 9 dés) la multipliait par 9. Sites modifiés (le DÉ uniquement, jamais les dégâts propres de la carte) : meteor, meteor_plus, dice_avalanche(+), pixie_volley, cursed_toss(+), fastball(+). ⚠️ **`meteor.gd` a TROIS appels `DMG_DEALT` : seule la ligne du dé perd le modifier, les 2 autres (dégâts X de la carte + sa preview) le gardent.** `statuses/artillery.gd` ne l'utilisait déjà pas → il devient cohérent gratuitement. **Conséquence à retenir : `Global.thrown_dice_bonus_fight` (Trebuchet, appliqué dans `card.gd::_on_thrown_die_landed`) est désormais LE SEUL scaler de dé jeté** — +2/+3 × 9 dés sur Avalanche, donc Trebuchet passe de bonus marginal à vraie carte de build. L'Exposed de la CIBLE s'applique toujours (il vit dans `take_damage`). Berserker (+50% socket rouge) ne les atteignait déjà pas (flag cleared avant l'atterrissage différé).
  - **⚠️⚠️ 3 TROUVAILLES QUI ONT CHANGÉ LE DESIGN, à ne pas re-découvrir :** (a) **`Exposed` ne scale PAS en magnitude** — `ExposedStatus.MODIFIER = 0.5` en dur, UNE seule `ModifierValue`, `stack_type = 2` (DURATION) : **Exposed 5 = +50% pendant 5 tours, exactement le même +50% qu'Exposed 2.** Corrode passe donc de « X AoE + Exposed 2 » à « Exposed 5 seul » = il troque ses dégâts contre 3 tours d'uptime — verdict de Julien en connaissance de cause : « yes that is what i wanna do, a setup card » (donc mort dans un combat de 2-3 tours, c'est voulu). (b) **Compound « start next turn with 5 Power » était INJOUABLE** tel que specé : `compound.gd` émet `dice_roll_reset` et la carte n'est pas Celestial → une carte non-Celestial est refusée avant le 1er roll du tour, et tout reset vide `roll_history` → la seule fenêtre légale aurait été « pendant que tu as une bank », qu'elle jetait pour moins. **Fix validé par Julien : Celestial** (donc plus de reset, cf. la règle « aucune Celestial ne reset »). (c) **Le X de Greed est ÉPINGLÉ à 7 par son propre gate Exact 7** — même piège qu'Unity (dont le « X12 » avait justement été réécrit en « 12 » le 07-28). **Verdict de Julien : garder la notation formule des DEUX côtés** — Greed écrit « X2 / X », et Unity **REPASSE** à « X12 »/« X18 » : le glyphe ✦ enseigne que ça vient de la bank, le nombre résolu suit entre parenthèses. Seule la clause de dégâts est résolue dans la preview de Greed (Block et Gold valent X exactement — une 2e/3e parenthèse répéterait le même chiffre).
  - **⚠️ CLUSTER EXACT 7 ASSUMÉ (objection soulevée, TRANCHÉE par Julien : « no, i want every exact 7 card »)** : Corrode, Greed ET Dicelord's Gift sont tous à Exact 7, plus Refinement qui pousse le Power au multiple de 7 supérieur. L'objection était que les gates EXACT sont anti-synergiques entre eux (on atterrit sur 7, on joue UNE carte, le Power reset, il faut remonter à exactement 7 pour la suivante) — donc en drafter deux en rend souvent une morte. C'est un build-around volontaire, **ne pas le re-proposer**.
  - **🆕 NOUVELLE ARCHITECTURE « passifs en main »** — 3 cartes désormais INJOUABLES par design (Dead Weight, Blood Oath, Talisman) : `would_no_op_now()` retourne true et le refus de prise en main de `card_clicked_state.gd` fait le reste. Tout passe par `Global.in_hand()`, qui **scanne la main VIVANTE** (aucun cache, donc aucune désync possible) : `in_hand_roll_bonus()` (Blood Oath +2 / + = +3 sur Red — ⚠️ **même chiffre que la relique Blood Sword, et les deux s'empilent** ; Dead Weight = Surge 1) ; **nouveau `in_hand_damage_bonus()`** (Dead Weight+ = +1 dégât) branché **dans `ModifierHandler.get_modified_value()`**, ajouté à `base` AVANT de déléguer → se comporte exactement comme un stack de Strength, et **les vrais dégâts ET les descriptions dynamiques le récupèrent gratuitement** sans qu'aucune carte ne sache qu'il existe ; garde `_is_player_handler()` (le handler d'un ennemi ne doit jamais voir la main du joueur) ; **nouveau `in_hand_six_block()`** (Talisman : 3 Block par 6 naturel, + = 5) branché sur le check `last_roll == 6` de `dice.gd` — **le MÊME point que Jackpot et Effigy**, donc les trois cartes « sixes » ne peuvent structurellement pas diverger sur ce qu'est un 6 (face rollée, jamais un 5→6 Boosté/Surge). ⚠️ **Talisman ne trimme plus la face basse** : `Global.current_face_values()` redevient purement « édits de carte/infusion ».
  - **Pattern `stacks`-comme-payload (2 nouveaux cas)** : `effigy.tres` (5) / `effigy_plus.tres` (8), et `status_dicelord_gift.tres` (1) / `_plus.tres` (2), partagent chacun UN script, le nombre voyageant dans `stacks` avec `stack_type = NONE` (donc payload, pas badge). C'est la seule façon de faire diverger deux `.tres` sur un script à `class_name`, et ça évite d'ajouter un `@export` (= évite l'incident de strip par l'éditeur live).
  - **COUPÉES (8, fichiers laissés sur disque, convention)** : Cursed Toss, Double or Nothing, Momentum, Repel, Bulwark, Reservoir, Fumigation, Counterfeit. ⚠️ **Conséquences signalées à Julien** : Bulwark était le SEUL payoff d'armure (le block redevient purement défensif) ; les sources de pioche passent de 4 à 2 (Eyepoke + Overclock — Eyepoke descend à Common, ce qui compense l'accès) ; **tout le code de la pièce de Double or Nothing (`Events.coin_flip`, `_coin_flip_anchor`, le toss dans dice.gd) devient orphelin** — laissé dormant, pas supprimé.
  - **Autres points notables** : Smash passe **AoE** (X2 à tous, Min 10 conservé — c'est le remplaçant de Fumigation) et son Exposed part sur le « + » ; **Corrode base passe `type` ATTACK → SKILL** (`card.gd:234` est le SEUL usage de ATTACK : il gate le slash directionnel — un slash sans dégâts derrière lit comme un bug) ; Artillery tire dans **les 9 types, possédés ou non** ; Meteor+ jette une face **7-12 écrite en dur** et PAS via `thrown_faces_for("giant")`, sinon un joueur ayant déjà l'infusion Bulky aurait un upgrade no-op ; **Trebuchet+ était DÉJÀ à +3**, rien à faire ; nouveau mot-clé **« Reroll »** dans `KeywordColorizer.KEYWORDS` + `tooltip.gd` (demandé pour Quicksilver, profite à tout le jeu).
  - **⚠️ 2 PIÈGES DE HARNAIS (chacun ~5 min perdues)** : (1) **`godot ... | tail -40` ne montre RIEN si Godot ne sort jamais** — `tail` bufferise jusqu'à la fermeture du pipe, donc un hang est indiscernable de « Godot n'imprime rien ». Rediriger vers un FICHIER. (2) **Le `.godot/imported` d'un worktree fraîchement créé est PÉRIMÉ** : il précède le batch d'art des 19 nouvelles cartes, donc ~20 PNG n'ont pas de `.ctex` → chaque `.tres` qui les référence échoue à charger → le `const POOL := preload(...)` du harnais échoue → **le script ne charge pas, `_ready` ne tourne jamais, le harnais pend à l'infini**. Fix : `--headless --import` AVANT tout harnais dans un worktree neuf. Les 233 erreurs venaient toutes de là, zéro liée au batch.
  - **BALAYAGE DE COMPLÉTUDE (question de Julien « am i missing anything? any upgrade version? ») — LE POOL EST MAINTENANT À 87/87 CARTES AVEC UN UPGRADE.** Une seule manquait : **Cadence** (Max 6, Uncommon, « Deal 7 damage per consecutive Dice roll ») → **Cadence+ = Max 8** (verdict de Julien ; élargir le gate vaut mieux qu'un +damage parce que la carte est QUADRATIQUE — plus de marge = plus de rolls, chacun à 7 — et ça suit le pattern « l'upgrade desserre le gate » du reste du batch). Elle **réutilise `cadence.gd`** (seul `requirement_number` diffère). **+ 2 MISMATCHES DE RARETÉ CORRIGÉS** : Eyepoke+ était resté Uncommon après le passage d'Eyepoke en Common (causé par ce batch), et **Earthquake+ était Common sous une base Rare — bug PRÉEXISTANT, vérifié contre HEAD**. **Nouvel invariant pool-wide dans le harnais : tout « + » hérite de la rareté de sa base** (86 paires vérifiées) — la gemme est cosmétique sur un « + » (jamais draftable) mais elle se voit en deck view et dans le before/after de la campfire. **Harnais final : 335 checks, 0 fail.**
  - **⚠️ 2 UPGRADES SANS AUCUN INDICE SUR LA FACE DE CARTE (signalés, non traités)** : **Hoard+ et Kaleidoscope+** ont exactement la même description ET le même badge de requirement que leur base — la SEULE différence est la teinte Celestial du cadre. Cohérent avec le langage Celestial du reste du jeu, mais dans le preview avant/après de la campfire les deux cartes sont quasi indiscernables. Si ça gêne au playtest, le fix est une ligne de texte, pas de la mécanique.
  - ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** (44 scripts + ~60 `.tres` édités hors éditeur, dont le pool draftable — c'est exactement la configuration de l'incident de vidage du pool du 07-30).

### H-044 (2026-08-19) Système typographique unifié (5 voix)

- **🔤 SYSTÈME TYPOGRAPHIQUE UNIFIÉ — AUDIT COMPLET + PASSE APPLIQUÉE (2026-08-19, demande de Julien après le passage des events à Belwe : « check every string in the game and whether it would be better changed to belwe bold or something else ») — VÉRIFIÉ PAR HARNAIS (`debug_font_pass_check.gd`/`.tscn` racine non committé, 37 checks 0 fail : résolution de police par surface + intégrité de chargement des 17 scènes touchées) + RENDUS (tutoriel, endscreens, dice shop, card shop, tooltips, achievements, toast, chrome de combat) + les 3 RÉGRESSIONS RE-PASSÉES (double_endturn ALL PASS, tutorial_lock ALL PASS, audit_changes 28/0). NON PLAYTESTÉ.**
  - **⚠️ LA TABLE DES 5 VOIX — c'est ÇA la référence, à consulter avant d'écrire le moindre nouveau label** : (1) **CinzelDecorative-Bold or + outline** = titres d'ÉCRAN/panneau (PAUSED, Rewards, Game Over, Campfire, Dice shop, Daily Deal, Remove a Card) — c'est AUSSI le `default_font` du thème projet à 26px, donc **toute node sans override tombe dessus par accident** ; (2) **Cinzel-Bold** = petits titres et chrome de carte (titres de carte, rubans de requirement, « Choose a card », nombres d'intent, header « Your Odyssey ») ; (3) **Belwe Bold crème + ombre** = **LA voix du texte lu et de TOUS les boutons** (events, tutoriel, tooltips-titres, menus, dialogues, lignes d'achievements, noms de dés en boutique) ; (4) **LuckiestGuy** (variation `fonts/luckiest_guy_numbers.tres`, baseline corrigée) = **TOUS les nombres de HUD** (top bar, prix, popups de dégâts/block, compteurs de dés/piles/reliques, valeurs du scoreboard) ; (5) **MinionPro** = descriptions de carte (Semibold 12), labels de barre de vie, bannières d'act/tour, message de refus. **NotoSans-Medium reste sur les CORPS de tooltip uniquement** (plus lisible que Belwe à 11-12px, et le plafond 3 lignes est calibré dessus).
  - **CE QUI A CHANGÉ (~25 fichiers)** : tutoriel entier (corps, gras, les 2 Continue, Skip — c'était un mix Roboto Condensed + Roboto Black + Cinzel + thème) ; toast d'achievement + lignes de la page Achievements ; pause menu (labels de sliders, %, note, corps de confirm-quit) ; les 3 popups tutoriel de l'écran de reward ; dialogue d'avertissement power-reset en combat (il portait **3 polices** dans une seule boîte) ; corps + boutons des end screens (Main Menu 16 NotoSans à côté de Try Again 28 Belwe = migration jamais finie) ; sous-titre + Cancel + X de la load-confirm ; popup « Enable tutorial? » du main menu ; **labels des lignes** du scoreboard (valeurs restent LuckiestGuy) ; Back/Upgrade/Cancel de la deck view (ratés par la passe des 67 boutons car ils n'utilisent pas les styleboxes shop) ; **titres de tooltip** (card/dice/icon rejoignent status en Belwe) ; **dice shop** (9 bannières de noms de dés + mot « Reroll » + X — le titre reste Cinzel) ; card shop (badge -20%, Sold!/Removed!, X du fan, nom du deal die). ⚠️ **Verdicts « garder tel quel » respectés** : End Turn/ROLL (paire héros Cinzel), nombre de Power (Cinzel-Black), faces de carte (MinionPro — la machinerie de fit sur 275 cartes est MESURÉE, Belwe +14% dégraderait les tailles pour rien), HP/block, bannières d'act/tour, refus, statuts (Roboto 700), Start New Run/Load Run, CTA infusion/loadout, corps de tooltip.
  - **⚠️ MESURES DE LARGEUR (à réutiliser avant tout futur swap)** : à px égal Belwe fait **+3.7% vs NotoSans-Medium, +14% vs MinionPro, +22% vs Roboto Condensed, −14% vs Cinzel-Bold, −21% vs CinzelDecorative**. Donc : remplacer du Cinzel par du Belwe est **toujours sûr** (ça rétrécit), remplacer du **Roboto Condensed** est le seul cas à risque — c'est pour ça que le tutoriel a été rendu en entier avant de valider (pire cas mesuré : 3 lignes, tient). À x-height égale il faut **Belwe 18 ≈ NotoSans/Roboto 16 ≈ MinionPro 19 ≈ Cinzel 14 ≈ LuckiestGuy 12**.
  - **3 VRAIS BUGS trouvés par l'audit, corrigés** : (a) **le chip de bonus-requirement des cartes n'avait AUCUNE police** (`card_ui_bonus_requirement_label.tres` ne contenait qu'un `font_size = 8`) → il rendait en CinzelDecorative 8px sans outline, seule anomalie sur une face de carte, sur les 11 cartes à bonus ; passé au Cinzel-Bold des rubans. (b) **Fallback `[b]` silencieux** : tooltip/intent/dice-tooltip et les Description/BonusEffect des 2 implémentations de carte overridaient `normal_font` mais **pas `bold_font`** (plusieurs en overridant quand même `bold_font_size`) → tout `[b]` retombait en CinzelDecorative ; les 6 nœuds ont maintenant `bold_font` explicite. (c) **`base_button.tres` embarquait 465 KB d'Open Sans en base64** (police qui n'existe nulle part sur disque) pour les boutons de mini-jeu des events → repointé sur Belwe en `ext_resource`, **le fichier passe de 476 KB à 3 KB**.
  - **NETTOYAGE DE PAYLOAD (archivé, pas détruit → `daiso_archive_2026-07-31\unused_fonts_2026-08-19\` et `\dead_scenes_2026-08-19\`)** : **5.4 MB de polices** jamais référencées retirées du projet (Creepster, Metamorphous, PirataOne — ces 2 dernières étaient **préchargées par `dice.tscn` pour rien** —, les 3 arbres Roboto en double, `Luckiest_Guy/` doublon, les 2 VariableFont) + 2 scènes MORTES (`relic_tooltip.tscn` que rien n'instanciait, `dice_scout.tscn` remplacée par le ScoutPanel inline de battle.tscn). **Il reste 13 fichiers de police, tous utilisés.** ⚠️ `fonts/roboto_status_numbers.tres` dépend de `Roboto/Roboto-VariableFont` (GARDÉ, seul `Roboto/static/` est parti) et les tooltips dépendent de `Noto_Sans/static/` (gardé) — **les 2 copies de NotoSans sur disque sont toutes les deux vivantes**, ne pas en supprimer une sans repointer ses consommateurs.
  - **3 RÉGRESSIONS DE FIT SIGNALÉES PAR JULIEN AU PLAYTEST LE JOUR MÊME, corrigées PAR MESURE (harnais `debug_font_fit_fix.gd`/`.tscn` racine non committé — il mesure les 3 points et sert de garde pour tout futur swap)** : (a) **le « 16 » du badge de deck n'était plus centré** — le badge était calé à la main autour du LuckiestGuy BRUT (qui assoit ses glyphes ~0.2em trop haut) ; en le passant à la variation baseline-corrigée, le nombre est descendu de 2px dans une capsule de 25px (mesuré `dy +2.0`) → `vertical_alignment` BOTTOM → **CENTER** (mesuré après : `dy −0.5`, le `dx −1.0` restant est le side bearing du chiffre lui-même, il varie selon la valeur, ne pas le chasser). **⚠️ Leçon : la variation baseline-corrigée n'est PAS un drop-in sur un badge déjà tuné à la main pour le brut — elle déplace le texte, il faut re-vérifier l'alignement du conteneur.** (b) **le corps du popup « Enable tutorial? » débordait du panneau** : Belwe 22 donnait une ligne de **643px dans un panneau de 607** (+36) → **22 → 19px** (556px, ~25px de gouttière de chaque côté). (c) **le texte de la DiceShopExplanationBox chevauchait l'image du gobelin** : Belwe 14 passe de 2 à **3 lignes** (hauteur 54 au lieu de 34) et mordait de **11px** sur l'image → **l'IMAGE est descendue de 20px** (`offset_top -52.5→-32.5`) plutôt que de rétrécir la police, parce que c'est un callout destiné à un joueur qui découvre le jeu et que le panneau avait 39px de marge libre en bas (gap final 9px). **⚠️ `Font.get_string_size()` sur une string MULTI-LIGNES la mesure comme UNE seule ligne** (1273px ici) — mesurer ligne par ligne, sinon le diagnostic est faux d'un facteur 2.
  - ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** (17 scènes + 6 scripts + 2 thèmes édités hors éditeur, et des `ext_resource` supprimés → un éditeur resté ouvert peut re-sauver une copie périmée, incident documenté 3×). **À surveiller au playtest** : lisibilité des lignes d'achievement à 13px ; le « X » Belwe des boutons de fermeture (scout, dice shop) ; et **tout AUTRE texte qui remplaçait du Roboto Condensed** (+22% en Belwe) — les 3 débordements trouvés étaient tous de cette famille, donc s'il en reste un, il sera là. ⚠️ Piège de harnais : `main_menu.tscn` ET `run.tscn` **HANGENT** dans un SubViewport ici (section A de `debug_font_fit_fix` est la seule à en payer le coût, pour les pixels) — mesurer dans l'arbre, une section par process (`FONT_FIT_ONLY=A|B|C`).

### H-045 (~2026-08-19) Recette des prompts d'art de carte (card_art_prompt_guide.md)

- **🎨 PROMPTS D'ART DE CARTE — RECETTE VALIDÉE, LIRE AVANT TOUTE GÉNÉRATION : [card_art_prompt_guide.md](card_art_prompt_guide.md).** Le template EST le prompt que Julien a écrit lui-même pour la carte Block ; on ne change que 3 slots (sujet / palette `[couleur]-and-black` / teinte du burst). **Clauses porteuses à ne JAMAIS retirer** : `flat cel-shaded` (sinon rendu 3D lustré), l'ancre `in the style of Slay the Spire card art`, `thick black linework`, et surtout le fond `filled edge to edge … never pure black` (GPT lit « dark background » comme du NOIR LITTÉRAL — nos cartes ont des champs de LUMIÈRE colorée, coins qui virent à une teinte plus sombre de la MÊME couleur). ⚠️ **Le sujet s'écrit depuis l'EFFET de la carte, jamais depuis son nom** — Dead Weight (injouable mais qui DONNE Surge) avait été peinte en malédiction enchaînée : « refer to the card effect and make it make sense ». ⚠️ **PAS d'images de référence** (GPT Image 2 les copie quasi littéralement). Ratio : Wide 3:2 cartes, Square 1:1 icônes. **5 directions mortes documentées au §4 du guide (painterly / burst+debris = confettis jaunes plein cadre / deep black = vides noirs / saturé+glowing = métal lustré / reference image)** — ne pas les re-tenter. Leçon méta : quand un prompt frère marche déjà, COPIER sa structure au lieu de théoriser un nouveau style (4 rounds perdus avant d'utiliser celui de Block). Deck live avec boutons copier + suivi : https://claude.ai/code/artifact/4c343e1e-5cc6-4303-bb44-c1b6ebda913e

### H-046 (2026-08-16) Audit global de balance ennemis + baseline (analyse)

- **⚖️ AUDIT GLOBAL DE BALANCE ENNEMIS + BASELINE CIBLE (2026-08-16, demandé par Julien : « global audit of enemy stats… use sts2 working formula and our existing model… make sure most fights have anti-stall mechanics… keep in mind bad cards ») — ANALYSE SEULE, RIEN D'IMPLÉMENTÉ, verdicts en attente (decision sheet §9 du doc). Voir [enemy_balance_baseline_2026-08.md](enemy_balance_baseline_2026-08.md).** (1) **Le décompilé STS2 (Desktop\sts2_ref, extrait gdre, C# propre) a été miné POUR DE VRAI** — dataset complet préservé dans [sts2_enemy_dataset_2026-08.md](sts2_enemy_dataset_2026-08.md) (chaque monstre act 1 A0 : HP/moves/patterns, élites, boss, junk cards avec doses, gold, baselines joueur ; l'ascension est encodée `GetValueIfAscension` donc les valeurs A0 sont sûres). **Census anti-stall de la référence : 24/33 monstres act-1 RAMPENT, 100% des élites/boss ont une horloge, et les non-rampeurs portent TOUJOURS un substitut** (debuff joueur, injection de junk, defense tech) — la directive de Julien est littéralement leur formule shippée. (2) **Modèle unifié qui reproduit les nombres des DEUX jeux : EHP = T×P×s** (T = tours cibles, P = Power on-curve, s = part offensive 0.7→0.55 selon tier) + DPT cible = block on-curve + attrition/tour. **⚠️ Courbe P AJUSTÉE POST-REPRICE DES DÉS (rappel de Julien en cours de session)** : le reprice du 08-13 met le dé #1 au 1er shop (~floor 5, aisle 115-165 avec les dés à GROS EV Golem 5.0/Ricochet 4.0) → fl 4-8 = 18-23 (était 17-21), fl 9-13 = 26-34 — **le reprice a re-donné ~7-8 HP de slack act-1, la plainte de juillet (« attrition budget got cheaper ») est REVENUE** ; et Greedy tick plus vite. (3) **Constats** : HP corrects PARTOUT (0 changement HP proposé — anti-bloat) ; **15/34 fights stall-safe** (les 12 critter fights du tier 0 + Lurker+Goblin + le 4-body + Hound) vs ~75%+substituts chez eux ; T2 solos 2-4 DPT sous cible (S1 confirmé indépendamment, aggravé par le reprice) ; **l'Absorb du Lich tick CHAQUE tour** (+Muscle = dernière face rollée — déjà la plus forte horloge du jeu) ; **Depleted = le SEUL statut joueur qu'aucun ennemi n'utilise**. (4) **Prescriptions** (toutes floors-pas-spikes, une règle/fight, riders sur beats existants via icon2) : **+1 Muscle rider sur Bigger Satyr (beat attaque-6) et Bigger Kraken (beat 7) = 11 fights couverts en 2 edits d'ennemi** ; Goblin rider §9 confirmé ; **Hound N4 6→7 + Molten Roar ≤50%** ; **Medusa : Weak 2 → Depleted 1 sur le beat 12 (« pétrifie un dé »)** ; Maelstrom 12→13 ; Leviathan +1 Sludge sur le beat Ink cap 3 (gated sur l'existence des cartes junk — le « 2e clock de boss » de la référence). **RÉVISIONS des plans précédents (data nouvelle)** : **SKIP le rider Lich §9** (Absorb EST l'horloge, one-clock-per-fight), **SKIP le Lurker creep** (Flux = le substitut, verdict §9.7-3 résolu), **backstop soft-enrage SHELVED définitivement** (0/122 monstres de la référence ne lisent le compteur de tours), **Gargantua buff §8 = OUI recommandé**. Coverage 16/34 → **31/34 horloges + 3 substituts, 0 stall-safe**. (5) **Junk cards** : ladder référence *s'évapore → persiste → blesse → blesse-ou-paye* avec doses réelles (slimes 1-2/cast dès le floor 1, boss 3/cycle) ; nos 3 specs act-2 tiennent, **⚠️ Hex est PLUS DUR que tout leur act 1** (rien chez eux ne lock la main entière — leur pire = Beckon « perds 6 ou paye ») → reco cap 1 Hex max en deck, verdict sémantique en attente ; backports act-1 optionnels (Leviathan, b.Kraken) APRÈS preuve act-2. (6) **Ledger re-checké** : −78 aujourd'hui (bloodless post-reprice) → **−92 après prescriptions, arrivée boss ~42-52/66** = le contrat A0 (block/camp/bien jouer nécessaires, gagnable en jouant bien). Rollout §8 du doc : Batch 0 playtest debt → wiring/honesty (tooltip Absorb !) → Wave A riders (XS, harnais parité DPT + icon2) → §8 batches + playtest → N4 si encore mou → junk cards act-2 → backports.**

### H-047 (2026-08-16) Correction : STS2 a bien une plaque de top bar (région d'atlas)

- **⚠️⚠️ CORRECTION FACTUELLE 2026-08-16 (catch de Julien sur screenshot : « they do have a plate, check again! ») — L'AFFIRMATION « STS2 NE SHIPPE AUCUNE ART DE TOP BAR » EST FAUSSE, NE PAS LA RE-PROPAGER. Root cause de la mauvaise sonde : elle cherchait dans `images/packed/**` alors que la barre est une région d'ATLAS — `scenes/ui/top_bar.tscn` → `BgImage` → `images/atlases/ui_atlas.sprites/top_bar/top_bar.tres` → `ui_atlas_0.png` `region = Rect2(1,1,2046,80)`. LEÇON GÉNÉRALE : une recherche par NOM DE FICHIER sur un seul sous-arbre d'images est une preuve faible — LIRE LES `.tscn` DE SCÈNE avant de conclure qu'un asset n'existe pas. Mesuré sur leur vraie barre : 2046×80 rendue en 2560×100 = 9.3% de la hauteur d'écran, ardoise plate `RGB(44,68,79)`, ZÉRO or / ZÉRO cap / ZÉRO ornement, bord BAS ébréché (pierre cassée), texte `outline_size 12` à font 32 (37%) d'une couleur = version SOMBRE de la teinte de la barre (jamais du noir pur), ratio icône:barre 0.79. **Le diagnostic §4e sur la DISTRIBUTION de l'ornement survit (leur plaque n'a aucun ornement) ; seule la conclusion « pas de plaque » était fausse — le fix est une plaque PLATE, pas l'absence de plaque.** Détail complet en §4g du plan d'action.**

### H-048 (2026-08-16) Redo du chrome : top bar plate, ~30 boutons, passe labels, rounds 2 à 4

- **🛑→✅ REDO TERMINÉ (2026-08-16 soir) — VÉRIFIÉ PAR RENDUS, NON PLAYTESTÉ. Détail complet en §4f de [sts2_action_plan_2026-08.md](sts2_action_plan_2026-08.md).** (1) **TOP BAR** : plaque PLATE (StyleBoxFlat) SANS bordure or ni caps, à la couleur du slab art `(0.2118,0.3098,0.3569)` ; icônes 80→64 (ratio 0.79 mesuré chez STS2) ; outlines de labels épaissis en teinte SOMBRE (jamais noir pur) ; **+ bord BAS ÉBRÉCHÉ** (`ui_top_bar_edge.png`, 2560×40, dessiné y78..96, stretch horizontal) → RelicBar 82→98. ⚠️ **Une version intermédiaire utilisait un SCRIM gradient sans plaque, bâtie sur la fausse prémisse ci-dessus : supprimée, ne pas la réintroduire.** ⚠️ **2 erreurs de découpe du bord à ne pas refaire** : couper dans le corps plein = ça RALLONGE la barre ; couper à la ligne de chips = strip tout sombre qui lit comme une BANDE. Bonne coupe = quelques lignes de CORPS, puis la lèvre sombre, puis les chips (`y436..466`). (2) **~30 BOUTONS** : plaques PLATES installées (teal_00 + gold_00 des 19 candidats de Julien — **13/19 étaient encore ornementés (greek keys) et jetés**), recette §4d, downscale BOX height 56, marges 9-slice MESURÉES (teal 14, gold 11), les 6 `.tres` réécrits **en gardant leurs uids** = zéro édit de scène. ⚠️ Le générateur a donné un **liseré OR fin, pas le rim teal ton-sur-ton demandé** — gardé (discret + cohérent avec le langage or du jeu), swappable. (3) **Vrai fix trouvé au rendu** : « Continue to Act 2 » était de l'or sur or, illisible (préexistant) → crème + outline 10 brun foncé (la règle texte §4.4 jamais appliquée). Les 2 caveats (badges « 12 »/« ! », plaque ROLL post-filtre) sont VÉRIFIÉS CLOS. Pipeline de keying réutilisable : `key_plate.py` au scratchpad. L'art rejeté (non committé, irrécupérable par git) est archivé dans `daiso_archive_2026-07-31\rejected_ui_chrome_2026-08-16\`. **ROUND 2 (retour playtest de Julien, même soir, détail en §4f)** : (a) bord de barre « pixelisé » = DOUBLE RESAMPLE (strip stocké 2560×40 dessiné 1280×18 → minification 2× sans mipmaps + chips à 0.66× l'échelle d'auteur) → règle : **un strip de bord se stocke à SA taille de dessin design-res exacte** ; rebuilt 1280×18, chips à 1.3× (crop 50% étiré) ; (b) boutons d'event → **rim teal ton-sur-ton livré** (recoloration programmatique du liseré or, pas de régénération — son pick d'origine) ; (c) **swap tray↔tooltip demandé par Julien** : la dice interface porte le cadre à coins ornés (`ui_panel_plate.png` marges 21/20), tooltip+scout prennent la plaque discrète (`ui_dice_tray.png` marges 9/8) — pur échange de `.tres`, uids gardés. Vérifié par composites PIL (éditeur ouvert = pas d'import CLI concurrent, jamais). **ROUND 3 (2e retour playtest, même soir, détail §4f)** : (a) tray « un peu petite pour les dés » → la plaque déborde de ±8px verticaux via `dice_interface.gd::_resize_panel_for_dice_inventory()` (CODE, pas la scène — éditeur ouvert) ; (b) **tooltips : Julien préfère l'ANCIEN style plat** → `ui_panel_plate.tres` réécrit de StyleBoxTexture en **clone StyleBoxFlat de `tooltip.tres`** (même uid, un .tres change de type librement) = tooltip + scout revertis en un fichier, zéro édit de scène ; `ui_dice_tray.png` devient orphelin sur disque ; règle : **les tooltips veulent le chrome plat navy/or, les plaques peintes sont pour le monde** ; (c) boutons d'event « simpler, pas fan du gradient » → corps APLATI (érosion géométrique 7px du silhouette — un masque par seuil de luminance RATAIT la bande de bevel à lum≈38 ; les masques géométriques battent les masques couleur pour « tout l'intérieur du rim ») = **3e confirmation de `feedback_flat_fills_preferred`** ; la plaque OR garde son bevel (langage ROLL approuvé, 1×/écran). **PASSE LABELS LIVRÉE 2026-08-17 (§4h) — les 3 TIERS de STS2 appliqués sur BELWE (choix de Julien après bake-off Podkova/Bitter/Alegreya rendus sur la vraie plaque : ⚠️ Bitter est le PLUS LARGE (58%) pas le plus étroit, et Podkova ne battait pas l'incumbent ; Alegreya gagnait en largeur (51%) mais Belwe = déjà shippé, 0 payload, plus de punch).** TIER 2 = **67 boutons / 28 scènes** (`label_pass.py`, backup pris) : 14 `Button` simples + **53 `RichTextLabel` enfants** — ⚠️ **les styleboxes ne se trouvent QUE par leur ID d'ExtResource dans les blocs de nœuds, jamais par nom de fichier** (une recherche par filename rend 0 bouton) ; Belwe + crème `#FFF6E2` + ombre noire 25% offset (3,2) + outlines à 0 ; **le texte OR sur plaque teal est RETIRÉ** (redondant maintenant qu'une plaque or porte l'emphase) ; les couleurs BBCode de mots-clés survivent par construction (`default_color` ne repeint que le texte non coloré). ⚠️ **Les labels d'event passent 20→18px = FIX DE FIT, pas du goût** : Belwe est **~13.6% plus large** que MinionPro et le plus long label atteindrait **510px dans un RichTextLabel de 492px** (mesuré sur le rendu) → wrap sur une 2e ligne clippée par le label de 40px ; **Belwe@18 et MinionPro@20 ont la MÊME hauteur d'x (9px)** donc le pas est optiquement invisible ; vérifié après coup : **53/53 tiennent, pire cas 94%** — tout futur label long doit être re-mesuré contre 492px. TIER 1 = End Turn et ROLL (tous deux à 26px) reçoivent outline **10** (~40%) teinté en version SOMBRE de la couleur de LEUR plaque (marron foncé sur ROLL, bordeaux sur End Turn — jamais du noir) + ombre (3,2), et End Turn a `shadow_outline_size = 10` pour que l'ombre porte tout le lettermark. Vérifié par rendus (events pire-cas, 7 écrans, cluster de dés), NON PLAYTESTÉ. ⚠️ REDÉMARRER COMPLÈTEMENT l'éditeur avant de jouer (dice_interface.gd édité hors éditeur). **ROUND 4 (2026-08-17, verdict de Julien sur l'état round-3 : « too many ornaments everywhere especially dice interface and end turn button. Also top bar is too much » + directive « fit with the rest of the game » — détail complet §4i)** : sur son batch de 31 candidats, picks par MESURE contre les ancres du jeu lui-même — **End Turn = #08 crimson PLAT** (corps le plus proche du corps du dé rouge, dist 20.8 ; baké 388×124 avec 9-slice au bake pour l'aspect 3.13 car la stylebox est full-stretch ; les candidats PIERRE, les plus beaux du lot, rejetés À LA TAILLE : silhouette irrégulière + rim + cracks = bruit à 194px, même classe d'échec que les grecques) ; **tray = structure de #03 RECOLORÉE au navy établi #1B2237** (#02 avait le navy EXACT dist 1.7 mais un liseré or ; la structure est dure à changer, la couleur = une fonction) → `ui_dice_tray.png` h=88 marges 15, tres repointé = le croisement de noms du round 2 est DÉFAIT, l'art à coins ornés archivé ; **top bar = bord ébréché RETIRÉ** (BottomEdge caché, strip dormant en fallback) + **bordure basse 2px quasi-noire sur la StyleBoxFlat** (procédurale — donne à la barre le langage de contour que tout le reste du jeu a déjà). **Nouveau harnais `debug_chrome_battle.gd/.tscn`** (racine, non committé) : boot du VRAI battle.tscn (recette double_endturn), une frame End Turn + tray + ROLL sur le fond de combat = LE shot de jugement de fit. ⚠️ Piège : `battle.battle_stats` prend le **`.tres`** du fight, PAS le `.tscn` — assigner la PackedScene tue `_ready` en silence et le harnais pend à l'infini. **ROUND 4b — « donner de la personnalité à la top bar » : 2 ÉCHECS, RETOUR AU PLAT (§4b du plan).** (1) corps du slab appliqué à toute la barre = vraies fissures → à 100% une fissure traverse le cœur HP et lit comme **UN CHEVEU sur l'écran**, à 55% = le même cheveu en plus pâle — **l'intensité n'était PAS le problème : une ligne fine isolée étirée sur 1280px ne peut pas lire « maçonnerie »** (chez STS2 les fissures vivent dans un champ de pierre, jamais seules) ; (2) lignes floutées + **variation tonale large amplifiée ×2.6** → Julien : « some oil on it, transparency issue? » — ce n'est PAS de l'alpha, un mottle amplifié lit comme des **taches / une nappe d'huile**. État final : `ui_top_bar_slab.png` = **1280×80 PLAT `#364F5B` + ligne de contour 2px quasi-noire bakée** (vérifié moteur : stddev **0.000**, médiane exactement (54,79,91) sur une bande sans icônes). **Si on veut du caractère un jour il faut un RÉSEAU de fissures sur toute la surface** — pas une ligne (cheveu), pas un lavis tonal (huile). ⚠️ Les 2 fixes livrés en **écrasement de PNG UNIQUEMENT** (zéro édit scène/.tres) parce que l'éditeur ET une partie debug étaient ouverts — le bon réflexe quand Julien est en plein playtest. **ROUND 4c** : tray #03 → **#04** (rim plus doux) ; son rayon de coin 14px rentre dans la marge 9-slice existante de 15 → **aucun édit de .tres**. ⚠️ **Verdict couleur INVERSÉ après coup** : #04 avait d'abord été recoloré au navy établi `#1B2237` (raisonnement « une variable à la fois »), mais Julien a choisi le **bleu NATIF plus clair RGB(20,45,88)** de la génération — c'est ce qui ship. La tray est donc volontairement plus claire/saturée que le navy des tooltips/panneaux : elle lit comme un OBJET, pas comme un conteneur de plus. La version navy-matched est gardée au scratchpad. Puis Julien : la police des compteurs de dés de mes mockups > celle du jeu — le mockup utilisait **LuckiestGuy**, la scène **MinionPro-Bold 13 + ombre** ; l'adopter est un gain de COHÉRENCE (les compteurs de dés de la TOP BAR sont DÉJÀ en LuckiestGuy 14 crème outline 3) → `LabelSettings_dsa2r` de `dice_interface.tscn` (partagé par les 10 slots, un seul édit) aligné exactement sur la top bar. **DOCTRINE à réutiliser : tout candidat de chrome se juge À LA TAILLE d'affichage réelle contre des ancres MESURÉES de l'art du jeu (teinte du corps vs dé rouge / navy établi, poids de contour vs outlines des dés) — le plus-beau-à-2K perd contre le rentre-à-62px, à chaque fois.** Le bullet d'origine ci-dessous reste valide comme DIAGNOSTIC :** CHROME PEINT : TOP BAR + BOUTONS **REJETÉS** PAR JULIEN (2026-08-16, « the buttons & top bar still look off » puis « no that's ugly ») — NE PAS CONTINUER À TUNER, LA DIRECTION EST FAUSSE, PAS LE RÉGLAGE. Détail + prompts + pointeurs référence en §4e de [sts2_action_plan_2026-08.md](sts2_action_plan_2026-08.md).** Diagnostic tenu face aux screenshots STS2 : ce n'est PAS la qualité de l'art, c'est la **DISTRIBUTION de l'ornement**. Mesuré : nos caps de top bar = **223px = 17% de la largeur d'écran** (halvés à ~8%, toujours rejeté), top bar = 13.3% de la hauteur d'écran, et **les ~30 boutons portent tous des grecques sur ~20% de leur largeur**. Chez eux : **UN seul élément décoré par écran** (le ruban parchemin), tout le reste s'efface pour que l'ART soit la chose bruyante. **⚠️⚠️ SONDE DÉCISIVE, NE PAS RE-DÉBATTRE : STS2 NE SHIPPE AUCUNE ART DE TOP BAR.** Une recherche de tout `sts2_ref/pck/images/packed/**` sur bar|header|panel|plate|frame|banner ne rend qu'une scrollbar, un submenu_panel, un relic_inspect_frame, les bandes de fond de map et une bannière de run history — **aucune texture de barre du haut** : leur HUD haut = icônes + texte posés DIRECTEMENT sur la scène. Toute la prémisse « top bar peinte » venait de moi, pas de la référence. **ANATOMIE DE LEURS BOUTONS (fichiers à lire avant de redessiner quoi que ce soit)** : `images/packed/combat_ui/end_turn_button.png` (512×256) + `end_turn_button_glow.png` = plaque + glow en couches SÉPARÉES ; `images/packed/common_ui/event_button.png` (284×110) + `event_button_outline.png` + **`event_button_sdf.png` (512×512)** — ils shippent un **SDF** à côté de l'art du bouton (idem `peek_button_sdf.png`), c'est comme ça que leurs boutons restent nets à toute échelle et obtiennent des contours animés propres, et c'est la pièce dont on n'a AUCUN équivalent. **CE QUI SURVIT (garder)** : les plaques ROLL et End Turn (elles lisent bien en combat, jamais visées par la plainte), tray de dés / tooltip / scout panel (discrets, OK), la **recette de keying** (§4d) et le **fix du filtre NEAREST** (§4d-ter) qui sont indépendants de la direction artistique. **CE QUI EST À REFAIRE** : la top bar (probablement en SUPPRIMANT la plaque et en laissant les icônes sur le monde façon STS2, éventuellement un scrim sombre doux pour la lisibilité sur fonds clairs) et les ~30 boutons partagés (plaques UNIES, zéro ornement de bout — la paire teal/or à grecques est précisément ce qui est rejeté). **Point de swap unique = `scenes/shop/shop_button_normal/hover/pressed.tres`** : les repasser en StyleBoxFlat restaure l'ancien look instantanément (git a les originaux).

### H-049 (2026-08-16) Chrome peint P1-P6 intégré (top bar et boutons rejetés ensuite)

- **🎨 CHROME PEINT LIVRÉ — P1-P6 INTÉGRÉS (2026-08-16, 26 candidats générés par Julien en une session, « i like what we got ») — VÉRIFIÉ PAR RENDUS + HARNAIS (feel_pass 35 checks, double_endturn, audit_changes 28, tutorial_lock, layout 0 violation), NON PLAYTESTÉ. Détail en §4d de [sts2_action_plan_2026-08.md](sts2_action_plan_2026-08.md).** Assets dans `assets/images/ui/` (ui_roll_plate, ui_endturn_plate, ui_dice_tray, ui_top_bar, ui_banner_ribbon, ui_panel_plate) + styleboxes `scenes/battle/end_turn_texture[_disabled].tres`, `scenes/dices/roll_button_texture.tres`, `scenes/dices/ui_dice_tray.tres`, `scenes/ui/ui_panel_plate.tres`, `scenes/run/ui_top_bar.tres`. **⚠️ SEULS 2 DES 6 SONT 9-SLICÉS** : End Turn / ROLL / top bar / bannière sont à TAILLE FIXE → textures simples stockées à 2× la taille design ; seuls le **tray de dés** (grandit avec le nombre de dés) et le **panel** (scout 336×128 + tooltip 204×108 — LA MÊME ART sert les deux) redimensionnent → stockés à ~1× avec margins MESURÉES sur l'art (tray 9px, panel 21px), **parce qu'un coin de 9-slice se dessine à la taille SOURCE en pixels** (une source 2× dessinerait des coins doubles). **La bannière ne peut PAS être 9-slicée : elle est COURBE, aucune colonne ne se répète.** **⚠️ 5 PIÈGES D'INTÉGRATION, chacun aurait shippé comme bug visible** : (1) **les états hover/pressed de ROLL étaient restés des StyleBoxFlat** (seul `normal` avait été swappé) → la plaque peinte disparaissait au survol ; les 4 états pointent maintenant sur la plaque (ROLL garde son kit scale, qui touche `scale` et PAS `position`, donc zéro conflit avec le dip du socle) ; (2) **2 écrivains sur `modulate` d'End Turn** (le pulse doré « plus rien à faire » + ButtonFeel au survol) → le nudge est désormais porté UNIQUEMENT par l'anneau, un nœud séparé ; (3) **un `.tscn` exige TOUS les `ext_resource` dans l'en-tête**, avant les sub_resources — insérer avant le premier `[node ...]` (qui vient APRÈS eux) donne `Parse Error: Unknown tag in file: ext_resource` et casse la scène ; (4) **remplir le « fond enclavé » a cuit du MAGENTA OPAQUE dans l'art** — les volutes et coins découpés sont de VRAIS trous, traiter toute zone enclavée comme intérieur est faux pour un ornement (c'était le liseré violet sous le cap du top bar) ; (5) **le générateur anti-aliase son contour noir CONTRE la couleur de clé et le cuit en pixels OPAQUES violet foncé** — assez loin du magenta pur pour passer un test de DISTANCE, donc survivant à toutes les passes ; fix = exclure les pixels de la TEINTE de clé du cœur de de-fringe pour que la vraie couleur de contour les recouvre. **⚠️ LANCZOS SONNE (ringing) sur un contour cel dur** — un-premultiplier ce dépassement à faible alpha laisse un liseré coloré d'1px : downscaler ces plaques en **BOX**. **RECETTE DE KEYING pour toute future plaque UI (les 6 vérifiées à 0 pixel de clé résiduel)** : rampe de distance 60→120 → retrait des specks par plus gros blob → **AUCUN remplissage de trou** → de-fringe depuis un cœur `distance > 140` **ET non teinté clé** → downscale **BOX** prémultiplié → un-premultiply → 2e de-fringe dans l'image réduite → alpha < 8% mis à 0. **Aussi** : contenu du top bar rentré de 84px de chaque côté (la pièce d'or se posait sur le cap doré = or sur or) et **3e print hot-path supprimé** (`"roll pressed"`, 5-15×/tour). **Restent en stylebox VOLONTAIREMENT** : boutons d'events / Skip / Leave / menus et les rangées de reward — ils passeront aux plaques génériques P7 (teal) / P8 (or) plus tard, car **une seule art restyle ~30 boutons via les 3 `shop_button_*.tres` partagés**, donc à ne faire qu'une fois le look cel validé en jeu.

### H-050 (2026-08-15) Audit layout & tailles des ennemis (END_TURN_LEFT/TOP)

- **📐 AUDIT LAYOUT & TAILLES DES ENNEMIS (2026-08-15, demandé par Julien après le déplacement du bouton End Turn : « make sure nothing overlaps… some enemies too small (small satyrs), some too big (lava hound) ») — VÉRIFIÉ PAR HARNAIS SUR LES 40 FIGHTS + RENDUS, NON PLAYTESTÉ. Détail en §4c de [sts2_action_plan_2026-08.md](sts2_action_plan_2026-08.md).** Nouveau **`debug_enemy_layout.gd`/`.tscn`** (racine, non committé) : contrairement à `debug_bg_audit` (qui rend et dump des rects de NŒUDS), il mesure la **bbox d'ENCRE alpha-scannée** de chaque sprite transformée par le transform live (la plupart des arts ennemis ont énormément de padding transparent → **le rect de nœud MENT sur où est le corps**), puis vérifie toutes les zones HUD, corps-vs-corps, barre-sur-le-voisin, hors-écran, et l'écart de ligne de sol, + un census de tailles. **⚠️⚠️ LE VRAI BUG, CAUSÉ PAR LE DÉPLACEMENT D'END TURN : `enemy.gd` clampe la rangée de statuts pour qu'elle n'atteigne pas le bouton, contre des COPIES EN DUR de son rect (`END_TURN_LEFT = 1060`, `END_TURN_TOP = 592`).** Le bouton ayant bougé de 38px à gauche et 38px en haut, ces constantes sont devenues périmées instantanément, laissant une bande de 38px sur CHAQUE axe où un statut peut se poser sur le bouton **pendant que le clamp croit que tout va bien — rien n'erreure quand elles dérivent, le clamp arrête juste de clamper**. Corrigées à 1022/554 + commentaire d'avertissement : **quiconque redéplace ce bouton DOIT les mettre à jour.** **TAILLES — les 2 intuitions de Julien confirmées PAR LA MESURE et corrigées** : (a) **Lava Hound était le corps LE PLUS LARGE DU JEU — plus large que le boss Leviathan** — et sur-dimensionnait les DEUX élites (aire 75.7k vs Lich 69.5k @85HP et Dragon Priest 67.8k @90HP) alors que c'est un tier-2 normal à 51 HP → boîte 320→275 (aire 55.9k, juste sous Medusa 56.7k = le bon rang) ; (b) **petits Satyrs ×8** (les plus petits corps du jeu, leur barre de HP était plus large que le corps) boîte 143→165, et **petits Krakens ×6** boîte 134→152. **⚠️ RECETTE « redimensionner sans casser l'ancrage au sol », à réutiliser : dériver la compensation de position.y du rapport centre→pieds MESURÉ (satyr 0.4825×boîte, kraken 0.485×boîte, hound 0.3906×boîte), PUIS vérifier que le nombre de pieds est IDENTIQUE après coup** — ici pieds satyrs toujours 516/518/520 et hound toujours 511, au pixel près. `tier_1_octopus_2_satyrs_2` volontairement NON touché (ses corps avaient déjà été compressés à la main pour en faire tenir 4). **RÉSULTAT : tous les fights ATTEIGNABLES sont à 0 HIGH / 0 MED.** Les seuls flags restants sont dans `tier_0_satyrs_octopus_3`, **absent de `battle_stats_pool.tres`** (un des 4 leftovers injouables documentés) — invisible en run. **⚠️ 2 CLASSES DE FAUX POSITIFS à connaître (le 1er audit rapportait 14 HIGH dont AUCUN réel — un harnais qui crie au loup se fait ignorer)** : (1) **« ART overlaps PowerNumber » (8 fights)** — le NŒUD du label de Power fait 90×96 mais un nombre à 1-2 chiffres n'en encre que ~46px ; vérifié au rendu qu'aucun ennemi ne touche visuellement le nombre → zone resserrée sur l'encre mesurée ; (2) **« status row overlaps Hand » (3 fights)** — la main est un **ARC** (la carte du milieu est ~17px plus haute que celles des bords), la modéliser en UN rectangle flaggait des icônes de statut qui sont en réalité dans le trou franc entre le bord droit de l'éventail et le bouton End Turn → zone scindée en HandCenter + HandOuter.

### H-051 (2026-08-15) Feel pass batch 1 (hit-stop, mort d'ennemi, End Turn) + N1/N2

- **🥊 BATCH 1 « FEEL PASS » + N1/N2 LIVRÉS (2026-08-15, verdicts de Julien sur le plan d'action : Batch 1 + N1-N3 + générer P1-P6 ; « no need for speed setting » en cours de route) — VÉRIFIÉ PAR HARNAIS (`debug_feel_pass.gd`/`.tscn` racine non committé, 35 checks 0 fail) + RENDUS (`debug_death_render.gd` → `death_render/`) + LES 6 RÉGRESSIONS RE-PASSÉES (double_endturn, audit_changes 28, golem_carryover 12, ricochet_reroll 26, cadence_promotion 21, tutorial_lock). NON PLAYTESTÉ. Détail complet en §4b de [sts2_action_plan_2026-08.md](sts2_action_plan_2026-08.md).** (1) **Échelle d'impact partagée** : nouveau `Shaker.Impact {VERY_WEAK…HUGE}` + tables `SHAKE_MAGNITUDE`/`SHAKE_DURATION`/`HIT_STOP_DURATION`/`HIT_STOP_TRANS` + `impact_for_damage()`/`impact_for_fraction()` — un seul cran pilote shake ET hit-stop, les 2 `clampf` bricolés de `damage_effect.gd` disparaissent, et le shake dure PLUS LONGTEMPS que le freeze à chaque cran par construction. (2) **Hit-stop refait : profondeur 0.02 → 0.1 et surtout RAMPE de sortie le long d'une courbe** au lieu d'un palier plat suivi d'un snap (c'est le snap qui lisait « hitch » ; l'ancien commentaire disait qu'on avait descendu à 0.02 parce que « imperceptible » — on compensait la FORME, pas la profondeur). L'expressivité passe de la durée à la COURBE (SINE→QUAD→CUBIC→QUART→EXPO). **⚠️⚠️ 3 PIÈGES GODOT MAJEURS TROUVÉS PAR LE HARNAIS, invisibles en relecture : (a) `Tween.set_ignore_time_scale()` N'EXISTE PAS EN GODOT 4.3** — l'appel throw DANS une coroutine, ce qui saute aussi le restore → **le jeu entier reste à 0.1 de vitesse POUR TOUJOURS** ; (b) **`tween_property(Engine, "time_scale", …)` n'anime rien** (Engine est un singleton hors scene tree) ; (c) remplacer naïvement la rampe en vol **casse la garantie « le plus long gagne »** que le ref-counting existait pour fournir (un petit coup après un haymaker coupait le freeze). **État final : la rampe est pilotée à la main dans `_process` contre `Time.get_ticks_msec()` (temps réel), avec `Tween.interpolate_value` utilisé comme helper STATIQUE pour les courbes, + un deadline temps réel pour « le plus long gagne ». Shaker passe `PROCESS_MODE_ALWAYS`** (sinon une pause en plein hit-stop laisse le jeu au ralenti). (3) **MORT D'ENNEMI v1** (`enemy.gd`) — était UN tween d'alpha de 0.4s : le corps **quitte le groupe `enemies` + son Area2D devient inerte + son UI de combat se cache IMMÉDIATEMENT** (⚠️ **c'est LA pièce porteuse** : ~20 sites visent via `get_nodes_in_group("enemies")`, un cadavre resté dans le groupe 1.3s mangerait l'AoE et absorberait les dés retargetés), puis pre-delay → fantôme à 0.4 alpha → **60 éclats de dé qui MONTENT** (gravité négative, cône 30°, émission continue `explosiveness 0.08`, courbe pop-hold-shrink, teintés par l'accent du dé actif comme le slash) → hold → fade. ~1.3s game time. Son placeholder. **⚠️ Le 1er rendu montrait ~5 éclats sur tout un corps → count 26→60 et tailles ×1.9** (le plancher « la MASSE prime sur la FORME », re-appliqué depuis des FRAMES RENDUES, pas depuis le code). (4) **Camera shake : enveloppe attaque-rapide/décroissance-douce** + token de génération (des shakes AoE concurrents ne se battent plus sur `offset`) ; **`Shaker.shake()` mort SUPPRIMÉ** (0 appelant, 2 commentaires qui déconseillaient de l'utiliser) remplacé par **`shake_horizontal()` X-ONLY à enveloppe sin×sin** (un ennemi planté au sol qui tremble VERTICALEMENT lit comme s'il lévitait — ça défaisait le travail de ground line). (5) **Nombres de dégâts : fondu en ease-IN** (le nombre reste lisible quasi toute sa vie) — ⚠️ **leur durée était DÉJÀ 1.0s** (`DamageEffect` override le défaut du popup ; le « 0.6s » de l'audit lisait le défaut, pas la réalité) ; même traitement sur block_popup. (6) **End Turn : 194×43 → 194×62, inset 26/85 → 64/104px.** ⚠️ **Ne PAS rétrécir la largeur** : la nôtre est déjà proportionnellement PLUS LARGE que la leur (15.2% vs 11.5% de l'écran), c'est la HAUTEUR qui manquait (6.0% vs 8.3%) — et « End Turn » en Cinzel 26px ne rentrerait pas dans 150px. Non-chevauchement avec la pile de défausse vérifié. (7) **Survoler End Turn FAIT FLASHER les cartes encore jouables** (`Hand.flash_playable_cards()`, modulate-only car le fan possède position/rotation et le hover possède scale ; garde anti-« modulate cuit » à la 2e flash) — le bouton répond « pourquoi je n'end pas mon tour ? » AVANT le clic. (8) **Nudge « shiny » : trigger INCHANGÉ** (le nôtre est plus précis que le leur : il compte le Power banké et les Celestial) + anneau additif qui s'étend/s'efface derrière le bouton. (9) **Nouveau `scenes/ui/button_feel.gd` (`ButtonFeel.attach(button)`)** : hover-in INSTANTANÉ / hover-out 0.5s EXPO (« snap vers l'état chaud, dérive au retour »), press = les visuels descendent de 4px, stylebox `focus` blanchie une fois pour toutes, et **l'état de hover est nettoyé sur `visibility_changed`** = fix STRUCTUREL de la famille de bugs « tooltip/hover collé » patchée 4 fois séparément. Attaché à End Turn seulement pour l'instant. (10) **N1 : `rare_weight` démarre à 0** → le TOUT PREMIER écran de reward ne peut pas sortir de Rare, la pity existante le remonte à la base dès le 2e (leur offset négatif traduit à notre granularité — le leur se purge en ~3 tirages de carte, on en tire 3 par écran) ; zéro nouvel état sauvegardé. **N2 : or élite act 1 45-60 → 70-90.** (11) 2 prints hot-path supprimés. **⚠️ DEUX CHOSES MESURÉES PLUTÔT QUE CONSTRUITES : (a) N3 (garantie de shop) est INUTILE et ABANDONNÉ** — `debug_map_composition.gd` sur 400 maps : shops min 2 / max 4 / moyenne 2.95, campfires min 5, élites min 3, **0 map sans shop**, 2% sans shop sur les floors 4-13 — la map utilise un **quota bag** (comptes exacts) depuis le 2026-07-04, donc le cadrage « nous on roll des poids, eux visent des comptes cibles » de l'audit (5.5) était déjà périmé ; **(b) le speed setting n'était pas porteur** (l'audit le posait comme dépendance de la mort allongée) : chaque ennemi mourant possède SON tween, donc **un wipe de 3 corps se termine en 1.37s au total, pas 3×1.3s** — les morts se CHEVAUCHENT. Julien l'a coupé, la mort garde sa durée. **À surveiller au playtest** : son de mort placeholder ; le hit-stop du coup fatal ralentit le DÉBUT de la mort donc un kill occupe ~2s d'horloge murale pour 1.3s de game time (voulu, mais c'est le 1er levier si ça traîne) ; **le timing du panneau de victoire après le dernier kill est la SEULE chose que le harnais n'a pas pu prouver** (son environnement ne reproduit pas le câblage de run.gd, et le comportement était IDENTIQUE avec les cadavres libérés à l'ancienne cadence → trou de harnais, pas régression ; ce qui EST prouvé c'est la précondition : un wipe complet vide le conteneur en 1.37s) ; et ButtonFeel à étendre à ROLL + boutons de shop/menu une fois le feel confirmé.

### H-052 (2026-08-15) Plan d'action STS2 → Dice Odyssey (analyse)

- **📊 PLAN D'ACTION STS2 → DICE ODYSSEY (2026-08-15, la couche DÉCISION au-dessus de l'audit — ANALYSE + PLAN, RIEN D'IMPLÉMENTÉ, verdicts de Julien en attente sur 3 forks) : voir [sts2_action_plan_2026-08.md](sts2_action_plan_2026-08.md).** (1) Recap des 4 différences structurantes : **l'énergie est ACHETABLE chez nous** (un dé = +25-35% d'output → le shop porte la courbe de puissance, donc la variance de présence des shops est un bug de difficulté pour NOUS, pas pour eux) ; variance = pré-roll control (côté ennemi : toujours déterministe) ; **pas de potions → le plafond de spike 35-45% est LE substitut du système de potions, sacré** ; densité de tour → combats courts, jamais d'EHP en plus. (2) **Nouvelles sondes de la référence (fermait un trou de l'audit)** : STS2 player HP = 80/70/**66 — le Necrobinder joue avec EXACTEMENT notre pool** ; 3 actes (acte 1 = 2 variantes Underdocks/Overgrowth, acte 2 = Hive, acte 3 = Glory) ; progression par acte : corps normaux ×2, élites ×1.5-1.75, boss 222→~360→~470, spikes act 3 = 30-35 dégâts (38-53% du pool HP fixe) — **nos ACT2_HP_MULT (1.3-1.75/1.6) tombent DANS leur bande mesurée, validation indépendante du placeholder act 2**. (3) Modèle de pression = 4 mécanismes (attrition / horloges par combat = notre §8-§9 quasi mot pour mot / course de stats / **contrôle de variance sur l'INFLOW de récompenses** — celui qu'on matche le moins). **5 points mous → 7 moves numériques concrets** : N1 zéro rare sur les 2 premiers écrans de reward, **N2 or élite 45-60→70-90 (prime de risque mesurée sous-payée : 1.2× le hallway chez nous vs 2.3-2.7× chez eux)**, N3 garantie ≥1 shop floors 4-13 dans le générateur, N4 dials T2 (Hound 6→7, Maelstrom 12→13) SEULEMENT après playtest §8, N5 rampes §9, N6 pity cap inchangé, N7 prix shop act 2 ×1.25 si pooling confirmé. + **Ledger d'attrition act 1** (§2.5 : ~−85 dégâts on-curve / +50-60 heals / arrivée boss ~45-55/66 — tout futur retune = un retune d'une ligne du ledger). (4) **Plan ROI en 5 batches : Batch 0 = PAYER LA DETTE DE PLAYTEST** (tout depuis le 08-13 est non-joué ; débloque les verdicts §8, le backstop, les 2 SFX) ; Batch 1 feel pass (hit-stop 0.02→0.1 + ramp-out en courbes nommées, mort d'ennemi v1 fragments de dés ~1.2-1.5s + speed setting ENSEMBLE, kit boutons hover-instant/sortie-0.5s-expo + press-down + shader HSV, End Turn ~150×62 + hover-flash-la-main + ring additif, dmg numbers 0.6→1.0s alpha ease-in, shake enveloppe sin×sin + horizontal-only, main qui drop+dim pendant le tour ennemi, 2 prints hot-path à supprimer) ; Batch 2 funnel itch (map spread-nudge ±22 gap-maximisant, 6 first-time tips, tooltip 204→320px + délai 1.0→0.2s, event pool exhaustion gracieuse) ; Batch 3 = §8/§9 + upgrades audit (`cannot_repeat`/`cooldown_n` en @export sur EnemyAction, openers par INDEX DE SLOT pour les paires jumelles — résout le verdict B.Kraken sans variance) + N1-N3 ; Batch 4 chrome peint. (5) **Inventaire UI complet (§4) : le MONDE est peint, le CHROME est 100% StyleBoxFlat** (zéro NinePatch dans tout le projet ; un `end-turn-button.PNG.import` orphelin prouve qu'un bouton peint a existé puis été perdu) → **6 commissions classées avec prompts P1-P6 prêts à générer** (ROLL 150×42 > End Turn > tray de slots > top bar 9-slice > ruban de bannière > scout panel ; style = cel plat 2 tons + contour quasi-noir, fond VERT pour sujets or/rouge / MAGENTA pour sujets navy/teal ; **une seule art par bouton** : ombre = sa propre texture en noir α0.25, outline hover dérivée par dilatation PIL, états = HSV brighten + shift de position — recette §4.4). Errata docs-vs-code au passage : Canalize seuil 12/+3, Parasite 15/+2, Greedy +2 Muscle/6 dés, theft Dicelord 10 (les docs balance de juillet sont périmées sur ces 4 points, le code fait foi).

### H-053 (2026-08-14) Charge = livraison physique de dés + signal typé dice_charged

- **⚡ CHARGE = LIVRAISON PHYSIQUE DE DÉS + SIGNAL TYPÉ `Events.dice_charged(dice_type, count)` (2026-08-14, « make the charge dice animation much more powerful » → « go ahead with the full thing ») — VÉRIFIÉ PAR HARNAIS (20 checks 0 fail, CONTRÔLE NÉGATIF inclus : reveal d'arrivée neutralisé → le check de timing D2 FAIL bien à 2180ms vs 725ms réels) + RENDUS MOVIE MAKER (8 frames clés gardées dans `charge_delivery_frames/`) + RÉGRESSIONS RE-PASSÉES (golem_carryover 12/12, ricochet_reroll 26/26, audit_changes 28/28), NON PLAYTESTÉ.** Le diagnostic : `charge_dice_animation` était SANS ARGUMENTS → la cérémonie surchargeait le dé ACTIF même quand un AUTRE type gagnait les dés (Electrify sur Blue flashait le dé Bleu, ring tinté `Global.dice_type` = mensonge), Charge 1 et Charge 4 étaient pixel-identiques, RIEN ne voyageait (seule ressource du jeu sans livraison), et un type tout neuf popait son slot sans cérémonie. **Fix en 4 morceaux :** (1) **Nouveau signal typé + migration des 63 émetteurs** (45 cartes, 9 reliques, 7 statuts, Sigil/Gnome dans dice.gd) — **les sources multi-types (Experiment, War Ritual) émettent UNE FOIS PAR DÉ dans leur boucle** → chaque livraison vole dans SA couleur, c'est le reveal du random ; l'ancien signal reste DÉCLARÉ dans events.gd (filet anti-crash si un émetteur a échappé au grep) mais plus AUCUN listener — **tout futur contenu qui charge doit émettre `dice_charged`**. ⚠️ Les statuts war_ritual/dicelord_gift/compound(+) ont PERDU leur `SFXPlayer.play(CHARGE_SOUND)` manuel — le handler de livraison joue le launch sound (coalescé 1×/frame). RunicBones migré (`_on_charge_dice(_type, _count, owner)`). (2) **`dice_interface.gd` = le système de livraison** (~250 lignes, constantes `CHARGE_*` en tête) : une icône par dé chargé (texture du SLOT cible = continuité visuelle, fallback `CHARGE_FALLBACK_FACE` — green est un d3, jamais supposer une face 6), origine = release de la carte si `card_played` même frame (miroir des règles d'origine des power orbs ; red socket / reliques / statuts / triggers → centre du dé actif), pop-lift de naissance puis **arc bezier accéléré EASE_IN** avec traînée de motes accent (throttle temps réel 30ms) + halo `show_behind_parent` (un dé LIVRÉ brille — ce qui le distingue d'un dé JETÉ/attaque), stagger 0.16s bam-bam-bam, cap visuel 6 icônes ; **arrivées : flash `die_halo_texture` accent + ghost additif du slot en OVERLAY LIBRE (jamais de scale sur un enfant de container — un changement de label déclenche le re-sort, leçon reward-cards) + plinks pitch montant par arrivée (le ladder gratuit) + clack final** ; volleys same-frame SÉQUENCÉES (`_next_volley_delay`). (3) **MATÉRIALISATION d'un slot tout neuf** : visible mais `modulate.a = 0` (l'espace layout est réservé, zéro reflow) jusqu'à la PREMIÈRE arrivée → bloom overbright 2.1 + anneau de 8 motes ; **`_materializing_slots` exempte le slot de la boucle de styling d'`update_selected_highlight`** (sinon elle stompe l'alpha tenu — même famille d'exemption que `_nudge_nodes`) ; failsafe timer idempotent à +1s. ⚠️ **`await process_frame` obligatoire avant de viser un slot fraîchement visible** (son global rect est stale avant le sort différé du container). (4) **`dice.gd` : la grosse cérémonie du dé actif est GATÉE sur `charged_type == dice_type`** (l'absorb ne ment plus jamais), ring tinté par le type chargé, **punch d'absorb laddered par count** (1.20 → 1.32 à 4+), launch sound RETIRÉ d'ici. **⚠️ PIÈGE MOVIE MAKER NOUVEAU** : des waits wall-clock (`Time.get_ticks_msec`) entre les beats COMPRESSENT l'espacement game-time dans une capture (le mur avance ~6× plus vite que le jeu pendant l'écriture des PNG) → les settles du harnais sont passés en `SceneTreeTimer` (game time) ; les `_await_until` wall-clock font du bruit d'assert en mode movie — **la vérité des asserts = le run SANS `--write-movie` (20/20)**. **Drive-by** : le check Electrify de `debug_audit_changes` était STALE depuis le revert « Depleted payé en Blue » du 07-30 (attendait `odd_dice_bonus_amount == -1`) → corrigé, 28/28. Harnais `debug_charge_delivery.gd`/`.tscn` racine (non committé), env `CHARGE_MOVIE=1` = phases visuelles seules. **À surveiller au playtest** : densité sonore d'une volée de 6 (plinks + clack — si bruyant, baisser `-6.0` dB des plinks), le ghost d'arrivée occulte le slot voisin ~0.26s, et les charges de début de tour (statuts/reliques) partent du dé actif — pas de l'icône de statut source (raffinement possible : porter l'origine dans le signal). ⚠️ **REDÉMARRER L'ÉDITEUR avant de jouer** (70 fichiers .gd édités hors éditeur — pas de nouveau class_name ni @export, mais la règle blanket s'applique).
  - **⚡⚡ SPLIT « ÉNERGIE » vs « POSSESSION » — LA VRAIE ARCHITECTURE DU BEAT (3e retour de Julien, le plus important du chantier : « le dé actif n'est PAS le sujet d'un charge, d'accord — MAIS il est tellement central qu'il doit quand même dire que quelque chose s'est chargé/énergisé ; c'est pour ça que j'étais parti sur le boom/shockwave générique ; et ça permettrait de moins forcer sur l'animation du dice interface »). VÉRIFIÉ PAR HARNAIS (24 checks, dont la paire A5/B3 qui épingle NUMÉRIQUEMENT la décision) + RENDUS.** Julien a raison et mon gate était trop large : un charge porte **DEUX affirmations différentes** qu'il ne faut pas confondre (c'est exactement ce que faisait l'ancien signal sans arguments) — **(1) « de l'énergie vient de jaillir » = TOUJOURS VRAI**, et comme le gros dé est le centre de gravité de l'écran, il DOIT la porter sinon tout l'événement se cache dans la petite rangée de slots ; **(2) « CE dé a absorbé des dés » = vrai seulement si type chargé == type actif**. Donc : **l'animation de charge D'ORIGINE (pulse d'aura + anneau de particules convergent) part À CHAQUE charge, teintée par le type CHARGÉ (jamais `dice_type`)** — l'onde dit « de l'énergie orange vient de partir », la livraison vers le slot orange répond « voilà où » ; le surge aura/emanation reste universel lui aussi car **`charge_heat` est un or chaud NEUTRE en type** (il affirme l'énergie sans affirmer un type). La cérémonie d'absorption (squash → punch → flash → pulse du Power + anneau convergent) reste, elle, gatée sur le type actif. **Et comme le gros dé porte enfin l'événement, la rangée de slots est REDESCENDUE** (motes d'arrivée 10→6, kick du panneau uniquement sur le dernier dé au lieu d'un par dé qui faisait vibrer la rangée en continu) — c'est le « ne pas forcer autant » demandé. **⚠️⚠️ DEUX VRAIS BUGS DE RENDU TROUVÉS À LA MESURE, tous deux invisibles en relisant le code : (a) `z_index = -1` sur l'anneau le faisait dessiner SOUS le Background plein écran opaque** (z_index trie sur TOUT le canvas, pas juste entre frères — le fond est un `Sprite2D` à z=0 qui vient plus tôt dans l'arbre) : les nœuds existaient, le harnais les comptait, et on ne voyait RIEN. **Fix = ORDRE D'ARBRE (`move_child(ring, 0)`), jamais un z_index négatif** → surface éclairée mesurée **24k → 142k px**. **(b) L'anneau naissait plus PETIT que le panneau opaque du dé, donc son instant le plus lumineux se passait entièrement caché derrière** ; l'échelle de départ est maintenant **DÉRIVÉE AU RUNTIME du rect réel du panneau** (`clear_radius / annulus_radius_at_1`) au lieu d'être codée en dur — retailler le dé un jour ne peut plus ré-enterrer l'onde silencieusement. **⚠️ 3e/4e/5e passes sur l'onde — ABOUTIES À UN REVERT, lire ceci avant de re-proposer un shockwave.** J'ai construit un vrai front d'onde custom (anneau SDF asymétrique, puis carré arrondi partageant le SDF de `die_halo_texture()`, 3 fronts échelonnés, bloom d'origine, 0.7s, 228k px éclairés). **Verdict final de Julien : « too much intensity & too fast ; i like the color identity but the animation i had before you touched it was more similar to what i have in mind ».** → **TOUT le code d'onde custom est SUPPRIMÉ** (`_spawn_charge_shockwave`, `_spawn_charge_origin_bloom`, `DicePalette.die_wave_texture()` + ses consts). **L'état final = l'ANIMATION D'ORIGINE (`animation_player.play("charge")` = pulse de l'aura + anneau de particules convergent `gpu_particles_2d`) simplement PROMUE en universelle et teintée par le type CHARGÉ** — c'est-à-dire exactement ce que Julien avait, plus l'identité couleur qu'il aime, plus le fait qu'elle joue même quand le type chargé n'est pas l'actif. Intensité mesurée : **34k px éclairés contre 228k pour l'onde custom (÷6.7)**. **NE PAS re-proposer un shockwave qui traverse l'écran** : la demande est un pulse CONTENU au niveau du dé. Leçons transférables des passes mortes, gardées car réutilisables ailleurs : (a) **`EXPO`/`QUINT` ease-out mettent ~90% du trajet dans le 1er cinquième du temps** → une onde paraît PLUS RAPIDE même en rallongeant sa durée, utiliser `QUAD` pour un mouvement qu'on doit voir voyager ; (b) un front trop fin étalé sur une grande circonférence **devient invisible** — il faut du CORPS, pas juste de la netteté ; (c) **z_index négatif = dessiné SOUS le Background plein écran opaque** (voir plus bas). **⚠️⚠️ PIÈGE DOCUMENTÉ RETOMBÉ, LE PLUS COÛTEUX : `var peak_alpha := [0.95, 0.6, 0.4][i]` = PARSE ERROR** (élément d'Array littéral = Variant) — **ça fait échouer le CHARGEMENT DE TOUT `dice.gd`** pendant que **gdtoolkit rapporte le fichier PROPRE** (parser syntaxique, zéro inférence de type). Deux itérations entières de rendus/mesures ont été faites sur un build où dice.gd ne chargeait pas. **C'est le harnais qui l'a attrapé, pas gdtoolkit et pas l'œil** → typer explicitement, et **quand un effet disparaît complètement, grep `SCRIPT ERROR` AVANT de re-tuner des valeurs**. ⚠️ **Un parse error dans un harnais le fait AUSSI HANG à l'infini** (le script ne charge pas → `_ready` ne tourne jamais → `quit()` jamais appelé) : un harnais qui timeout = suspecter un parse error. **⚠️ MÉTHODO : la moyenne de luminosité d'une région est un TRÈS mauvais indicateur pour un anneau fin** (elle diluait un anneau saturé à +8.9 « sous le plancher ») — mesurer en DIFF pixel contre une frame calme et compter la surface réellement éclairée. **Checks qui verrouillent le design : A5 (charge non-actif → déviation de `dice_display.scale` = 0.000, donc AUCUNE absorption) vs B3 (charge actif → 0.198, absorption bien jouée), + A4/B2 (l'onde part dans les DEUX cas).**
  - **PASSE « PLUS D'IMPACT » (même session, 2e demande de Julien « you can make it feel more impactful ») — VÉRIFIÉE PAR MESURE DE PIXELS, pas à l'œil.** Diagnostic : l'arrivée n'avait que flash+ghost là où le bash de dés jetés (le beat qui MARCHE dans ce jeu) empile flare + anneau de choc + MASSE de débris + hit-stop + shake ; et le ghost était à **modulate 1.25, SOUS le plancher documenté de ~1.9** (donc littéralement invisible à vitesse de jeu). Ajouté : anneau de choc (alpha bas, la couche de masse), **spray de 10 motes par dé / ~18 sur le dernier** (« la MASSE prime sur la FORME » — leçon du slash), ghost 1.25→**2.4** qui explose de 0.82→2.1, flash blanc-chaud (accent+0.45) au lieu d'accent saturé, **punch du LABEL de compteur** (le nombre EST le payload d'un charge — modulate porte le poids, immunisé au re-sort de container ; scale en bonus), **kick du panneau** (léger par dé, franc sur le dernier), **hit-stop sur la dernière arrivée seulement** (scalé par count) et **flare de départ** à l'origine (la carte ÉJECTE les dés). Trail 2× plus dense, icônes 44→54px. **⚠️ PIÈGE ADDITIF RETOMBÉ ET MESURÉ : le flash tenait son alpha de crête pendant TOUTE l'expansion** (`set_parallel` + `chain()` = l'alpha ne commence à tomber qu'après la fin du scale de 0.26s) → **mesuré 1.63s de blanc saturé au total, pire run 0.5s** = un wash, pas un flash, avec l'art du dé illisible. **Fix : DEUX tweens séparés** (l'alpha snap puis retombe immédiatement en 0.17s, le scale continue en parallèle sur son propre tween qui meurt avec le nœud) → **0.20s au total, pire run 3 frames (0.10s), crête toujours à +12.7 de luminosité au-dessus du fond** (plancher ~10% franchi). **La méthode compte : mesurer le nombre de frames saturées (>=250) dans la région du panneau, pas juger à l'œil sur une still.** + cooldown de 350ms sur le hit-stop (un charge multi-types = N volées d'1 dé → N freezes enchaînés = stutter).

### H-054 (2026-08-14) Gate « cet event demande ce dé » + inventaire de dés réparé

- **🚫 GATE « CET EVENT DEMANDE CE DÉ » + INVENTAIRE DE DÉS RÉPARÉ (2026-08-14, 1er playtest des loadouts par Julien : « problem with events removing blue dice when you dont have 1 ») — VÉRIFIÉ PAR HARNAIS (36 checks 0 fail, CONTRÔLE NÉGATIF INCLUS : `debug_no_blue_start.gd`/`.tscn` racine non committé) + régressions re-passées (debug_dice_loadout 28/28, debug_reroll_offer 1007/1007), NON PLAYTESTÉ.** Depuis le picker, un run peut légitimement posséder 0 Blue (Elf) et/ou 0 Red (Elf) — or **2 events DÉPENSENT un dé précis** : `event_dice_machine` (fond un Blue → Giant) et `event_hollow_idol` (nourrit l'idole d'un Red). Mesuré avant fix : les deux sortaient normalement du tirage à 0 dé possédé, et **le dice machine posait `blue_dice_max_amount = -1` + offrait un Giant gratuit** (l'idole, elle, avait déjà son garde `if red < 1: return` + son bouton caché — c'est le SEUL des deux qui était protégé). **Fix en 2 couches, les deux justifiées indépendamment** : (1) **nouveau `EventStats.required_dice_type: String` (@export, "" = pas de gate)** filtré par `EventStatsPool.get_random_event_for_tier` via le nouveau `_owns_required_dice()` — **lit `Global.<type>_dice_max_amount` DIRECTEMENT au lieu de passer par un paramètre** (contrairement à `min_relics_required` qui doit recevoir `relic_count` : la possession de dés vit déjà dans Global, donc aucun appelant futur ne peut oublier de la passer) ; `max_amount` et PAS `current_amount` (les dés bonus fight-scoped n'existent plus quand on atteint une salle event). (2) **garde runtime dans chaque event** : `event_dice_machine.gd` gagne un `setup()` qui cache le bouton Convert à 0 Blue + un early-return dans le handler (calqué sur ce que l'idole faisait déjà). **La couche (2) est le filet anti-strip** : si l'@export se fait un jour stripper par l'éditeur (incident documenté 3×), l'event redevient tirable mais ne peut plus rien casser — dégradation en « proposé mais inoffensif » au lieu de « compteur négatif ». **⚠️ `Global.dice_inventory` ÉTAIT FAUX DEPUIS LE PICKER** : `reset_run_state()` le sème à `["blue","red"]` et `_apply_loadout` ne le touchait pas → un Elf déclarait posséder Blue+Red toute la run (liste SAUVEGARDÉE avec le run et lue par le deal die du card shop). `_apply_loadout` le **RECONSTRUIT** maintenant depuis le set. Au passage : les 9 handlers d'achat de `shop.gd` testaient `if <type>_dice_current_amount == 1` pour ajouter à l'inventaire (donc **doublon "blue" garanti** puisqu'il y était déjà, mesuré : 2 entrées après achat) → passés au `if not dice_inventory.has(type)` (le pattern que `card_shop.gd` utilisait déjà) ; et dépenser son DERNIER Blue/Red retire enfin le type de l'inventaire. **⚠️⚠️ L'AUTRE MOITIÉ DU RAPPORT DE JULIEN N'A PAS PU ÊTRE REPRODUITE** (« buying/getting 1st blue dice doesn't show blue in dice interface top bar ») : le harnais couvre les 4 chemins réels sur le VRAI code (achat dice shop, deal die du card shop, slot d'interface en combat avec 1 Blue possédé, et le dé invité du Dice Bag sur un loadout sans Blue) — **tous PASSENT**, top bar `x1` visible et slot bleu affiché/dimensionné. Vérifié aussi que `blue_dice.visible` est bien dans le commit tip (`fca3f0a`) et que les 3 exports web sur disque datent tous d'AVANT le picker (3-7 août) — donc il ne peut pas avoir joué un build périmé. **Piste non écartée à re-tester avec lui : le top bar ne montre QUE `max_amount`, donc un Blue purement fight-scoped (dé invité du Dice Bag à chaque combat sur un Elf, Spark/Compound/Echo Chamber) apparaît dans l'interface de combat mais JAMAIS dans le top bar — c'est le comportement voulu, mais ça se lit comme un bug.** ⚠️ **REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de jouer** : `event_stats.gd` a gagné un @export ET les 2 `.tres` l'utilisent — c'est exactement la configuration de l'incident de strip.

### H-055 (2026-08-14) Flèches Mech, icône de reroll Ricochet, flèches du tuto remplacées

- **🏹 FLÈCHES MECH + ICÔNE DE REROLL RICOCHET + FLÈCHES DE TUTORIEL REMPLACÉES (2026-08-14, gens Firefly de Julien) — VÉRIFIÉ PAR MESURE (ink bounds) + RENDUS (4 directions de pointer, cluster mech, cluster ricochet), NON PLAYTESTÉ.** (1) **⚠️ DÉCOUPLAGE : les flèches Mech ±1 EMPRUNTAIENT `tutorial_arrow_down.png`** (increase = le même PNG en `flip_v`) — elles ont maintenant leur fichier dédié **`mech_arrow.png`** (acier riveté), `dice.tscn` repointé (l'ext_resource `5_ti466` change de path ET **perd son `uid=`** — garder l'uid de l'ancien fichier ferait résoudre Godot vers le MAUVAIS PNG). Sans ce découplage, restyler le tuto aurait silencieusement restylé les boutons Mech. (2) **⚠️⚠️ PIÈGE DE CROP RÉSOLU : les boutons Mech font 57×85 en `stretch_mode = 6` (KEEP_ASPECT_COVERED)** → une texture carrée rend en 85×85 et perd (85−57)/2 = 14px **par côté**, donc **seuls les 67% CENTRAUX de la largeur sont visibles** ; l'art brut (ink w=0.774) aurait eu ses ailes de pointe COUPÉES (même classe de bug que House Money). L'ink est donc normalisée à **w=0.540**, dans la bande sûre avec marge. (3) **⚠️ GÉOMÉTRIE DU TUTO PRÉSERVÉE AU MILLIÈME** : `tutorial_overlay.gd` **code en dur `ARROW_ART_TOP_FRAC = 0.184` / `BOTTOM_FRAC = 0.818`**, et le commentaire du fichier dit que l'espacement des pointers est **tuné à l'œil sur chaque étape** — la nouvelle art est donc posée à **exactement ink y 0.184..0.820** (mesuré identique à l'ancienne), ce qui rend le swap invisible pour tout le tuning existant : **aucune constante à retoucher, aucune étape à re-tuner**. (4) **⚠️ `tutorial_arrow.png` pointe en fait à DROITE, pas en haut** (d'où le commentaire « doesn't actually point straight up » et le fait que `show_pointer` dérive TOUTES les directions de la texture DOWN) — il n'est vivant que dans `run.tscn::TopBar/DiceShopExplanationBox/Arrow`, **tourné de 270°** pour viser le bouton Dice Shop. Remplacé par la nouvelle art **tournée pour pointer à droite** et fittée dans l'ANCIENNE boîte d'ink (y 0.276..0.720) → la rotation retombe pareil. **Vérifié par géométrie, pas par rendu** (seul élément non rendu de la passe). (5) **Choix** : les 2 candidats mech étaient **byte-identiques** (md5 `57ba00fe6e`) et 2 des 3 rerolls aussi (`5a4b3e4479`) — le seul vrai arbitrage était **boucle complète vs « C » ouvert** : boucle retenue (glyphe universel refresh, lit mieux à 57px, et c'est la forme de l'icône sortante donc zéro réapprentissage). (6) **Teintes `modulate` par bouton RETIRÉES** (mech `0.75,0.78,0.82` gris acier, ricochet `1,0.94,0.84` crème) : elles compensaient de l'art générique, et l'art porte maintenant sa propre couleur (rivets cuivre, orange Ricochet). **Sans risque : le dim d'indisponibilité vit sur la SECTION (`mech_section.modulate.a` / `ricochet_section.modulate.a`), jamais sur le bouton.** (7) **Harnais** : nouveau **`debug_arrow_render.gd`/`.tscn`** (racine, non committé) — pousse le VRAI `TutorialOverlay.show_pointer()` dans les 4 directions avec un point rouge à la cible (seul moyen d'attraper une flèche mal orientée/mal espacée, la mesure du PNG ne le peut pas) ; **`debug_dice_glow.gd` gagne `DICE_GLOW_BUTTONS=1`** (force sections Mech/Ricochet visibles+opaques : le harnais ne fait jamais de VRAI roll donc elles rendraient à 0.3 alpha, inutilisable pour juger l'art). (8) **À surveiller au playtest** : l'icône de reroll orange est adjacente au dé Ricochet orange (lisible au rendu grâce au contour sombre + l'écart, mais si ça gêne le fix est 1 `modulate` crème) ; et l'ancien duo d'`ext_resource` orphelins de `battle.tscn` (lignes 42/44, **0 nœud utilisateur**, reliquat du vieux tuto 15-panneaux) charge toujours les 2 textures pour rien — nettoyage optionnel, non fait.

### H-056 (2026-08-14) Intents combo façon STS2 (rider icon2) + nouvelle famille d'icônes

- **🗡️ INTENTS COMBO FAÇON STS2 (« rider » `icon2`) + NOUVELLE FAMILLE D'ICÔNES D'INTENT INTÉGRÉE (2026-08-14, verdict Julien sur screenshot STS2 : « do it like sts2 does » — les icônes COMPOSITES sont MORTES, un combo = 2 icônes séparées pleine taille côte à côte) — VÉRIFIÉ PAR HARNAIS (famille 7 rangées + tooltips composés ; cadence 21 checks 0 fail) + RENDU IN-GAME (Medusa act 1+2), NON PLAYTESTÉ.** (1) **Plomberie** : `Intent.icon2` (@export, null = mono-icône) + `IconSlot2/Icon2` dans `intent_ui.tscn` (**cacher le SLOT, pas le TextureRect** — un slot caché sort du layout, les intents mono-icône gardent leur empreinte exacte) ; le root HBox est en `grow_horizontal = 2` donc la paire reste centrée au-dessus de la tête GRATUITEMENT ; le bob = MÊME tween, legs `parallel()` pour icon2 (jamais un 2e tween) ; tooltip composé via nouveau `_rider_tooltip_text_for_texture()` (« This enemy will attack you. It will also… ») — **même règle basename que le match principal : toute NOUVELLE icône sans case = rider silencieusement muet dans le tooltip**. Layout = **[NOMBRE][icône primaire][rider]** — 2e verdict Julien le jour même (« sts2 goes number first », et le sandwich [icône][nombre][icône] du 1er jet jugé « really weird ») : le nombre ancre la gauche, TOUTES les icônes se groupent après lui ; l'ordre = l'ordre des nœuds LabelSlot/IconSlot/IconSlot2 dans `intent_ui.tscn`, rien d'autre à changer. (2) **Migrations d'honnêteté** : `buff_block_intent.png` RETIRÉ des 3 scènes (Medusa block, Leviathan block_buff, Defender block → [shield]+[arrow]) et **les 3 attaques du Leviathan (ink/weak/dice_theft) + l'attack_weak de Medusa mènent enfin à l'ÉPÉE + skull rider** — avant, elles affichaient un SKULL portant le NOMBRE DE DÉGÂTS (la classe de mensonge d'intent dénoncée en §2.4). `buff_block_intent.png` = orphelin sur disque (case tooltip legacy conservée) ; **⚠️ ordre des enfants du Leviathan INTOUCHÉ** (child 0 = ink attack, anti-freeze act 1). (3) **Nouvelles icônes** (gens Firefly de Julien, épée `813856` choisie entre les 2 candidates) écrasées en place — `attack_icon_intent`, `block_icon_intent`, `buff_icon_intent`+`buff_icon` (arrow ×2), `debuff_intent`+`debuff_icon`+`debuff_icon_3` (skull ×3) — pipeline chroma-key habituel (ramp 60→120, blob principal, interior fill, resize prémultiplié), 500×500 à 86%, `--headless --import` FAIT ; l'ancien shield soft/gloss est remplacé, **verdict : la famille se swap EN BLOC, jamais panachée**. (4) **Harnais** : `debug_cadence_promotion` 19→**21** checks (icône block ET rider buff assertés séparément — perdre l'un des deux recrée « la rampe invisible sur le beat qui la produit ») ; `debug_intent_family` = colonne rider + 3 rangées combo. **⚠️ Piège retombé : `var x := ICONS[i][0]…` sur un élément d'Array = parse error** (élément Variant, inférence impossible) — **et un parse error dans un harnais Movie Maker = capture INFINIE** (le script ne charge pas → personne n'appelle quit → 1969 frames avant kill manuel) : typer explicitement, et surveiller le 1er run d'un harnais movie-maker. (5) **⚠️⚠️ REDÉMARRER COMPLÈTEMENT L'ÉDITEUR avant de rejouer** : `intent.gd` a un nouveau `@export` — un éditeur resté ouvert avec la copie périmée STRIPPERAIT `icon2` des 3 AI .tscn au premier re-save (pattern live-editor documenté, 3 incidents). (6) **Reste** : les autres beats multi-effets encore mono-icône (satyr/goblin/gargantua/hound/vortex/octopus…) adoptent le rider en 1 ligne chacun au batch §8/§9 ; option future = icône dédiée « vol de dé » pour le rider de dice_theft (partage le skull de weak pour l'instant) ; **MAGMA « ELECTRIC VEINS » INTÉGRÉ le jour même (pick de Julien parmi 3 designs candidats)** : les 6 faces `assets/images/magma1-6.png` reconstruites — corps = la gen Firefly `178628 (1)` de Downloads keyée/fittée sur la bbox existante (1024 plein canvas), **pips PRÉLEVÉS tels quels sur les ANCIENNES faces** (sprite du pip central de magma1 sous masque circulaire feather, positions MESURÉES par détection des blobs crème — elles retombent PILE sur la grille documentée 291/512/732 r≈81, orientation des diagonales préservée par construction, script `build_magma_faces.py` au scratchpad) ; vérifié par planche des 6 faces + rendu combat `debug_dice_glow` (halo magma inchangé, pip lisible sur le centre calme, veines au bord) ; anciennes faces récupérables via git ; les fallbacks « vein frame »/« crust plates » restent dans Downloads.

### H-057 (2026-08-13) « Le Vœu » : picker de loadout de dés au début des runs 2+

- **🌠 « LE VŒU » — PICKER DE LOADOUT DE DÉS AU DÉBUT DE CHAQUE RUN #2+ (2026-08-13, demandé par Julien juste après son 2e full clear, sets EXACTEMENT tels qu'il les a specés) — VÉRIFIÉ PAR HARNAIS (28 checks 0 fail : `debug_dice_loadout.gd`/`.tscn` racine non committé — rendus réels + matrice d'application des 5 sets + gating sur le VRAI run.tscn avec backup/restore du save) + RÉGRESSIONS RE-PASSÉES (double_endturn 9/9, tutorial_lock 20/20), NON PLAYTESTÉ.** Nouvelle scène **`scenes/dice_loadout/dice_loadout.gd`/`.tscn`** (pas de class_name — pas de régen de cache), cérémonie calquée sur dice infusion : titre « A NEW ODYSSEY » / « The dice are listening. What do you wish for on this journey? », 5 panneaux avec halos `DicePalette.die_halo_texture()` **respirant en `_process` phasé au nombre d'or** (jamais des tweens loopés — leçon shop), motes par panneau (timer partagé cyclant ses dés), **tooltip de type au survol de CHAQUE dé** (`dice_tooltip.tscn`, `mouse_filter=PASS` + `gui_input` aussi câblé sur le dé pour que cliquer l'art choisisse le panneau), sélection = bordure accent + dim des autres, gather→détonation (hit_stop+flash)→settle→`Events.dice_loadout_completed` (nouveau signal). **⚠️ AUCUN texte sous les dés (2e retour de Julien : « remove the captions ») — la ligne de composition colorée ET la flavor ont été RETIRÉES (champs supprimés de `LOADOUTS`, pas de donnée morte) ; un panneau = titre + dés + badges ×N, et les tooltips au survol sont désormais le SEUL endroit qui enseigne les noms de types (Golem/Ricochet/Pixie) — ne pas les retirer sans remplacer ce rôle.** **⚠️ Entrance = tweens indépendants par panneau à délais échelonnés** — un seul tween séquentiel fait BLOQUER chaque fade par le précédent (~2s pour 5 panneaux, vu au rendu). **Sets (constante `LOADOUTS` en tête = LA table de tuning : id/titre/counts)** : Disciple 2 Blue+1 Red (= start classique), Acrobat 2 Ricochet+1 Red, Elf 4 Pixie, Titan Giant+Golem+Blue, Architect Magma+Blue+Mech — **⚠️ 3 sets SANS Red (choix explicite de Julien)** : All In/Bulletproof/Flurry/Kamikaze (+ « + ») sont des drafts morts pour eux tant qu'un Red n'est pas acheté en boutique ; les cartes Charge/Throw restent vivantes (elles conjurent leurs propres types). **Gating** : stat lifetime `runs_started` dans `AchievementManager` (+ `get_stat()`/`flush()` publics), comptée dans `_start_run` AVANT le branchement, picker si ≥1 ET `not tutorial_on` (le tuto scripte des rolls Blue + un socket Red → un run tuto tardif saute le picker) ; **backfill dans `_load_from_disk`** : tout profil ayant un unlock OU une stat > 0 reçoit `runs_started = 1` → **Julien verra le picker dès son prochain « New Run »**. **⚠️ PAS de `_save_checkpoint` avant le choix** — la save du run PRÉCÉDENT survit jusqu'à la confirmation (quitter en plein picker n'abandonne rien) ; le 1er checkpoint du nouveau run = le `_show_map()` post-choix. Le picker écrit les 9 types (max ET current) puis `Global.dice_type` ; le save dict sérialise déjà tous les types → load d'un run loadout gratuit. **⚠️ 3 hypothèses « Blue est permanent » levées (premier start sans Blue de l'histoire du jeu)** : (1) top bar `blue_dice.visible = max > 0` (comme red) ; (2) `dice_interface.gd::initialize_dices()` + `_resize_panel_for_dice_inventory()` — check de visibilité blue incluant `blue_dice_bonus_amount_fight` (Emanation donne du Blue fight-scoped à qui n'en possède pas) ; (3) **nouveau `Global.default_active_dice_type()`** (1er type possédé dans l'ordre des slots, **RED EN DERNIER** — un combat ne s'ouvre jamais sur le dé gamble) remplaçant les `dice_type = "blue"` en dur de `battle.gd::start_battle` et `run.gd::_on_battle_won` — l'Acrobat ouvre sur Ricochet, l'Elf sur Pixie, comportement inchangé pour tout owner de Blue. **Effets de bord assumés, à surveiller au playtest** : Dice Bag donne toujours +1 Blue au tour 1 (l'Elf reçoit un « dé invité » bleu à chaque combat — re-skinner la relique si ça gêne) ; l'écran d'infusion d'act 2 n'offre que les types POSSÉDÉS (un Elf pur n'a que Gnome en candidat) ; balance des floors 1-3 par set jamais jouée (le knob = les counts dans `LOADOUTS`, l'Architect avec Magma au floor 1 est le point chaud probable).

### H-058 (2026-08-13) Passe variété de dés (prix retournés, reroll garanti, restyle de la dice shop)

- **🎰 PASSE « VARIÉTÉ DE DÉS » (2026-08-13, sur constat de Julien : « ce qui rendra les runs fun c'est de découvrir de nouveaux dés, pas d'être en blue+red toute la run puis d'acheter un 3e blue ») — VÉRIFIÉ PAR HARNAIS (1007 checks, 0 fail) + RENDU, NON PLAYTESTÉ. Deux morceaux :**
  - **TABLE DE PRIX RETOURNÉE (`global.gd::DICE_BASE_PRICES`) + escalation 1.4 → 1.5.** Root cause mesurée : Blue/Red à 180 SOUS-COTAIENT tous les exotiques sauf Pixie (Mech 200, Golem/Ricochet/Evil/Giant 240, Magma 270) — **la boutique disait littéralement « un 3e Blue est l'option budget, les dés fun sont le luxe », donc la run ennuyeuse était pricée dans la table.** Nouveau : aisle DÉCOUVERTE sous les starters (Pixie 115, Mech 145, Golem/Ricochet 165) < **Blue/Red 170** < Evil/Giant 185 < Magma 220. ⚠️ **Julien a explicitement demandé « entre l'actuel et la proposition de Claude »** — une 1re proposition plus agressive (aisle 80-130) a été écartée : **il y a déjà eu un bug historique de « 1er achat trop tôt parce que les prix étaient trop bas »**, et un achat au floor 3 est un choix NON INFORMÉ (le deck n'a pas encore de direction) + un spike de puissance. Cible atteinte : 1er dé = engagement au 1er shop (~floor 5, ~150-200 d'or gagné, il faut sauter la rangée de cartes pour un Golem/Ricochet) ; dé #2 à ×1.5 (fin d'act 1) ; dé #3 à ×2.25 (trophée d'act 2). **L'escalation est GLOBALE (tout achat renchérit TOUS les types), c'est ce qui rend des bases basses sûres** — en per-type, des bases basses laisseraient acheter un de chaque au prix de base = machine à sous à 7 dés. **Cette approche remplace le start-of-run dice picker comme plan A** (elle évite tout le problème du swap de Red : aucun red_only ne devient du contenu mort, pas de filtrage de draft à écrire). ⚠️ Effets de bord à surveiller : les floors 4-8 deviennent plus faciles (~+33% de rolls/tour) — absorbable par la passe de beats ennemis du §8 ; et le badge vert « tu peux t'offrir un dé » (`Global.cheapest_dice_price`) s'allumera plus souvent — si ça devient du bruit, le fix est de le keyer sur le type le moins cher NON POSSÉDÉ.
  - **REROLL DE LA DICE SHOP : les 3 nouveaux dés sont GARANTIS différents des 3 affichés** (`shop.gd::reroll_shop_dice`) + **tooltip au survol du bouton**. Avant, le reroll tirait 3 types parmi les 9 SANS exclure l'offre courante — **~72% des rerolls rendaient au moins un dé déjà affiché** (mesuré par le contrôle négatif), ce qui lit comme un bouton cassé et rend le prix de 20 or impossible à juger. Maintenant : on tire dans les 6 types NON affichés (garanti ≥3, `buy` ne retire jamais un dé de l'affichage donc `shop_dice_selection` fait toujours 3). **Vrai bug corrigé au passage : `reroll_shop_dice` n'appelait pas `update_dice_price()`** → `Global.cheapest_dice_price` (qui pilote le badge de map) décrivait l'ANCIENNE offre jusqu'au prochain événement de prix, et les couleurs abordable/rouge ne voyaient pas les 20 or dépensés. Tooltip = **nouveau `IconTooltip.spawn_body_below()`** (frère statique de `spawn_below` pour la variante corps de texte — la 3e duplication de ce pattern, extraite au lieu d'être recopiée depuis `map_room.gd`) ; texte « Offers three new Dice. All three are guaranteed to be different types from the ones shown now. » — **la garantie est énoncée explicitement parce que c'est TOUTE la raison de payer**. ⚠️ Le tooltip est signalé en CODE dans `_ready()` (le bouton y est déjà résolu) et libéré par `_exit_tree` + `mouse_exited` (pattern anti-leak habituel).
  - **RESTYLE VISUEL DE LA DICE SHOP (même session, DEMANDÉ par Julien — ça lève l'ancien verdict « garder le vieux look » du 07-03 pour le panneau/titre, mais PAS pour le reste) — VÉRIFIÉ PAR RENDU + PLANCHE DE 4 VARIANTES.** (1) **Panneau — ⚠️ le navy `#141A29` a été REJETÉ par Julien au 1er retour (« i dont like navy for the dice shop, it should be more aligned with the dice textures, less realistic / detailed ui »)** — le langage navy/or des events N'EST PAS universel, la dice shop doit parler « matière dés » (cel plat). État final = **variante B de la planche (`debug_shop_panel_variants.gd`, 4 rendus : charcoal / felt teal / casino green / vieux teal cel) : feutre teal profond PLAT `Color(0.129, 0.259, 0.227)`, bordure ÉPAISSE quasi-noire 6px (le langage outline de l'art des dés, PAS l'or fin), rayon 34 (écho des coins des dés), ombre serrée 8/45%** (l'ombre douce 14px lisait « réaliste »). Les écartées : charcoal = runner-up neutre, casino green = monochrome dès que des dés verts sont en stock, vieux teal = trop clair, tue les glows. Les boutons teal (styleboxes locales `StyleBoxFlat_buy_*`) restent. (2) **Titre** : il était à 12px du bord haut (plainte de Julien « too close to top edge ») → descendu à ~28px (offsets -238/-190 → -222/-174) + la rangée de dés descendue de 16px pour garder le gap (HBox offset_top -171 → -155) — **⚠️ ça a AUSSI descendu les boutons Buy de 16px (les enfants du VBox s'empilent depuis le HAUT — la 1re analyse « absorbé par la zone morte » était fausse, le SHRINK_END du bouton n'épingle PAS au bas du HBox), collant les Buy au Reroll → RerollButton descendu de 16px en compensation (offsets -177/-129 → -161/-113, ~16px de gap restauré, tooltip vérifié encore à l'écran)** ; recette standardisée or `#EEB52A` outline 6 `#170F05` + ombre (était jaune vif `(1,1,0.227)` outline 3 sans couleur). La police reste le défaut CinzelDecorative (small-caps rendent « DICE SHOP », pas de double-O à pâté). (3) **Bouton X** : ses styleboxes normal/hover étaient des boîtes TEAL invisibles sur l'ancien panneau teal → passées transparentes, glyphe or atténué 0.65, hover blanc conservé. **⚠️ 2e retour de Julien : appuyer sur X affichait un pavé clair — le bouton n'avait AUCUN override `pressed` NI `focus`, donc le thème par défaut fournissait ces boîtes-là** (invisible tant que normal/hover étaient opaques par-dessus le même teal) → `pressed` + `focus` pointés sur la stylebox transparente + `font_pressed_color` blanc. **Règle : en rendant transparentes les styleboxes d'un bouton, couvrir les QUATRE états (normal/hover/pressed/focus) — les états non overridés retombent sur le thème et deviennent visibles.** (4) **GLOW DES DÉS (demande explicite « like on dice infusion »)** : halo respirant + motes montantes teintés `DicePalette.accent(type)` (donc infusion-aware gratuit) sur chaque dé — **la recette glow (texture radiale + material additif) est EXTRAITE dans `DicePalette.glow_texture()`/`additive_material()`, le foyer canonique** (dice_infusion.gd et card_shop.gd gardent leurs copies privées d'avant l'extraction ; tout futur consommateur passe par DicePalette au lieu d'une 4e copie). Implémentation `shop.gd::_setup_dice_glow` : enfant du `Dice{N}Texture` en `show_behind_parent` + `MOUSE_FILTER_IGNORE`. **⚠️ Désync des respirations — 2 itérations : le 1er jet (lead-in aléatoire 0-1.4s + périodes 1.35-1.7s en tweens) a été vu SYNCHRONISÉ par Julien (« all 3 dice have the exact same behaviour ») — un délai aléatoire uniforme peut clusteriser, et pendant le lead-in les 3 halos idlent à la même valeur basse, donc le 1er cycle (= l'essentiel d'une visite) lit en lockstep. Fix structurel : respiration SINUSOÏDALE en `_process` avec phases espacées au NOMBRE D'OR par index de colonne (`fmod(i*0.618 + offset_aléatoire, 1)*TAU` — n'importe quel trio de colonnes visibles tombe sur des points bien séparés du cycle DÈS la 1re frame) + période par dé 2.7-3.4s. Un loop de Tween commence toujours à une frontière de leg — c'est précisément ça le problème de synchro, d'où le passage en `_process`. Vérifié par MESURE (le harnais échantillonne les alphas des 3 halos à 3 instants : valeurs distinctes, trajectoires opposées — un monte pendant qu'un autre descend).** ; motes via UN timer partagé qui cycle les dés VISIBLES (lit `shop_dice_selection` en live → un reroll redirige les motes tout seul). **⚠️ `shop.gd::MOTE_INTERVAL` est un taux GLOBAL À L'ÉCRAN (÷3 pour le taux PAR DÉ), contrairement à `dice_infusion.gd::MOTE_INTERVAL` et `card_shop.gd::DEAL_MOTE_INTERVAL` qui sont PAR DÉ (un timer chacun) — c'est pourquoi la shop était 3× plus clairsemée à valeur égale.** Passe « +motes » du 2026-08-13 (demande de Julien, les 3 menus) : shop 0.42→**0.26** (≈0.78s/dé) + alpha 0.3-0.5→0.38-0.62 + taille 10-20→11-22 ; infusion 0.4→**0.3** + alpha 0.35-0.6→0.42-0.7 ; deal die 0.35→**0.27** + alpha 0.4-0.7→0.46-0.78 + taille 9-18→10-20. **⚠️ Piège de calibration : l'art du dé est OPAQUE et couvre le centre lumineux du halo — seul l'anneau au-delà du bord se voit.** Le 1er jet radial (264px, alpha 0.34) était invisible au rendu (~6% de luminosité effective) ; il faut un halo nettement plus grand que le dé. **⚠️ 3e retour de Julien : le halo CIRCULAIRE derrière un dé CARRÉ « n'a pas de forme » — et il avait la même gêne sur l'écran d'infusion depuis le début.** Fix à la source : **nouveau `DicePalette.die_halo_texture()`** — texture SDF de rectangle arrondi (cœur = 52% du canvas, caché SOUS l'art du dé ; falloff quadratique vers le bord ; contrainte : halo ≤ die_px/0.52 sinon le bord dur du cœur dépasse) qui SUIT la silhouette du dé, branchée sur LES TROIS consommateurs (dice shop, écran d'infusion, deal die du card shop — alphas rebaissés partout car l'anneau shaped est bien plus lumineux au bord que la queue radiale : shop 0.32-0.5, infusion 0.3/0.28-0.42, deal inchangé mais halo 150→166). **Les MOTES restent sur la `glow_texture()` radiale (des étincelles rondes, c'est correct — Julien les adore).** Vérifié par rendu des 3 écrans (`debug_dice_shop_render`, `debug_dice_infusion_render`, `debug_shop_rework`). Le léger wash du halo sur le bas de la bannière du même dé est ASSUMÉ (même famille de couleur — lit comme « le dé éclaire son enseigne »). Harnais : `debug_dice_shop_render.gd` attend maintenant 90 frames (le lead-in aléatoire + les motes doivent avoir démarré pour qu'une still les montre).
  - **Harnais `debug_reroll_offer.gd`/`.tscn` à la racine (non committé)** : boot du VRAI `dice_shop.tscn`, **200 rerolls** × 5 assertions (3 dés, types distincts, zéro report de l'offre précédente, deal die hors offre, colonnes visibles == sélection) + les 9 types restent atteignables + `cheapest_dice_price` recalculé + géométrie/cycle de vie du tooltip, puis rendu PNG. **⚠️ CONTRÔLE NÉGATIF FAIT (méthodo maison) : en neutralisant l'exclusion, le harnais tombe à 143 FAIL** — un test qui ne peut pas échouer ne prouve rien. ⚠️ Pour capturer un tooltip dans un SubViewport il faut le REPARENTER : `Global.add_tooltip()` le parente à la vraie racine (voulu en jeu), donc il est invisible du SubViewport (même bypass que `debug_icon_tooltip_render.gd`). ⚠️ **`Godot_v4.3-stable_win64.exe` est un DOSSIER** qui contient `Godot_v4.3-stable_win64_console.exe` — lancer le « .exe » directement ne fait rien, silencieusement.

### H-059 (2026-08-12) Rework Even/Odd → Golem/Ricochet

- **🎲 REWORK EVEN/ODD → GOLEM/RICOCHET — IMPLÉMENTÉ ET COMMITTÉ (décidé 2026-08-12, noms VERROUILLÉS par Julien ; appliqué le jour même sur la branche `dice/golem-ricochet-rework`, commits `2190ef9` rework + `6ecd494` textures ; harnais `debug_golem_carryover` 12 checks + `debug_ricochet_reroll` 26 checks à la racine ; les 2 dés repricés 240 = tier Evil/Giant, **puis toute la table repricée le 2026-08-13 (passe « variété de dés » : aisle découverte 115-165 SOUS Blue/Red 170, Evil/Giant 185, Magma 220, escalation 1.4→1.5 — verdict Julien « entre l'actuel et la proposition », 1er achat = engagement au 1er shop, pas un impulse floor 3)** ; ⚠️ carryover Golem SANS CAP pour l'instant ; 1er playtest rapide de Julien 2026-08-13 : stall Golem validé « really cool, changes how you plan & play your turn », Ricochet encore peu testé — le paragraphe ci-dessous reste la spec d'origine).** Constat de Julien : Even/Odd sont les 2 seuls dés sans « âme » (la parité est une contrainte, pas une fantasy) ; parité rétrogradée en spice, on n'ajoutera JAMAIS de cartes parité. Décision : les 2 types gardent leurs strings internes (`"odd"`/`"even"`, précédent Pixie = rename display-only via `DICE_TYPE_DISPLAY_NAME_OVERRIDES`) et leurs faces, mais reçoivent de nouvelles identités + leurs CORPS ÉCHANGÉS : **`"odd"` = RICOCHET Dice (faces 1/3/5/7, corps ORANGE pimpant ex-even, 1 reroll autorisé par roll — implémentation = le pattern post-roll-adjustment de Mech)** et **`"even"` = GOLEM Dice (faces 2/4/6/8, corps OCRE craquelé ex-odd, les dés non dépensés se REPORTENT au tour suivant — exception dans le refill de dice_interface)**. Le 3e candidat **Sticky (« ses rolls ne resettent jamais le Power ») est PARQUÉ comme future INFUSION, pas un dé** (dissoudrait la tension centrale des chaînes + dévaluerait Eclipse/Shockwave). **Le body-swap est DÉJÀ PROTOTYPÉ et vérifié** (chirurgie de pips, zéro IA : les 2 corps partagent la MÊME grille 291/512/732 r=81 du pipeline de juillet ; script `dice_body_swap.py` au scratchpad, déterministe, régénérable en secondes ; preview sheet validée par Julien). ⚠️ 2 pièges de clonage documentés : sens de `np.roll` inversé (échantillonnait la bordure du dé) et l'offset d'échantillonnage doit dépasser cover+outline (~3.5r) sinon le pip décalé re-bave en croissant. **Checklist d'application (post-launch)** : backup des 8 PNG → overwrite in place (chemins construits par string → tout le jeu suit) → `--headless --import` → échanger les entrées even/odd dans DicePalette + les 2 `*_dice_shader.tres` + KeywordColorizer (les couleurs suivent les corps) → passe texte (~6 cartes ; ⚠️ « an Odd/Even Dice » devient « a Ricochet/Golem Dice », les articles changent) ; les 2 nouvelles mécaniques exigent chacune un harnais de régression. Infusions inchangées (Octet sur even/Golem, Bulwark sur odd/Ricochet — clés par string de type).

### H-060 (2026-08-12) Paper split du pool en 2 personnages (design seulement)

- **✂️ PAPER SPLIT DU POOL EN 2 PERSONNAGES (2026-08-12, exploration demandée par Julien « trop d'archétypes sur un seul perso » — DESIGN DOC SEULEMENT, RIEN D'IMPLÉMENTÉ, AUCUN VERDICT) : voir [character_split_2026-08.md](character_split_2026-08.md).** Axe = accès aux DÉS (pas une coupe arbitraire) : Char 1 « Bruiser/Gambler » Blue/Red/Evil/Giant/Magma (bank+multiplicateurs, red gamble, throw lourd, refuel) vs Char 2 « Tinkerer » Blue/Pixie/Mech/Even/Odd (précision Exact/Mult, parité, swarm/count, Scout, low-roll). **Résultat : 29 / 39 / 12 (C1/C2/partagé) — le pool actuel penche DÉJÀ Tinkerer**, la moitié du warrior appartient à un perso qui n'existe pas encore. 3 découvertes : (1) les 9 infusions se partitionnent 4/4+Blue PILE sur cet axe, zéro conflit ; (2) **la Strength appartient mécaniquement au Char 2** (Kickstart Max 3 + Fortify Even + l'infusion Octet donne de la Str + bonus flat PAR HIT × nombre de hits = swarm ; C1 scale par multiplicateurs X2/X3/X4) — résout les 2 cartes-tension, verdict Julien requis ; (3) vocabulaire de gates par perso : C1 = none/Min/Red, C2 = Max/Exact/Mult/Even/Odd — chaque perso enseigne la moitié du système. Gaps à autorer : C1 ~8-12 cartes (red density, throws, heal, payoffs Evil — Mulligan/Backdraft du §6 de l'audit y vont), C2 ~6-10 (payoffs Even, Mech, package Strength — Bastion/Prophecy y vont). Recos : POST-launch uniquement ; d'ici là, autorer les nouvelles cartes parité/swarm/Strength dans une side-list SUR DISQUE hors pool (convention cartes coupées) au lieu de gonfler le pool live. Swing cards listées en §5 (Bullseye et Crescendo = les 2 pertes qui font mal au warrior actuel).

### H-061 (2026-08-08) Analyse ennemis / rythme de combat (design doc, §8-9 actifs)

- **📐 ANALYSE ENNEMIS / RYTHME DE COMBAT (2026-08-08, demande de Julien « add spice, tension, variety & pace » — DESIGN DOC SEULEMENT, RIEN D'IMPLÉMENTÉ) : voir [enemy_design_analysis_2026-08.md](enemy_design_analysis_2026-08.md).** Basé sur un deep-dive des wikis STS1+STS2 (grammaire IA à 5 pièces, « chaque combat est une horloge », counterplay écrit DANS l'ennemi, vol « avec reçu », doctrine « complexity threshold » — MegaCrit a SUPPRIMÉ un boss shippé pour ça) + un dossier code des 21 ennemis. **Constat mesuré : 14/21 ennemis = boucles déterministes plates, 3 cadences dans tout le jeu, 4 mécaniques réactives (toutes élites/T1-solo), zéro seuil de HP / timer / summon / soigneur / protecteur / injection de cartes.** La famille de taxes de bank (Flux/Parasite/Canalize/Greedy/Absorb) est en revanche EN AVANCE sur STS1 — sur-couverte, ne plus en ajouter. **2 bugs d'honnêteté trouvés au passage (§2.4 du doc, à fixer avant tout le reste)** : le tooltip de Flux MENT (le code refuse TOUT re-roll tant que le Power n'est pas reset — pas « même type 2× de suite ») et **Absorb n'a AUCUN tooltip** (« No description available » sur LA mécanique du Lich). Propositions par vagues : act 1 = léger (desync des packs jumeaux, caps de répétition, seuil 50% sur Hound, + 2 fights ADDITIFS : Goblin Cutpurse voleur d'or qui FUIT avec le butin, Satyr Shaman support/kill-order) ; act 2 = les 17 identités reskin reçoivent de vrais kits via des action nodes gated `current_act >= 2` (le pattern Dice Theft, zéro risque act-1) — Harlequin vole un dé JUSQU'À SA MORT, Cinderlord = timer rituel (Canalize droppé en act 2, contradiction avec un timer), Gorgon = +50% des cartes socketées red (« elle ne pétrifie pas qui ne la regarde pas »), Onlooker = attaque miroir du bank courant, Tempest force le type du 1er roll, Dicelord phase 2 ROLLE SES PROPRES DÉS (roulés À LA DÉCLARATION D'INTENT — variance télégraphiée, jamais d'embuscade sur l'allocation de block) ; + système de cartes-junk injectées (Sludge/Cinder/Hex — l'injection est fight-scoped GRATUITEMENT puisque draw_pile est rebuilt par combat, et le refus de prise en main du 07-29 gère déjà le messaging). Toutes les idées de Julien mappées en §3.1 (Strict/Stuck/Red Sensitive de ses vieilles notes planifiées y ressuscitent). Framework de balance : mécaniques pricées en EHP virtuel (calibration = Flux ≈ 15-20 sur un corps 18 HP), règle « tax don't delete », anti-bloat (la critique n°1 de STS2 = combats rallongés). 8 questions ouvertes en §7. **⚠️ SCOPE RÉDUIT PAR JULIEN le jour même (focus polish/marketing) → LE PLAN ACTIF EST LE §8 DU DOC** : uniquement (a) une passe patterns/nombres sur les ennemis EXISTANTS (vocabulaire de beats FLOOR/RIDER/SPIKE/GUARD/EVENT, table par ennemi avant→après à parité DPT — Goblin 7→9 au beat 2, Oculus buff+block4 puis 8, Sigil 12/7/block, Venom Bloom 3+Weak1/buff/7 ⚠️ propage au T0 solo validé — verdict requis, Lich 8/10/5+Weak2, DP 12 / 8&block6 / 15, Gargantua 8/2×4/11, Hound event « Molten Roar » à ≤50% HP — Skeleton et Defender sont les modèles, on n'y touche pas) et (b) 2-3 cartes curse act-2-only portées par les identités reskin existantes via le gate `current_act >= 2` (Hex « à jouer d'abord » → Bog Hag, Cinder « brûle en main » → Ember Fiend, Sludge optionnelle → Deepling — injection fight-scoped gratuite, refus de prise en main du 07-29 réutilisé pour le messaging). Prérequis avant TOUT : la passe wiring/honnêteté (is_performable explicite sur Lich/Gargantua/Sigil qui tournent sur le fallback `get_child(0)`, tooltip Absorb à ÉCRIRE, tooltip Flux à aligner sur le code). Le reste (§4 : nouveaux fights Cutpurse/Shaman, vol de dés Harlequin, timer Cinderlord, summons Necromancer, Dicelord P2 qui rolle) = PARQUÉ post-launch, ne pas re-proposer spontanément. **ÉTENDU LE 2026-08-13 → §9 DU DOC (demande Julien : « pretty much every fight should have an implicit timer (enemy scaling) » — ANALYSE SEULE, AUCUN VERDICT, RIEN D'IMPLÉMENTÉ)** : census code-vérifié des vraies horloges — 6 ramps promises (Marauder/Defender/Oculus/Venom Bloom/Sigil/Maelstrom), 3 ramps RNG (Skeleton/Medusa/Leviathan via block+Muscle pondéré), et le constat central : **les taxes de greed (Canalize/Absorb/Parasite/Flux) sont ESQUIVABLES, donc un joueur discipliné joue les élites en stable-state** (DP 11 flat pour toujours en dépensant petit) — sauf Greedy, quasi-inesquivable, devenu la plus rapide horloge du jeu depuis la passe de prix des dés (+rolls/tour) ; les blocks+Muscle de Lich/Gargantua sont du code MORT derrière le fallback `get_child(0)`. 4 outils : rider +N Muscle par cycle (modèle Defender), damage-step par utilisation (« ramp the spike, not the floor » — attention au piège snapshot Chimera), promotion de cadence des beats block+M pondérés (plomberie `% 4` du Crab), backstop global optionnel « soft enrage » (+1 Str/tour/ennemi dès par+2 : T0 t6 / T1 t7 / T2 t8 / élites t9 / boss t11 — invisible en jeu normal, tue mathématiquement le turtle, 1 statut + 1 hook, badge+tooltip OBLIGATOIRES jour 1). Colonne RAMP par ennemi qui s'implémente DANS les mêmes batches que §8 : Goblin +1 Muscle sur le 9, Lich +1 sur le soul-sap, DP spike 15→18→21, Medusa/Leviathan beat block+M forcé tous les 4 tours, Lurker 6→7→8→9 EN VERDICT (contredit « flatness is the point ») ; tier 0 intouché (staller un combat gagné ne paie rien — aucune ressource cross-fight). **Motivation n°1 = le carryover Golem SANS CAP : le timer est le prix du stall, préféré à un cap (ne jamais faire les deux).** Rejetés : accélération de cadence (casse la lisibilité), escalade de debuffs (spirale de misère). 6 verdicts listés en §9.7. **✅ PREMIER MORCEAU LIVRÉ LE 2026-08-13 (§9.8 du doc) : LES 2 PROMOTIONS DE CADENCE — VÉRIFIÉ PAR HARNAIS (19 checks, 0 fail, contrôle négatif inclus : `debug_cadence_promotion.gd`/`.tscn` à la racine, non committé), NON PLAYTESTÉ.** Medusa et Leviathan avaient leur beat block+Muscle en pick PONDÉRÉ (4/15 et 4/14) plafonné « jamais 2× de suite » → taux réel ~21-22% en régime permanent, donc **leur SEULE rampe était aléatoire**. Passés en CONDITIONAL sur cadence fixe : Medusa `fight_turn % 4 == 3` (l'expression du spike du Skeleton, la seule cadence déjà playtestée), Leviathan `% 4 == 2`. **⚠️ « Promotion » = RETRAIT du pool de chance, pas ajout par-dessus** (le laisser pondéré ET conditionnel doublerait sa fréquence) — donc dans le `.tscn` on RETIRE les lignes `type = 1` ET `chance_weight` (omettre `type` = CONDITIONAL, cf. Temple Defender ; un `chance_weight` résiduel sur un nœud CONDITIONAL est de la donnée MORTE, exactement le leurre dénoncé au §2.4). Vérifié par `total_weight` : Medusa 15→11, Leviathan 14→10. **Parité MESURÉE par le harnais** : DPT Medusa 10.76→10.23 (−4.9%), Leviathan 12.83→12.35 (−3.7%), pendant que la rampe passe de 0.63→0.75 et 0.89→1.00 Muscle/tour **GARANTIS** — un peu plus doux tôt, fiablement plus lourd tard, c'est le troc voulu. **⚠️⚠️ LES 2 PHASES DIFFÈRENT EXPRÈS : l'AI du Leviathan est PARTAGÉE avec le Dicelord d'act 2, dont le vol de dé est CONDITIONAL sur `% 3 == 1`.** Deux cadences conditionnelles sur modulos 4 et 3 se percutent tous les 12 tours, et le picker tranche par ORDRE DES ENFANTS — un des deux beats est silencieusement mangé. `% 4 == 3` percute au **tour 7** (en plein dans la cible boss 6-9 tours) ; `% 4 == 2` repousse ça au **tour 10**, hors cible. En ceinture-bretelles, `dice_theft` est REMONTÉ avant `block_buff` dans le .tscn → en cas de collision le move signature du boss gagne toujours sur son métronome (vérifié : act 2 vol sur 1/4/7/10/13/16/19/22, garde sur 2/6/14/18). **Toute modif d'un des 2 modulos oblige à refaire ce calcul de collision.** ⚠️ Vérifié aussi que l'enfant 0 du picker Leviathan reste `leviathan_ink_attack` : le `get_child(0)` anti-freeze ne doit JAMAIS devenir le nœud de vol act-2-only (un Leviathan d'act 1 volerait des dés). **+ FIX D'HONNÊTETÉ D'INTENT dans la même passe** : les 2 beats donnent du Muscle (3 et 4) mais s'affichaient avec `block_icon_intent.png` — la rampe était invisible sur le beat même qui la produit ; passés à `buff_block_intent.png` (asset + tooltip qui existaient déjà, utilisés par le SEUL Temple Defender). **⚠️ DETTE D'ICÔNES D'INTENT (§9.9, soulevée par Julien) — ✅ RÉGLÉE LE 2026-08-14 par le rider `icon2` façon STS2 (voir bullet dédié en tête de TL;DR) : plus AUCUNE icône combinée à peindre, un combo = 2 icônes séparées côte à côte. Historique : il n'existait AUCUNE icône attack+buff ni attack+block** — il faut les 2 pour la suite du plan (riders Muscle du Goblin/Lich en §9, guarded strike du DP en §8) ; la convention actuelle pour les combos non couverts est « prendre l'icône la plus notable, le nombre = les dégâts » (attack+debuff de Medusa). Reco : peindre les 2 icônes (précédent buff_block) pour le batch scopé, garder en poche l'option **composite** (2e IconSlot dans `intent_ui.tscn`, l'HBox existe déjà) pour la vague act 2 où les combos explosent. **⚠️ `intent_ui.gd::_get_tooltip_text_for_icon()` matche sur le BASENAME du fichier** → toute nouvelle icône sans case dans le `match` retombe silencieusement sur « This enemy is preparing something. » (même classe de bug que le tooltip Absorb manquant).

### H-062 (2026-08-07) Face affichée écrasée après le roll (0.2.7 buggé) corrigée

- **⚠️ RÉGRESSION CORRIGÉE À CHAUD (2026-08-07, signalée par Julien pendant son playtest du tuto : « je suis censé rouler 1 & 1, je vois bien Power 2, mais la face du dé affiche 3 ») — 0.2.7 EST BUGGÉ, NE PAS LE DISTRIBUER, utiliser 0.2.8.** Root cause = **une course entre deux tweens que J'AI introduite** en refactorant l'anim de roll : les swaps de face vivaient AVANT dans le MÊME tween que le mouvement (donc séquentiels, race impossible) ; je les ai sortis dans `_roll_flip_tween` séparé pour qu'ils ne stallent pas le vol — et **rien ne garantissait plus qu'ils finissent avant l'atterrissage**. Un flip encore en attente tire APRÈS `_apply_roll_result` et **écrase la face résolue par une face aléatoire** (le Power, lui, reste juste — d'où le mismatch visible). **Devenu atteignable à cause de la soudure bouton→dé** : le coil saute l'étape de wind-up de 0.05s, ce qui ramène la fin du vol sous le planning de flips de 0.29s **sur les rolls bas/moyens uniquement** (les hauts ont le hang d'apex qui les rallonge) — et le tuto force justement des 1. **Fix en 2 couches** : (a) `_on_roll_landed` **tue `_roll_flip_tween` en TOUTE PREMIÈRE ligne**, avant de poser la face → l'ordre devient structurel et non plus une coïncidence de timing, aucun futur retuning ne peut le rouvrir ; (b) `CALM_FLIP_DELAYS` 0.29 → **0.23** pour que les flips finissent naturellement dans le vol le plus court possible (0.285s = press tenu + roll bas + jitter rapide), le kill n'étant plus qu'un filet. **Vérifié par régression dans `debug_roll_feel.gd` (nouveau `_assert_face_matches_value` : 40 rolls forcés via le chemin coil, compare la texture affichée à la valeur roulée) — 40/40 PASS avec le fix, et le CONTRÔLE NÉGATIF (kill retiré + anciens délais restaurés) reproduit bien 3/40 mismatches (~7.5%, cohérent avec « ça m'est arrivé quelques fois »).** ⚠️ **Rien à voir avec le gate du tutoriel** (l'hypothèse de Julien) : les forced rolls posent bien `roll_index`, seul l'AFFICHAGE était écrasé. ⚠️ **Leçon réutilisable** : sortir des callbacks d'un tween séquentiel vers un tween parallèle enlève une garantie d'ordre qui était implicite — si l'un des deux écrit dans un état que l'autre finalise, il faut re-créer l'ordre explicitement (kill/await), jamais compter sur les durées.

### H-063 (2026-08-07) Refonte du feel du roll + slash directionnel

- **🎲 REFONTE COMPLÈTE DU « FEEL » DU ROLL + SLASH DIRECTIONNEL SUR LES COUPS (2026-08-07, session marketing→juice : les 2 posts Reddit ont floppé, diagnostic de Julien « c'est le juice/look du combat qui n'accroche pas les gens qui ne connaissent pas les mécaniques ») — VÉRIFIÉ PAR RENDUS MOVIE MAKER À CHAQUE ÉTAPE (harnais `debug_roll_feel.gd`/`.tscn` racine non committé), NON PLAYTESTÉ EN JEU. COMMITTÉ (voir [juice_audit_2026-08.md](juice_audit_2026-08.md) pour l'audit complet qui a piloté tout ça).**
  - **CONSTAT FONDATEUR DE L'AUDIT : le jeu n'était pas SOUS-jucé, il était INÉGALEMENT jucé.** Un dé JETÉ (Meteor/Avalanche) avait ~0.95s de cinéma (windup → arc → tumble → hang avec glint → haymaker → flare + débris + shake) pendant que **le dé PRINCIPAL — le verbe qu'on exécute 5-15× par tour, dont TOUS les clips sont faits — avait 0.11s de squash, un wiggle de ±10px, 3 swaps de texture SUR PLACE, et un snap.** Il ne quittait jamais son socle. Un joueur ne le remarque pas (le SENS porté par le nombre de Power le masque) ; **un inconnu qui regarde un clip muet voit : un carré change d'image.** C'était ça, la platitude.
  - **⚠️ LEÇON DE MÉTHODE LA PLUS IMPORTANTE DE LA SESSION : un effet de moins de ~4px ou ~10% de luminosité N'EXISTE PAS à la vitesse de jeu.** 3 effets livrés « vérifiés » (dip du socle 2px, sag du dud, slash fin) ont tous été invisibles pour Julien. Vérifier qu'un effet *s'exécute* ≠ vérifier qu'il *se perçoit*. Tout futur micro-polish doit franchir ce plancher ou ne pas être fait.
  - **ANIMATION DE ROLL : 5 styles construits (`RollStyle` enum + `ROLL_STYLE_DEFAULT` en tête de dice.gd, un builder `_build_roll_*` par style, tous branchés sur UN `_on_roll_landed()` partagé)** — HOP (saut+tumble), TOSS (grossit vers la caméra), SPIN (roulette sur place), DROP (chute+rebonds), **CALM (= DÉFAUT RETENU)**. ⚠️ **Les 4 styles à ROTATION sont morts pour de bon : Julien a eu le VERTIGE en les testant** (« i'm getting dizzy »). Ce n'est pas un goût, c'est une **contrainte d'accessibilité** — faire tourner un gros élément centré 5-15× par tour rend malade sur un run de 30 étages. CALM = le hop sans le tumble : le dé reste droit, 3 swaps de face LENTS (pas un strobe), hauteur/timing pilotés par la valeur. Ne pas re-proposer de rotation.
  - **LE LADDER PAR VALEUR (le cœur du système) : tout, du décollage à l'atterrissage, scale sur `val_frac` = roll / face max du dé — PAS sur la valeur brute.** ⚠️ C'est un vrai fix, pas du style : l'ancien hit-stop `roll_val * 0.013` donnait 0.04s (imperceptible) au meilleur roll d'un d3 pendant que les rolls MOYENS d'un d12 battaient le max d'un d6. Chaque type de dé couvre maintenant la même plage ressentie — « un 3 sur un d3 » atterrit comme le triomphe que c'est. Scalent sur val_frac : hauteur du hop (55%→100%), vitesse de chute, squash d'impact + écrasement vertical, poussière, rattle (^1.5), hit-stop, flash d'impact, volume du thud. **Le MAX garde un genre de célébration EXCLUSIF** (hang ~0.15s avec shiver → chute cubique plus rapide → flash or + burst + flare + smear de 3 fantômes) : le ladder se gagne par le haut, jamais en aplatissant le milieu.
  - **SON : l'atterrissage était MUET** (tout l'audio de roll partait au CLIC du bouton). Nouveau thud à chaque landing dont **le PITCH monte avec la chaîne** (+0.07/roll consécutif, cap 6 — le truc de Balatro ; compte double sur TikTok où le son est ON) pendant que **le VOLUME suit la valeur** : 2 axes lisibles. Max = un impact plus lourd EN PLUS du thud (même ladder, pas un événement à part). Les 2 streams sont des **PLACEHOLDERS** : `LAND_THUD_SOUND` / `LAND_SMASH_SOUND` en tête de dice.gd, Julien a des candidats à tester.
  - **LA CHAÎNE PARTICIPE ENFIN AU BEAT** : le punch du nombre de Power scale avec la PROFONDEUR de chaîne (le 4e roll consécutif frappe plus fort que le 1er à valeur égale), le rack d'historique **punch en sync avec l'atterrissage** (la mini-face ARRIVE au lieu d'apparaître — c'est ce qui rend la chaîne lisible pour un inconnu), et **franchir 18 Power banké** déclenche un flare d'ignition one-shot + freeze sur le nombre (`POWER_TIER_THRESHOLD`). ⚠️ Volontairement **aucune teinte persistante** au-dessus du seuil : les chemins de couleur du Power sont un champ de mines documenté de bugs restore-to-snapshot, un beat one-shot ne peut pas se battre avec eux.
  - **SOUDURE BOUTON→DÉ (demande explicite de Julien, « feel your ROLL press makes the dice hop »)** : les boutons Godot émettent `pressed` au RELÂCHÉ, ce qui est exploité — `button_down` **ARME le dé** (`Dice.coil_die()`, il se comprime et RESTE comprimé tant qu'on tient), le relâché lance le hop DEPUIS cette compression (les builders sautent leur propre squash si `_die_coiled`). Le clic COMPRIME littéralement le dé, lâcher = le lancer. Chemins d'annulation gérés : curseur qui sort du bouton (`_on_mouse_exited` → `release_die_coil()`), roll refusé (plus de dés, Flux), type déplété (refuse d'armer, comme le flash du bouton).
  - **GLOW QUI SUIT LE DÉ** : un mirroir dans `_process` copie l'offset-depuis-le-repos du dé sur l'aura (1:1, c'est son propre halo) et l'emanation (**0.35, elle TRAÎNE comme une lumière lourde** — et ça protège la bande de dégagement codée en dur du shader sous la rangée de dés). ⚠️ Fait en `_process` et PAS en tweens parallèles exprès : toute source de mouvement (hop, shiver, rattle, rebond, et tout futur ajout) est couverte gratuitement, et au repos l'offset vaut 0 donc les 2 couches sont épinglées chez elles.
  - **SLASH DIRECTIONNEL SUR LES COUPS — REMPLACE le burst radial de particules sur les cartes ATTACK** (`Card.play()` gate maintenant sur `type != Type.ATTACK` ; skills/blessings gardent leur burst). Verdict de Julien : « un slash a plus de sens qu'un cercle de particules pour une attaque », + il suit la couleur du dé actif comme les particules le faisaient. Anatomie finale : **flash blanc → lame → cône de masse → sparks**, soit 4 essais successifs pour arriver à visible :
    - ⚠️ **(1) Le smear spawnait DANS le flash blanc de `take_damage`** — de la lumière additive sur une silhouette blanche = invisible. Décalé de 0.055s : le beat est maintenant flash qui POP, PUIS lame qui balaye le sprite restauré.
    - ⚠️ **(2) `z_index` : le Sprite2D de l'ennemi est à 7 (enemy.tscn)** — mes couches à 5/6 rendaient DERRIÈRE le corps. **C'était la vraie cause du « je le vois à peine »**, plus que n'importe quel réglage d'intensité. Tout est passé à 8-9 (au-dessus du sprite, sous l'IntentUI à 10).
    - ⚠️ **(3) DEUX modes de blend, un par mode d'échec** : le balayage large est en blend NORMAL et en accent SATURÉ (l'additif délave en pastel et meurt sur les fonds clairs — c'est ce qui le rend franchement BLEU/ORANGE), le cœur fin est additif blanc-chaud (ce qui survit sur les fonds sombres de l'act 2). Entre les deux, une couche est toujours forte sur n'importe quel fond du jeu.
    - ⚠️ **(4) La MASSE compte plus que la forme** : « les particules étaient 10× plus visibles » — un streak est une FORME, l'œil est calibré sur la MASSE. Ajout d'un `CPUParticles2D` one-shot de **40-90 motes** (scale avec les dégâts) en **cône de 26°** le long de la coupe : la mass du vieux burst, mais VISÉE (force venue de la gauche) au lieu de radiale.
    - Timing final : balayage RAPIDE (45% de la vie) puis **HOLD à pleine luminosité** puis fondu lent — vies 0.72s / 0.46s. Un fondu pendant le mouvement était à moitié parti avant que l'œil n'arrive.
  - **Autres livrés** : dip du socle sur gros atterrissages (⚠️ **le bouton ROLL n'est PAS dans le Panel — c'est un frère sur la racine Dice** ; ne dipper que le Panel bougeait le dé sans bouger le bouton que Julien regardait → les 2 sont dippés ensemble maintenant, 6-9px, **posés INSTANTANÉMENT** parce que le hit-stop du même callback gelait une descente tweenée sur sa 1re frame non-dippée), sag+**assombrissement** du dud (la luminosité se lit là où quelques px d'échelle ne se lisaient pas — le dud est l'inverse exact du max : l'un flashe, l'autre s'éteint), variation par roll (jitter de hauteur/arc/timing, rebond optionnel 35%).
  - **Harnais `debug_roll_feel.gd`/`.tscn`** : boot du VRAI `battle.tscn`, `ROLL_STYLE=HOP|TOSS|SPIN|DROP|CALM` en env, force `[2,5,1,6,5]` (couvre bas/moyen/dud/max/franchissement du palier 18), passe par la soudure coil→release sur le 1er roll, puis tape 2× un ennemi pour les slashes. ⚠️ **Mute le bus Music dans `_ready`** (`MusicPlayer.stop()` NE SUFFIT PAS — la piste continuait dans les captures) sinon le wav est inanalysable. ⚠️ **Un harnais qui inflige des dégâts DOIT revalider sa cible entre 2 coups** — le 1er coup a tué un Satyr low-HP et le `take_damage` suivant sur le nœud freed a avorté la coroutine ET laissé Movie Maker tourner à l'infini.
  - **RESTE À FAIRE (rangé par valeur, voir §4-5 de l'audit)** : choisir les 2 SFX de landing (Julien a des candidats), morts d'ennemis qui éclatent en fragments de dés (le beat le plus « clippable » non fait), zoom-punch caméra sur gros coups. **⚠️ Et surtout : PLAYTESTER puis TOURNER LE CLIP** — tout ce travail existe pour du footage, voir [trailer_shot_list.md](trailer_shot_list.md).

### H-064 (2026-08-06) Marketing : contacts, trailer, capsule, subreddits

- **📣 MARKETING : contacts, trailer, capsule — 4 docs créés (2026-08-06/07, Julien découragé par 2 posts Reddit à 0 upvote).** [promotion_contacts_2026-08.md](promotion_contacts_2026-08.md) (5 créateurs à contacter en priorité avec emails vérifiés — Alpha Beta Gamer d'abord, c'est LEUR créneau exact : alphas/betas/démos navigateur ; + Retromation/Olexa/Splattercat/Wanderbots pour plus tard), [trailer_shot_list.md](trailer_shot_list.md) (28 clips + montage 60s + réglages de capture ; **le concept d'arc est de Julien** : « la chance nous a abandonnés » → bad rolls → « jusqu'à ce qu'on la contrôle » → refuel/gros dégâts → l'encre du Kraken coupe la musique → montage rapide ; ma contribution = compresser l'intro à ~3s et donner une PUNCHLINE au gag de l'encre : roll à l'aveugle, gros dégâts quand même), [capsule_art_brief.md](capsule_art_brief.md) (brief à envoyer à un illustrateur ; ⚠️ **son logo est déjà bon** — commander la key art SEULE, pas le lockup), [subreddit_list.md](subreddit_list.md) (tiers + tracker). **⚠️ 3 CONTRAINTES ÉTABLIES** : (1) **r/godot ET r/DestroyMyGame interdisent l'IA** (règle 11 de DMG nomme Claude explicitement, exemption sous 15% avec preuve aux mods — Julien n'y est pas éligible, code ET art). Ne pas l'y pousser : gamedev Reddit est petit et interconnecté, l'étiquette « AI slop dev » ne part jamais. **Stratégie = sauter la couche gamedev, viser les JOUEURS** (TikTok/Shorts sans limite de répétition, subs de joueurs, streamers). (2) **Steam exigera une déclaration IA publique sur la page boutique** — à prévoir, et ça rend la capsule commandée à un humain stratégique, pas juste qualitative. (3) Les posts Reddit sont **one-shot** (l'usure de nouveauté, pas la modération) : garder r/roguelites et r/slaythespire pour quand la page Steam existera.

### H-065 (2026-08-06) Soft-lock du tutoriel réparé

- **⚠️ SOFT-LOCK DU TUTORIEL RÉPARÉ (2026-08-06, 2e signalement itch — nemonic : screenshot de l'étape « Play Reinforce! » avec SKIP TUTORIAL comme SEUL bouton cliquable, « I don't think I followed the steps in order, kinda tried to confuse it intentionally. Just started clicking things lmao ») — VÉRIFIÉ PAR HARNAIS MOTEUR (20 checks ALL PASS, `debug_tutorial_lock.gd`/`.tscn` racine non committé), NON PLAYTESTÉ EN JEU, NON COMMITTÉ.** **Le screenshot se décode entièrement** : boîte = `_step_t2_7`, Power **9**, main = Low Blow + Block + Block, Skeleton 18/31. La main forcée du tour 2 est Low Blow/Recombobulate/Reinforce/Block/Block → **Recombobulate ET Reinforce ont été joués, Low Blow non** (18 HP = 31 − les 13 du Strike T1 ; Low Blow aurait mis 9). Et 9 Power = 3 (roll T2.4) **+ 2 (Reinforce)** + 4 (roll T2.6) : le joueur a joué Reinforce **pendant l'étape Low Blow**, ce qui a décalé tout le script d'un cran, et 2 étapes plus loin le script a demandé Reinforce… déjà en défausse. **Root cause en 3 couches, toutes réparées** : (1) **`_wait(Events.card_played, 1)` avançait sur N'IMPORTE QUELLE carte** — il n'y a qu'UN émetteur (`Card.play`, card.gd:228) et il ne dit pas laquelle, donc un seul play hors-script décalait TOUT le reste ; (2) **`Hand.add_card()` créait chaque CardUI au défaut `disabled = false`** et le seul ré-application du gate était `player_hand_drawn`, **émis à la FIN de la distribution de ~1.25s** (5 cartes × `HAND_DRAW_INTERVAL`) → **chaque tour scripté s'ouvrait sur une main entièrement jouable pendant plus d'une seconde** ; (3) **`Hand._on_card_ui_reparent_requested()` faisait `set_deferred("disabled", false)` inconditionnel** → un seul drag annulé dé-gatait cette carte définitivement. Puis le lock lui-même : `_apply_gate` désactive main + ROLL + les 9 slots de dés + End Turn d'un coup, donc une étape dont la carte a disparu ne laisse **rien** de cliquable. **Fix (3 fichiers)** : (a) **nouveau `_wait_for(sig, accepts)` + `_wait_card_played()` / `_wait_card_charged()`** qui **lisent les ids DANS `_current_gate`** (donc gate et wait ne peuvent structurellement plus diverger — c'est le point clé, un futur edit du gate qui oublie le wait est impossible) ; connexion NON one-shot, démontée par `_clear_pending()` ; les 6 étapes carte migrées (t1_4b, t1_8, t2_3, t2_5, t2_7, t2_8). (b) **`Hand.tutorial_card_gate`** (null = pas de gate) + `tutorial_gate_allows()`, consultés par `add_card()` ET `_on_card_ui_reparent_requested()`, miroité par `_apply_gate` — ferme les 2 fuites ; **strictement no-op hors tutoriel** (gate null → allows true → `disabled = false`, exactement l'ancien défaut). (c) **garde `disabled` dans `card_clicked_state`** (BASE teste au press, la promotion vers DRAGGING arrive des frames plus tard ; bounce silencieux, pas de `play_pickup_refusal` — une carte lockée par le tuto n'est pas une erreur du joueur). (d) **FILET DE SÉCURITÉ : `_gate_is_actionable()` + `_stuck_timer`** (`STUCK_CHECK_DELAY 2.5s`, re-armé à chaque `_apply_gate`) — si l'étape courante n'a **rien de cliquable** (carte absente/invisible, ROLL sans dé du type actif ou red sans carte socketée, aucun slot, pas de Continue ni de panneau Scout), le tutoriel **se retire tout seul** via le nouveau `_release_tutorial(closing_note)` (partagé avec Skip) : tout ré-activé, forced rolls/scout faces vidés, `tutorial_on = false`, + une boîte qui explique et rappelle les contrôles. Anti-faux-positif : re-check différé, ré-armement si `Global.dragging_card` (une carte draggée vit sur l'ui_layer, hors de la main) et gate vide = actionnable (Continue/Scout). **⚠️ Ne PAS raccourcir `STUCK_CHECK_DELAY` sous ~2s** : la distribution de tour fait déjà 1.25s et les 2 `_delay_step` 0.9-1.0s. **Harnais `debug_tutorial_lock.gd`** (boot du VRAI `battle.tscn` en `tutorial_on`, recette `debug_double_endturn`) : 6 scénarios — A play hors-script ignoré + la bonne carte marche toujours ; **B le MÊME play contre l'ancien `_wait` désynchronise bel et bien (preuve que le harnais attrape le bug)** ; C carte distribuée en plein step + carte revenant d'un drag annulé restent gatées ; **D le lock signalé reproduit à l'identique (ROLL/End Turn/toutes cartes morts) puis auto-réparé** ; E aucun faux positif sur une étape saine ; F le filtre `card_charged` du socket. **⚠️ PIÈGE DE HARNAIS : il faut équiper la relique de départ (`warrior.starting_relic`, Dice Bag) — sans elle le tour 1 n'a que 2 dés Blue au lieu de 3 et le 3e roll du script n'a rien à rouler** (le 1er run a échoué comme ça… et le stuck guard l'a correctement attrapé, ce qui l'a validé accidentellement). Régression jeu normal vérifiée : `debug_double_endturn` toujours ALL PASS. **Reste du feedback nemonic** : rien d'autre signalé.

### H-066 (2026-08-03) Double End Turn réparé (1er playtest itch) + HUD caché sur les écrans de fin

- **⚠️ BUG CRITIQUE DU 1er PLAYTEST EXTERNE (itch) RÉPARÉ : DOUBLE END TURN (2026-08-03, repro de l'ami de Julien « les ennemis m'ont tapé 2x leurs dégâts (2x11 / 2x29) puis j'ai pioché plein de cartes ») — VÉRIFIÉ PAR HARNAIS MOTEUR (9 checks ALL PASS, `debug_double_endturn.gd`/`.tscn` racine non committé), NON PLAYTESTÉ EN JEU, NON COMMITTÉ.** L'hypothèse de Julien (« un end turn qui trigger 2 fois ») était la bonne. Root cause : `battle_ui.gd::_on_end_turn_button_pressed` ré-activait le bouton End Turn sur un **timer plat de 3s** — qui tombe EN PLEIN tour ennemi dès que le cycle complet (défausse ~1.25s + actions ennemies + pioche ~1.25s) dépasse 3s, càd presque tout combat multi-ennemis et quasi tout sur l'export web (plus lent/janky). Le bouton se RALLUME visiblement en plein tour ennemi → un 2e clic émet un 2e `player_turn_ended` par-dessus le cycle en vol → `end_turn()` re-incrémente `fight_turn`, `discard_cards()` sur main déjà vide émet `player_hand_discarded` IMMÉDIATEMENT → `enemy_handler.start_turn()` re-remplit `acting_enemies` → **chaque ennemi rejoue son action** (même intent), les 2 chaînes se vident → 2× `enemy_turn_ended` → 2× `start_turn()` → **2× pioche de 5 = 10 cartes**. Julien n'a jamais vu le bug localement parce que LUI ne re-clique jamais pendant le tour ennemi — un nouveau joueur si, surtout quand le bouton se rallume sous son curseur. **Fix (battle_ui.gd seul) : nouveau `_begin_turn_cycle()` = LE SEUL émetteur de `player_turn_ended`** (le clic ET `force_end_turn` d'Emergency/Tension y passent — `_on_force_end_turn` désactive maintenant le bouton pendant sa grâce d'1s, sinon un clic manuel dans la fenêtre doublait aussi), gardé par `_turn_cycle_active` (posé à l'emit, levé sur `player_hand_drawn` = la vraie fin de cycle) + **token de génération `_turn_cycle_id`** (un watchdog périmé du cycle N ne peut pas ré-activer pendant le cycle N+1 — famille `feedback_stale_coroutine_after_await`). Le timer 3s devient un **watchdog 20s** qui ne ré-active QUE si le cycle est réellement bloqué — **ne jamais le raccourcir** (cycle légitime worst case ~15s sur un 4-corps web). Harnais : boot du VRAI `battle.tscn` par `start_battle()` (recette `debug_itch_screenshots`), 3 scénarios — (A) triple clic gardé = 1 seul cycle, chaque ennemi agit 1×, 5 cartes ; (B) **double emit brut (= le monde pré-fix) reproduit EXACTEMENT le symptôme : 3 ennemis agissent chacun 2×, 10 cartes, 2 tours ennemis** — la preuve que le harnais attrape le bug ET la confirmation définitive de la root cause ; (C) `force_end_turn` + clic pendant la grâce = 1 cycle. **⚠️ À vérifier au playtest : le feel du bouton** — il reste maintenant désactivé pendant TOUT le tour ennemi (c'est le but), le watchdog ne se voit jamais en jeu normal. **+ RelicBar/pin Discord CACHÉES pendant les écrans de fin** (2e signalement du même overlap — le screenshot « DUNGEON CONQUERED! » de l'ami montre les icônes de reliques + un compteur « 10 » posés SUR le titre, exactement le chip du 07-25 jamais exécuté) : nouveau signal `Events.end_screen_hud_visibility(bool)` émis par `battle_over_panel.gd` (branche LOSE) et `battle_reward.gd::_show_gg_panel` (les 2 variantes), **restauré par le Continue d'Act 1 Complete** (la seule sortie d'end-panel où le run continue — les autres détruisent la scène) ; handler `run.gd::_on_end_screen_hud_visibility` (chemins `TopBar/RelicBar` + `CanvasLayer/JoinDiscordControl` vérifiés contre run.tscn) — vérifié par chemins + gdtoolkit, PAS rendu. **Reste du feedback ami NON traité (verdicts Julien)** : « je comprends pas la map » (onboarding map — rien construit), Earthquake « surcheater » (watch-list balance), le « 2×-6 kamikaze » = très probablement 2 plays volontaires, pas lié au bug (le self-damage part au play, hors cycle de tour).
